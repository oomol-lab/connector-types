import "@oomol-lab/connector";

declare module "@oomol-lab/connector" {
  interface ActionRegistry {
    /** Attach achievements to a Game Center activity so players can earn them while the activity is played. Achievements already attached are left as they are. */
    "app_store_connect.add_achievements_to_game_center_activity": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
        /**
         * App Store Connect identifiers of the achievements to attach.
         * @minItems 1
         */
        gameCenterAchievementIds: Array<string>;
      };
      output: {
        /** The activity the achievements were added to. */
        gameCenterActivityId: string;
        /** Identifiers of the achievements that were added. */
        gameCenterAchievementIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        added: boolean;
      };
    };
    /** Add existing TestFlight testers to one group so they receive the builds that group can install. */
    "app_store_connect.add_beta_testers_to_group": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
        /**
         * Identifiers of the testers to add.
         * @minItems 1
         */
        betaTesterIds: Array<string>;
      };
      output: {
        /** The TestFlight group the testers were added to. */
        betaGroupId: string;
        /** Identifiers of the testers that were added. */
        betaTesterIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        added: boolean;
      };
    };
    /** Make one build available to TestFlight groups so their testers can install it. */
    "app_store_connect.add_build_to_beta_groups": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /**
         * Identifiers of the TestFlight groups to add the build to.
         * @minItems 1
         */
        betaGroupIds: Array<string>;
      };
      output: {
        /** The build that was distributed. */
        buildId: string;
        /** Identifiers of the groups the build was added to. */
        betaGroupIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        added: boolean;
      };
    };
    /** Make several builds available to one TestFlight group so its testers can install them. */
    "app_store_connect.add_builds_to_beta_group": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
        /**
         * Identifiers of the builds to add to the group.
         * @minItems 1
         */
        buildIds: Array<string>;
      };
      output: {
        /** The group the builds were added to. */
        betaGroupId: string;
        /** Identifiers of the builds that were added. */
        buildIds: Array<string>;
        /** Whether the builds were added. Always true once App Store Connect confirmed the change. */
        added: boolean;
      };
    };
    /** Mark earlier Game Center app versions as multiplayer compatible with this one. Adds to the versions already listed instead of replacing them. */
    "app_store_connect.add_compatibility_versions_to_game_center_app_version": {
      input: {
        /**
         * App Store Connect identifier of the Game Center app version to add compatibility to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAppVersionId: string;
        /**
         * App Store Connect identifiers of the Game Center app versions that stay multiplayer compatible with it.
         * @minItems 1
         */
        compatibilityVersionIds: Array<string>;
      };
      output: {
        /** The Game Center app version the compatible versions were added to. */
        gameCenterAppVersionId: string;
        /** Identifiers of the Game Center app versions that were added. */
        compatibilityVersionIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        added: boolean;
      };
    };
    /** Assign existing TestFlight testers to one build individually so they can install it without belonging to a group. */
    "app_store_connect.add_individual_testers_to_build": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /**
         * Identifiers of the testers to assign.
         * @minItems 1
         */
        betaTesterIds: Array<string>;
      };
      output: {
        /** The build the testers were assigned to. */
        buildId: string;
        /** Identifiers of the testers that were assigned. */
        betaTesterIds: Array<string>;
        /** Whether the testers were assigned. Always true once App Store Connect confirmed the change. */
        added: boolean;
      };
    };
    /** Attach leaderboards to a Game Center activity so scores made while it is played are posted to them. Leaderboards already attached are left as they are. */
    "app_store_connect.add_leaderboards_to_game_center_activity": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
        /**
         * App Store Connect identifiers of the leaderboards to attach.
         * @minItems 1
         */
        gameCenterLeaderboardIds: Array<string>;
      };
      output: {
        /** The activity the leaderboards were added to. */
        gameCenterActivityId: string;
        /** Identifiers of the leaderboards that were added. */
        gameCenterLeaderboardIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        added: boolean;
      };
    };
    /** Put more existing leaderboards into a Game Center leaderboard set. The leaderboards already in the set are kept. */
    "app_store_connect.add_leaderboards_to_game_center_leaderboard_set": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
        /**
         * Identifiers of the leaderboards to add.
         * @minItems 1
         */
        gameCenterLeaderboardIds: Array<string>;
      };
      output: {
        /** The leaderboard set the leaderboards were added to. */
        gameCenterLeaderboardSetId: string;
        /** Identifiers of the leaderboards that were added. */
        gameCenterLeaderboardIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        added: boolean;
      };
    };
    /** Add one record to an open review submission. Give exactly one of appStoreVersionId, appCustomProductPageVersionId, appStoreVersionExperimentId, appStoreVersionExperimentV2Id, or appEventId. The item can be removed again with delete_review_submission_item before the submission is sent. */
    "app_store_connect.add_review_submission_item": {
      input: {
        /**
         * App Store Connect identifier of the review submission.
         * @minLength 1
         * @pattern \S
         */
        reviewSubmissionId: string;
        /**
         * App Store Connect identifier of the App Store version to submit.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId?: string;
        /**
         * App Store Connect identifier of the custom product page version to submit.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageVersionId?: string;
        /**
         * App Store Connect identifier of the product page optimization experiment (v1) to submit.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentId?: string;
        /**
         * App Store Connect identifier of the product page optimization experiment (v2) to submit.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentV2Id?: string;
        /**
         * App Store Connect identifier of the in-app event to submit.
         * @minLength 1
         * @pattern \S
         */
        appEventId?: string;
      };
      output: {
        /** One item inside a review submission. Exactly one of the identifier fields is set, naming the record being reviewed. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        reviewSubmissionItem: {
          /** App Store Connect identifier for the review submission item. */
          id: string;
          /** Review state of this item. */
          state?: "READY_FOR_REVIEW" | "ACCEPTED" | "APPROVED" | "REJECTED" | "REMOVED" | null;
          /** App Store Connect identifier of the App Store version this item reviews, or null. */
          appStoreVersionId: string | null;
          /** App Store Connect identifier of the custom product page version this item reviews, or null. */
          appCustomProductPageVersionId: string | null;
          /** App Store Connect identifier of the product page optimization experiment (v1) this item reviews, or null. */
          appStoreVersionExperimentId: string | null;
          /** App Store Connect identifier of the product page optimization experiment (v2) this item reviews, or null. */
          appStoreVersionExperimentV2Id: string | null;
          /** App Store Connect identifier of the in-app event this item reviews, or null. */
          appEventId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Make additional apps visible to a team member who does not have access to all apps. */
    "app_store_connect.add_visible_apps_to_user": {
      input: {
        /**
         * App Store Connect identifier of the team member.
         * @minLength 1
         * @pattern \S
         */
        userId: string;
        /**
         * Identifiers of the apps to add.
         * @minItems 1
         */
        appIds: Array<string>;
      };
      output: {
        /** The team member the apps were made visible to. */
        userId: string;
        /** Identifiers of the apps that were added. */
        appIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        added: boolean;
      };
    };
    /** Cancel a review submission, removing it from the App Review queue. Its items return to their previous state and a new submission is needed to submit them again. */
    "app_store_connect.cancel_review_submission": {
      input: {
        /**
         * App Store Connect identifier of the review submission.
         * @minLength 1
         * @pattern \S
         */
        reviewSubmissionId: string;
      };
      output: {
        /** A review submission that bundles the items sent to App Review together. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        reviewSubmission: {
          /** App Store Connect identifier for the review submission. */
          id: string;
          /** Content platform the submission targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** When the submission was sent to App Review, as an ISO 8601 timestamp, or null while it is still being prepared. */
          submittedDate?: string | null;
          /** Where the submission stands in App Review. */
          state?: "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "UNRESOLVED_ISSUES" | "CANCELING" | "COMPLETING" | "COMPLETE" | null;
          /** App Store Connect identifier of the App Store version under review, or null when the submission carries no version. */
          appStoreVersionForReviewId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Cancel a pending team invitation. The activation link stops working; invite the person again if needed. */
    "app_store_connect.cancel_user_invitation": {
      input: {
        /**
         * App Store Connect identifier of the invitation.
         * @minLength 1
         * @pattern \S
         */
        userInvitationId: string;
      };
      output: {
        /** App Store Connect identifier of the cancelled invitation. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Erase the purchase history of sandbox testers so they can test first-time purchases again. The history cannot be restored. */
    "app_store_connect.clear_sandbox_tester_purchase_history": {
      input: {
        /**
         * Identifiers of the sandbox testers to reset.
         * @minItems 1
         */
        sandboxTesterIds: Array<string>;
      };
      output: {
        /** App Store Connect identifier of the clear-history request. */
        id: string;
        /** Identifiers of the sandbox testers that were reset. */
        sandboxTesterIds: Array<string>;
        /** Always true once App Store Connect accepted the request. */
        cleared: boolean;
      };
    };
    /** Create a draft accessibility declaration for one device family of an app. The draft is not shown on the App Store until update_accessibility_declaration publishes it. */
    "app_store_connect.create_accessibility_declaration": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Device family the declaration describes. */
        deviceFamily: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION";
        /** Whether the app supports audio descriptions. */
        supportsAudioDescriptions?: boolean;
        /** Whether the app supports captions. */
        supportsCaptions?: boolean;
        /** Whether the app supports a dark interface. */
        supportsDarkInterface?: boolean;
        /** Whether the app conveys information without relying on color alone. */
        supportsDifferentiateWithoutColorAlone?: boolean;
        /** Whether the app supports larger text. */
        supportsLargerText?: boolean;
        /** Whether the app supports reduced motion. */
        supportsReducedMotion?: boolean;
        /** Whether the app supports sufficient contrast. */
        supportsSufficientContrast?: boolean;
        /** Whether the app supports Voice Control. */
        supportsVoiceControl?: boolean;
        /** Whether the app supports VoiceOver. */
        supportsVoiceover?: boolean;
      };
      output: {
        /** An accessibility declaration (Accessibility Nutrition Label) for one device family. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        accessibilityDeclaration: {
          /** App Store Connect identifier for the accessibility declaration. */
          id: string;
          /** Device family the declaration describes. */
          deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION" | null;
          /** Whether the declaration is a draft, published, or replaced by a newer one. */
          state?: "DRAFT" | "PUBLISHED" | "REPLACED" | null;
          /** Whether the app supports audio descriptions. */
          supportsAudioDescriptions?: boolean | null;
          /** Whether the app supports captions. */
          supportsCaptions?: boolean | null;
          /** Whether the app supports a dark interface. */
          supportsDarkInterface?: boolean | null;
          /** Whether the app conveys information without relying on color alone. */
          supportsDifferentiateWithoutColorAlone?: boolean | null;
          /** Whether the app supports larger text. */
          supportsLargerText?: boolean | null;
          /** Whether the app supports reduced motion. */
          supportsReducedMotion?: boolean | null;
          /** Whether the app supports sufficient contrast. */
          supportsSufficientContrast?: boolean | null;
          /** Whether the app supports Voice Control. */
          supportsVoiceControl?: boolean | null;
          /** Whether the app supports VoiceOver. */
          supportsVoiceover?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Register the base web domain that serves a marketplace app or web distribution app. All app pages and the marketplace sitemap must live on this domain, and it is enabled for every alternative distribution app on the account. */
    "app_store_connect.create_alternative_distribution_domain": {
      input: {
        /**
         * Base domain, such as marketplace.example.com.
         * @minLength 1
         * @pattern \S
         */
        domain: string;
        /**
         * Display name for the domain in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
      };
      output: {
        /** A base web domain registered for alternative distribution. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        alternativeDistributionDomain: {
          /** App Store Connect identifier of the alternative distribution domain. */
          id: string;
          /** Base domain that serves the marketplace or web distribution app. */
          domain?: string | null;
          /** Display name chosen for the domain in App Store Connect. */
          referenceName?: string | null;
          /** When the domain was added, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Upload the public half of an alternative distribution key pair. Without appId the key applies to every alternative distribution app on the account; with appId it is tied to that marketplace or web distribution app. The private half never goes to Apple and signs the marketplace JWTs or install verification. */
    "app_store_connect.create_alternative_distribution_key": {
      input: {
        /**
         * PEM-encoded public key, including the BEGIN and END PUBLIC KEY lines.
         * @minLength 1
         * @pattern \S
         */
        publicKey: string;
        /**
         * App Store Connect identifier of the app to tie the key to. Leave empty for an account-wide key.
         * @minLength 1
         * @pattern \S
         */
        appId?: string;
      };
      output: {
        /** A public key registered for an alternative marketplace or web distribution app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        alternativeDistributionKey: {
          /** App Store Connect identifier of the alternative distribution key. */
          id: string;
          /** PEM-encoded public key that was uploaded. */
          publicKey?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Generate the alternative distribution package for an already approved App Store version. Versions approved after alternative distribution was enabled get their package automatically; use this for versions that were approved before. */
    "app_store_connect.create_alternative_distribution_package": {
      input: {
        /**
         * App Store Connect identifier of the approved App Store version to package.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** An alternative distribution package generated for an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        alternativeDistributionPackage: {
          /** App Store Connect identifier of the alternative distribution package. */
          id: string;
          /** Checksums of the source file the package was built from. */
          sourceFileChecksum?: {
            /** Checksum of the source file. */
            file?: {
              /** Hex-encoded checksum value. */
              hash?: string | null;
              /** Algorithm the checksum was computed with. */
              algorithm?: "MD5" | "SHA_256" | null;
              [key: string]: unknown;
            } | null;
            /** Composite checksum of the source file. */
            composite?: {
              /** Hex-encoded checksum value. */
              hash?: string | null;
              /** Algorithm the checksum was computed with. */
              algorithm?: "MD5" | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Ask App Store Connect to generate analytics reports for an app. ONGOING produces new daily, weekly, and monthly reports until the request is deleted or stops for inactivity; ONE_TIME_SNAPSHOT delivers historical data once. Reports appear asynchronously, so list them later. */
    "app_store_connect.create_analytics_report_request": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** How often reports are produced. */
        accessType: "ONE_TIME_SNAPSHOT" | "ONGOING";
      };
      output: {
        /** A request that makes App Store Connect generate analytics reports for one app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        analyticsReportRequest: {
          /** App Store Connect identifier for the report request. */
          id: string;
          /** ONGOING keeps producing daily, weekly, and monthly reports; ONE_TIME_SNAPSHOT delivers historical data once. */
          accessType?: "ONE_TIME_SNAPSHOT" | "ONGOING" | null;
          /** True once App Store Connect stopped the request because its reports were not read for a long time; create a new request to resume. */
          stoppedDueToInactivity?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Map an Android app to this app by its package name and signing certificate fingerprints, so Android users migrating to iPhone are offered this app. */
    "app_store_connect.create_android_to_ios_app_mapping_detail": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Android package name, such as com.example.app.
         * @minLength 1
         * @pattern \S
         */
        packageName: string;
        /**
         * SHA-256 fingerprints of the Android app signing key certificates.
         * @minItems 1
         */
        appSigningKeyPublicCertificateSha256Fingerprints: Array<string>;
      };
      output: {
        /** A mapping from an Android app to this iOS app, used for Android to iPhone migration. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        androidToIosAppMappingDetail: {
          /** App Store Connect identifier for the mapping detail. */
          id: string;
          /** Android package name of the mapped app. */
          packageName?: string | null;
          /** SHA-256 fingerprints of the Android app signing key certificates. */
          appSigningKeyPublicCertificateSha256Fingerprints?: Array<string> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set the territory availability of an app: whether it is offered in new territories automatically, and for each listed territory whether it is available, its release date and whether pre-orders are enabled. The new configuration replaces the current availability of the app in every territory, so list every territory the app should stay available in. */
    "app_store_connect.create_app_availability": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Automatically make the app available in territories Apple adds to the App Store later. */
        availableInNewTerritories: boolean;
        /**
         * Availability of the app per territory. Territories left out become unavailable.
         * @minItems 1
         */
        territoryAvailabilities: Array<{
          /**
           * Territory the entry applies to. ISO 3166-1 alpha-3 territory code, such as USA.
           * @minLength 1
           * @pattern \S
           */
          territoryId: string;
          /** Make the app available in the territory. */
          available: boolean;
          /**
           * Day the app is released in the territory, as YYYY-MM-DD. Required for a pre-order.
           * @format date
           */
          releaseDate?: string;
          /** Offer the app for pre-order in the territory until the release date. */
          preOrderEnabled?: boolean;
        }>;
      };
      output: {
        /** The territory availability configuration of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appAvailability: {
          /** App Store Connect identifier for the app availability. */
          id: string;
          /** Whether the app is automatically made available in territories Apple adds to the App Store later. */
          availableInNewTerritories?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create an advanced App Clip experience for an invocation URL with its localized card text and optional place. App Store Connect requires a header image reserved beforehand through the appClipAdvancedExperienceImages endpoint (a binary upload, available through the proxy); pass its identifier as headerImageId. */
    "app_store_connect.create_app_clip_advanced_experience": {
      input: {
        /**
         * App Store Connect identifier of the App Clip.
         * @minLength 1
         * @pattern \S
         */
        appClipId: string;
        /**
         * App Store Connect identifier of the reserved header image (appClipAdvancedExperienceImages record).
         * @minLength 1
         * @pattern \S
         */
        headerImageId: string;
        /**
         * Invocation URL of the experience.
         * @format uri
         */
        link: string;
        /** Default language of the experience. */
        defaultLanguage: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH";
        /** Whether the experience is submitted by a platform provider that serves multiple businesses. */
        isPoweredBy: boolean;
        /** Call-to-action verb shown on the App Clip card. */
        action?: "OPEN" | "VIEW" | "PLAY";
        /** Business category of the experience. */
        businessCategory?: "AUTOMOTIVE" | "BEAUTY" | "BIKES" | "BOOKS" | "CASINO" | "EDUCATION" | "EDUCATION_JAPAN" | "ENTERTAINMENT" | "EV_CHARGER" | "FINANCIAL_USD" | "FINANCIAL_CNY" | "FINANCIAL_GBP" | "FINANCIAL_JPY" | "FINANCIAL_EUR" | "FITNESS" | "FOOD_AND_DRINK" | "GAS" | "GROCERY" | "HEALTH_AND_MEDICAL" | "HOTEL_AND_TRAVEL" | "MUSIC" | "PARKING" | "PET_SERVICES" | "PROFESSIONAL_SERVICES" | "SHOPPING" | "TICKETING" | "TRANSIT";
        /** Physical place to tie the experience to, so Siri Suggestions and Apple Maps can offer it. */
        place?: {
          /** Apple Maps place identifier when the place is already known to Apple Maps. */
          placeId?: string;
          /** Names of the place. */
          names?: Array<string>;
          /** Main address of the place. */
          mainAddress?: {
            /** Full address as a single line. */
            fullAddress?: string;
            /** Address split into its parts. */
            structuredAddress?: {
              /** Street address lines. */
              streetAddress?: Array<string>;
              /** Floor within the building. */
              floor?: string;
              /** Neighborhood of the place. */
              neighborhood?: string;
              /** City or locality. */
              locality?: string;
              /** State or province. */
              stateProvince?: string;
              /** Postal code. */
              postalCode?: string;
              /** ISO 3166-1 alpha-2 country code, such as US. */
              countryCode?: string;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          };
          /** Where the pin is placed on the map. */
          displayPoint?: {
            /** Geographic coordinates of the pin. */
            coordinates?: {
              /** Latitude in decimal degrees. */
              latitude?: number;
              /** Longitude in decimal degrees. */
              longitude?: number;
              [key: string]: unknown;
            };
            /** Whether the pin was calculated from the address or placed manually. */
            source?: "CALCULATED" | "MANUALLY_PLACED";
            [key: string]: unknown;
          };
          /** Action offered on the place card in Apple Maps. */
          mapAction?: "BUY_TICKETS" | "VIEW_AVAILABILITY" | "VIEW_PRICING" | "HOTEL_BOOK_ROOM" | "PARKING_RESERVE_PARKING" | "RESTAURANT_JOIN_WAITLIST" | "RESTAURANT_ORDER_DELIVERY" | "RESTAURANT_ORDER_FOOD" | "RESTAURANT_ORDER_TAKEOUT" | "RESTAURANT_RESERVATION" | "SCHEDULE_APPOINTMENT" | "RESTAURANT_VIEW_MENU" | "THEATER_NOW_PLAYING" | "AIRLINE_BOOK_TRAVEL" | "AIRLINE_CHECK_IN" | "AIRLINE_FLIGHT_STATUS" | "APPLY" | "BOOK" | "BOOK_ACTIVITIES" | "BOOK_RIDES" | "BOOK_TEETIMES" | "BOOK_TOURS" | "CAREERS" | "CHARGE_EV" | "COUPONS" | "DONATE" | "EVENTS" | "EVENTS_SHOWS" | "EVENTS_SPORTS" | "GIFT_CARD" | "HOTEL_AMENITIES" | "JOIN" | "PARKING_AVAILABLE" | "RESTAURANT_PICKUP" | "RETAIL_SERVICE_QUOTE" | "RETAIL_STORE_DELIVERY" | "RETAIL_STORE_PICKUP" | "RETAIL_STORE_SHOP" | "SERVICES" | "SUPPORT" | "PAY_TO_PARK";
          /** Relationship between the submitter and the place. */
          relationship?: "OWNER" | "AUTHORIZED" | "OTHER";
          /** Phone number of the place. */
          phoneNumber?: {
            /** Phone number of the place. */
            number?: string;
            /** Kind of phone line. */
            type?: "FAX" | "LANDLINE" | "MOBILE" | "TOLLFREE";
            /** What the number is for, such as reservations. */
            intent?: string;
            [key: string]: unknown;
          };
          /** Website of the place. */
          homePage?: string;
          /** Apple Maps categories of the place. */
          categories?: Array<string>;
          [key: string]: unknown;
        };
        /**
         * Localized title and subtitle of the App Clip card, at least one entry.
         * @minItems 1
         */
        localizations: Array<{
          /** Language of this localization. */
          language: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH";
          /**
           * Title shown on the App Clip card in this language.
           * @minLength 1
           * @pattern \S
           */
          title?: string;
          /**
           * Subtitle shown on the App Clip card in this language.
           * @minLength 1
           * @pattern \S
           */
          subtitle?: string;
        }>;
      };
      output: {
        /** An advanced App Clip experience launched from a URL, QR code, NFC tag, or a place in Apple Maps. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipAdvancedExperience: {
          /** App Store Connect identifier for the advanced App Clip experience. */
          id: string;
          /** Invocation URL of the experience. */
          link?: string | null;
          /** Build version of the App Clip the experience belongs to. */
          version?: number | null;
          /** Whether users can invoke the experience (RECEIVED), it is deactivated, or its app is being transferred. */
          status?: "RECEIVED" | "DEACTIVATED" | "APP_TRANSFER_IN_PROGRESS" | null;
          /** Call-to-action verb shown on the App Clip card. */
          action?: "OPEN" | "VIEW" | "PLAY" | null;
          /** Whether the experience was submitted by a platform provider that serves multiple businesses. */
          isPoweredBy?: boolean | null;
          /** Physical place the experience is tied to, or null. */
          place?: {
            /** Apple Maps place identifier when the place is already known to Apple Maps. */
            placeId?: string;
            /** Names of the place. */
            names?: Array<string>;
            /** Main address of the place. */
            mainAddress?: {
              /** Full address as a single line. */
              fullAddress?: string;
              /** Address split into its parts. */
              structuredAddress?: {
                /** Street address lines. */
                streetAddress?: Array<string>;
                /** Floor within the building. */
                floor?: string;
                /** Neighborhood of the place. */
                neighborhood?: string;
                /** City or locality. */
                locality?: string;
                /** State or province. */
                stateProvince?: string;
                /** Postal code. */
                postalCode?: string;
                /** ISO 3166-1 alpha-2 country code, such as US. */
                countryCode?: string;
                [key: string]: unknown;
              };
              [key: string]: unknown;
            };
            /** Where the pin is placed on the map. */
            displayPoint?: {
              /** Geographic coordinates of the pin. */
              coordinates?: {
                /** Latitude in decimal degrees. */
                latitude?: number;
                /** Longitude in decimal degrees. */
                longitude?: number;
                [key: string]: unknown;
              };
              /** Whether the pin was calculated from the address or placed manually. */
              source?: "CALCULATED" | "MANUALLY_PLACED";
              [key: string]: unknown;
            };
            /** Action offered on the place card in Apple Maps. */
            mapAction?: "BUY_TICKETS" | "VIEW_AVAILABILITY" | "VIEW_PRICING" | "HOTEL_BOOK_ROOM" | "PARKING_RESERVE_PARKING" | "RESTAURANT_JOIN_WAITLIST" | "RESTAURANT_ORDER_DELIVERY" | "RESTAURANT_ORDER_FOOD" | "RESTAURANT_ORDER_TAKEOUT" | "RESTAURANT_RESERVATION" | "SCHEDULE_APPOINTMENT" | "RESTAURANT_VIEW_MENU" | "THEATER_NOW_PLAYING" | "AIRLINE_BOOK_TRAVEL" | "AIRLINE_CHECK_IN" | "AIRLINE_FLIGHT_STATUS" | "APPLY" | "BOOK" | "BOOK_ACTIVITIES" | "BOOK_RIDES" | "BOOK_TEETIMES" | "BOOK_TOURS" | "CAREERS" | "CHARGE_EV" | "COUPONS" | "DONATE" | "EVENTS" | "EVENTS_SHOWS" | "EVENTS_SPORTS" | "GIFT_CARD" | "HOTEL_AMENITIES" | "JOIN" | "PARKING_AVAILABLE" | "RESTAURANT_PICKUP" | "RETAIL_SERVICE_QUOTE" | "RETAIL_STORE_DELIVERY" | "RETAIL_STORE_PICKUP" | "RETAIL_STORE_SHOP" | "SERVICES" | "SUPPORT" | "PAY_TO_PARK";
            /** Relationship between the submitter and the place. */
            relationship?: "OWNER" | "AUTHORIZED" | "OTHER";
            /** Phone number of the place. */
            phoneNumber?: {
              /** Phone number of the place. */
              number?: string;
              /** Kind of phone line. */
              type?: "FAX" | "LANDLINE" | "MOBILE" | "TOLLFREE";
              /** What the number is for, such as reservations. */
              intent?: string;
              [key: string]: unknown;
            };
            /** Website of the place. */
            homePage?: string;
            /** Apple Maps categories of the place. */
            categories?: Array<string>;
            [key: string]: unknown;
          } | null;
          /** Whether Apple Maps matched the place to a point of interest. */
          placeStatus?: "PENDING" | "MATCHED" | "NO_MATCH" | null;
          /** Business category of the experience. */
          businessCategory?: "AUTOMOTIVE" | "BEAUTY" | "BIKES" | "BOOKS" | "CASINO" | "EDUCATION" | "EDUCATION_JAPAN" | "ENTERTAINMENT" | "EV_CHARGER" | "FINANCIAL_USD" | "FINANCIAL_CNY" | "FINANCIAL_GBP" | "FINANCIAL_JPY" | "FINANCIAL_EUR" | "FITNESS" | "FOOD_AND_DRINK" | "GAS" | "GROCERY" | "HEALTH_AND_MEDICAL" | "HOTEL_AND_TRAVEL" | "MUSIC" | "PARKING" | "PET_SERVICES" | "PROFESSIONAL_SERVICES" | "SHOPPING" | "TICKETING" | "TRANSIT" | null;
          /** Default language of the experience. */
          defaultLanguage?: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH" | null;
          /** Localized card text of the experience, empty when none was returned. */
          localizations: Array<{
            /** App Store Connect identifier for the localization. */
            id: string;
            /** Language of the localization. */
            language?: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH" | null;
            /** Title shown on the App Clip card. */
            title?: string | null;
            /** Subtitle shown on the App Clip card. */
            subtitle?: string | null;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        };
      };
    };
    /** Provide the invocation URLs App Review uses to test a default App Clip experience. Each experience has at most one review detail; use update_app_clip_app_store_review_detail to change it. */
    "app_store_connect.create_app_clip_app_store_review_detail": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceId: string;
        /** Invocation URLs App Review can use to launch the App Clip. */
        invocationUrls?: Array<string>;
      };
      output: {
        /** Invocation URLs App Review uses to test a default App Clip experience. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipAppStoreReviewDetail: {
          /** App Store Connect identifier for the App Clip App Store review detail. */
          id: string;
          /** Invocation URLs App Review can use to launch the App Clip. */
          invocationUrls?: Array<string> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a default experience for an App Clip, optionally attached to the App Store version it ships with or copied from an existing experience. */
    "app_store_connect.create_app_clip_default_experience": {
      input: {
        /**
         * App Store Connect identifier of the App Clip.
         * @minLength 1
         * @pattern \S
         */
        appClipId: string;
        /** Call-to-action verb shown on the App Clip card. */
        action?: "OPEN" | "VIEW" | "PLAY";
        /**
         * App Store Connect identifier of the App Store version to release the experience with.
         * @minLength 1
         * @pattern \S
         */
        releaseWithAppStoreVersionId?: string;
        /**
         * Identifier of an existing default experience to copy the settings and localizations from.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceTemplateId?: string;
      };
      output: {
        /** The default App Clip experience that ships with an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipDefaultExperience: {
          /** App Store Connect identifier for the default App Clip experience. */
          id: string;
          /** Call-to-action verb shown on the App Clip card. */
          action?: "OPEN" | "VIEW" | "PLAY" | null;
          /** App Store Connect identifier of the App Store version the experience is released with, or null when none is attached. */
          releaseWithAppStoreVersionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a locale to a default App Clip experience with the subtitle shown on the App Clip card in that locale. The header image is uploaded separately in App Store Connect. */
    "app_store_connect.create_app_clip_default_experience_localization": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceId: string;
        /**
         * App Store locale to add, such as de-DE.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Subtitle shown on the App Clip card in this locale.
         * @minLength 1
         * @pattern \S
         */
        subtitle?: string;
      };
      output: {
        /** Localized subtitle of a default App Clip experience for one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipDefaultExperienceLocalization: {
          /** App Store Connect identifier for the default App Clip experience localization. */
          id: string;
          /** Locale the subtitle is written in, such as en-US. */
          locale?: string | null;
          /** Subtitle shown on the App Clip card in this locale. */
          subtitle?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a custom product page for an app. Copy the metadata of an App Store version (appStoreVersionTemplateId) or of another custom product page (customProductPageTemplateId), or create the first page version inline with a deep link and localized promotional text. The page starts hidden until its first version passes App Review; use list_app_custom_product_page_versions to find the version to work on. */
    "app_store_connect.create_app_custom_product_page": {
      input: {
        /**
         * App Store Connect identifier of the app the page belongs to.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Internal name of the custom product page.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * App Store version whose metadata is copied into the first page version. Mutually exclusive with customProductPageTemplateId.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionTemplateId?: string;
        /**
         * Existing custom product page whose metadata is copied into the first page version. Mutually exclusive with appStoreVersionTemplateId.
         * @minLength 1
         * @pattern \S
         */
        customProductPageTemplateId?: string;
        /**
         * Deep link for the first page version, created inline together with the page.
         * @format uri
         */
        deepLink?: string;
        /**
         * Locales to create inline on the first page version, each with optional promotional text.
         * @minItems 1
         */
        localizations?: Array<{
          /**
           * App Store locale of the localization, such as en-US.
           * @minLength 1
           * @pattern \S
           */
          locale: string;
          /**
           * Promotional text shown above the description, up to 170 characters.
           * @minLength 1
           * @maxLength 170
           * @pattern \S
           */
          promotionalText?: string;
        }>;
      };
      output: {
        /** A custom product page of an app. Each page has its own versions that go through App Review and can be reached through a dedicated App Store URL. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appCustomProductPage: {
          /** App Store Connect identifier for the custom product page. */
          id: string;
          /** Internal name of the custom product page shown in App Store Connect. */
          name?: string | null;
          /** App Store URL that opens this custom product page. */
          url?: string | null;
          /** Whether the page is visible on the App Store. A hidden page keeps its URL but is not shown. */
          visible?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a locale to a custom product page version with optional promotional text. Screenshots and app previews for the locale are uploaded separately. App Store Connect rejects a locale the version already has. */
    "app_store_connect.create_app_custom_product_page_localization": {
      input: {
        /**
         * App Store Connect identifier of the custom product page version.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageVersionId: string;
        /**
         * App Store locale to add, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Promotional text shown above the description, up to 170 characters.
         * @minLength 1
         * @maxLength 170
         * @pattern \S
         */
        promotionalText?: string;
      };
      output: {
        /** The metadata of a custom product page version in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appCustomProductPageLocalization: {
          /** App Store Connect identifier for the custom product page localization. */
          id: string;
          /** Locale the metadata is written in, such as en-US. */
          locale?: string | null;
          /** Promotional text shown above the description on this custom product page. */
          promotionalText?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new editable version of a custom product page, for example to change it after the current version was approved. App Store Connect rejects a new version while the page already has one that is still being prepared or reviewed. */
    "app_store_connect.create_app_custom_product_page_version": {
      input: {
        /**
         * App Store Connect identifier of the custom product page.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageId: string;
        /**
         * Deep link opened when a user taps Get or Open on the custom product page.
         * @format uri
         */
        deepLink?: string;
      };
      output: {
        /** One version of a custom product page. A new version is created for each round of edits and App Review. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appCustomProductPageVersion: {
          /** App Store Connect identifier for the custom product page version. */
          id: string;
          /** Version number App Store Connect assigned to this page version. */
          version?: string | null;
          /** Review state of the page version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | null;
          /** Deep link opened when a user taps Get or Open on this custom product page, or null. */
          deepLink?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create an export compliance declaration for an app that uses non-exempt encryption. App Store Connect reviews it and assigns a compliance code; the declaration cannot be edited afterwards, only replaced by a new one. Supporting documents must be uploaded separately. */
    "app_store_connect.create_app_encryption_declaration": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Description of how the app uses encryption.
         * @minLength 1
         * @pattern \S
         */
        appDescription: string;
        /** Whether the app contains proprietary or non-standard cryptography. */
        containsProprietaryCryptography: boolean;
        /** Whether the app contains third-party cryptography. */
        containsThirdPartyCryptography: boolean;
        /** Whether the app will be distributed on the France storefront. */
        availableOnFrenchStore: boolean;
      };
      output: {
        /** An export compliance declaration describing the encryption an app uses. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appEncryptionDeclaration: {
          /** App Store Connect identifier for the app encryption declaration. */
          id: string;
          /** Description of how the app uses encryption. */
          appDescription?: string | null;
          /** When the declaration was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the encryption qualifies for an export exemption. */
          exempt?: boolean | null;
          /** Whether the app contains proprietary or non-standard cryptography. */
          containsProprietaryCryptography?: boolean | null;
          /** Whether the app contains third-party cryptography. */
          containsThirdPartyCryptography?: boolean | null;
          /** Whether the app may be distributed on the France storefront. */
          availableOnFrenchStore?: boolean | null;
          /** Content platform the declaration applies to. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Review state of the declaration. */
          appEncryptionDeclarationState?: "CREATED" | "IN_REVIEW" | "APPROVED" | "REJECTED" | "INVALID" | "EXPIRED" | null;
          /** Export compliance code App Store Connect assigned, to reuse in Info.plist or on upload. */
          codeValue?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a draft in-app event for an app. Add localizations with create_app_event_localization before submitting the event for review in App Store Connect. */
    "app_store_connect.create_app_event": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Internal reference name of the event, not shown on the App Store.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /**
         * Deep link opened in the app when a user taps the event.
         * @format uri
         */
        deepLink?: string;
        /** Badge that labels the kind of event. */
        badge?: "LIVE_EVENT" | "PREMIERE" | "CHALLENGE" | "COMPETITION" | "NEW_SEASON" | "MAJOR_UPDATE" | "SPECIAL_EVENT";
        /**
         * Purchase required to take part: NO_COST_ASSOCIATED or IN_APP_PURCHASE.
         * @minLength 1
         * @pattern \S
         */
        purchaseRequirement?: string;
        /**
         * Primary locale of the event metadata, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        primaryLocale?: string;
        /** Priority relative to the other events of the app. */
        priority?: "HIGH" | "NORMAL";
        /** Audience the event is meant for. */
        purpose?: "APPROPRIATE_FOR_ALL_USERS" | "ATTRACT_NEW_USERS" | "KEEP_ACTIVE_USERS_INFORMED" | "BRING_BACK_LAPSED_USERS";
        /** Territory schedules of the event. Each entry gives one publish, start, and end time for a group of territories; the whole list replaces the current schedules. */
        territorySchedules?: Array<{
          /**
           * ISO 3166-1 alpha-3 territories this schedule applies to.
           * @minItems 1
           */
          territories?: Array<string>;
          /**
           * When the event becomes visible on the App Store in these territories, as an ISO 8601 timestamp.
           * @format date-time
           */
          publishStart?: string;
          /**
           * When the event starts, as an ISO 8601 timestamp.
           * @format date-time
           */
          eventStart?: string;
          /**
           * When the event ends, as an ISO 8601 timestamp.
           * @format date-time
           */
          eventEnd?: string;
        }>;
      };
      output: {
        /** An in-app event shown on the App Store product page. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appEvent: {
          /** App Store Connect identifier for the in-app event. */
          id: string;
          /** Internal reference name of the event, not shown on the App Store. */
          referenceName?: string | null;
          /** Badge that labels the kind of event. */
          badge?: "LIVE_EVENT" | "PREMIERE" | "CHALLENGE" | "COMPETITION" | "NEW_SEASON" | "MAJOR_UPDATE" | "SPECIAL_EVENT" | null;
          /** Review and publication state of the event. */
          eventState?: "DRAFT" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "ACCEPTED" | "APPROVED" | "PUBLISHED" | "PAST" | "ARCHIVED" | null;
          /** Deep link opened in the app when a user taps the event. */
          deepLink?: string | null;
          /** Purchase required to take part, such as NO_COST_ASSOCIATED or IN_APP_PURCHASE. */
          purchaseRequirement?: string | null;
          /** Primary locale of the event metadata, such as en-US. */
          primaryLocale?: string | null;
          /** Priority relative to the other events of the app. */
          priority?: "HIGH" | "NORMAL" | null;
          /** Audience the event is meant for. */
          purpose?: "APPROPRIATE_FOR_ALL_USERS" | "ATTRACT_NEW_USERS" | "KEEP_ACTIVE_USERS_INFORMED" | "BRING_BACK_LAPSED_USERS" | null;
          /** Territory schedules of the event, or null when none are set. */
          territorySchedules?: Array<{
            /** ISO 3166-1 alpha-3 territories this schedule applies to. */
            territories?: Array<string> | null;
            /** When the event becomes visible on the App Store, as an ISO 8601 timestamp. */
            publishStart?: string | null;
            /** When the event starts, as an ISO 8601 timestamp. */
            eventStart?: string | null;
            /** When the event ends, as an ISO 8601 timestamp. */
            eventEnd?: string | null;
            [key: string]: unknown;
          }> | null;
          /** Schedules of past occurrences App Store Connect archived, or null when there are none. */
          archivedTerritorySchedules?: Array<{
            /** ISO 3166-1 alpha-3 territories this schedule applies to. */
            territories?: Array<string> | null;
            /** When the event becomes visible on the App Store, as an ISO 8601 timestamp. */
            publishStart?: string | null;
            /** When the event starts, as an ISO 8601 timestamp. */
            eventStart?: string | null;
            /** When the event ends, as an ISO 8601 timestamp. */
            eventEnd?: string | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a locale to an in-app event with the name and descriptions shown in that locale. App Store Connect rejects a locale the event already has. */
    "app_store_connect.create_app_event_localization": {
      input: {
        /**
         * App Store Connect identifier of the in-app event.
         * @minLength 1
         * @pattern \S
         */
        appEventId: string;
        /**
         * App Store locale to add, such as de-DE.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Event name shown on the App Store in this locale.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * Short description shown on the event card.
         * @minLength 1
         * @pattern \S
         */
        shortDescription?: string;
        /**
         * Long description shown on the event details page.
         * @minLength 1
         * @pattern \S
         */
        longDescription?: string;
      };
      output: {
        /** Localized name and descriptions of an in-app event for one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appEventLocalization: {
          /** App Store Connect identifier for the in-app event localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Event name shown on the App Store in this locale. */
          name?: string | null;
          /** Short description shown on the event card. */
          shortDescription?: string | null;
          /** Long description shown on the event details page. */
          longDescription?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a locale to an app info record with the app name and optional subtitle and privacy URLs shown in that locale. App Store Connect rejects a locale the record already has. */
    "app_store_connect.create_app_info_localization": {
      input: {
        /**
         * App Store Connect identifier of the app info record.
         * @minLength 1
         * @pattern \S
         */
        appInfoId: string;
        /**
         * App Store locale to add, such as de-DE.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * App name shown on the App Store in this locale.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Subtitle shown under the app name.
         * @minLength 1
         * @pattern \S
         */
        subtitle?: string;
        /**
         * Privacy policy URL for this locale.
         * @format uri
         */
        privacyPolicyUrl?: string;
        /**
         * URL where users can manage their privacy choices.
         * @format uri
         */
        privacyChoicesUrl?: string;
        /**
         * Privacy policy text shown on Apple TV.
         * @minLength 1
         * @pattern \S
         */
        privacyPolicyText?: string;
      };
      output: {
        /** Localized app-level App Store information for one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appInfoLocalization: {
          /** App Store Connect identifier for the app info localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** App name shown on the App Store in this locale. */
          name?: string | null;
          /** Subtitle shown under the app name in this locale. */
          subtitle?: string | null;
          /** Privacy policy URL published for this locale. */
          privacyPolicyUrl?: string | null;
          /** URL where users can manage their privacy choices, published for this locale. */
          privacyChoicesUrl?: string | null;
          /** Privacy policy text shown on Apple TV, where a URL cannot be opened. */
          privacyPolicyText?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set the price schedule of an app: the base territory plus the manual prices, each a price point (which already encodes its territory) with an optional date range. App Store Connect derives the prices of every other territory from the base territory price. The new schedule replaces the current prices and any scheduled price changes of the app. Find price point identifiers with list_app_price_points. */
    "app_store_connect.create_app_price_schedule": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Territory whose price the automatic prices of every other territory are derived from. ISO 3166-1 alpha-3 territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        baseTerritoryId: string;
        /**
         * Manual prices to schedule. Include the price for the base territory; add further entries for territories that should not follow the automatic price, and several entries for one territory to schedule a price change.
         * @minItems 1
         */
        manualPrices: Array<{
          /**
           * App Store Connect identifier of the price point, from list_app_price_points.
           * @minLength 1
           * @pattern \S
           */
          appPricePointId: string;
          /**
           * Day the price starts applying, as YYYY-MM-DD. Omit for the price that takes effect immediately.
           * @format date
           */
          startDate?: string;
          /**
           * Day the price stops applying, as YYYY-MM-DD, which is the start day of the next scheduled price. Omit for a price with no end.
           * @format date
           */
          endDate?: string;
        }>;
      };
      output: {
        /** The price schedule of an app: the base territory plus the manual and automatic prices App Store Connect derives from it. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appPriceSchedule: {
          /** App Store Connect identifier for the price schedule. */
          id: string;
          /** Territory whose price the automatic prices of every other territory are derived from, such as USA. */
          baseTerritoryId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Enter the App Review contact details, demo account, and notes for an App Store version that has none yet. */
    "app_store_connect.create_app_store_review_detail": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
        /**
         * First name of the person App Review may contact.
         * @minLength 1
         * @pattern \S
         */
        contactFirstName?: string;
        /**
         * Last name of the person App Review may contact.
         * @minLength 1
         * @pattern \S
         */
        contactLastName?: string;
        /**
         * Phone number App Review may call, including country code.
         * @minLength 1
         * @pattern \S
         */
        contactPhone?: string;
        /**
         * Email address App Review may write to.
         * @format email
         */
        contactEmail?: string;
        /**
         * Username of a demo account App Review can sign in with.
         * @minLength 1
         * @pattern \S
         */
        demoAccountName?: string;
        /**
         * Password of the demo account.
         * @minLength 1
         * @pattern \S
         */
        demoAccountPassword?: string;
        /** Whether App Review needs a demo account to sign in. */
        demoAccountRequired?: boolean;
        /**
         * Additional notes for the App Review team.
         * @minLength 1
         * @pattern \S
         */
        notes?: string;
      };
      output: {
        /** The App Review contact and demo account information of an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreReviewDetail: {
          /** App Store Connect identifier for the review detail. */
          id: string;
          /** First name of the person App Review may contact. */
          contactFirstName?: string | null;
          /** Last name of the person App Review may contact. */
          contactLastName?: string | null;
          /** Phone number App Review may call. */
          contactPhone?: string | null;
          /** Email address App Review may write to. */
          contactEmail?: string | null;
          /** Username of the demo account provided to App Review. */
          demoAccountName?: string | null;
          /** Password of the demo account provided to App Review. */
          demoAccountPassword?: string | null;
          /** Whether App Review needs a demo account to sign in. */
          demoAccountRequired?: boolean | null;
          /** Additional notes for the App Review team. */
          notes?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new App Store version for an app on one platform. The version starts in PREPARE_FOR_SUBMISSION and can be deleted again until it is submitted for review. */
    "app_store_connect.create_app_store_version": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Content platform the version is for. */
        platform: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
        /**
         * Version string shown on the App Store, such as 1.4.0.
         * @minLength 1
         * @pattern \S
         */
        versionString: string;
        /**
         * Copyright line published with the version.
         * @minLength 1
         * @pattern \S
         */
        copyright?: string;
        /** Review track for the version: APP_STORE for App Review, NOTARIZATION for Mac apps distributed outside the App Store. */
        reviewType?: "APP_STORE" | "NOTARIZATION";
        /** How the version is released after approval: MANUAL waits for release_app_store_version, AFTER_APPROVAL releases immediately, SCHEDULED releases at earliestReleaseDate. */
        releaseType?: "MANUAL" | "AFTER_APPROVAL" | "SCHEDULED";
        /**
         * Earliest release time for a SCHEDULED release, as an ISO 8601 timestamp.
         * @format date-time
         */
        earliestReleaseDate?: string;
        /**
         * App Store Connect identifier of the build to attach to the version.
         * @minLength 1
         * @pattern \S
         */
        buildId?: string;
      };
      output: {
        /** An App Store version of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersion: {
          /** App Store Connect identifier for the version. */
          id: string;
          /** Content platform the version targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Version string shown on the App Store, such as 1.4.0. */
          versionString?: string | null;
          /** Current review and release state. */
          appVersionState?: "ACCEPTED" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "INVALID_BINARY" | "METADATA_REJECTED" | "PENDING_APPLE_RELEASE" | "PENDING_DEVELOPER_RELEASE" | "PREPARE_FOR_SUBMISSION" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "READY_FOR_REVIEW" | "REJECTED" | "REPLACED_WITH_NEW_VERSION" | "WAITING_FOR_EXPORT_COMPLIANCE" | "WAITING_FOR_REVIEW" | null;
          /** Copyright line published with the version. */
          copyright?: string | null;
          /** Review track the version goes through. */
          reviewType?: "APP_STORE" | "NOTARIZATION" | null;
          /** Release behavior after approval. */
          releaseType?: "MANUAL" | "AFTER_APPROVAL" | "SCHEDULED" | null;
          /** Earliest scheduled release time, as an ISO 8601 timestamp. */
          earliestReleaseDate?: string | null;
          /** Whether the version is downloadable. */
          downloadable?: boolean | null;
          /** When the version record was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a product page optimization test for an app on one platform. The test starts in PREPARE_FOR_SUBMISSION; add treatments with create_app_store_version_experiment_treatment, then start it with update_app_store_version_experiment once its treatments are approved. */
    "app_store_connect.create_app_store_version_experiment": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Test name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /** Content platform the test runs on. */
        platform: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
        /** Percentage of App Store traffic directed to the treatments instead of the default product page. */
        trafficProportion: number;
      };
      output: {
        /** A product page optimization test (App Store version experiment v2) that splits App Store traffic between the default product page and its treatments. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionExperiment: {
          /** App Store Connect identifier for the experiment. */
          id: string;
          /** Test name shown in App Store Connect. */
          name?: string | null;
          /** Content platform the test runs on. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Percentage of App Store traffic directed to the treatments instead of the default product page. */
          trafficProportion?: number | null;
          /** Review and run state of the test. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REJECTED" | "COMPLETED" | "STOPPED" | null;
          /** Whether the treatments must pass App Review before the test can start. */
          reviewRequired?: boolean | null;
          /** When the test started, as an ISO 8601 timestamp. */
          startDate?: string | null;
          /** When the test ended or was stopped, as an ISO 8601 timestamp. */
          endDate?: string | null;
          /** App Store Connect identifier of the App Store version currently serving as the control (the default product page), or null when App Store Connect did not return it. */
          latestControlVersionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a treatment to a product page optimization test, optionally using an alternate app icon bundled with the app. Add locales to the treatment with create_app_store_version_experiment_treatment_localization before uploading its screenshots. */
    "app_store_connect.create_app_store_version_experiment_treatment": {
      input: {
        /**
         * App Store Connect identifier of the product page optimization test.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentId: string;
        /**
         * Treatment name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Name of an alternate app icon asset included in the build, shown instead of the default icon.
         * @minLength 1
         * @pattern \S
         */
        appIconName?: string;
      };
      output: {
        /** One treatment of a product page optimization test: an alternative product page shown to part of the traffic. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionExperimentTreatment: {
          /** App Store Connect identifier for the treatment. */
          id: string;
          /** Treatment name shown in App Store Connect. */
          name?: string | null;
          /** Name of the alternate app icon asset the treatment uses, or null for the default icon. */
          appIconName?: string | null;
          /** Template URL and pixel size of the treatment app icon. */
          appIcon?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Icon width in pixels. */
            width?: number;
            /** Icon height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** When the treatment was promoted to the default product page, as an ISO 8601 timestamp, or null. */
          promotedDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a locale to a treatment so screenshots and app previews can be uploaded for it. App Store Connect rejects a locale the treatment already has. */
    "app_store_connect.create_app_store_version_experiment_treatment_localization": {
      input: {
        /**
         * App Store Connect identifier of the treatment.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentTreatmentId: string;
        /**
         * App Store locale to add, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
      };
      output: {
        /** A locale of a treatment. Screenshots and app previews for the treatment are attached to this localization. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionExperimentTreatmentLocalization: {
          /** App Store Connect identifier for the treatment localization. */
          id: string;
          /** Locale the treatment metadata is shown in, such as en-US. */
          locale?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add App Store metadata for one locale to an App Store version. The locale must be enabled for the app and must not already have a localization on this version. */
    "app_store_connect.create_app_store_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
        /**
         * App Store locale the metadata is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * App description shown on the product page, up to 4,000 characters.
         * @minLength 1
         * @maxLength 4000
         * @pattern \S
         */
        description?: string;
        /**
         * Comma-separated App Store search keywords, up to 100 characters in total.
         * @minLength 1
         * @maxLength 100
         * @pattern \S
         */
        keywords?: string;
        /**
         * Marketing website URL shown on the product page.
         * @format uri
         */
        marketingUrl?: string;
        /**
         * Promotional text shown above the description, up to 170 characters.
         * @minLength 1
         * @maxLength 170
         * @pattern \S
         */
        promotionalText?: string;
        /**
         * Support website URL shown on the product page.
         * @format uri
         */
        supportUrl?: string;
        /**
         * Release notes for this version, up to 4,000 characters.
         * @minLength 1
         * @maxLength 4000
         * @pattern \S
         */
        whatsNew?: string;
      };
      output: {
        /** The App Store metadata of one version in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionLocalization: {
          /** App Store Connect identifier for the version localization. */
          id: string;
          /** Locale the metadata is written in, such as en-US. */
          locale?: string | null;
          /** App description shown on the product page. */
          description?: string | null;
          /** Comma-separated App Store search keywords. */
          keywords?: string | null;
          /** Marketing website URL shown on the product page. */
          marketingUrl?: string | null;
          /** Promotional text shown above the description, editable without a new version. */
          promotionalText?: string | null;
          /** Support website URL shown on the product page. */
          supportUrl?: string | null;
          /** Release notes shown for this version. */
          whatsNew?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Enable phased release for an App Store version so an approved update rolls out to automatic-update users over seven days. Can be removed again with delete_app_store_version_phased_release before the version is released. */
    "app_store_connect.create_app_store_version_phased_release": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
        /** Initial rollout state. Defaults to INACTIVE, which starts the rollout automatically once the version is released. */
        phasedReleaseState?: "INACTIVE" | "ACTIVE" | "PAUSED" | "COMPLETE";
      };
      output: {
        /** The phased release configuration of an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionPhasedRelease: {
          /** App Store Connect identifier for the phased release. */
          id: string;
          /** Whether the seven day phased rollout is inactive, active, paused, or complete. */
          phasedReleaseState?: "INACTIVE" | "ACTIVE" | "PAUSED" | "COMPLETE" | null;
          /** When the phased release started, as an ISO 8601 timestamp. */
          startDate?: string | null;
          /** Total number of days the rollout has been paused. */
          totalPauseDuration?: number | null;
          /** Current day of the seven day rollout, starting at 1. */
          currentDayNumber?: number | null;
          [key: string]: unknown;
        };
      };
    };
    /** Promote the winning treatment of a product page optimization test to the default product page: its screenshots, previews, and app icon are applied to the given App Store version, replacing that version's current product page assets. Use list_app_store_version_experiments to find the control version (latestControlVersionId) the test ran against. */
    "app_store_connect.create_app_store_version_promotion": {
      input: {
        /**
         * App Store Connect identifier of the App Store version whose default product page is replaced.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
        /**
         * App Store Connect identifier of the treatment to promote.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentTreatmentId: string;
      };
      output: {
        /** The promotion record App Store Connect created when a treatment became the default product page. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionPromotion: {
          /** App Store Connect identifier for the promotion. */
          id: string;
          [key: string]: unknown;
        };
      };
    };
    /** Create an Apple-hosted background asset pack record for an app. The record starts without versions; create a version next, then upload and commit the asset pack archive through the generic proxy or Xcode. */
    "app_store_connect.create_background_asset": {
      input: {
        /**
         * App Store Connect identifier of the app the asset pack belongs to.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Identifier the app uses to request this asset pack. It must match the identifier in the asset pack manifest.
         * @minLength 1
         * @pattern \S
         */
        assetPackIdentifier: string;
      };
      output: {
        /** An Apple-hosted background asset pack of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        backgroundAsset: {
          /** App Store Connect identifier of the background asset pack. */
          id: string;
          /** Asset pack identifier the app uses to request the pack, chosen when the pack was created. */
          assetPackIdentifier?: string | null;
          /** Whether the asset pack has been archived. */
          archived?: boolean | null;
          /** When the asset pack record was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Storage in bytes the asset pack occupies. */
          usedBytes?: number | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          appStoreVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          internalBetaVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          externalBetaVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new version record for a background asset pack. App Store Connect assigns the next version number automatically; the version stays in AWAITING_UPLOAD until the asset pack archive is uploaded and committed, which needs the binary upload endpoints outside these actions. */
    "app_store_connect.create_background_asset_version": {
      input: {
        /**
         * App Store Connect identifier of the background asset pack.
         * @minLength 1
         * @pattern \S
         */
        backgroundAssetId: string;
      };
      output: {
        /** One version of an Apple-hosted background asset pack. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        backgroundAssetVersion: {
          /** App Store Connect identifier of the background asset version. */
          id: string;
          /** Version number App Store Connect assigned when the version was created; it increases automatically. */
          version?: string | null;
          /** Upload and processing state of the version. AWAITING_UPLOAD until the asset pack archive is uploaded and committed. */
          state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
          /** Validation errors, warnings, and informational messages from processing. */
          stateDetails?: {
            /** Errors that stopped processing. */
            errors?: Array<{
              /** Machine-readable code of the message. */
              code?: string | null;
              /** Human-readable text of the message. */
              description?: string | null;
              [key: string]: unknown;
            }>;
            /** Warnings raised during processing. */
            warnings?: Array<{
              /** Machine-readable code of the message. */
              code?: string | null;
              /** Human-readable text of the message. */
              description?: string | null;
              [key: string]: unknown;
            }>;
            /** Informational messages from processing. */
            infos?: Array<{
              /** Machine-readable code of the message. */
              code?: string | null;
              /** Human-readable text of the message. */
              description?: string | null;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          } | null;
          /** Locale declared in the asset pack manifest, when the pack is localized. */
          locale?: string | null;
          /** Platforms the asset pack version targets, taken from the uploaded manifest. */
          platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
          /** When the version was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** The asset pack the version belongs to, or null when App Store Connect did not return it. */
          backgroundAsset: {
            /** App Store Connect identifier of the background asset pack. */
            id: string;
            /** Asset pack identifier the app uses to request the pack. */
            assetPackIdentifier?: string | null;
            [key: string]: unknown;
          } | null;
          /** The internal TestFlight release of this version. Null when the version has no such release yet or App Store Connect did not return it. */
          internalBetaRelease: {
            /** App Store Connect identifier of the release record. */
            id: string;
            /** State of the release. */
            state?: "READY_FOR_TESTING" | "SUPERSEDED" | null;
            [key: string]: unknown;
          } | null;
          /** The external TestFlight release of this version. Null when the version has no such release yet or App Store Connect did not return it. */
          externalBetaRelease: {
            /** App Store Connect identifier of the release record. */
            id: string;
            /** State of the release. */
            state?: "READY_FOR_BETA_SUBMISSION" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "PROCESSING_FOR_TESTING" | "READY_FOR_TESTING" | "SUPERSEDED" | null;
            [key: string]: unknown;
          } | null;
          /** The App Store release of this version. Null when the version has no such release yet or App Store Connect did not return it. */
          appStoreRelease: {
            /** App Store Connect identifier of the release record. */
            id: string;
            /** State of the release. */
            state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "REJECTED" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "SUPERSEDED" | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add an App Clip experience that testers launch from the TestFlight app, creating its localized titles in the same request. At least one localization is required. */
    "app_store_connect.create_beta_app_clip_invocation": {
      input: {
        /**
         * App Store Connect identifier of the App Clip build bundle the invocation belongs to.
         * @minLength 1
         * @pattern \S
         */
        buildBundleId: string;
        /**
         * Invocation URL the App Clip is launched with, such as https://example.com/clip.
         * @format uri
         */
        url: string;
        /**
         * Localized titles to create with the invocation, one per locale.
         * @minItems 1
         */
        betaAppClipInvocationLocalizations: Array<{
          /**
           * Title testers see for the invocation in this locale.
           * @minLength 1
           * @pattern \S
           */
          title: string;
          /**
           * Locale the title is written in, such as en-US.
           * @minLength 1
           * @pattern \S
           */
          locale: string;
        }>;
      };
      output: {
        /** An App Clip experience testers can launch from the TestFlight app, with its localized titles. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppClipInvocation: {
          /** App Store Connect identifier for the App Clip invocation. */
          id: string;
          /** Invocation URL the App Clip is launched with from TestFlight. */
          url?: string | null;
          /** Localized titles App Store Connect returned alongside the invocation. */
          betaAppClipInvocationLocalizations: Array<{
            /** App Store Connect identifier for the localization. */
            id: string;
            /** Title testers see for the App Clip invocation in this locale. */
            title?: string | null;
            /** Locale the title is written in, such as en-US. */
            locale?: string | null;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        };
      };
    };
    /** Add a localized title in one more locale to an existing TestFlight App Clip invocation. */
    "app_store_connect.create_beta_app_clip_invocation_localization": {
      input: {
        /**
         * App Store Connect identifier of the App Clip invocation.
         * @minLength 1
         * @pattern \S
         */
        betaAppClipInvocationId: string;
        /**
         * Title testers see for the invocation in this locale.
         * @minLength 1
         * @pattern \S
         */
        title: string;
        /**
         * Locale the title is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
      };
      output: {
        /** A localized title for a TestFlight App Clip invocation. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppClipInvocationLocalization: {
          /** App Store Connect identifier for the localization. */
          id: string;
          /** Title testers see for the App Clip invocation in this locale. */
          title?: string | null;
          /** Locale the title is written in, such as en-US. */
          locale?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create the TestFlight test information of an app for a locale that has none yet: the description, feedback email, and URLs testers see in TestFlight. */
    "app_store_connect.create_beta_app_localization": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * TestFlight locale the test information is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Description of the app shown to testers in TestFlight.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
        /**
         * Email address testers send feedback to.
         * @format email
         */
        feedbackEmail?: string;
        /**
         * Marketing URL shown to testers.
         * @format uri
         */
        marketingUrl?: string;
        /**
         * Privacy policy URL shown to testers.
         * @format uri
         */
        privacyPolicyUrl?: string;
        /**
         * Privacy policy text shown to tvOS testers.
         * @minLength 1
         * @pattern \S
         */
        tvOsPrivacyPolicy?: string;
      };
      output: {
        /** TestFlight test information of an app in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppLocalization: {
          /** App Store Connect identifier for the beta app localization. */
          id: string;
          /** Locale the test information is written in, such as en-US. */
          locale?: string | null;
          /** Description of the app shown to testers in TestFlight. */
          description?: string | null;
          /** Email address testers send feedback to. */
          feedbackEmail?: string | null;
          /** Marketing URL shown to testers. */
          marketingUrl?: string | null;
          /** Privacy policy URL shown to testers. */
          privacyPolicyUrl?: string | null;
          /** Privacy policy text shown to tvOS testers. */
          tvOsPrivacyPolicy?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create the "What to Test" notes of one build for a locale that has none yet. App Store Connect rejects a second record for the same locale; use update_build_test_notes to change existing notes. */
    "app_store_connect.create_beta_build_localization": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /**
         * TestFlight locale the notes are written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Test notes shown to testers in that locale.
         * @minLength 1
         * @pattern \S
         */
        whatsNew?: string;
      };
      output: {
        /** The "What to Test" notes of one build in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaBuildLocalization: {
          /** App Store Connect identifier for the beta build localization. */
          id: string;
          /** Locale the test notes belong to, such as en-US. */
          locale?: string | null;
          /** Test notes shown to testers for this locale. */
          whatsNew?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a TestFlight group for an app, optionally enabling its public invitation link. */
    "app_store_connect.create_beta_group": {
      input: {
        /**
         * App Store Connect identifier of the app the group belongs to.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Group name shown in TestFlight.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /** Enable a public TestFlight invitation link for the group. */
        publicLinkEnabled?: boolean;
        /** Enforce a maximum number of testers joining through the public link. */
        publicLinkLimitEnabled?: boolean;
        /**
         * Maximum number of testers allowed to join through the public link, from 1 to 10,000.
         * @minimum 1
         * @maximum 10000
         */
        publicLinkLimit?: number;
        /** Let testers send feedback from TestFlight. */
        feedbackEnabled?: boolean;
        /** Automatically give the group every new build of the app. */
        hasAccessToAllBuilds?: boolean;
      };
      output: {
        /** A TestFlight beta group. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaGroup: {
          /** App Store Connect identifier for the beta group. */
          id: string;
          /** Group name shown in TestFlight. */
          name?: string | null;
          /** When the group was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the group is an internal group of team members. */
          isInternalGroup?: boolean | null;
          /** Whether the group automatically receives every new build. */
          hasAccessToAllBuilds?: boolean | null;
          /** Whether a public TestFlight link is enabled for the group. */
          publicLinkEnabled?: boolean | null;
          /** Identifier segment of the public TestFlight link. */
          publicLinkId?: string | null;
          /** Full public TestFlight invitation link. */
          publicLink?: string | null;
          /** Whether the public link enforces a tester limit. */
          publicLinkLimitEnabled?: boolean | null;
          /** Maximum number of testers who may join through the public link. */
          publicLinkLimit?: number | null;
          /** Whether testers can send feedback from TestFlight. */
          feedbackEnabled?: boolean | null;
          /** Whether iOS builds are offered to Apple silicon Macs. */
          iosBuildsAvailableForAppleSiliconMac?: boolean | null;
          /** Whether iOS builds are offered to Apple Vision Pro. */
          iosBuildsAvailableForAppleVision?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set recruitment criteria on a TestFlight group so only testers on the listed device families and OS versions can join through its public link. A group can hold one set of criteria; update or delete it afterwards. */
    "app_store_connect.create_beta_recruitment_criterion": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
        /**
         * Device families and OS version ranges a tester must match to join through the public link.
         * @minItems 1
         */
        deviceFamilyOsVersionFilters: Array<{
          /** Device family the tester must use. */
          deviceFamily: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION";
          /**
           * Lowest OS version the device may run, inclusive, such as 17.0.
           * @minLength 1
           * @pattern \S
           */
          minimumOsInclusive?: string;
          /**
           * Highest OS version the device may run, inclusive, such as 18.4.
           * @minLength 1
           * @pattern \S
           */
          maximumOsInclusive?: string;
        }>;
      };
      output: {
        /** Recruitment criteria that limit who can join a TestFlight group through its public link. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaRecruitmentCriterion: {
          /** App Store Connect identifier for the recruitment criteria. */
          id: string;
          /** When the criteria were last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** Device families and OS version ranges a tester must match. */
          deviceFamilyOsVersionFilters?: Array<{
            /** Device family the tester must use. */
            deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION";
            /** Lowest OS version allowed, inclusive. */
            minimumOsInclusive?: string | null;
            /** Highest OS version allowed, inclusive. */
            maximumOsInclusive?: string | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Invite a TestFlight tester by email. App Store Connect only creates a tester that is assigned to something, so pass at least one of betaGroupIds or buildIds. */
    "app_store_connect.create_beta_tester": {
      input: {
        /**
         * Email address the TestFlight invitation is sent to.
         * @format email
         */
        email: string;
        /**
         * Tester first name.
         * @minLength 1
         * @pattern \S
         */
        firstName?: string;
        /**
         * Tester last name.
         * @minLength 1
         * @pattern \S
         */
        lastName?: string;
        /**
         * TestFlight groups to add the tester to. Required unless buildIds is given.
         * @minItems 1
         */
        betaGroupIds?: Array<string>;
        /**
         * Builds to assign to the tester individually. Required unless betaGroupIds is given.
         * @minItems 1
         */
        buildIds?: Array<string>;
      };
      output: {
        /** A TestFlight beta tester. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaTester: {
          /** App Store Connect identifier for the beta tester. */
          id: string;
          /** Email address the invitation was sent to. */
          email?: string | null;
          /** Tester first name. */
          firstName?: string | null;
          /** Tester last name. */
          lastName?: string | null;
          /** How the tester was invited. */
          inviteType?: "EMAIL" | "PUBLIC_LINK" | null;
          /** Where the tester stands in the invitation flow. */
          state?: "NOT_INVITED" | "INVITED" | "ACCEPTED" | "INSTALLED" | "REVOKED" | null;
          /** Devices the tester has installed the app on. */
          appDevices?: Array<{
            /** Device model name. */
            model?: string;
            /** Platform of the device. */
            platform?: "IOS" | "MAC_OS" | "TV_OS" | "WATCH_OS" | "VISION_OS";
            /** Operating system version on the device. */
            osVersion?: string;
            /** Build number installed on the device. */
            appBuildVersion?: string;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Register a new bundle ID (App ID) with the team. Capabilities are added afterwards with enable_bundle_id_capability. */
    "app_store_connect.create_bundle_id": {
      input: {
        /**
         * Reverse-DNS bundle identifier, such as com.example.app. A wildcard identifier ends in an asterisk, such as com.example.*.
         * @minLength 1
         * @pattern \S
         */
        identifier: string;
        /**
         * Display name for the bundle ID.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /** Platform to register the bundle ID for. */
        platform: "IOS" | "MAC_OS" | "UNIVERSAL";
        /**
         * Seed ID (team ID prefix) to register the bundle ID under. Leave out to use the team default.
         * @minLength 1
         * @pattern \S
         */
        seedId?: string;
      };
      output: {
        /** A bundle ID (App ID) registered with the team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        bundleId: {
          /** App Store Connect identifier of the bundle ID record. */
          id: string;
          /** Display name given to the bundle ID when it was registered. */
          name?: string | null;
          /** Platform the bundle ID is registered for. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Reverse-DNS bundle identifier string, such as com.example.app, or a wildcard ending in *. */
          identifier?: string | null;
          /** Team ID prefix (seed ID) associated with the bundle ID. */
          seedId?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Issue a new signing certificate from a certificate signing request (CSR). Apple Pay and pass type certificates also take the merchant ID or pass type ID they belong to. Developer ID certificates for macOS can only be created in the Apple Developer website or Xcode, and Apple caps how many certificates of each type a team may hold. */
    "app_store_connect.create_certificate": {
      input: {
        /**
         * Certificate signing request in PEM format, as produced by Keychain Access or openssl, including the BEGIN and END lines.
         * @minLength 1
         * @pattern \S
         */
        csrContent: string;
        /** Kind of certificate to issue. */
        certificateType: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC";
        /**
         * App Store Connect identifier of the merchant ID record (the opaque resource id, not the merchant.com.example identifier string). Required for Apple Pay merchant certificates.
         * @minLength 1
         * @pattern \S
         */
        merchantIdId?: string;
        /**
         * App Store Connect identifier of the pass type ID record (the opaque resource id, not the pass.com.example identifier string). Required for pass type ID certificates.
         * @minLength 1
         * @pattern \S
         */
        passTypeIdId?: string;
      };
      output: {
        /** A signing certificate issued by Apple. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        certificate: {
          /** App Store Connect identifier of the certificate. */
          id: string;
          /** Certificate name. */
          name?: string | null;
          /** Kind of certificate. */
          certificateType?: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC" | null;
          /** Certificate display name shown in the developer portal. */
          displayName?: string | null;
          /** Serial number of the certificate. */
          serialNumber?: string | null;
          /** Platform the certificate applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** When the certificate expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          /** Base64-encoded DER certificate. Decode it to a .cer file to install it in a keychain. */
          certificateContent?: string | null;
          /** Whether the certificate is activated. Only Apple Pay payment processing certificates carry an activation state. */
          activated?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create an Xcode Cloud workflow for a product. Give it at least one action and, for automatic builds, one or more start conditions; without start conditions it can only be started manually. */
    "app_store_connect.create_ci_workflow": {
      input: {
        /**
         * App Store Connect identifier of the Xcode Cloud product the workflow belongs to.
         * @minLength 1
         * @pattern \S
         */
        ciProductId: string;
        /**
         * App Store Connect identifier of the repository the workflow builds from.
         * @minLength 1
         * @pattern \S
         */
        scmRepositoryId: string;
        /**
         * App Store Connect identifier of the Xcode version to build with. Use list_ci_xcode_versions to find one.
         * @minLength 1
         * @pattern \S
         */
        ciXcodeVersionId: string;
        /**
         * App Store Connect identifier of the macOS version to build on. Use list_ci_mac_os_versions to find one compatible with the Xcode version.
         * @minLength 1
         * @pattern \S
         */
        ciMacOsVersionId: string;
        /**
         * Workflow name shown in App Store Connect and Xcode.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /** Description shown under the workflow name. */
        description: string;
        /** Start a build when a matching branch changes. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchstartcondition */
        branchStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
          filesAndFoldersRule?: {
            /** Whether matching files start a build or prevent one. */
            mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
            /** Files and folders the rule matches. */
            matchers?: Array<{
              /** Directory path relative to the repository root. */
              directory?: string;
              /** File extension without the leading dot. */
              fileExtension?: string;
              /** Exact file name. */
              fileName?: string;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Cancel builds that are still running for the same source when a newer change arrives. */
          autoCancel?: boolean;
          [key: string]: unknown;
        };
        /** Start a build when a matching tag is created. See https://developer.apple.com/documentation/appstoreconnectapi/citagstartcondition */
        tagStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
          filesAndFoldersRule?: {
            /** Whether matching files start a build or prevent one. */
            mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
            /** Files and folders the rule matches. */
            matchers?: Array<{
              /** Directory path relative to the repository root. */
              directory?: string;
              /** File extension without the leading dot. */
              fileExtension?: string;
              /** Exact file name. */
              fileName?: string;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Cancel builds that are still running for the same source when a newer change arrives. */
          autoCancel?: boolean;
          [key: string]: unknown;
        };
        /** Start a build when a matching pull request is opened or updated. See https://developer.apple.com/documentation/appstoreconnectapi/cipullrequeststartcondition */
        pullRequestStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          destination?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
          filesAndFoldersRule?: {
            /** Whether matching files start a build or prevent one. */
            mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
            /** Files and folders the rule matches. */
            matchers?: Array<{
              /** Directory path relative to the repository root. */
              directory?: string;
              /** File extension without the leading dot. */
              fileExtension?: string;
              /** Exact file name. */
              fileName?: string;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Cancel builds that are still running for the same source when a newer change arrives. */
          autoCancel?: boolean;
          [key: string]: unknown;
        };
        /** Start builds on a schedule. See https://developer.apple.com/documentation/appstoreconnectapi/cischeduledstartcondition */
        scheduledStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** When the schedule fires. */
          schedule?: {
            /** How often the build starts. */
            frequency?: "WEEKLY" | "DAILY" | "HOURLY";
            /** Days of the week for a WEEKLY schedule. */
            days?: Array<"SUNDAY" | "MONDAY" | "TUESDAY" | "WEDNESDAY" | "THURSDAY" | "FRIDAY" | "SATURDAY">;
            /** Hour of the day the build starts. */
            hour?: number;
            /** Minute of the hour the build starts. */
            minute?: number;
            /** IANA time zone name, such as America/Los_Angeles. */
            timezone?: string;
            [key: string]: unknown;
          };
          [key: string]: unknown;
        };
        /** Branches a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualbranchstartcondition */
        manualBranchStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          [key: string]: unknown;
        };
        /** Tags a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualtagstartcondition */
        manualTagStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          [key: string]: unknown;
        };
        /** Pull requests a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualpullrequeststartcondition */
        manualPullRequestStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          destination?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          [key: string]: unknown;
        };
        /**
         * Actions the workflow runs, in order.
         * @minItems 1
         */
        actions: Array<{
          /** Action name shown in Xcode Cloud. */
          name?: string;
          /** What the action does. */
          actionType?: "BUILD" | "ANALYZE" | "TEST" | "ARCHIVE";
          /** Device or simulator family the action targets. */
          destination?: "ANY_IOS_DEVICE" | "ANY_IOS_SIMULATOR" | "ANY_TVOS_DEVICE" | "ANY_TVOS_SIMULATOR" | "ANY_WATCHOS_DEVICE" | "ANY_WATCHOS_SIMULATOR" | "ANY_MAC" | "ANY_MAC_CATALYST" | "ANY_VISIONOS_DEVICE" | "ANY_VISIONOS_SIMULATOR";
          /** Who may install the build an ARCHIVE action produces. */
          buildDistributionAudience?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE";
          /** Test plan configuration for a TEST action. */
          testConfiguration?: {
            /** Use the scheme test settings or a specific test plan. */
            kind?: "USE_SCHEME_SETTINGS" | "SPECIFIC_TEST_PLANS";
            /** Name of the test plan to run when kind is SPECIFIC_TEST_PLANS. */
            testPlanName?: string;
            /** Simulators and Macs the tests run on. */
            testDestinations?: Array<{
              /** Device type name, such as iPhone 15. */
              deviceTypeName?: string;
              /** Device type identifier. */
              deviceTypeIdentifier?: string;
              /** Runtime name, such as iOS 17.4. */
              runtimeName?: string;
              /** Runtime identifier. */
              runtimeIdentifier?: string;
              /** Whether the destination is a simulator or a Mac. */
              kind?: "SIMULATOR" | "MAC";
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Xcode scheme the action builds. */
          scheme?: string;
          /** Platform the action builds for. */
          platform?: "MACOS" | "IOS" | "TVOS" | "WATCHOS" | "VISIONOS";
          /** Whether the whole build run fails when this action fails. */
          isRequiredToPass?: boolean;
          [key: string]: unknown;
        }>;
        /** Enable the workflow so its start conditions can start builds. */
        isEnabled: boolean;
        /** Lock the workflow so it cannot be edited in Xcode. */
        isLockedForEditing?: boolean;
        /** Start every build from a clean environment without cached derived data. */
        clean: boolean;
        /**
         * Path of the Xcode project, workspace, or Swift package the workflow builds, relative to the repository root.
         * @minLength 1
         * @pattern \S
         */
        containerFilePath: string;
      };
      output: {
        /** An Xcode Cloud workflow. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciWorkflow: {
          /** App Store Connect identifier for the workflow. */
          id: string;
          /** Workflow name shown in App Store Connect and Xcode. */
          name?: string | null;
          /** Description shown under the workflow name. */
          description?: string | null;
          /** Start a build when a matching branch changes. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchstartcondition */
          branchStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start a build when a matching tag is created. See https://developer.apple.com/documentation/appstoreconnectapi/citagstartcondition */
          tagStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start a build when a matching pull request is opened or updated. See https://developer.apple.com/documentation/appstoreconnectapi/cipullrequeststartcondition */
          pullRequestStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            destination?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start builds on a schedule. See https://developer.apple.com/documentation/appstoreconnectapi/cischeduledstartcondition */
          scheduledStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** When the schedule fires. */
            schedule?: {
              /** How often the build starts. */
              frequency?: "WEEKLY" | "DAILY" | "HOURLY";
              /** Days of the week for a WEEKLY schedule. */
              days?: Array<"SUNDAY" | "MONDAY" | "TUESDAY" | "WEDNESDAY" | "THURSDAY" | "FRIDAY" | "SATURDAY">;
              /** Hour of the day the build starts. */
              hour?: number;
              /** Minute of the hour the build starts. */
              minute?: number;
              /** IANA time zone name, such as America/Los_Angeles. */
              timezone?: string;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Branches a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualbranchstartcondition */
          manualBranchStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Tags a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualtagstartcondition */
          manualTagStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Pull requests a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualpullrequeststartcondition */
          manualPullRequestStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            destination?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Actions the workflow runs, in order. */
          actions?: Array<{
            /** Action name shown in Xcode Cloud. */
            name?: string;
            /** What the action does. */
            actionType?: "BUILD" | "ANALYZE" | "TEST" | "ARCHIVE";
            /** Device or simulator family the action targets. */
            destination?: "ANY_IOS_DEVICE" | "ANY_IOS_SIMULATOR" | "ANY_TVOS_DEVICE" | "ANY_TVOS_SIMULATOR" | "ANY_WATCHOS_DEVICE" | "ANY_WATCHOS_SIMULATOR" | "ANY_MAC" | "ANY_MAC_CATALYST" | "ANY_VISIONOS_DEVICE" | "ANY_VISIONOS_SIMULATOR";
            /** Who may install the build an ARCHIVE action produces. */
            buildDistributionAudience?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE";
            /** Test plan configuration for a TEST action. */
            testConfiguration?: {
              /** Use the scheme test settings or a specific test plan. */
              kind?: "USE_SCHEME_SETTINGS" | "SPECIFIC_TEST_PLANS";
              /** Name of the test plan to run when kind is SPECIFIC_TEST_PLANS. */
              testPlanName?: string;
              /** Simulators and Macs the tests run on. */
              testDestinations?: Array<{
                /** Device type name, such as iPhone 15. */
                deviceTypeName?: string;
                /** Device type identifier. */
                deviceTypeIdentifier?: string;
                /** Runtime name, such as iOS 17.4. */
                runtimeName?: string;
                /** Runtime identifier. */
                runtimeIdentifier?: string;
                /** Whether the destination is a simulator or a Mac. */
                kind?: "SIMULATOR" | "MAC";
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Xcode scheme the action builds. */
            scheme?: string;
            /** Platform the action builds for. */
            platform?: "MACOS" | "IOS" | "TVOS" | "WATCHOS" | "VISIONOS";
            /** Whether the whole build run fails when this action fails. */
            isRequiredToPass?: boolean;
            [key: string]: unknown;
          }> | null;
          /** Whether the workflow is enabled and can start builds. */
          isEnabled?: boolean | null;
          /** Whether the workflow is locked against editing in Xcode. */
          isLockedForEditing?: boolean | null;
          /** Whether every build starts from a clean environment without cached derived data. */
          clean?: boolean | null;
          /** Path of the Xcode project, workspace, or Swift package the workflow builds, relative to the repository root. */
          containerFilePath?: string | null;
          /** When the workflow was last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** Identifier of the Xcode Cloud product the workflow belongs to. */
          ciProductId: string | null;
          /** Identifier of the repository the workflow builds from. */
          scmRepositoryId: string | null;
          /** Identifier of the Xcode version the workflow builds with. */
          ciXcodeVersionId: string | null;
          /** Identifier of the macOS version the workflow builds on. */
          ciMacOsVersionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** End the pre-order of an app in the given territories and release it there immediately. This is how a pre-order for the whole app is ended: pass the territory availabilities of every territory the app is on pre-order in. The pre-order cannot be resumed afterwards. */
    "app_store_connect.create_end_app_availability_pre_order": {
      input: {
        /**
         * Identifiers of the territory availabilities to release, from list_territory_availabilities.
         * @minItems 1
         */
        territoryAvailabilityIds: Array<string>;
      };
      output: {
        /** The request App Store Connect recorded for ending the pre-order. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        endAppAvailabilityPreOrder: {
          /** App Store Connect identifier for the end pre-order request. */
          id: string;
          [key: string]: unknown;
        };
        /** Identifiers of the territory availabilities whose pre-order was ended. */
        territoryAvailabilityIds: Array<string>;
      };
    };
    /** Attach a custom end user license agreement to an app for the given territories. Apple's standard EULA keeps applying elsewhere. App Store Connect rejects a second agreement for the same app. */
    "app_store_connect.create_end_user_license_agreement": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Full text of the custom license agreement.
         * @minLength 1
         * @pattern \S
         */
        agreementText: string;
        /**
         * Territories the custom agreement applies in.
         * @minItems 1
         */
        territoryIds: Array<string>;
      };
      output: {
        /** A custom end user license agreement that replaces Apple's standard EULA in some territories. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        endUserLicenseAgreement: {
          /** App Store Connect identifier for the end user license agreement. */
          id: string;
          /** Full text of the custom license agreement. */
          agreementText?: string | null;
          /** ISO 3166-1 alpha-3 territories the custom agreement applies in. */
          territoryIds: Array<string> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a Game Center achievement together with its first version. Pass gameCenterDetailId when the achievement belongs to a single app, or gameCenterGroupId when it is shared by every app of a Game Center group. The achievement has no player-facing text yet; read the first version with list_game_center_achievement_versions and add locales with create_game_center_achievement_localization. */
    "app_store_connect.create_game_center_achievement": {
      input: {
        /**
         * Internal achievement name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /**
         * Achievement identifier the game reports progress against, unique within the app or group. It cannot be changed later.
         * @minLength 1
         * @pattern \S
         */
        vendorIdentifier: string;
        /** Points the achievement is worth, from 0 to 100. */
        points: number;
        /** Whether players can see the achievement before they earn it. */
        showBeforeEarned: boolean;
        /** Whether players can earn the achievement more than once. */
        repeatable: boolean;
        /** Key-value properties the achievement contributes to a game activity. */
        activityProperties?: Record<string, unknown>;
        /**
         * App Store Connect identifier of the Game Center detail that owns the achievement. Pass this or gameCenterGroupId, not both.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId?: string;
        /**
         * App Store Connect identifier of the Game Center group that shares the achievement. Pass this or gameCenterDetailId, not both.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId?: string;
      };
      output: {
        /** A Game Center achievement. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAchievement: {
          /** App Store Connect identifier for the achievement. */
          id: string;
          /** Internal achievement name shown in App Store Connect. */
          referenceName?: string | null;
          /** Achievement identifier the game reports scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** Points the achievement is worth, from 0 to 100. */
          points?: number | null;
          /** Whether players can see the achievement before they earn it. */
          showBeforeEarned?: boolean | null;
          /** Whether players can earn the achievement more than once. */
          repeatable?: boolean | null;
          /** Whether the achievement is archived and no longer offered to players. */
          archived?: boolean | null;
          /** Key-value properties the achievement contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a locale to a Game Center achievement version with the name and the two descriptions players see before and after they earn the achievement. The achievement image for the locale is uploaded separately. App Store Connect rejects a locale the version already has. */
    "app_store_connect.create_game_center_achievement_localization": {
      input: {
        /**
         * App Store Connect identifier of the achievement version the locale is added to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementVersionId: string;
        /**
         * Locale of the text, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Achievement name shown to players in this locale.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Description shown before the player earns the achievement.
         * @minLength 1
         * @pattern \S
         */
        beforeEarnedDescription: string;
        /**
         * Description shown after the player earns the achievement.
         * @minLength 1
         * @pattern \S
         */
        afterEarnedDescription: string;
      };
      output: {
        /** The achievement text in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAchievementLocalization: {
          /** App Store Connect identifier for the achievement localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Achievement name shown to players in this locale. */
          name?: string | null;
          /** Description shown before the player earns the achievement. */
          beforeEarnedDescription?: string | null;
          /** Description shown after the player earns the achievement. */
          afterEarnedDescription?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new version of a Game Center achievement. Editing the player-facing text of an achievement that is already live goes through a new version: add or change its localizations, and the version carries them through App Review. */
    "app_store_connect.create_game_center_achievement_version": {
      input: {
        /**
         * App Store Connect identifier of the achievement to version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementId: string;
      };
      output: {
        /** One version of a Game Center achievement. A new version carries the localizations through App Review. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAchievementVersion: {
          /** App Store Connect identifier for the achievement version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a Game Center activity for one app (gameCenterDetailId) or for a group of apps (gameCenterGroupId). Pass fallbackUrl to create the first activity version together with the activity; leave it out and no version is created, so add one afterwards with create_game_center_activity_version before the activity can be localized. */
    "app_store_connect.create_game_center_activity": {
      input: {
        /**
         * Internal activity name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /**
         * Activity identifier the game starts sessions with, unique within the app or group.
         * @minLength 1
         * @pattern \S
         */
        vendorIdentifier: string;
        /** Whether players take turns or play at the same time. */
        playStyle?: "ASYNCHRONOUS" | "SYNCHRONOUS";
        /** Fewest players an activity session needs. */
        minimumPlayersCount?: number;
        /** Most players an activity session accepts. */
        maximumPlayersCount?: number;
        /** Whether players can join the activity with a party code. */
        supportsPartyCode?: boolean;
        /** Key-value properties the game reads when it starts the activity. */
        properties?: Record<string, unknown>;
        /**
         * App Store Connect identifier of the Game Center detail the activity belongs to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId?: string;
        /**
         * App Store Connect identifier of the Game Center group the activity belongs to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId?: string;
        /**
         * Web page opened when a player without the game follows an activity invitation. When set, the first activity version is created together with the activity and carries this URL.
         * @format uri
         */
        fallbackUrl?: string;
      };
      output: {
        /** A Game Center activity: a play session other players can be invited into, with its own achievements and leaderboards. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivity: {
          /** App Store Connect identifier for the activity. */
          id: string;
          /** Internal activity name shown in App Store Connect. */
          referenceName?: string | null;
          /** Activity identifier the game starts sessions with, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** Whether players take turns or play at the same time. */
          playStyle?: "ASYNCHRONOUS" | "SYNCHRONOUS" | null;
          /** Fewest players an activity session needs. */
          minimumPlayersCount?: number | null;
          /** Most players an activity session accepts. */
          maximumPlayersCount?: number | null;
          /** Whether players can join the activity with a party code. */
          supportsPartyCode?: boolean | null;
          /** Whether the activity is archived and no longer offered. */
          archived?: boolean | null;
          /** Key-value properties the game reads when it starts the activity. */
          properties?: Record<string, unknown> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a locale to a Game Center activity version with the name and description players see in it. App Store Connect rejects a locale the version already has. */
    "app_store_connect.create_game_center_activity_localization": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityVersionId: string;
        /**
         * Locale to add, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Activity name shown to players in this locale.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Activity description shown to players in this locale.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** The activity text in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivityLocalization: {
          /** App Store Connect identifier for the activity localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Activity name shown to players in this locale. */
          name?: string | null;
          /** Activity description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new version of a Game Center activity, for example to change its localizations or its default image after the current version was released. The new version starts in PREPARE_FOR_SUBMISSION. */
    "app_store_connect.create_game_center_activity_version": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
        /**
         * Web page opened when a player without the game follows an activity invitation.
         * @format uri
         */
        fallbackUrl?: string;
      };
      output: {
        /** One version of a Game Center activity. A new version carries the localizations and the default image through App Review. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivityVersion: {
          /** App Store Connect identifier for the activity version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          /** Web page opened when a player without the game follows an activity invitation. */
          fallbackUrl?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Turn Game Center on for one App Store version by creating its Game Center app version record. Everything Game Center knows about that release hangs off this record: use update_game_center_app_version to toggle it again, and add_compatibility_versions_to_game_center_app_version to keep earlier releases multiplayer compatible with it. */
    "app_store_connect.create_game_center_app_version": {
      input: {
        /**
         * App Store Connect identifier of the App Store version to enable Game Center for.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** The Game Center enablement of one App Store version, plus the earlier versions it stays multiplayer compatible with. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAppVersion: {
          /** App Store Connect identifier for the Game Center app version. */
          id: string;
          /** Whether Game Center is enabled for this App Store version. */
          enabled?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a Game Center challenge on the Game Center detail of one app or on a group, scored on one leaderboard. The challenge starts without a version; create its first version with create_game_center_challenge_version and add the localized text to that version. */
    "app_store_connect.create_game_center_challenge": {
      input: {
        /**
         * Internal challenge name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /**
         * Challenge identifier the game refers to, unique within the app or group.
         * @minLength 1
         * @pattern \S
         */
        vendorIdentifier: string;
        /** What the challenge is based on. */
        challengeType: "LEADERBOARD";
        /** Whether a player can take the challenge more than once. */
        repeatable?: boolean;
        /**
         * Game Center detail the challenge belongs to, for a challenge of a single app.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId?: string;
        /**
         * Game Center group the challenge belongs to, for a challenge shared by the games of the group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId?: string;
        /**
         * App Store Connect identifier of the leaderboard the challenge is scored on.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId?: string;
      };
      output: {
        /** A Game Center challenge: an invitation to beat a score on one leaderboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterChallenge: {
          /** App Store Connect identifier for the challenge. */
          id: string;
          /** Internal challenge name shown in App Store Connect. */
          referenceName?: string | null;
          /** Challenge identifier the game refers to, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** What the challenge is based on. */
          challengeType?: "LEADERBOARD" | null;
          /** Whether a player can take the challenge more than once. */
          repeatable?: boolean | null;
          /** Whether the challenge is archived and no longer offered. */
          archived?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a locale with the name and description players see to a Game Center challenge version. App Store Connect rejects a locale the version already has. */
    "app_store_connect.create_game_center_challenge_localization": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeVersionId: string;
        /**
         * Locale the text is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Challenge name shown to players in this locale.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Challenge description shown to players in this locale.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** The challenge text in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterChallengeLocalization: {
          /** App Store Connect identifier for the challenge localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Challenge name shown to players in this locale. */
          name?: string | null;
          /** Challenge description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new version of a Game Center challenge, for example to change its localized text after the current version went live. The version starts empty; add its locales with create_game_center_challenge_localization. */
    "app_store_connect.create_game_center_challenge_version": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeId: string;
      };
      output: {
        /** One version of a Game Center challenge. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterChallengeVersion: {
          /** App Store Connect identifier for the challenge version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Enable Game Center for an app by creating its Game Center detail. The detail owns the achievements, leaderboards and leaderboard sets of the app, and an app has at most one of them. */
    "app_store_connect.create_game_center_detail": {
      input: {
        /**
         * App Store Connect identifier of the app to enable.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
      };
      output: {
        /** The Game Center configuration of one app. It owns the app's achievements, leaderboards, leaderboard sets, activities and challenges, and links the app to a Game Center group when it shares them with other apps. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterDetail: {
          /** App Store Connect identifier for the Game Center detail. */
          id: string;
          /** Whether the app is distributed through Apple Arcade. */
          arcadeEnabled?: boolean | null;
          /** Game Center group the app belongs to, or null when the app is not grouped or App Store Connect did not return the relationship. */
          gameCenterGroupId: string | null;
          /** Leaderboard shown by default in the Game Center dashboard, or null when there is none. */
          defaultLeaderboardId: string | null;
          /** Group leaderboard shown by default when the app belongs to a group, or null when there is none. */
          defaultGroupLeaderboardId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a Game Center group. The group starts empty: an app joins it through its Game Center detail, and the achievements, leaderboards and leaderboard sets it shares are set with the replace actions. */
    "app_store_connect.create_game_center_group": {
      input: {
        /**
         * Internal group name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName?: string;
      };
      output: {
        /** A Game Center group. Apps in a group share achievements, leaderboards and leaderboard sets. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterGroup: {
          /** App Store Connect identifier for the Game Center group. */
          id: string;
          /** Internal group name shown in App Store Connect. */
          referenceName?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a Game Center leaderboard owned by one app or shared by a group. App Store Connect always creates the first leaderboard version alongside it, so the player-facing text is added with create_game_center_leaderboard_localization against that version. Give recurrenceStartDate, recurrenceDuration and recurrenceRule together to make the leaderboard recurring. */
    "app_store_connect.create_game_center_leaderboard": {
      input: {
        /**
         * Internal leaderboard name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /**
         * Leaderboard identifier the game submits scores against, unique within the app or group. It cannot be changed afterwards.
         * @minLength 1
         * @pattern \S
         */
        vendorIdentifier: string;
        /** How scores are formatted unless a localization overrides it. */
        defaultFormatter: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN";
        /** Which submitted score is kept for a player. */
        submissionType: "BEST_SCORE" | "MOST_RECENT_SCORE";
        /** Whether a higher or a lower score ranks first. */
        scoreSortType: "ASC" | "DESC";
        /**
         * Lowest accepted score, as a decimal string.
         * @minLength 1
         * @pattern \S
         */
        scoreRangeStart?: string;
        /**
         * Highest accepted score, as a decimal string.
         * @minLength 1
         * @pattern \S
         */
        scoreRangeEnd?: string;
        /**
         * When the first recurring run of the leaderboard starts, as an ISO 8601 timestamp.
         * @format date-time
         */
        recurrenceStartDate?: string;
        /**
         * How long each recurring run lasts, as an ISO 8601 duration such as P1D.
         * @minLength 1
         * @pattern \S
         */
        recurrenceDuration?: string;
        /**
         * ICS recurrence rule that schedules the recurring runs.
         * @minLength 1
         * @pattern \S
         */
        recurrenceRule?: string;
        /** Key-value properties the leaderboard contributes to a game activity. */
        activityProperties?: Record<string, unknown>;
        /** Whether the leaderboard is shown to players. */
        visibility?: "SHOW_FOR_ALL" | "HIDE_FOR_ALL";
        /**
         * Game Center detail of the app that owns the leaderboard.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId?: string;
        /**
         * Game Center group that owns the leaderboard when several apps share it.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId?: string;
        /**
         * Leaderboard sets the new leaderboard is added to.
         * @minItems 1
         */
        gameCenterLeaderboardSetIds?: Array<string>;
      };
      output: {
        /** A Game Center leaderboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboard: {
          /** App Store Connect identifier for the leaderboard. */
          id: string;
          /** Internal leaderboard name shown in App Store Connect. */
          referenceName?: string | null;
          /** Leaderboard identifier the game submits scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** How scores are formatted unless a localization overrides it. */
          defaultFormatter?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
          /** Which submitted score is kept for a player. */
          submissionType?: "BEST_SCORE" | "MOST_RECENT_SCORE" | null;
          /** Whether a higher or a lower score ranks first. */
          scoreSortType?: "ASC" | "DESC" | null;
          /** Lowest accepted score, as a decimal string. */
          scoreRangeStart?: string | null;
          /** Highest accepted score, as a decimal string. */
          scoreRangeEnd?: string | null;
          /** When the first recurring run of the leaderboard starts, as an ISO 8601 timestamp. */
          recurrenceStartDate?: string | null;
          /** How long each recurring run lasts, as an ISO 8601 duration such as P1D. */
          recurrenceDuration?: string | null;
          /** ICS recurrence rule that schedules the recurring runs. */
          recurrenceRule?: string | null;
          /** Whether the leaderboard is archived and no longer accepting scores. */
          archived?: boolean | null;
          /** Key-value properties the leaderboard contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          /** Whether the leaderboard is shown to players. */
          visibility?: "SHOW_FOR_ALL" | "HIDE_FOR_ALL" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a locale to a Game Center leaderboard version with the name and score text players see there. The image shown next to the leaderboard is uploaded separately and is not covered by this connector. */
    "app_store_connect.create_game_center_leaderboard_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard version the locale is added to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardVersionId: string;
        /**
         * Locale to add, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Leaderboard name shown to players in this locale.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /** Score formatter used in this locale instead of the leaderboard default. */
        formatterOverride?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN";
        /**
         * Suffix appended to plural score values, such as points.
         * @minLength 1
         * @pattern \S
         */
        formatterSuffix?: string;
        /**
         * Suffix appended to a score value of one, such as point.
         * @minLength 1
         * @pattern \S
         */
        formatterSuffixSingular?: string;
        /**
         * Score description shown to players in this locale.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** The leaderboard text and score formatting in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardLocalization: {
          /** App Store Connect identifier for the leaderboard localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Leaderboard name shown to players in this locale. */
          name?: string | null;
          /** Score formatter used in this locale instead of the leaderboard default. */
          formatterOverride?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
          /** Suffix appended to plural score values, such as points. */
          formatterSuffix?: string | null;
          /** Suffix appended to a score value of one, such as point. */
          formatterSuffixSingular?: string | null;
          /** Score description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a Game Center leaderboard set that groups related leaderboards under one entry in the Game Center dashboard, and optionally put existing leaderboards into it. App Store Connect always creates the first set version together with the set; add the localized set names with create_game_center_leaderboard_set_localization. */
    "app_store_connect.create_game_center_leaderboard_set": {
      input: {
        /**
         * Internal set name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /**
         * Set identifier the game refers to, unique within the app or group.
         * @minLength 1
         * @pattern \S
         */
        vendorIdentifier: string;
        /**
         * Game Center detail of the app the set belongs to. Pass this or gameCenterGroupId.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId?: string;
        /**
         * Game Center group the set belongs to when several apps share it. Pass this or gameCenterDetailId.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId?: string;
        /**
         * Existing leaderboards to put into the new set.
         * @minItems 1
         */
        gameCenterLeaderboardIds?: Array<string>;
      };
      output: {
        /** A Game Center leaderboard set that groups related leaderboards under one entry in the Game Center dashboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSet: {
          /** App Store Connect identifier for the leaderboard set. */
          id: string;
          /** Internal set name shown in App Store Connect. */
          referenceName?: string | null;
          /** Set identifier the game refers to, unique within the app or group. */
          vendorIdentifier?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a locale to a Game Center leaderboard set version with the set name players see in that locale. App Store Connect rejects a locale the version already has. */
    "app_store_connect.create_game_center_leaderboard_set_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetVersionId: string;
        /**
         * Locale the name is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Set name shown to players in this locale.
         * @minLength 1
         * @pattern \S
         */
        name: string;
      };
      output: {
        /** The leaderboard set name in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSetLocalization: {
          /** App Store Connect identifier for the leaderboard set localization. */
          id: string;
          /** Locale the name is written in, such as en-US. */
          locale?: string | null;
          /** Set name shown to players in this locale. */
          name?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Give one leaderboard a name of its own inside one leaderboard set, for one locale. Without a member localization the leaderboard keeps its usual localized name inside the set. */
    "app_store_connect.create_game_center_leaderboard_set_member_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
        /**
         * App Store Connect identifier of the leaderboard inside that set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId: string;
        /**
         * Locale the name is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale?: string;
        /**
         * Name the leaderboard is shown under inside the set.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
      };
      output: {
        /** The name one leaderboard is shown under inside one leaderboard set, in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSetMemberLocalization: {
          /** App Store Connect identifier for the leaderboard set member localization. */
          id: string;
          /** Locale the name is written in, such as en-US. */
          locale?: string | null;
          /** Name the leaderboard is shown under inside the set. */
          name?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new editable version of a Game Center leaderboard set, for example to change its localized names after the current version went live. */
    "app_store_connect.create_game_center_leaderboard_set_version": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
      };
      output: {
        /** One version of a Game Center leaderboard set. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSetVersion: {
          /** App Store Connect identifier for the leaderboard set version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new version of a Game Center leaderboard so its localized text can be edited and taken through App Review again. Add the text with create_game_center_leaderboard_localization against the new version. */
    "app_store_connect.create_game_center_leaderboard_version": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard the version belongs to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId: string;
      };
      output: {
        /** One version of a Game Center leaderboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardVersion: {
          /** App Store Connect identifier for the leaderboard version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a Game Center matchmaking queue and attach the rule set its requests are matched with. Optionally attach a second rule set to experiment with, and list the bundle identifiers whose classic matchmaking requests should reach this queue. */
    "app_store_connect.create_game_center_matchmaking_queue": {
      input: {
        /**
         * Internal queue name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /**
         * Bundle identifiers whose classic matchmaking requests use this queue.
         * @minItems 1
         */
        classicMatchmakingBundleIds?: Array<string>;
        /**
         * App Store Connect identifier of the rule set the queue matches requests with.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId: string;
        /**
         * App Store Connect identifier of a second rule set matched against a share of the queue traffic while an experiment runs.
         * @minLength 1
         * @pattern \S
         */
        experimentRuleSetId?: string;
      };
      output: {
        /** A Game Center matchmaking queue. Requests that reach the queue are matched with the rule set attached to it. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingQueue: {
          /** App Store Connect identifier for the matchmaking queue. */
          id: string;
          /** Internal queue name shown in App Store Connect. */
          referenceName?: string | null;
          /** Bundle identifiers whose classic matchmaking requests use this queue. */
          classicMatchmakingBundleIds?: Array<string> | null;
          /** Rule set the queue matches requests with. */
          ruleSetId: string | null;
          /** Rule set a share of the queue traffic is matched with while an experiment runs, or null when no experiment is running. */
          experimentRuleSetId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a rule to a Game Center matchmaking rule set. The expression is evaluated for every candidate match: a COMPATIBLE, DISTANCE or TEAM rule decides whether the candidate is acceptable, and a MATCH rule scores it. Try the rule set with test_game_center_matchmaking_rule_set before attaching it to a queue. */
    "app_store_connect.create_game_center_matchmaking_rule": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule set the rule belongs to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId: string;
        /**
         * Internal rule name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /**
         * What the rule checks, for the team reading the rule set.
         * @minLength 1
         * @pattern \S
         */
        description: string;
        /** How the rule result is used. */
        type: "COMPATIBLE" | "DISTANCE" | "MATCH" | "TEAM";
        /**
         * Rule expression evaluated for every candidate match.
         * @minLength 1
         * @pattern \S
         */
        expression: string;
        /** How much the rule counts against the other rules of the same type when Game Center scores a candidate match. */
        weight?: number;
      };
      output: {
        /** One rule of a Game Center matchmaking rule set. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingRule: {
          /** App Store Connect identifier for the rule. */
          id: string;
          /** Internal rule name shown in App Store Connect. */
          referenceName?: string | null;
          /** What the rule checks, for the team reading the rule set. */
          description?: string | null;
          /** How the rule result is used. */
          type?: "COMPATIBLE" | "DISTANCE" | "MATCH" | "TEAM" | null;
          /** Rule expression evaluated for every candidate match. */
          expression?: string | null;
          /** How much the rule counts against the other rules of the same type when Game Center scores a candidate match. */
          weight?: number | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a Game Center matchmaking rule set with the player limits a match must stay within. Add its teams and rules afterwards with create_game_center_matchmaking_team and create_game_center_matchmaking_rule, then attach the rule set to a queue. */
    "app_store_connect.create_game_center_matchmaking_rule_set": {
      input: {
        /**
         * Internal rule set name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /** Version of the rule expression language the rules of this rule set are written in. */
        ruleLanguageVersion: number;
        /** Fewest players a match created from this rule set may have. */
        minPlayers: number;
        /** Most players a match created from this rule set may have. */
        maxPlayers: number;
      };
      output: {
        /** A Game Center matchmaking rule set: the team layout, player limits and rules a queue matches requests with. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingRuleSet: {
          /** App Store Connect identifier for the rule set. */
          id: string;
          /** Internal rule set name shown in App Store Connect. */
          referenceName?: string | null;
          /** Version of the rule expression language the rules use. */
          ruleLanguageVersion?: number | null;
          /** Fewest players a match created from this rule set may have. */
          minPlayers?: number | null;
          /** Most players a match created from this rule set may have. */
          maxPlayers?: number | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add a team to a Game Center matchmaking rule set. Rule expressions refer to the team by its name, and Game Center fills the team with between minPlayers and maxPlayers players. */
    "app_store_connect.create_game_center_matchmaking_team": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule set the team belongs to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId: string;
        /**
         * Internal team name the rule expressions refer to.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /** Fewest players the team needs. */
        minPlayers: number;
        /** Most players the team accepts. */
        maxPlayers: number;
      };
      output: {
        /** One team of a Game Center matchmaking rule set. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingTeam: {
          /** App Store Connect identifier for the team. */
          id: string;
          /** Internal team name the rule expressions refer to. */
          referenceName?: string | null;
          /** Fewest players the team needs. */
          minPlayers?: number | null;
          /** Most players the team accepts. */
          maxPlayers?: number | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create an in-app purchase for an app. The product identifier is permanent and cannot be reused after deletion, so choose it carefully. The purchase starts in MISSING_METADATA until localizations, a price schedule, and a review screenshot are added. */
    "app_store_connect.create_in_app_purchase": {
      input: {
        /**
         * App Store Connect identifier of the app the purchase belongs to.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Reference name shown in App Store Connect, up to 64 characters.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Product identifier the app passes to StoreKit, such as com.example.app.coins100. Unique within the app and permanent.
         * @minLength 1
         * @pattern \S
         */
        productId: string;
        /** Kind of in-app purchase. */
        inAppPurchaseType: "CONSUMABLE" | "NON_CONSUMABLE" | "NON_RENEWING_SUBSCRIPTION";
        /**
         * Note for App Review, for example how to reach the purchase.
         * @minLength 1
         * @pattern \S
         */
        reviewNote?: string;
        /** Allow the purchase to be shared through Family Sharing. */
        familySharable?: boolean;
      };
      output: {
        /** An in-app purchase of an app: a consumable, non-consumable, or non-renewing subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        inAppPurchase: {
          /** App Store Connect identifier for the in-app purchase. */
          id: string;
          /** Reference name shown in App Store Connect. */
          name?: string | null;
          /** Product identifier the app passes to StoreKit to request the purchase. */
          productId?: string | null;
          /** Kind of in-app purchase. */
          inAppPurchaseType?: "CONSUMABLE" | "NON_CONSUMABLE" | "NON_RENEWING_SUBSCRIPTION" | null;
          /** Where the in-app purchase stands in metadata preparation, review, and sale. */
          state?: "MISSING_METADATA" | "WAITING_FOR_UPLOAD" | "PROCESSING_CONTENT" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED" | null;
          /** Note shown to App Review alongside the purchase. */
          reviewNote?: string | null;
          /** Whether the purchase can be shared through Family Sharing. */
          familySharable?: boolean | null;
          /** Whether Apple hosts the downloadable content of the purchase. */
          contentHosting?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set the territories an in-app purchase is sold in. The list replaces the current availability entirely, so territories left out are removed from sale. */
    "app_store_connect.create_in_app_purchase_availability": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
        /** Automatically offer the purchase in territories Apple adds to the App Store later. */
        availableInNewTerritories: boolean;
        /**
         * Territories the purchase is sold in.
         * @minItems 1
         */
        availableTerritories: Array<string>;
      };
      output: {
        /** Territory availability of an in-app purchase. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        availability: {
          /** App Store Connect identifier for the availability record. */
          id: string;
          /** Whether the purchase automatically becomes available in territories Apple adds later. */
          availableInNewTerritories?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add the display name and description of an in-app purchase for one locale. Each locale can have only one localization per purchase. */
    "app_store_connect.create_in_app_purchase_localization": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
        /**
         * Locale the text is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Display name customers see, up to 30 characters.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Description customers see, up to 45 characters.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** A localized display name and description of an in-app purchase. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        localization: {
          /** App Store Connect identifier for the localization. */
          id: string;
          /** Display name customers see in this locale. */
          name?: string | null;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Description customers see in this locale. */
          description?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create an offer code configuration for an in-app purchase: who may redeem it and the discounted price per territory. Generate redeemable codes afterwards with the custom code or one-time-use code actions. */
    "app_store_connect.create_in_app_purchase_offer_code": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
        /**
         * Reference name of the offer, up to 64 characters.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Customer groups allowed to redeem the offer.
         * @minItems 1
         */
        customerEligibilities: Array<"NON_SPENDER" | "ACTIVE_SPENDER" | "CHURNED_SPENDER">;
        /**
         * Discounted price per territory. Every territory the purchase is sold in needs an entry.
         * @minItems 1
         */
        prices: Array<{
          /**
           * ISO 3166-1 alpha-3 territory code, such as USA.
           * @minLength 1
           * @pattern \S
           */
          territory: string;
          /**
           * Price point for the discounted price in that territory, from list_in_app_purchase_price_points.
           * @minLength 1
           * @pattern \S
           */
          inAppPurchasePricePointId: string;
        }>;
      };
      output: {
        /** An offer code configuration that lets customers redeem an in-app purchase at a discounted price. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        offerCode: {
          /** App Store Connect identifier for the offer code. */
          id: string;
          /** Reference name of the offer. */
          name?: string | null;
          /** Customer groups allowed to redeem the offer. */
          customerEligibilities?: Array<"NON_SPENDER" | "ACTIVE_SPENDER" | "CHURNED_SPENDER"> | null;
          /** Number of one-time-use codes generated for the production environment. */
          productionCodeCount?: number | null;
          /** Number of one-time-use codes generated for the sandbox environment. */
          sandboxCodeCount?: number | null;
          /** Whether the offer can currently be redeemed. */
          active?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a custom code for an in-app purchase offer that customers redeem by typing it, with a redemption limit and optional expiration date. */
    "app_store_connect.create_in_app_purchase_offer_code_custom_code": {
      input: {
        /**
         * App Store Connect identifier of the offer code.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseOfferCodeId: string;
        /**
         * Text customers enter to redeem the offer, 3 to 64 letters and digits.
         * @minLength 1
         * @pattern \S
         */
        customCode: string;
        /**
         * Maximum number of redemptions the code allows, up to 25,000.
         * @minimum 1
         * @maximum 25000
         */
        numberOfCodes: number;
        /**
         * Last day the code can be redeemed, as YYYY-MM-DD.
         * @format date
         */
        expirationDate?: string;
      };
      output: {
        /** A custom offer code that many customers can redeem by typing the same text. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        customCode: {
          /** App Store Connect identifier for the custom code. */
          id: string;
          /** The code customers enter, such as SPRING2026. */
          customCode?: string | null;
          /** How many redemptions the custom code allows. */
          numberOfCodes?: number | null;
          /** When the code was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the code can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the code can currently be redeemed. */
          active?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Generate a batch of one-time-use codes for an in-app purchase offer. Download the generated code values from App Store Connect afterwards; they are not returned here. */
    "app_store_connect.create_in_app_purchase_offer_code_one_time_use_code": {
      input: {
        /**
         * App Store Connect identifier of the offer code.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseOfferCodeId: string;
        /**
         * Number of codes to generate, up to 25,000.
         * @minimum 1
         * @maximum 25000
         */
        numberOfCodes: number;
        /**
         * Last day the codes can be redeemed, as YYYY-MM-DD.
         * @format date
         */
        expirationDate: string;
        /** Environment the codes redeem in. Defaults to PRODUCTION. */
        environment?: "PRODUCTION" | "SANDBOX";
      };
      output: {
        /** A batch of one-time-use offer codes; the code values themselves are downloaded from App Store Connect as a CSV file. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        oneTimeUseCode: {
          /** App Store Connect identifier for the batch. */
          id: string;
          /** Number of codes in the batch. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the codes can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the codes can currently be redeemed. */
          active?: boolean | null;
          /** Environment the codes redeem in. */
          environment?: "PRODUCTION" | "SANDBOX" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set the price schedule of an in-app purchase: a base territory plus manual prices for specific territories. Territories without a manual price get automatic prices equalized from the base territory. Posting a schedule replaces the prices App Store Connect currently has for the purchase. */
    "app_store_connect.create_in_app_purchase_price_schedule": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
        /**
         * Territory the automatic prices are equalized from, as an ISO 3166-1 alpha-3 territory code, such as USA. It must also appear in manualPrices.
         * @minLength 1
         * @pattern \S
         */
        baseTerritory: string;
        /**
         * Manual prices, one per territory. Each entry names the price point to charge and optionally when it applies.
         * @minItems 1
         */
        manualPrices: Array<{
          /**
           * Price point to charge, taken from list_in_app_purchase_price_points for that territory.
           * @minLength 1
           * @pattern \S
           */
          inAppPurchasePricePointId: string;
          /**
           * First day the price applies, as YYYY-MM-DD. Omit to apply it immediately.
           * @format date
           */
          startDate?: string;
          /**
           * Last day the price applies, as YYYY-MM-DD. Omit to keep it until replaced.
           * @format date
           */
          endDate?: string;
        }>;
      };
      output: {
        /** The price schedule of an in-app purchase. */
        priceSchedule: {
          /** App Store Connect identifier for the price schedule. */
          id: string;
          /** Territory whose price the automatic prices of the other territories are derived from, as an ISO 3166-1 alpha-3 territory code, such as USA. */
          baseTerritory: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new metadata version of an in-app purchase so its display text and images can be edited and reviewed without taking the approved version off sale. */
    "app_store_connect.create_in_app_purchase_version": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
      };
      output: {
        /** A metadata version of an in-app purchase that App Review evaluates as a unit. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        version: {
          /** App Store Connect identifier for the in-app purchase version. */
          id: string;
          /** Sequential version number assigned by App Store Connect. */
          version?: number | null;
          /** Review state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add the display name and description of an in-app purchase version for one locale. */
    "app_store_connect.create_in_app_purchase_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase version.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseVersionId: string;
        /**
         * Locale the text is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Display name customers see, up to 30 characters.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Description customers see, up to 45 characters.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** A localized display name and description attached to one in-app purchase version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        localization: {
          /** App Store Connect identifier for the localization. */
          id: string;
          /** Display name customers see in this locale. */
          name?: string | null;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Description customers see in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set the sitemap catalog URL of an alternative marketplace app so Apple can crawl it and include the marketplace apps in Spotlight search. Each marketplace app holds one search detail. */
    "app_store_connect.create_marketplace_search_detail": {
      input: {
        /**
         * App Store Connect identifier of the marketplace app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * HTTPS URL of the marketplace sitemap, hosted on the registered domain.
         * @format uri
         */
        catalogUrl: string;
      };
      output: {
        /** The search catalog configuration of an alternative marketplace app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        marketplaceSearchDetail: {
          /** App Store Connect identifier of the marketplace search detail. */
          id: string;
          /** URL of the marketplace sitemap Apple crawls to include its apps in Spotlight search. */
          catalogUrl?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Register an Apple Pay merchant ID with the team. Payment processing and merchant identity certificates are issued for it afterwards with create_certificate. */
    "app_store_connect.create_merchant_id": {
      input: {
        /**
         * Display name for the merchant ID.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Merchant identifier in reverse-DNS form starting with merchant., such as merchant.com.example.shop.
         * @minLength 1
         * @pattern \S
         */
        identifier: string;
      };
      output: {
        /** An Apple Pay merchant ID registered with the team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        merchantId: {
          /** App Store Connect identifier of the merchant ID record. */
          id: string;
          /** Display name of the merchant ID. */
          name?: string | null;
          /** Merchant identifier string, such as merchant.com.example.shop. */
          identifier?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a featuring nomination for one or more apps. With submitted false it stays a draft you can edit; with submitted true it is sent to Apple's editorial team right away. */
    "app_store_connect.create_nomination": {
      input: {
        /**
         * Apps the nomination is about.
         * @minItems 1
         */
        relatedAppIds: Array<string>;
        /**
         * Name of the nomination.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /** What the nomination is about. */
        type: "APP_LAUNCH" | "APP_ENHANCEMENTS" | "NEW_CONTENT";
        /**
         * Description of the launch or content for the editorial team.
         * @minLength 1
         * @pattern \S
         */
        description: string;
        /** Submit the nomination to Apple immediately when true, or keep it as a draft when false. */
        submitted: boolean;
        /**
         * When the launch or content becomes available, as an ISO 8601 timestamp.
         * @format date-time
         */
        publishStartDate: string;
        /**
         * When the content stops being available, as an ISO 8601 timestamp.
         * @format date-time
         */
        publishEndDate?: string;
        /**
         * Additional notes for the editorial team.
         * @minLength 1
         * @pattern \S
         */
        notes?: string;
        /**
         * Device families the nomination applies to.
         * @minItems 1
         */
        deviceFamilies?: Array<"IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION">;
        /**
         * Locales the launch or content is available in.
         * @minItems 1
         */
        locales?: Array<string>;
        /** URLs of supplemental materials, such as press kits or videos, for the editorial team. */
        supplementalMaterialsUris?: Array<string>;
        /** Whether in-app events accompany the launch. */
        hasInAppEvents?: boolean;
        /** Whether the launch happens in selected markets before the rest. */
        launchInSelectMarketsFirst?: boolean;
        /** Whether the app is available for pre-order. */
        preOrderEnabled?: boolean;
        /**
         * In-app events that accompany the launch.
         * @minItems 1
         */
        inAppEventIds?: Array<string>;
        /**
         * Territories the launch or content is available in.
         * @minItems 1
         */
        supportedTerritoryIds?: Array<string>;
      };
      output: {
        /** A featuring nomination that tells Apple's editorial team about an upcoming launch, enhancement, or content drop. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        nomination: {
          /** App Store Connect identifier for the nomination. */
          id: string;
          /** Name of the nomination. */
          name?: string | null;
          /** What the nomination is about. */
          type?: "APP_LAUNCH" | "APP_ENHANCEMENTS" | "NEW_CONTENT" | null;
          /** Description of the launch or content for the editorial team. */
          description?: string | null;
          /** When the nomination was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the nomination was last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** When the nomination was submitted, as an ISO 8601 timestamp, or null while it is a draft. */
          submittedDate?: string | null;
          /** Whether the nomination is a draft, submitted, or archived. */
          state?: "DRAFT" | "SUBMITTED" | "ARCHIVED" | null;
          /** When the launch or content becomes available, as an ISO 8601 timestamp. */
          publishStartDate?: string | null;
          /** When the content stops being available, as an ISO 8601 timestamp, or null. */
          publishEndDate?: string | null;
          /** Device families the nomination applies to. */
          deviceFamilies?: Array<"IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION"> | null;
          /** Locales the launch or content is available in. */
          locales?: Array<string> | null;
          /** URLs of supplemental materials for the editorial team. */
          supplementalMaterialsUris?: Array<string> | null;
          /** Whether in-app events accompany the launch. */
          hasInAppEvents?: boolean | null;
          /** Whether the launch happens in selected markets before the rest. */
          launchInSelectMarketsFirst?: boolean | null;
          /** Additional notes for the editorial team. */
          notes?: string | null;
          /** Whether the app is available for pre-order. */
          preOrderEnabled?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Register a Wallet pass type ID with the team. Pass signing certificates are issued for it afterwards with create_certificate. */
    "app_store_connect.create_pass_type_id": {
      input: {
        /**
         * Display name for the pass type ID.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Pass type identifier in reverse-DNS form starting with pass., such as pass.com.example.ticket.
         * @minLength 1
         * @pattern \S
         */
        identifier: string;
      };
      output: {
        /** A Wallet pass type ID registered with the team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        passTypeId: {
          /** App Store Connect identifier of the pass type ID record. */
          id: string;
          /** Display name of the pass type ID. */
          name?: string | null;
          /** Pass type identifier string, such as pass.com.example.ticket. */
          identifier?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a provisioning profile for one bundle ID from the given certificates and, for development and ad hoc profiles, the devices allowed to install the app. App Store and in-house profile types take no devices. */
    "app_store_connect.create_profile": {
      input: {
        /**
         * Profile name shown in the developer portal and in Xcode.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /** Distribution method and platform of the profile. */
        profileType: "IOS_APP_DEVELOPMENT" | "IOS_APP_STORE" | "IOS_APP_ADHOC" | "IOS_APP_INHOUSE" | "MAC_APP_DEVELOPMENT" | "MAC_APP_STORE" | "MAC_APP_DIRECT" | "TVOS_APP_DEVELOPMENT" | "TVOS_APP_STORE" | "TVOS_APP_ADHOC" | "TVOS_APP_INHOUSE" | "MAC_CATALYST_APP_DEVELOPMENT" | "MAC_CATALYST_APP_STORE" | "MAC_CATALYST_APP_DIRECT";
        /**
         * App Store Connect identifier of the bundle ID record (the opaque resource id, not the reverse-DNS identifier string).
         * @minLength 1
         * @pattern \S
         */
        bundleIdId: string;
        /**
         * Certificates to embed in the profile.
         * @minItems 1
         */
        certificateIds: Array<string>;
        /**
         * Devices the profile allows, for development and ad hoc profile types.
         * @minItems 1
         */
        deviceIds?: Array<string>;
      };
      output: {
        /** A provisioning profile. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        profile: {
          /** App Store Connect identifier of the provisioning profile. */
          id: string;
          /** Profile name. */
          name?: string | null;
          /** Platform the profile applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Distribution method and platform of the profile. */
          profileType?: "IOS_APP_DEVELOPMENT" | "IOS_APP_STORE" | "IOS_APP_ADHOC" | "IOS_APP_INHOUSE" | "MAC_APP_DEVELOPMENT" | "MAC_APP_STORE" | "MAC_APP_DIRECT" | "TVOS_APP_DEVELOPMENT" | "TVOS_APP_STORE" | "TVOS_APP_ADHOC" | "TVOS_APP_INHOUSE" | "MAC_CATALYST_APP_DEVELOPMENT" | "MAC_CATALYST_APP_STORE" | "MAC_CATALYST_APP_DIRECT" | null;
          /** Whether the profile is still valid. A profile becomes INVALID when a certificate it contains is revoked or a device it contains is disabled. */
          profileState?: "ACTIVE" | "INVALID" | null;
          /** Base64-encoded provisioning profile. Decode it to a .mobileprovision or .provisionprofile file to install it. */
          profileContent?: string | null;
          /** UUID embedded in the profile. */
          uuid?: string | null;
          /** When the profile was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the profile expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Promote an in-app purchase or a subscription on the App Store product page of an app. Pass exactly one of inAppPurchaseId or subscriptionId. A promotional image must still be uploaded in App Store Connect before the promotion can be submitted. */
    "app_store_connect.create_promoted_purchase": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * In-app purchase to promote. Mutually exclusive with subscriptionId.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId?: string;
        /**
         * Auto-renewable subscription to promote. Mutually exclusive with inAppPurchaseId.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId?: string;
        /** Show the promotion to every customer; when false only customers the app selects through StoreKit see it. */
        visibleForAllUsers: boolean;
        /** Enable the promotion right away. */
        enabled?: boolean;
      };
      output: {
        /** An in-app purchase or subscription promoted on the App Store product page of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        promotedPurchase: {
          /** App Store Connect identifier for the promoted purchase. */
          id: string;
          /** Whether every customer sees the promotion, or only those the app selects through StoreKit. */
          visibleForAllUsers?: boolean | null;
          /** Whether the promotion is enabled. */
          enabled?: boolean | null;
          /** Review state of the promotion. */
          state?: "APPROVED" | "IN_REVIEW" | "PREPARE_FOR_SUBMISSION" | "REJECTED" | null;
          /** The promoted in-app purchase, or null when the promotion is for a subscription or the link was not returned. */
          inAppPurchaseId: string | null;
          /** The promoted subscription, or null when the promotion is for an in-app purchase or the link was not returned. */
          subscriptionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Start a new review submission for an app on one platform. Add items with add_review_submission_item, then send it with submit_review_submission. An app can have only one open submission per platform. */
    "app_store_connect.create_review_submission": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Content platform the submission is for. */
        platform: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
      };
      output: {
        /** A review submission that bundles the items sent to App Review together. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        reviewSubmission: {
          /** App Store Connect identifier for the review submission. */
          id: string;
          /** Content platform the submission targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** When the submission was sent to App Review, as an ISO 8601 timestamp, or null while it is still being prepared. */
          submittedDate?: string | null;
          /** Where the submission stands in App Review. */
          state?: "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "UNRESOLVED_ISSUES" | "CANCELING" | "COMPLETING" | "COMPLETE" | null;
          /** App Store Connect identifier of the App Store version under review, or null when the submission carries no version. */
          appStoreVersionForReviewId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create an auto-renewable subscription inside a subscription group. The product identifier cannot be changed afterwards, and the subscription still needs localizations, a price, availability, and a review submission before it can go on sale. */
    "app_store_connect.create_subscription": {
      input: {
        /**
         * App Store Connect identifier of the group the subscription belongs to.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupId: string;
        /**
         * Internal reference name of the subscription.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Product identifier the app uses to purchase the subscription. Unique per app and permanent.
         * @minLength 1
         * @pattern \S
         */
        productId: string;
        /** Length of one subscription period. */
        subscriptionPeriod?: "ONE_WEEK" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR";
        /** Allow the subscription to be shared with Family Sharing. */
        familySharable?: boolean;
        /**
         * Note shown to App Review alongside the subscription.
         * @minLength 1
         * @pattern \S
         */
        reviewNote?: string;
        /**
         * Rank of the subscription within its group, where 1 is the highest level of service.
         * @minimum 1
         */
        groupLevel?: number;
      };
      output: {
        /** An auto-renewable subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscription: {
          /** App Store Connect identifier for the subscription. */
          id: string;
          /** Internal reference name of the subscription. */
          name?: string | null;
          /** Product identifier used by the app to purchase the subscription. */
          productId?: string | null;
          /** Whether the subscription can be shared with Family Sharing. */
          familySharable?: boolean | null;
          /** Review and sale state of the subscription. */
          state?: "MISSING_METADATA" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED" | null;
          /** Length of one subscription period. */
          subscriptionPeriod?: "ONE_WEEK" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** Note shown to App Review alongside the subscription. */
          reviewNote?: string | null;
          /** Rank of the subscription within its group, where 1 is the highest level of service. */
          groupLevel?: number | null;
          /** App Store Connect identifier of the group the subscription belongs to. */
          subscriptionGroupId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a subscription group for an app. Subscriptions are created inside a group, and customers can switch between the subscriptions of one group. */
    "app_store_connect.create_subscription_group": {
      input: {
        /**
         * App Store Connect identifier of the app the group belongs to.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Internal reference name of the group, not shown to customers.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
      };
      output: {
        /** A subscription group that holds the auto-renewable subscriptions a customer can switch between. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroup: {
          /** App Store Connect identifier for the subscription group. */
          id: string;
          /** Internal reference name of the group, not shown to customers. */
          referenceName?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add the customer-facing name of a subscription group in one locale. Each locale can only be added once per group. */
    "app_store_connect.create_subscription_group_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription group.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupId: string;
        /**
         * Locale the localization is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Group name shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Alternative app name shown with the group in this locale.
         * @minLength 1
         * @pattern \S
         */
        customAppName?: string;
      };
      output: {
        /** Customer-facing name of a subscription group in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroupLocalization: {
          /** App Store Connect identifier for the subscription group localization. */
          id: string;
          /** Group name shown to customers in this locale. */
          name?: string | null;
          /** Alternative app name shown with the group in this locale, when one is set. */
          customAppName?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new draft version of a subscription group so its localized names can be edited and submitted for review while the approved version stays live. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.create_subscription_group_version": {
      input: {
        /**
         * App Store Connect identifier of the subscription group.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupId: string;
      };
      output: {
        /** A draft version of a subscription group that carries its localized names through App Review. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroupVersion: {
          /** App Store Connect identifier for the subscription group version. */
          id: string;
          /** Sequential version number assigned by App Store Connect. */
          version?: number | null;
          /** Review state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add the customer-facing name of a subscription group in one locale to a subscription group version. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.create_subscription_group_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription group version.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupVersionId: string;
        /**
         * Locale the localization is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Group name shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Alternative app name shown with the group in this locale.
         * @minLength 1
         * @pattern \S
         */
        customAppName?: string;
      };
      output: {
        /** Customer-facing name of a subscription group in one locale, attached to a subscription group version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroupLocalization: {
          /** App Store Connect identifier for the subscription group version localization. */
          id: string;
          /** Group name shown to customers in this locale. */
          name?: string | null;
          /** Alternative app name shown with the group in this locale, when one is set. */
          customAppName?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create an introductory offer for an auto-renewable subscription. A FREE_TRIAL offer needs no price point; PAY_AS_YOU_GO and PAY_UP_FRONT offers need the subscription price point of the territory. Apple allows one active introductory offer per territory at a time. Sandbox may take up to an hour to reflect the change. */
    "app_store_connect.create_subscription_introductory_offer": {
      input: {
        /**
         * App Store Connect identifier of the auto-renewable subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /** Length of one offer period, such as ONE_MONTH. */
        duration: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR";
        /** How the customer pays during the offer: PAY_AS_YOU_GO charges the offer price every period, PAY_UP_FRONT charges once for the whole offer, FREE_TRIAL charges nothing. */
        offerMode: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL";
        /**
         * Number of offer periods. Apple requires 1 for PAY_UP_FRONT and FREE_TRIAL offers.
         * @minimum 1
         */
        numberOfPeriods: number;
        /**
         * First day the offer is available, as YYYY-MM-DD. Omit to start immediately.
         * @format date
         */
        startDate?: string;
        /**
         * Last day the offer is available, as YYYY-MM-DD. Omit for no end date.
         * @format date
         */
        endDate?: string;
        /**
         * ISO 3166-1 alpha-3 code of the territory the offer applies to, such as USA. Omit to apply the offer to every territory the subscription is sold in, which Apple only accepts for FREE_TRIAL offers.
         * @minLength 1
         * @pattern \S
         */
        territoryId?: string;
        /**
         * Subscription price point that sets the offer price in that territory. Required for PAY_AS_YOU_GO and PAY_UP_FRONT offers.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPricePointId?: string;
        /** Subscription plan type the offer targets when the subscription has both a monthly and an upfront plan. */
        targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT";
      };
      output: {
        /** An introductory offer of an auto-renewable subscription in one territory. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionIntroductoryOffer: {
          /** App Store Connect identifier for the introductory offer. */
          id: string;
          /** First day the offer is available, as YYYY-MM-DD. */
          startDate?: string | null;
          /** Last day the offer is available, as YYYY-MM-DD. */
          endDate?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          /** The territory the price applies to, or null when App Store Connect did not return it. */
          territory: {
            /** ISO 3166-1 alpha-3 territory code, such as USA. */
            id: string;
            /** ISO 4217 currency of the territory, such as USD. */
            currency?: string | null;
            [key: string]: unknown;
          } | null;
          /** The subscription price point charged for the offer, or null when App Store Connect did not return it. */
          subscriptionPricePoint: {
            /** App Store Connect identifier for the subscription price point. */
            id: string;
            /** Price the customer pays in the territory currency, as a decimal string. */
            customerPrice?: string | null;
            /** Developer proceeds per period during the first year, as a decimal string. */
            proceeds?: string | null;
            /** Developer proceeds per period after the first year, as a decimal string. */
            proceedsYear2?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add the customer-facing name and description of a subscription in one locale. Each locale can only be added once per subscription. */
    "app_store_connect.create_subscription_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Locale the localization is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Subscription name shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Subscription description shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** Customer-facing name and description of a subscription in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionLocalization: {
          /** App Store Connect identifier for the subscription localization. */
          id: string;
          /** Subscription name shown to customers in this locale. */
          name?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Subscription description shown to customers in this locale. */
          description?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create an offer code configuration for an auto-renewable subscription together with its territory prices. Generate the redeemable codes afterwards with create_subscription_offer_code_custom_code or create_subscription_offer_code_one_time_use_code. */
    "app_store_connect.create_subscription_offer_code": {
      input: {
        /**
         * App Store Connect identifier of the auto-renewable subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Reference name of the configuration shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Customer groups allowed to redeem the codes: NEW, EXISTING, or EXPIRED subscribers.
         * @minItems 1
         */
        customerEligibilities: Array<"NEW" | "EXISTING" | "EXPIRED">;
        /** Whether the offer stacks with the introductory offer a new subscriber is entitled to, or replaces it. */
        offerEligibility: "STACK_WITH_INTRO_OFFERS" | "REPLACE_INTRO_OFFERS";
        /** Length of one offer period, such as ONE_MONTH. */
        duration: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR";
        /** How the customer pays during the offer: PAY_AS_YOU_GO charges the offer price every period, PAY_UP_FRONT charges once for the whole offer, FREE_TRIAL charges nothing. */
        offerMode: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL";
        /**
         * Number of offer periods. Apple requires 1 for PAY_UP_FRONT and FREE_TRIAL offers.
         * @minimum 1
         */
        numberOfPeriods: number;
        /** Whether the subscription renews at the regular price after the offer ends. */
        autoRenewEnabled?: boolean;
        /** Subscription plan type the offer targets when the subscription has both a monthly and an upfront plan. */
        targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT";
        /**
         * Territory prices submitted together with the offer. Each territory may appear once.
         * @minItems 1
         */
        prices: Array<{
          /**
           * ISO 3166-1 alpha-3 code of the territory the price applies to, such as USA.
           * @minLength 1
           * @pattern \S
           */
          territoryId: string;
          /**
           * Subscription price point to charge in that territory, taken from the subscription price points of the same territory. Omit for FREE_TRIAL offers.
           * @minLength 1
           * @pattern \S
           */
          subscriptionPricePointId?: string;
        }>;
      };
      output: {
        /** An offer code configuration of an auto-renewable subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionOfferCode: {
          /** App Store Connect identifier for the offer code configuration. */
          id: string;
          /** Reference name of the offer code configuration. */
          name?: string | null;
          /** Customer groups eligible to redeem the codes. */
          customerEligibilities?: Array<"NEW" | "EXISTING" | "EXPIRED"> | null;
          /** Whether the offer stacks with or replaces introductory offers. */
          offerEligibility?: "STACK_WITH_INTRO_OFFERS" | "REPLACE_INTRO_OFFERS" | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          /** Total number of codes generated so far. */
          totalNumberOfCodes?: number | null;
          /** Number of codes generated for production. */
          productionCodeCount?: number | null;
          /** Number of codes generated for the sandbox. */
          sandboxCodeCount?: number | null;
          /** Whether the configuration is active and its codes redeemable. */
          active?: boolean | null;
          /** Whether the subscription renews automatically after the offer ends. */
          autoRenewEnabled?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Generate a custom code for an offer code configuration, which many customers can redeem up to the given number of times. The code text cannot be changed once created. */
    "app_store_connect.create_subscription_offer_code_custom_code": {
      input: {
        /**
         * App Store Connect identifier of the offer code configuration.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeId: string;
        /**
         * The code customers type in, 3 to 64 characters of letters, digits, and underscores.
         * @minLength 1
         * @pattern \S
         */
        customCode: string;
        /**
         * Number of redemptions the code allows, up to 25,000.
         * @minimum 1
         * @maximum 25000
         */
        numberOfCodes: number;
        /**
         * Last day the code can be redeemed, as YYYY-MM-DD. Omit for no expiration.
         * @format date
         */
        expirationDate?: string;
      };
      output: {
        /** A custom code batch of an offer code configuration. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionOfferCodeCustomCode: {
          /** App Store Connect identifier for the custom code batch. */
          id: string;
          /** The custom code customers redeem. */
          customCode?: string | null;
          /** Number of redemptions the custom code allows. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the code can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the custom code is active and redeemable. */
          active?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Generate a batch of one-time use codes for an offer code configuration. Each code can be redeemed once; download the code values from App Store Connect afterwards. */
    "app_store_connect.create_subscription_offer_code_one_time_use_code": {
      input: {
        /**
         * App Store Connect identifier of the offer code configuration.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeId: string;
        /**
         * Number of single-use codes to generate, up to 25,000.
         * @minimum 1
         * @maximum 25000
         */
        numberOfCodes: number;
        /**
         * Last day the codes can be redeemed, as YYYY-MM-DD.
         * @format date
         */
        expirationDate: string;
        /** Environment the codes redeem in. Defaults to PRODUCTION on Apple's side. */
        environment?: "PRODUCTION" | "SANDBOX";
      };
      output: {
        /** A batch of one-time use codes of an offer code configuration. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionOfferCodeOneTimeUseCode: {
          /** App Store Connect identifier for the one-time use code batch. */
          id: string;
          /** Number of single-use codes in the batch. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the codes can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the batch is active and its codes redeemable. */
          active?: boolean | null;
          /** Environment the codes redeem in. */
          environment?: "PRODUCTION" | "SANDBOX" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Make one payment plan of a subscription available in a set of territories. Each plan type can only have one availability record per subscription; use update_subscription_plan_availability to change an existing one. */
    "app_store_connect.create_subscription_plan_availability": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /** Payment plan of the subscription: UPFRONT charges the full price at the start of each period, MONTHLY is a 12-month commitment billed monthly (App Store Connect API 4.4). */
        planType: "MONTHLY" | "UPFRONT";
        /**
         * Territories the plan is sold in.
         * @minItems 1
         */
        availableTerritoryIds: Array<string>;
        /** Also make the plan available automatically in territories the App Store adds later. */
        availableInNewTerritories?: boolean;
      };
      output: {
        /** Territory availability of one payment plan of a subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionPlanAvailability: {
          /** App Store Connect identifier for the subscription plan availability. */
          id: string;
          /** Whether the plan becomes available automatically in territories the App Store adds later. */
          availableInNewTerritories?: boolean | null;
          /** Payment plan of the subscription: UPFRONT charges the full price at the start of each period, MONTHLY is a 12-month commitment billed monthly (App Store Connect API 4.4). */
          planType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set the price of a subscription in one territory from a subscription price point, either as the initial price or as a scheduled price change. Use list_subscription_price_points to find the price point identifier. A scheduled change can be removed again with delete_subscription_price. */
    "app_store_connect.create_subscription_price": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * App Store Connect identifier of the subscription price point that defines the new price.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPricePointId: string;
        /**
         * Territory the price applies to. Omit to let App Store Connect use the territory of the price point. ISO 3166-1 alpha-3 App Store territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territoryId?: string;
        /**
         * Date the price takes effect, as YYYY-MM-DD. Omit when setting the initial price of a subscription that has not been on sale yet.
         * @format date
         */
        startDate?: string;
        /** Keep existing subscribers at their current price instead of moving them to the new price. */
        preserveCurrentPrice?: boolean;
        /** Payment plan of the subscription: UPFRONT charges the full price at the start of each period, MONTHLY is a 12-month commitment billed monthly (App Store Connect API 4.4). */
        planType?: "MONTHLY" | "UPFRONT";
      };
      output: {
        /** A configured price of a subscription in one territory. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionPrice: {
          /** App Store Connect identifier for the subscription price. */
          id: string;
          /** Date the price takes effect, as YYYY-MM-DD, or null for a price that is not scheduled. */
          startDate?: string | null;
          /** Whether existing subscribers keep this price after a later price increase. */
          preserved?: boolean | null;
          /** Payment plan of the subscription: UPFRONT charges the full price at the start of each period, MONTHLY is a 12-month commitment billed monthly (App Store Connect API 4.4). */
          planType?: "MONTHLY" | "UPFRONT" | null;
          /** Territory the price applies to. ISO 3166-1 alpha-3 App Store territory code, such as USA. */
          territoryId: string | null;
          /** The price point the price is based on. Only the id is filled in when App Store Connect did not return the price point record; null when the price has no price point. */
          subscriptionPricePoint: {
            /** App Store Connect identifier for the subscription price point. */
            id: string;
            /** Price the customer pays, as a decimal string in the territory currency. */
            customerPrice?: string | null;
            /** Developer proceeds during the first year of a subscription, as a decimal string. */
            proceeds?: string | null;
            /** Developer proceeds after the first year of a subscription, as a decimal string. */
            proceedsYear2?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a promotional offer for an auto-renewable subscription together with its territory prices. The offer code becomes the identifier your app passes to StoreKit and cannot be changed later. */
    "app_store_connect.create_subscription_promotional_offer": {
      input: {
        /**
         * App Store Connect identifier of the auto-renewable subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Reference name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Offer identifier your app passes to StoreKit, up to 64 characters of letters, digits, underscores, and periods.
         * @minLength 1
         * @pattern \S
         */
        offerCode: string;
        /** Length of one offer period, such as ONE_MONTH. */
        duration: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR";
        /** How the customer pays during the offer: PAY_AS_YOU_GO charges the offer price every period, PAY_UP_FRONT charges once for the whole offer, FREE_TRIAL charges nothing. */
        offerMode: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL";
        /**
         * Number of offer periods. Apple requires 1 for PAY_UP_FRONT and FREE_TRIAL offers.
         * @minimum 1
         */
        numberOfPeriods: number;
        /** Subscription plan type the offer targets when the subscription has both a monthly and an upfront plan. */
        targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT";
        /**
         * Territory prices submitted together with the offer. Each territory may appear once.
         * @minItems 1
         */
        prices: Array<{
          /**
           * ISO 3166-1 alpha-3 code of the territory the price applies to, such as USA.
           * @minLength 1
           * @pattern \S
           */
          territoryId: string;
          /**
           * Subscription price point to charge in that territory, taken from the subscription price points of the same territory. Omit for FREE_TRIAL offers.
           * @minLength 1
           * @pattern \S
           */
          subscriptionPricePointId?: string;
        }>;
      };
      output: {
        /** A promotional offer of an auto-renewable subscription for existing or lapsed subscribers. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionPromotionalOffer: {
          /** App Store Connect identifier for the promotional offer. */
          id: string;
          /** Reference name shown in App Store Connect. */
          name?: string | null;
          /** Offer identifier your app passes to StoreKit when presenting the offer. */
          offerCode?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a new draft version of a subscription so its localized metadata can be edited and submitted for review while the approved version stays live. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.create_subscription_version": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
      };
      output: {
        /** A draft version of a subscription that carries its localized metadata through App Review. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionVersion: {
          /** App Store Connect identifier for the subscription version. */
          id: string;
          /** Sequential version number assigned by App Store Connect. */
          version?: number | null;
          /** Review state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Add the customer-facing name and description of a subscription in one locale to a subscription version. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.create_subscription_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription version.
         * @minLength 1
         * @pattern \S
         */
        subscriptionVersionId: string;
        /**
         * Locale the localization is written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Subscription name shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * Subscription description shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** Customer-facing name and description of a subscription in one locale, attached to a subscription version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionLocalization: {
          /** App Store Connect identifier for the subscription version localization. */
          id: string;
          /** Subscription name shown to customers in this locale. */
          name?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Subscription description shown to customers in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Register a webhook on an app so App Store Connect posts a signed notification to the URL whenever one of the chosen events happens. Use ping_webhook afterwards to test the endpoint. */
    "app_store_connect.create_webhook": {
      input: {
        /**
         * App Store Connect identifier of the app the webhook belongs to.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Display name for the webhook.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /**
         * HTTPS endpoint App Store Connect posts notifications to.
         * @format uri
         */
        url: string;
        /**
         * Shared secret App Store Connect uses to sign every notification: the x-apple-signature header carries hmacsha256=<hex HMAC-SHA256 of the request body>. Choose a value that is hard to guess.
         * @minLength 1
         * @pattern \S
         */
        secret: string;
        /**
         * Event types the webhook receives.
         * @minItems 1
         */
        eventTypes: Array<"ALTERNATIVE_DISTRIBUTION_PACKAGE_AVAILABLE_UPDATED" | "ALTERNATIVE_DISTRIBUTION_PACKAGE_VERSION_CREATED" | "ALTERNATIVE_DISTRIBUTION_TERRITORY_AVAILABILITY_UPDATED" | "APP_STORE_VERSION_APP_VERSION_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_APP_STORE_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_EXTERNAL_BETA_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_INTERNAL_BETA_RELEASE_CREATED" | "BACKGROUND_ASSET_VERSION_STATE_UPDATED" | "BETA_FEEDBACK_CRASH_SUBMISSION_CREATED" | "BETA_FEEDBACK_SCREENSHOT_SUBMISSION_CREATED" | "BUILD_BETA_DETAIL_EXTERNAL_BUILD_STATE_UPDATED" | "BUILD_UPLOAD_STATE_UPDATED">;
        /** Whether the webhook starts enabled. Defaults to true. */
        enabled?: boolean;
      };
      output: {
        /** A webhook registered on an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        webhook: {
          /** App Store Connect identifier for the webhook. */
          id: string;
          /** Display name of the webhook. */
          name?: string | null;
          /** HTTPS endpoint App Store Connect posts notifications to. */
          url?: string | null;
          /** Whether App Store Connect currently sends notifications to the webhook. */
          enabled?: boolean | null;
          /** Event types the webhook is subscribed to. */
          eventTypes?: Array<"ALTERNATIVE_DISTRIBUTION_PACKAGE_AVAILABLE_UPDATED" | "ALTERNATIVE_DISTRIBUTION_PACKAGE_VERSION_CREATED" | "ALTERNATIVE_DISTRIBUTION_TERRITORY_AVAILABILITY_UPDATED" | "APP_STORE_VERSION_APP_VERSION_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_APP_STORE_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_EXTERNAL_BETA_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_INTERNAL_BETA_RELEASE_CREATED" | "BACKGROUND_ASSET_VERSION_STATE_UPDATED" | "BETA_FEEDBACK_CRASH_SUBMISSION_CREATED" | "BETA_FEEDBACK_SCREENSHOT_SUBMISSION_CREATED" | "BUILD_BETA_DETAIL_EXTERNAL_BUILD_STATE_UPDATED" | "BUILD_UPLOAD_STATE_UPDATED"> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Create a win-back offer for lapsed subscribers of an auto-renewable subscription together with its prices, one subscription price point per territory. The offer identifier, duration, mode, and period count cannot be changed later. */
    "app_store_connect.create_win_back_offer": {
      input: {
        /**
         * App Store Connect identifier of the auto-renewable subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Reference name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
        /**
         * Offer identifier your app passes to StoreKit, up to 64 characters of letters, digits, underscores, and periods.
         * @minLength 1
         * @pattern \S
         */
        offerId: string;
        /** Length of one offer period, such as ONE_MONTH. */
        duration: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR";
        /** How the customer pays during the offer: PAY_AS_YOU_GO charges the offer price every period, PAY_UP_FRONT charges once for the whole offer, FREE_TRIAL charges nothing. */
        offerMode: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL";
        /**
         * Number of offer periods. Apple requires 1 for PAY_UP_FRONT and FREE_TRIAL offers.
         * @minimum 1
         */
        periodCount: number;
        /**
         * Minimum number of months the customer must have paid for the subscription before lapsing.
         * @minimum 0
         */
        customerEligibilityPaidSubscriptionDurationInMonths: number;
        /** Inclusive range in months. Leave a bound out to keep that side open. */
        customerEligibilityTimeSinceLastSubscribedInMonths: {
          /**
           * Lower bound of the range in months.
           * @minimum 0
           */
          minimum?: number;
          /**
           * Upper bound of the range in months.
           * @minimum 0
           */
          maximum?: number;
        };
        /**
         * Months a customer must wait after redeeming a win-back offer before another one is offered. Omit for no waiting period.
         * @minimum 0
         */
        customerEligibilityWaitBetweenOffersInMonths?: number;
        /**
         * First day the offer is available, as YYYY-MM-DD.
         * @format date
         */
        startDate: string;
        /**
         * Last day the offer is available, as YYYY-MM-DD. Omit for no end date.
         * @format date
         */
        endDate?: string;
        /** Priority the App Store uses when several win-back offers apply to the same customer. */
        priority: "HIGH" | "NORMAL";
        /** Whether Apple may promote the offer on the App Store with auto-generated assets. */
        promotionIntent?: "NOT_PROMOTED" | "USE_AUTO_GENERATED_ASSETS";
        /** Subscription plan type the offer targets when the subscription has both a monthly and an upfront plan. */
        targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT";
        /**
         * Prices submitted together with the offer, one subscription price point per territory.
         * @minItems 1
         */
        prices: Array<{
          /**
           * Subscription price point to charge, taken from the subscription price points of the territory it belongs to.
           * @minLength 1
           * @pattern \S
           */
          subscriptionPricePointId: string;
        }>;
      };
      output: {
        /** A win-back offer that targets lapsed subscribers of an auto-renewable subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        winBackOffer: {
          /** App Store Connect identifier for the win-back offer. */
          id: string;
          /** Reference name shown in App Store Connect. */
          referenceName?: string | null;
          /** Offer identifier your app passes to StoreKit. */
          offerId?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          periodCount?: number | null;
          /** Minimum number of months the customer must have paid for the subscription before lapsing. */
          customerEligibilityPaidSubscriptionDurationInMonths?: number | null;
          /** Inclusive range of months since the customer last subscribed. */
          customerEligibilityTimeSinceLastSubscribedInMonths?: {
            /** Lower bound in months. */
            minimum?: number | null;
            /** Upper bound in months. */
            maximum?: number | null;
            [key: string]: unknown;
          } | null;
          /** Months a customer must wait after redeeming a win-back offer before another one is offered. */
          customerEligibilityWaitBetweenOffersInMonths?: number | null;
          /** First day the offer is available, as YYYY-MM-DD. */
          startDate?: string | null;
          /** Last day the offer is available, as YYYY-MM-DD. */
          endDate?: string | null;
          /** Priority the App Store uses when several win-back offers apply. */
          priority?: "HIGH" | "NORMAL" | null;
          /** Whether Apple may promote the offer on the App Store with auto-generated assets. */
          promotionIntent?: "NOT_PROMOTED" | "USE_AUTO_GENERATED_ASSETS" | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Delete an accessibility declaration. */
    "app_store_connect.delete_accessibility_declaration": {
      input: {
        /**
         * App Store Connect identifier of the accessibility declaration.
         * @minLength 1
         * @pattern \S
         */
        accessibilityDeclarationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted accessibility declaration. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an alternative distribution domain. Apps served from that domain can no longer be installed through it. */
    "app_store_connect.delete_alternative_distribution_domain": {
      input: {
        /**
         * App Store Connect identifier of the alternative distribution domain.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionDomainId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted alternative distribution domain. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove an alternative distribution key from the account. Tokens signed with the matching private key stop being accepted. */
    "app_store_connect.delete_alternative_distribution_key": {
      input: {
        /**
         * App Store Connect identifier of the alternative distribution key.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionKeyId: string;
      };
      output: {
        /** App Store Connect identifier of the removed alternative distribution key. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an analytics report request. App Store Connect stops producing its reports and the existing report instances are no longer listed. */
    "app_store_connect.delete_analytics_report_request": {
      input: {
        /**
         * App Store Connect identifier of the report request.
         * @minLength 1
         * @pattern \S
         */
        analyticsReportRequestId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted report request. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an Android app mapping. */
    "app_store_connect.delete_android_to_ios_app_mapping_detail": {
      input: {
        /**
         * App Store Connect identifier of the Android app mapping.
         * @minLength 1
         * @pattern \S
         */
        androidToIosAppMappingDetailId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted Android app mapping. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an advanced App Clip experience. App Store Connect has no DELETE endpoint for it; the experience is removed by setting its documented removed flag, after which users can no longer invoke it. */
    "app_store_connect.delete_app_clip_advanced_experience": {
      input: {
        /**
         * App Store Connect identifier of the advanced App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipAdvancedExperienceId: string;
      };
      output: {
        /** App Store Connect identifier of the removed advanced App Clip experience. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a default App Clip experience together with its localizations and review detail. */
    "app_store_connect.delete_app_clip_default_experience": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted default App Clip experience. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a locale from a default App Clip experience together with its header image. */
    "app_store_connect.delete_app_clip_default_experience_localization": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience localization.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted default App Clip experience localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a custom product page together with all of its versions and localizations. Its App Store URL stops working. */
    "app_store_connect.delete_app_custom_product_page": {
      input: {
        /**
         * App Store Connect identifier of the custom product page.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted custom product page. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a locale from a custom product page version, including the screenshots and previews attached to it. */
    "app_store_connect.delete_app_custom_product_page_localization": {
      input: {
        /**
         * App Store Connect identifier of the custom product page localization.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an in-app event together with its localizations. A published event is removed from the App Store. */
    "app_store_connect.delete_app_event": {
      input: {
        /**
         * App Store Connect identifier of the in-app event.
         * @minLength 1
         * @pattern \S
         */
        appEventId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted in-app event. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a locale from an in-app event together with the screenshots and video clips uploaded for it. */
    "app_store_connect.delete_app_event_localization": {
      input: {
        /**
         * App Store Connect identifier of the in-app event localization.
         * @minLength 1
         * @pattern \S
         */
        appEventLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted in-app event localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a locale from an app info record. The app stops being listed in that language once the change is submitted. */
    "app_store_connect.delete_app_info_localization": {
      input: {
        /**
         * App Store Connect identifier of the app info localization.
         * @minLength 1
         * @pattern \S
         */
        appInfoLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted app info localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an App Store version that has not been submitted yet, together with its localizations and review information. */
    "app_store_connect.delete_app_store_version": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted version. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a product page optimization test together with its treatments. A running test is stopped; results already collected are lost. */
    "app_store_connect.delete_app_store_version_experiment": {
      input: {
        /**
         * App Store Connect identifier of the product page optimization test.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted test. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a treatment from a product page optimization test, including its localizations and screenshots. */
    "app_store_connect.delete_app_store_version_experiment_treatment": {
      input: {
        /**
         * App Store Connect identifier of the treatment.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentTreatmentId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted treatment. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a locale from a treatment, including the screenshots and previews attached to it. */
    "app_store_connect.delete_app_store_version_experiment_treatment_localization": {
      input: {
        /**
         * App Store Connect identifier of the treatment localization.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentTreatmentLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted treatment localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove the App Store metadata of one locale from an App Store version, including its screenshots and previews. */
    "app_store_connect.delete_app_store_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the version localization.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted version localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Turn off phased release for an App Store version so the approved update reaches every user at once. */
    "app_store_connect.delete_app_store_version_phased_release": {
      input: {
        /**
         * App Store Connect identifier of the phased release.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionPhasedReleaseId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted phased release. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a TestFlight App Clip invocation and its localized titles. Testers can no longer launch that experience from TestFlight. */
    "app_store_connect.delete_beta_app_clip_invocation": {
      input: {
        /**
         * App Store Connect identifier of the App Clip invocation.
         * @minLength 1
         * @pattern \S
         */
        betaAppClipInvocationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted App Clip invocation. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete one localized title of a TestFlight App Clip invocation. */
    "app_store_connect.delete_beta_app_clip_invocation_localization": {
      input: {
        /**
         * App Store Connect identifier of the localized title.
         * @minLength 1
         * @pattern \S
         */
        betaAppClipInvocationLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted localized title. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete the TestFlight test information of an app in one locale. Testers in that locale fall back to the primary locale. */
    "app_store_connect.delete_beta_app_localization": {
      input: {
        /**
         * App Store Connect identifier of the beta app localization.
         * @minLength 1
         * @pattern \S
         */
        betaAppLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted beta app localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete the "What to Test" notes of one build in one locale. Testers in that locale fall back to the notes of the primary locale. */
    "app_store_connect.delete_beta_build_localization": {
      input: {
        /**
         * App Store Connect identifier of the beta build localization.
         * @minLength 1
         * @pattern \S
         */
        betaBuildLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted beta build localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a crash feedback submission from App Store Connect. The crash log and comment are removed permanently. */
    "app_store_connect.delete_beta_feedback_crash_submission": {
      input: {
        /**
         * App Store Connect identifier of the crash feedback submission to delete.
         * @minLength 1
         * @pattern \S
         */
        betaFeedbackCrashSubmissionId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted crash feedback submission. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a screenshot feedback submission from App Store Connect. The screenshots and comment are removed permanently. */
    "app_store_connect.delete_beta_feedback_screenshot_submission": {
      input: {
        /**
         * App Store Connect identifier of the screenshot feedback submission to delete.
         * @minLength 1
         * @pattern \S
         */
        betaFeedbackScreenshotSubmissionId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted screenshot feedback submission. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a TestFlight group. Testers who only belonged to that group lose access to its builds. */
    "app_store_connect.delete_beta_group": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted TestFlight group. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete the recruitment criteria of a TestFlight group, so anyone with the public link can join again. */
    "app_store_connect.delete_beta_recruitment_criterion": {
      input: {
        /**
         * App Store Connect identifier of the recruitment criteria.
         * @minLength 1
         * @pattern \S
         */
        betaRecruitmentCriterionId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted recruitment criteria. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a TestFlight tester from the team, revoking their access to every build and group. */
    "app_store_connect.delete_beta_tester": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight tester.
         * @minLength 1
         * @pattern \S
         */
        betaTesterId: string;
      };
      output: {
        /** App Store Connect identifier of the removed TestFlight tester. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a bundle ID from the team. App Store Connect refuses to delete a bundle ID that an app record still uses. */
    "app_store_connect.delete_bundle_id": {
      input: {
        /**
         * App Store Connect identifier of the bundle ID record (the opaque resource id, not the reverse-DNS identifier string).
         * @minLength 1
         * @pattern \S
         */
        bundleIdId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted bundle ID. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an Xcode Cloud product, which removes every workflow and build history of that app or framework from Xcode Cloud. */
    "app_store_connect.delete_ci_product": {
      input: {
        /**
         * App Store Connect identifier of the Xcode Cloud product.
         * @minLength 1
         * @pattern \S
         */
        ciProductId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted Xcode Cloud product. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an Xcode Cloud workflow together with its build history. Builds already delivered to TestFlight or App Store Connect are kept. */
    "app_store_connect.delete_ci_workflow": {
      input: {
        /**
         * App Store Connect identifier of the workflow.
         * @minLength 1
         * @pattern \S
         */
        ciWorkflowId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted workflow. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a published developer response from an App Store review. */
    "app_store_connect.delete_customer_review_response": {
      input: {
        /**
         * App Store Connect identifier of the developer response.
         * @minLength 1
         * @pattern \S
         */
        customerReviewResponseId: string;
      };
      output: {
        /** App Store Connect identifier of the removed developer response. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove the custom end user license agreement of an app so Apple's standard EULA applies in every territory. */
    "app_store_connect.delete_end_user_license_agreement": {
      input: {
        /**
         * App Store Connect identifier of the license agreement.
         * @minLength 1
         * @pattern \S
         */
        endUserLicenseAgreementId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted license agreement. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a Game Center achievement together with all of its versions and localizations. Players lose the achievement and the progress recorded for it. */
    "app_store_connect.delete_game_center_achievement": {
      input: {
        /**
         * App Store Connect identifier of the achievement.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted achievement. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a locale from a Game Center achievement version, including the achievement image uploaded for it. */
    "app_store_connect.delete_game_center_achievement_localization": {
      input: {
        /**
         * App Store Connect identifier of the achievement localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted achievement localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a Game Center activity together with its versions, localizations and images. Archive it with update_game_center_activity instead when players may still hold invitations to it. */
    "app_store_connect.delete_game_center_activity": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted Game Center activity. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a locale from a Game Center activity version, including the image attached to it. */
    "app_store_connect.delete_game_center_activity_localization": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted activity localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a Game Center challenge together with all of its versions, localizations and images. */
    "app_store_connect.delete_game_center_challenge": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted Game Center challenge. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove one locale from a Game Center challenge version, including the image attached to it. */
    "app_store_connect.delete_game_center_challenge_localization": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a Game Center group. The apps that were in the group keep their own Game Center records and stop sharing progress with each other. */
    "app_store_connect.delete_game_center_group": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted Game Center group. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a Game Center leaderboard together with its versions, localizations and the scores players submitted to it. */
    "app_store_connect.delete_game_center_leaderboard": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted leaderboard. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a locale from a Game Center leaderboard version, including the image uploaded for it. */
    "app_store_connect.delete_game_center_leaderboard_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted leaderboard localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a Game Center leaderboard set together with its versions and localizations. The leaderboards that were in the set are kept and stay available on their own. */
    "app_store_connect.delete_game_center_leaderboard_set": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted leaderboard set. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a locale from a Game Center leaderboard set version, including the image uploaded for that locale. */
    "app_store_connect.delete_game_center_leaderboard_set_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted leaderboard set localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete the name a leaderboard is shown under inside a leaderboard set for one locale. The leaderboard stays in the set and falls back to its usual localized name. */
    "app_store_connect.delete_game_center_leaderboard_set_member_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set member localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetMemberLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted member localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a Game Center matchmaking queue. Requests the game sends to the deleted queue are no longer matched. */
    "app_store_connect.delete_game_center_matchmaking_queue": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking queue.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted matchmaking queue. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a rule from its Game Center matchmaking rule set. Candidate matches are no longer checked against it. */
    "app_store_connect.delete_game_center_matchmaking_rule": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted matchmaking rule. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a Game Center matchmaking rule set together with its teams and rules. A queue that still points at the rule set stops matching requests. */
    "app_store_connect.delete_game_center_matchmaking_rule_set": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted matchmaking rule set. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a team from its Game Center matchmaking rule set. Rule expressions that still refer to the team by name stop working. */
    "app_store_connect.delete_game_center_matchmaking_team": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking team.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingTeamId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted matchmaking team. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an in-app purchase that has never been approved. Its product identifier can never be used again for this app. */
    "app_store_connect.delete_in_app_purchase": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted in-app purchase. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete one locale of an in-app purchase's display name and description. */
    "app_store_connect.delete_in_app_purchase_localization": {
      input: {
        /**
         * App Store Connect identifier of the localization.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete one locale from an in-app purchase version. */
    "app_store_connect.delete_in_app_purchase_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the version localization.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseVersionLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted version localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete the marketplace search detail of an alternative marketplace app. Apple stops crawling the catalog URL. */
    "app_store_connect.delete_marketplace_search_detail": {
      input: {
        /**
         * App Store Connect identifier of the marketplace search detail.
         * @minLength 1
         * @pattern \S
         */
        marketplaceSearchDetailId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted marketplace search detail. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an Apple Pay merchant ID from the team. Apple Pay transactions and certificates that reference it stop working. */
    "app_store_connect.delete_merchant_id": {
      input: {
        /**
         * App Store Connect identifier of the merchant ID record (the opaque resource id, not the merchant.com.example identifier string).
         * @minLength 1
         * @pattern \S
         */
        merchantIdId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted merchant ID. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a featuring nomination. */
    "app_store_connect.delete_nomination": {
      input: {
        /**
         * App Store Connect identifier of the nomination.
         * @minLength 1
         * @pattern \S
         */
        nominationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted nomination. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a Wallet pass type ID from the team. Passes signed for it can no longer be updated and its certificates stop working. */
    "app_store_connect.delete_pass_type_id": {
      input: {
        /**
         * App Store Connect identifier of the pass type ID record (the opaque resource id, not the pass.com.example identifier string).
         * @minLength 1
         * @pattern \S
         */
        passTypeIdId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted pass type ID. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a provisioning profile so it can no longer be downloaded or used to sign new builds. Create a replacement with create_profile when the bundle ID still needs one. */
    "app_store_connect.delete_profile": {
      input: {
        /**
         * App Store Connect identifier of the provisioning profile.
         * @minLength 1
         * @pattern \S
         */
        profileId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted profile. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a promotion from the App Store product page of an app. */
    "app_store_connect.delete_promoted_purchase": {
      input: {
        /**
         * App Store Connect identifier of the promoted purchase.
         * @minLength 1
         * @pattern \S
         */
        promotedPurchaseId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted promoted purchase. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an item from a review submission that has not been sent to App Review yet. */
    "app_store_connect.delete_review_submission_item": {
      input: {
        /**
         * App Store Connect identifier of the review submission item.
         * @minLength 1
         * @pattern \S
         */
        reviewSubmissionItemId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted review submission item. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an auto-renewable subscription. App Store Connect only deletes subscriptions that have never been approved; approved subscriptions can only be removed from sale. */
    "app_store_connect.delete_subscription": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted subscription. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a subscription group. App Store Connect only deletes groups that no longer contain subscriptions that have been approved. */
    "app_store_connect.delete_subscription_group": {
      input: {
        /**
         * App Store Connect identifier of the subscription group.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted subscription group. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete the customer-facing name of a subscription group in one locale. */
    "app_store_connect.delete_subscription_group_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription group localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted subscription group localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete one localization from a subscription group version. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.delete_subscription_group_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription group version localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted subscription group version localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete an introductory offer. Customers who already redeemed it keep their discounted period. */
    "app_store_connect.delete_subscription_introductory_offer": {
      input: {
        /**
         * App Store Connect identifier of the introductory offer.
         * @minLength 1
         * @pattern \S
         */
        subscriptionIntroductoryOfferId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted introductory offer. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete the customer-facing name and description of a subscription in one locale. */
    "app_store_connect.delete_subscription_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted subscription localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a scheduled price change of a subscription before it takes effect. The price that is already in effect cannot be deleted. */
    "app_store_connect.delete_subscription_price": {
      input: {
        /**
         * App Store Connect identifier of the subscription price.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPriceId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted subscription price. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a promotional offer. Apps can no longer present it, and customers who already redeemed it keep their discounted period. */
    "app_store_connect.delete_subscription_promotional_offer": {
      input: {
        /**
         * App Store Connect identifier of the promotional offer.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPromotionalOfferId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted promotional offer. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete one localization from a subscription version. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.delete_subscription_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription version localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionLocalizationId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted subscription version localization. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Remove a member from the App Store Connect team. They lose access to every app and to any API keys tied to their account. */
    "app_store_connect.delete_user": {
      input: {
        /**
         * App Store Connect identifier of the team member.
         * @minLength 1
         * @pattern \S
         */
        userId: string;
      };
      output: {
        /** App Store Connect identifier of the removed team member. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a webhook. App Store Connect stops sending notifications to its URL and its delivery history is no longer reachable. */
    "app_store_connect.delete_webhook": {
      input: {
        /**
         * App Store Connect identifier of the webhook.
         * @minLength 1
         * @pattern \S
         */
        webhookId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted webhook. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Delete a win-back offer. Apps can no longer present it, and customers who already redeemed it keep their discounted period. */
    "app_store_connect.delete_win_back_offer": {
      input: {
        /**
         * App Store Connect identifier of the win-back offer.
         * @minLength 1
         * @pattern \S
         */
        winBackOfferId: string;
      };
      output: {
        /** App Store Connect identifier of the deleted win-back offer. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Disable a capability on a bundle ID and discard its configuration. Provisioning profiles that relied on the capability become invalid. */
    "app_store_connect.disable_bundle_id_capability": {
      input: {
        /**
         * App Store Connect identifier of the bundle ID capability.
         * @minLength 1
         * @pattern \S
         */
        bundleIdCapabilityId: string;
      };
      output: {
        /** App Store Connect identifier of the disabled capability. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Enable a capability such as push notifications or associated domains on a bundle ID. Provisioning profiles created for the bundle ID before the change may need to be regenerated to carry the new entitlement. */
    "app_store_connect.enable_bundle_id_capability": {
      input: {
        /**
         * App Store Connect identifier of the bundle ID record (the opaque resource id, not the reverse-DNS identifier string).
         * @minLength 1
         * @pattern \S
         */
        bundleIdId: string;
        /** Capability to enable. */
        capabilityType: "ICLOUD" | "IN_APP_PURCHASE" | "GAME_CENTER" | "PUSH_NOTIFICATIONS" | "WALLET" | "INTER_APP_AUDIO" | "MAPS" | "ASSOCIATED_DOMAINS" | "PERSONAL_VPN" | "APP_GROUPS" | "HEALTHKIT" | "HOMEKIT" | "WIRELESS_ACCESSORY_CONFIGURATION" | "APPLE_PAY" | "DATA_PROTECTION" | "SIRIKIT" | "NETWORK_EXTENSIONS" | "MULTIPATH" | "HOT_SPOT" | "NFC_TAG_READING" | "CLASSKIT" | "AUTOFILL_CREDENTIAL_PROVIDER" | "ACCESS_WIFI_INFORMATION" | "NETWORK_CUSTOM_PROTOCOL" | "COREMEDIA_HLS_LOW_LATENCY" | "SYSTEM_EXTENSION_INSTALL" | "USER_MANAGEMENT" | "APPLE_ID_AUTH";
        /**
         * Capability settings to configure, for capabilities such as ICLOUD, DATA_PROTECTION, and APPLE_ID_AUTH that expose options. Each setting names its key and the options to enable.
         * @minItems 1
         */
        settings?: Array<{
          /** Setting key. */
          key: "ICLOUD_VERSION" | "DATA_PROTECTION_PERMISSION_LEVEL" | "APPLE_ID_AUTH_APP_CONSENT";
          /**
           * Options of the setting to configure.
           * @minItems 1
           */
          options?: Array<{
            /** Option key. */
            key: "XCODE_5" | "XCODE_6" | "COMPLETE_PROTECTION" | "PROTECTED_UNLESS_OPEN" | "PROTECTED_UNTIL_FIRST_USER_AUTH" | "PRIMARY_APP_CONSENT";
            /** Whether to enable the option. */
            enabled?: boolean;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        }>;
      };
      output: {
        /** A capability enabled on a bundle ID. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        bundleIdCapability: {
          /** App Store Connect identifier of the bundle ID capability. */
          id: string;
          /** Capability that is enabled. */
          capabilityType?: "ICLOUD" | "IN_APP_PURCHASE" | "GAME_CENTER" | "PUSH_NOTIFICATIONS" | "WALLET" | "INTER_APP_AUDIO" | "MAPS" | "ASSOCIATED_DOMAINS" | "PERSONAL_VPN" | "APP_GROUPS" | "HEALTHKIT" | "HOMEKIT" | "WIRELESS_ACCESSORY_CONFIGURATION" | "APPLE_PAY" | "DATA_PROTECTION" | "SIRIKIT" | "NETWORK_EXTENSIONS" | "MULTIPATH" | "HOT_SPOT" | "NFC_TAG_READING" | "CLASSKIT" | "AUTOFILL_CREDENTIAL_PROVIDER" | "ACCESS_WIFI_INFORMATION" | "NETWORK_CUSTOM_PROTOCOL" | "COREMEDIA_HLS_LOW_LATENCY" | "SYSTEM_EXTENSION_INSTALL" | "USER_MANAGEMENT" | "APPLE_ID_AUTH" | null;
          /** Configured settings of the capability. */
          settings?: Array<{
            /** Setting key. */
            key?: "ICLOUD_VERSION" | "DATA_PROTECTION_PERMISSION_LEVEL" | "APPLE_ID_AUTH_APP_CONSENT" | null;
            /** Setting name shown in the developer portal. */
            name?: string | null;
            /** Setting description shown in the developer portal. */
            description?: string | null;
            /** Whether the setting is enabled unless configured otherwise. */
            enabledByDefault?: boolean | null;
            /** Whether the setting is shown in the developer portal. */
            visible?: boolean | null;
            /** How many options of the setting may be enabled at once. */
            allowedInstances?: "ENTRY" | "SINGLE" | "MULTIPLE" | null;
            /** Minimum number of options that must be enabled. */
            minInstances?: number | null;
            /** Options available for the setting. */
            options?: Array<{
              /** Option key. */
              key?: "XCODE_5" | "XCODE_6" | "COMPLETE_PROTECTION" | "PROTECTED_UNLESS_OPEN" | "PROTECTED_UNTIL_FIRST_USER_AUTH" | "PRIMARY_APP_CONSENT" | null;
              /** Option name shown in the developer portal. */
              name?: string | null;
              /** Option description shown in the developer portal. */
              description?: string | null;
              /** Whether the option is enabled unless configured otherwise. */
              enabledByDefault?: boolean | null;
              /** Whether the option is currently enabled. */
              enabled?: boolean | null;
              /** Whether the option applies to wildcard bundle IDs. */
              supportsWildcard?: boolean | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one accessibility declaration by its identifier. */
    "app_store_connect.get_accessibility_declaration": {
      input: {
        /**
         * App Store Connect identifier of the accessibility declaration.
         * @minLength 1
         * @pattern \S
         */
        accessibilityDeclarationId: string;
      };
      output: {
        /** An accessibility declaration (Accessibility Nutrition Label) for one device family. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        accessibilityDeclaration: {
          /** App Store Connect identifier for the accessibility declaration. */
          id: string;
          /** Device family the declaration describes. */
          deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION" | null;
          /** Whether the declaration is a draft, published, or replaced by a newer one. */
          state?: "DRAFT" | "PUBLISHED" | "REPLACED" | null;
          /** Whether the app supports audio descriptions. */
          supportsAudioDescriptions?: boolean | null;
          /** Whether the app supports captions. */
          supportsCaptions?: boolean | null;
          /** Whether the app supports a dark interface. */
          supportsDarkInterface?: boolean | null;
          /** Whether the app conveys information without relying on color alone. */
          supportsDifferentiateWithoutColorAlone?: boolean | null;
          /** Whether the app supports larger text. */
          supportsLargerText?: boolean | null;
          /** Whether the app supports reduced motion. */
          supportsReducedMotion?: boolean | null;
          /** Whether the app supports sufficient contrast. */
          supportsSufficientContrast?: boolean | null;
          /** Whether the app supports Voice Control. */
          supportsVoiceControl?: boolean | null;
          /** Whether the app supports VoiceOver. */
          supportsVoiceover?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one audit-log actor by identifier. */
    "app_store_connect.get_actor": {
      input: {
        /**
         * App Store Connect identifier of the actor.
         * @minLength 1
         * @pattern \S
         */
        actorId: string;
      };
      output: {
        /** An actor from the App Store Connect audit log: the person, API key, or system that performed an action. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        actor: {
          /** App Store Connect identifier for the actor. */
          id: string;
          /** Kind of actor. */
          actorType?: "USER" | "API_KEY" | "XCODE_CLOUD" | "APPLE" | null;
          /** First name when the actor is a team member. */
          userFirstName?: string | null;
          /** Last name when the actor is a team member. */
          userLastName?: string | null;
          /** Email address when the actor is a team member. */
          userEmail?: string | null;
          /** Key ID when the actor is an App Store Connect API key. */
          apiKeyId?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the age rating questionnaire answers attached to an app info record, including any manual rating overrides. */
    "app_store_connect.get_age_rating_declaration": {
      input: {
        /**
         * App Store Connect identifier of the app info record.
         * @minLength 1
         * @pattern \S
         */
        appInfoId: string;
      };
      output: {
        /** The age rating questionnaire answers for an app info record. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ageRatingDeclaration: {
          /** App Store Connect identifier for the age rating declaration. */
          id: string;
          /** Whether the app shows advertising. */
          advertising?: boolean | null;
          /** Whether the app performs age assurance. */
          ageAssurance?: boolean | null;
          /** How often alcohol, tobacco, or drug use or references appear. */
          alcoholTobaccoOrDrugUseOrReferences?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often contests appear. */
          contests?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Whether the app offers real gambling. */
          gambling?: boolean | null;
          /** How often simulated gambling appears. */
          gamblingSimulated?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often guns or other weapons appear. */
          gunsOrOtherWeapons?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Whether the app covers health or wellness topics. */
          healthOrWellnessTopics?: boolean | null;
          /** How often horror or fear themes appear. */
          horrorOrFearThemes?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Kids age band the app is designed for, or null. */
          kidsAgeBand?: "FIVE_AND_UNDER" | "SIX_TO_EIGHT" | "NINE_TO_ELEVEN" | null;
          /** Whether the app sells loot boxes or similar random rewards. */
          lootBox?: boolean | null;
          /** How often mature or suggestive themes appear. */
          matureOrSuggestiveThemes?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often medical or treatment information appears. */
          medicalOrTreatmentInformation?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Whether the app offers messaging or chat. */
          messagingAndChat?: boolean | null;
          /** Whether the app offers parental controls. */
          parentalControls?: boolean | null;
          /** How often profanity or crude humor appears. */
          profanityOrCrudeHumor?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often graphic sexual content or nudity appears. */
          sexualContentGraphicAndNudity?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often sexual content or nudity appears. */
          sexualContentOrNudity?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Whether the app includes social media features. */
          socialMedia?: boolean | null;
          /** Whether the social media features are restricted by age. */
          socialMediaAgeRestricted?: boolean | null;
          /** Whether the app offers unrestricted web access. */
          unrestrictedWebAccess?: boolean | null;
          /** Whether the app shows user-generated content. */
          userGeneratedContent?: boolean | null;
          /** How often cartoon or fantasy violence appears. */
          violenceCartoonOrFantasy?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often realistic violence appears. */
          violenceRealistic?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often prolonged graphic or sadistic realistic violence appears. */
          violenceRealisticProlongedGraphicOrSadistic?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Manual override raising the derived age rating, or NONE. */
          ageRatingOverrideV2?: "NONE" | "NINE_PLUS" | "THIRTEEN_PLUS" | "SIXTEEN_PLUS" | "EIGHTEEN_PLUS" | "UNRATED" | null;
          /** Manual override of the Korea age rating, or NONE. */
          koreaAgeRatingOverride?: "NONE" | "FIFTEEN_PLUS" | "NINETEEN_PLUS" | null;
          /** URL with more information about the age rating, published on the product page. */
          developerAgeRatingInfoUrl?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one alternative distribution domain. */
    "app_store_connect.get_alternative_distribution_domain": {
      input: {
        /**
         * App Store Connect identifier of the alternative distribution domain.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionDomainId: string;
      };
      output: {
        /** A base web domain registered for alternative distribution. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        alternativeDistributionDomain: {
          /** App Store Connect identifier of the alternative distribution domain. */
          id: string;
          /** Base domain that serves the marketplace or web distribution app. */
          domain?: string | null;
          /** Display name chosen for the domain in App Store Connect. */
          referenceName?: string | null;
          /** When the domain was added, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one alternative distribution key and its public key. */
    "app_store_connect.get_alternative_distribution_key": {
      input: {
        /**
         * App Store Connect identifier of the alternative distribution key.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionKeyId: string;
      };
      output: {
        /** A public key registered for an alternative marketplace or web distribution app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        alternativeDistributionKey: {
          /** App Store Connect identifier of the alternative distribution key. */
          id: string;
          /** PEM-encoded public key that was uploaded. */
          publicKey?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one alternative distribution package and the checksums of the source build it was generated from. Use list_alternative_distribution_package_versions for its downloadable versions. */
    "app_store_connect.get_alternative_distribution_package": {
      input: {
        /**
         * App Store Connect identifier of the alternative distribution package.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionPackageId: string;
      };
      output: {
        /** An alternative distribution package generated for an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        alternativeDistributionPackage: {
          /** App Store Connect identifier of the alternative distribution package. */
          id: string;
          /** Checksums of the source file the package was built from. */
          sourceFileChecksum?: {
            /** Checksum of the source file. */
            file?: {
              /** Hex-encoded checksum value. */
              hash?: string | null;
              /** Algorithm the checksum was computed with. */
              algorithm?: "MD5" | "SHA_256" | null;
              [key: string]: unknown;
            } | null;
            /** Composite checksum of the source file. */
            composite?: {
              /** Hex-encoded checksum value. */
              hash?: string | null;
              /** Algorithm the checksum was computed with. */
              algorithm?: "MD5" | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one delta update of an alternative distribution package version, with its download URL and encrypted key blob. */
    "app_store_connect.get_alternative_distribution_package_delta": {
      input: {
        /**
         * App Store Connect identifier of the package delta.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionPackageDeltaId: string;
      };
      output: {
        /** An incremental update between two alternative distribution package versions. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        delta: {
          /** App Store Connect identifier of the package delta. */
          id: string;
          /** Time-limited download URL of the file. */
          url?: string | null;
          /** When the download URL stops working, as an ISO 8601 timestamp. */
          urlExpirationDate?: string | null;
          /** Encrypted key blob the marketplace or web server needs to process the file. Treat it as a secret. */
          alternativeDistributionKeyBlob?: string | null;
          /** SHA-256 checksum of the file. */
          fileChecksum?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one variant of an alternative distribution package version, with its download URL and encrypted key blob. */
    "app_store_connect.get_alternative_distribution_package_variant": {
      input: {
        /**
         * App Store Connect identifier of the package variant.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionPackageVariantId: string;
      };
      output: {
        /** A device-specific variant of an alternative distribution package version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        variant: {
          /** App Store Connect identifier of the package variant. */
          id: string;
          /** Time-limited download URL of the file. */
          url?: string | null;
          /** When the download URL stops working, as an ISO 8601 timestamp. */
          urlExpirationDate?: string | null;
          /** Encrypted key blob the marketplace or web server needs to process the file. Treat it as a secret. */
          alternativeDistributionKeyBlob?: string | null;
          /** SHA-256 checksum of the file. */
          fileChecksum?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one alternative distribution package version and its time-limited download URL. */
    "app_store_connect.get_alternative_distribution_package_version": {
      input: {
        /**
         * App Store Connect identifier of the alternative distribution package version.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionPackageVersionId: string;
      };
      output: {
        /** One version of an alternative distribution package. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        alternativeDistributionPackageVersion: {
          /** App Store Connect identifier of the alternative distribution package version. */
          id: string;
          /** Version string of the packaged app. */
          version?: string | null;
          /** Whether this is the current package version or has been replaced by a newer one. */
          state?: "COMPLETED" | "REPLACED" | null;
          /** Time-limited download URL of the package version manifest. */
          url?: string | null;
          /** When the download URL stops working, as an ISO 8601 timestamp. */
          urlExpirationDate?: string | null;
          /** Checksum of the package version file. */
          fileChecksum?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one analytics report type by identifier. */
    "app_store_connect.get_analytics_report": {
      input: {
        /**
         * App Store Connect identifier of the report.
         * @minLength 1
         * @pattern \S
         */
        analyticsReportId: string;
      };
      output: {
        /** One analytics report type produced by a report request. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        analyticsReport: {
          /** App Store Connect identifier for the report. */
          id: string;
          /** Report name, such as App Store Downloads Standard. */
          name?: string | null;
          /** Report category. */
          category?: "APP_USAGE" | "APP_STORE_ENGAGEMENT" | "COMMERCE" | "FRAMEWORK_USAGE" | "PERFORMANCE" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one analytics report instance by identifier. */
    "app_store_connect.get_analytics_report_instance": {
      input: {
        /**
         * App Store Connect identifier of the report instance.
         * @minLength 1
         * @pattern \S
         */
        analyticsReportInstanceId: string;
      };
      output: {
        /** One time-bounded instance of an analytics report. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        analyticsReportInstance: {
          /** App Store Connect identifier for the report instance. */
          id: string;
          /** Reporting period covered by the instance. */
          granularity?: "DAILY" | "WEEKLY" | "MONTHLY" | null;
          /** Date the instance was processed, as YYYY-MM-DD. */
          processingDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one analytics report request by identifier. */
    "app_store_connect.get_analytics_report_request": {
      input: {
        /**
         * App Store Connect identifier of the report request.
         * @minLength 1
         * @pattern \S
         */
        analyticsReportRequestId: string;
      };
      output: {
        /** A request that makes App Store Connect generate analytics reports for one app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        analyticsReportRequest: {
          /** App Store Connect identifier for the report request. */
          id: string;
          /** ONGOING keeps producing daily, weekly, and monthly reports; ONE_TIME_SNAPSHOT delivers historical data once. */
          accessType?: "ONE_TIME_SNAPSHOT" | "ONGOING" | null;
          /** True once App Store Connect stopped the request because its reports were not read for a long time; create a new request to resume. */
          stoppedDueToInactivity?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Android app mapping by its identifier. */
    "app_store_connect.get_android_to_ios_app_mapping_detail": {
      input: {
        /**
         * App Store Connect identifier of the Android app mapping.
         * @minLength 1
         * @pattern \S
         */
        androidToIosAppMappingDetailId: string;
      };
      output: {
        /** A mapping from an Android app to this iOS app, used for Android to iPhone migration. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        androidToIosAppMappingDetail: {
          /** App Store Connect identifier for the mapping detail. */
          id: string;
          /** Android package name of the mapped app. */
          packageName?: string | null;
          /** SHA-256 fingerprints of the Android app signing key certificates. */
          appSigningKeyPublicCertificateSha256Fingerprints?: Array<string> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one app record by its App Store Connect identifier. */
    "app_store_connect.get_app": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
      };
      output: {
        /** An app registered in App Store Connect. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        app: {
          /** App Store Connect identifier for the app. */
          id: string;
          /** App name shown on the App Store. */
          name?: string | null;
          /** Bundle identifier registered for the app. */
          bundleId?: string | null;
          /** SKU chosen when the app record was created. */
          sku?: string | null;
          /** Primary App Store locale, such as en-US. */
          primaryLocale?: string | null;
          /** Whether the app is or has ever been part of the Kids category. */
          isOrEverWasMadeForKids?: boolean | null;
          /** Third-party content rights declared for the app. */
          contentRightsDeclaration?: "DOES_NOT_USE_THIRD_PARTY_CONTENT" | "USES_THIRD_PARTY_CONTENT" | null;
          /** Whether streamlined purchasing is enabled for the app. */
          streamlinedPurchasingEnabled?: boolean | null;
          /** Accessibility information URL published with the app. */
          accessibilityUrl?: string | null;
          /** Production server-to-server subscription status URL. */
          subscriptionStatusUrl?: string | null;
          /** Version of the production subscription status URL. */
          subscriptionStatusUrlVersion?: "V1" | "V2" | null;
          /** Sandbox server-to-server subscription status URL. */
          subscriptionStatusUrlForSandbox?: string | null;
          /** Version of the sandbox subscription status URL. */
          subscriptionStatusUrlVersionForSandbox?: "V1" | "V2" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the alternative distribution key tied to one app, or null when the app has no key of its own. */
    "app_store_connect.get_app_alternative_distribution_key": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
      };
      output: {
        /** A public key registered for an alternative marketplace or web distribution app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        alternativeDistributionKey: {
          /** App Store Connect identifier of the alternative distribution key. */
          id: string;
          /** PEM-encoded public key that was uploaded. */
          publicKey?: string | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read the availability configuration of an app, which says whether it is automatically offered in new territories. Returns null when App Store Connect has no availability record for the app yet. Use list_territory_availabilities for the per-territory settings. */
    "app_store_connect.get_app_availability": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
      };
      output: {
        /** The territory availability configuration of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appAvailability: {
          /** App Store Connect identifier for the app availability. */
          id: string;
          /** Whether the app is automatically made available in territories Apple adds to the App Store later. */
          availableInNewTerritories?: boolean | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read TestFlight usage metrics for one app: sessions, crashes, and feedback over the chosen period, either as one aggregate row or as one row per tester. */
    "app_store_connect.get_app_beta_tester_usages": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Length of the reporting window ending now: 7, 30, 90, or 365 days. */
        period?: "P7D" | "P30D" | "P90D" | "P365D";
        /** Return one row per tester instead of a single aggregate row. */
        groupByBetaTester?: boolean;
        /**
         * Return only the usage of this tester.
         * @minLength 1
         * @pattern \S
         */
        betaTesterId?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Usage rows returned for this page. */
        usages: Array<{
          /** App Store Connect identifier of the tester this row covers, or null for the aggregate row. */
          betaTesterId: string | null;
          /** The TestFlight tester the record belongs to, or null when App Store Connect did not return it. */
          betaTester: {
            /** App Store Connect identifier for the beta tester. */
            id: string;
            /** Email address the tester was invited with. */
            email?: string | null;
            /** Tester first name. */
            firstName?: string | null;
            /** Tester last name. */
            lastName?: string | null;
            [key: string]: unknown;
          } | null;
          /** Usage counters per reporting interval. */
          dataPoints: Array<{
            /** Start of the reporting interval, as an ISO 8601 timestamp. */
            start: string | null;
            /** End of the reporting interval, as an ISO 8601 timestamp. */
            end: string | null;
            /** Crashes reported during the interval. */
            crashCount: number | null;
            /** App sessions during the interval. */
            sessionCount: number | null;
            /** Feedback submissions sent during the interval. */
            feedbackCount: number | null;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read one App Store category with its platforms, parent category, and subcategory identifiers. */
    "app_store_connect.get_app_category": {
      input: {
        /**
         * Category identifier, such as GAMES or GAMES_ACTION.
         * @minLength 1
         * @pattern \S
         */
        appCategoryId: string;
      };
      output: {
        /** An App Store category or subcategory. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appCategory: {
          /** Category identifier used in category relationships, such as GAMES or GAMES_ACTION. */
          id: string;
          /** Content platforms the category is offered on. */
          platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
          /** Identifier of the parent category, or null for a top-level category. */
          parentId: string | null;
          /** Identifiers of the subcategories under this category; empty for a subcategory. */
          subcategoryIds: Array<string> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one App Clip by its identifier. */
    "app_store_connect.get_app_clip": {
      input: {
        /**
         * App Store Connect identifier of the App Clip.
         * @minLength 1
         * @pattern \S
         */
        appClipId: string;
      };
      output: {
        /** An App Clip that belongs to an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClip: {
          /** App Store Connect identifier for the App Clip. */
          id: string;
          /** Bundle identifier of the App Clip. */
          bundleId?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one advanced App Clip experience with its invocation URL, place, status, and localized card text. */
    "app_store_connect.get_app_clip_advanced_experience": {
      input: {
        /**
         * App Store Connect identifier of the advanced App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipAdvancedExperienceId: string;
      };
      output: {
        /** An advanced App Clip experience launched from a URL, QR code, NFC tag, or a place in Apple Maps. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipAdvancedExperience: {
          /** App Store Connect identifier for the advanced App Clip experience. */
          id: string;
          /** Invocation URL of the experience. */
          link?: string | null;
          /** Build version of the App Clip the experience belongs to. */
          version?: number | null;
          /** Whether users can invoke the experience (RECEIVED), it is deactivated, or its app is being transferred. */
          status?: "RECEIVED" | "DEACTIVATED" | "APP_TRANSFER_IN_PROGRESS" | null;
          /** Call-to-action verb shown on the App Clip card. */
          action?: "OPEN" | "VIEW" | "PLAY" | null;
          /** Whether the experience was submitted by a platform provider that serves multiple businesses. */
          isPoweredBy?: boolean | null;
          /** Physical place the experience is tied to, or null. */
          place?: {
            /** Apple Maps place identifier when the place is already known to Apple Maps. */
            placeId?: string;
            /** Names of the place. */
            names?: Array<string>;
            /** Main address of the place. */
            mainAddress?: {
              /** Full address as a single line. */
              fullAddress?: string;
              /** Address split into its parts. */
              structuredAddress?: {
                /** Street address lines. */
                streetAddress?: Array<string>;
                /** Floor within the building. */
                floor?: string;
                /** Neighborhood of the place. */
                neighborhood?: string;
                /** City or locality. */
                locality?: string;
                /** State or province. */
                stateProvince?: string;
                /** Postal code. */
                postalCode?: string;
                /** ISO 3166-1 alpha-2 country code, such as US. */
                countryCode?: string;
                [key: string]: unknown;
              };
              [key: string]: unknown;
            };
            /** Where the pin is placed on the map. */
            displayPoint?: {
              /** Geographic coordinates of the pin. */
              coordinates?: {
                /** Latitude in decimal degrees. */
                latitude?: number;
                /** Longitude in decimal degrees. */
                longitude?: number;
                [key: string]: unknown;
              };
              /** Whether the pin was calculated from the address or placed manually. */
              source?: "CALCULATED" | "MANUALLY_PLACED";
              [key: string]: unknown;
            };
            /** Action offered on the place card in Apple Maps. */
            mapAction?: "BUY_TICKETS" | "VIEW_AVAILABILITY" | "VIEW_PRICING" | "HOTEL_BOOK_ROOM" | "PARKING_RESERVE_PARKING" | "RESTAURANT_JOIN_WAITLIST" | "RESTAURANT_ORDER_DELIVERY" | "RESTAURANT_ORDER_FOOD" | "RESTAURANT_ORDER_TAKEOUT" | "RESTAURANT_RESERVATION" | "SCHEDULE_APPOINTMENT" | "RESTAURANT_VIEW_MENU" | "THEATER_NOW_PLAYING" | "AIRLINE_BOOK_TRAVEL" | "AIRLINE_CHECK_IN" | "AIRLINE_FLIGHT_STATUS" | "APPLY" | "BOOK" | "BOOK_ACTIVITIES" | "BOOK_RIDES" | "BOOK_TEETIMES" | "BOOK_TOURS" | "CAREERS" | "CHARGE_EV" | "COUPONS" | "DONATE" | "EVENTS" | "EVENTS_SHOWS" | "EVENTS_SPORTS" | "GIFT_CARD" | "HOTEL_AMENITIES" | "JOIN" | "PARKING_AVAILABLE" | "RESTAURANT_PICKUP" | "RETAIL_SERVICE_QUOTE" | "RETAIL_STORE_DELIVERY" | "RETAIL_STORE_PICKUP" | "RETAIL_STORE_SHOP" | "SERVICES" | "SUPPORT" | "PAY_TO_PARK";
            /** Relationship between the submitter and the place. */
            relationship?: "OWNER" | "AUTHORIZED" | "OTHER";
            /** Phone number of the place. */
            phoneNumber?: {
              /** Phone number of the place. */
              number?: string;
              /** Kind of phone line. */
              type?: "FAX" | "LANDLINE" | "MOBILE" | "TOLLFREE";
              /** What the number is for, such as reservations. */
              intent?: string;
              [key: string]: unknown;
            };
            /** Website of the place. */
            homePage?: string;
            /** Apple Maps categories of the place. */
            categories?: Array<string>;
            [key: string]: unknown;
          } | null;
          /** Whether Apple Maps matched the place to a point of interest. */
          placeStatus?: "PENDING" | "MATCHED" | "NO_MATCH" | null;
          /** Business category of the experience. */
          businessCategory?: "AUTOMOTIVE" | "BEAUTY" | "BIKES" | "BOOKS" | "CASINO" | "EDUCATION" | "EDUCATION_JAPAN" | "ENTERTAINMENT" | "EV_CHARGER" | "FINANCIAL_USD" | "FINANCIAL_CNY" | "FINANCIAL_GBP" | "FINANCIAL_JPY" | "FINANCIAL_EUR" | "FITNESS" | "FOOD_AND_DRINK" | "GAS" | "GROCERY" | "HEALTH_AND_MEDICAL" | "HOTEL_AND_TRAVEL" | "MUSIC" | "PARKING" | "PET_SERVICES" | "PROFESSIONAL_SERVICES" | "SHOPPING" | "TICKETING" | "TRANSIT" | null;
          /** Default language of the experience. */
          defaultLanguage?: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH" | null;
          /** Localized card text of the experience, empty when none was returned. */
          localizations: Array<{
            /** App Store Connect identifier for the localization. */
            id: string;
            /** Language of the localization. */
            language?: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH" | null;
            /** Title shown on the App Clip card. */
            title?: string | null;
            /** Subtitle shown on the App Clip card. */
            subtitle?: string | null;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        };
      };
    };
    /** Read one default App Clip experience with its action and the App Store version it is released with. */
    "app_store_connect.get_app_clip_default_experience": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceId: string;
      };
      output: {
        /** The default App Clip experience that ships with an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipDefaultExperience: {
          /** App Store Connect identifier for the default App Clip experience. */
          id: string;
          /** Call-to-action verb shown on the App Clip card. */
          action?: "OPEN" | "VIEW" | "PLAY" | null;
          /** App Store Connect identifier of the App Store version the experience is released with, or null when none is attached. */
          releaseWithAppStoreVersionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one default App Clip experience localization by its identifier. */
    "app_store_connect.get_app_clip_default_experience_localization": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience localization.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceLocalizationId: string;
      };
      output: {
        /** Localized subtitle of a default App Clip experience for one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipDefaultExperienceLocalization: {
          /** App Store Connect identifier for the default App Clip experience localization. */
          id: string;
          /** Locale the subtitle is written in, such as en-US. */
          locale?: string | null;
          /** Subtitle shown on the App Clip card in this locale. */
          subtitle?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the invocation URLs App Review uses to test a default App Clip experience, or null when none were provided yet. */
    "app_store_connect.get_app_clip_default_experience_review_detail": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceId: string;
      };
      output: {
        /** Invocation URLs App Review uses to test a default App Clip experience. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipAppStoreReviewDetail: {
          /** App Store Connect identifier for the App Clip App Store review detail. */
          id: string;
          /** Invocation URLs App Review can use to launch the App Clip. */
          invocationUrls?: Array<string> | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one custom product page by its App Store Connect identifier. */
    "app_store_connect.get_app_custom_product_page": {
      input: {
        /**
         * App Store Connect identifier of the custom product page.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageId: string;
      };
      output: {
        /** A custom product page of an app. Each page has its own versions that go through App Review and can be reached through a dedicated App Store URL. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appCustomProductPage: {
          /** App Store Connect identifier for the custom product page. */
          id: string;
          /** Internal name of the custom product page shown in App Store Connect. */
          name?: string | null;
          /** App Store URL that opens this custom product page. */
          url?: string | null;
          /** Whether the page is visible on the App Store. A hidden page keeps its URL but is not shown. */
          visible?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one custom product page localization by its App Store Connect identifier. */
    "app_store_connect.get_app_custom_product_page_localization": {
      input: {
        /**
         * App Store Connect identifier of the custom product page localization.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageLocalizationId: string;
      };
      output: {
        /** The metadata of a custom product page version in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appCustomProductPageLocalization: {
          /** App Store Connect identifier for the custom product page localization. */
          id: string;
          /** Locale the metadata is written in, such as en-US. */
          locale?: string | null;
          /** Promotional text shown above the description on this custom product page. */
          promotionalText?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one custom product page version by its App Store Connect identifier. */
    "app_store_connect.get_app_custom_product_page_version": {
      input: {
        /**
         * App Store Connect identifier of the custom product page version.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageVersionId: string;
      };
      output: {
        /** One version of a custom product page. A new version is created for each round of edits and App Review. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appCustomProductPageVersion: {
          /** App Store Connect identifier for the custom product page version. */
          id: string;
          /** Version number App Store Connect assigned to this page version. */
          version?: string | null;
          /** Review state of the page version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | null;
          /** Deep link opened when a user taps Get or Open on this custom product page, or null. */
          deepLink?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one export compliance (encryption) declaration by its identifier. */
    "app_store_connect.get_app_encryption_declaration": {
      input: {
        /**
         * App Store Connect identifier of the app encryption declaration.
         * @minLength 1
         * @pattern \S
         */
        appEncryptionDeclarationId: string;
      };
      output: {
        /** An export compliance declaration describing the encryption an app uses. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appEncryptionDeclaration: {
          /** App Store Connect identifier for the app encryption declaration. */
          id: string;
          /** Description of how the app uses encryption. */
          appDescription?: string | null;
          /** When the declaration was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the encryption qualifies for an export exemption. */
          exempt?: boolean | null;
          /** Whether the app contains proprietary or non-standard cryptography. */
          containsProprietaryCryptography?: boolean | null;
          /** Whether the app contains third-party cryptography. */
          containsThirdPartyCryptography?: boolean | null;
          /** Whether the app may be distributed on the France storefront. */
          availableOnFrenchStore?: boolean | null;
          /** Content platform the declaration applies to. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Review state of the declaration. */
          appEncryptionDeclarationState?: "CREATED" | "IN_REVIEW" | "APPROVED" | "REJECTED" | "INVALID" | "EXPIRED" | null;
          /** Export compliance code App Store Connect assigned, to reuse in Info.plist or on upload. */
          codeValue?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one in-app event with its state, badge, purpose, and territory schedules. */
    "app_store_connect.get_app_event": {
      input: {
        /**
         * App Store Connect identifier of the in-app event.
         * @minLength 1
         * @pattern \S
         */
        appEventId: string;
      };
      output: {
        /** An in-app event shown on the App Store product page. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appEvent: {
          /** App Store Connect identifier for the in-app event. */
          id: string;
          /** Internal reference name of the event, not shown on the App Store. */
          referenceName?: string | null;
          /** Badge that labels the kind of event. */
          badge?: "LIVE_EVENT" | "PREMIERE" | "CHALLENGE" | "COMPETITION" | "NEW_SEASON" | "MAJOR_UPDATE" | "SPECIAL_EVENT" | null;
          /** Review and publication state of the event. */
          eventState?: "DRAFT" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "ACCEPTED" | "APPROVED" | "PUBLISHED" | "PAST" | "ARCHIVED" | null;
          /** Deep link opened in the app when a user taps the event. */
          deepLink?: string | null;
          /** Purchase required to take part, such as NO_COST_ASSOCIATED or IN_APP_PURCHASE. */
          purchaseRequirement?: string | null;
          /** Primary locale of the event metadata, such as en-US. */
          primaryLocale?: string | null;
          /** Priority relative to the other events of the app. */
          priority?: "HIGH" | "NORMAL" | null;
          /** Audience the event is meant for. */
          purpose?: "APPROPRIATE_FOR_ALL_USERS" | "ATTRACT_NEW_USERS" | "KEEP_ACTIVE_USERS_INFORMED" | "BRING_BACK_LAPSED_USERS" | null;
          /** Territory schedules of the event, or null when none are set. */
          territorySchedules?: Array<{
            /** ISO 3166-1 alpha-3 territories this schedule applies to. */
            territories?: Array<string> | null;
            /** When the event becomes visible on the App Store, as an ISO 8601 timestamp. */
            publishStart?: string | null;
            /** When the event starts, as an ISO 8601 timestamp. */
            eventStart?: string | null;
            /** When the event ends, as an ISO 8601 timestamp. */
            eventEnd?: string | null;
            [key: string]: unknown;
          }> | null;
          /** Schedules of past occurrences App Store Connect archived, or null when there are none. */
          archivedTerritorySchedules?: Array<{
            /** ISO 3166-1 alpha-3 territories this schedule applies to. */
            territories?: Array<string> | null;
            /** When the event becomes visible on the App Store, as an ISO 8601 timestamp. */
            publishStart?: string | null;
            /** When the event starts, as an ISO 8601 timestamp. */
            eventStart?: string | null;
            /** When the event ends, as an ISO 8601 timestamp. */
            eventEnd?: string | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one in-app event localization by its identifier. */
    "app_store_connect.get_app_event_localization": {
      input: {
        /**
         * App Store Connect identifier of the in-app event localization.
         * @minLength 1
         * @pattern \S
         */
        appEventLocalizationId: string;
      };
      output: {
        /** Localized name and descriptions of an in-app event for one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appEventLocalization: {
          /** App Store Connect identifier for the in-app event localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Event name shown on the App Store in this locale. */
          name?: string | null;
          /** Short description shown on the event card. */
          shortDescription?: string | null;
          /** Long description shown on the event details page. */
          longDescription?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the Game Center configuration of one app, including the group it belongs to and the leaderboards shown by default. Returns null when the app has never been enabled for Game Center. */
    "app_store_connect.get_app_game_center_detail": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
      };
      output: {
        /** The Game Center configuration of one app. It owns the app's achievements, leaderboards, leaderboard sets, activities and challenges, and links the app to a Game Center group when it shares them with other apps. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterDetail: {
          /** App Store Connect identifier for the Game Center detail. */
          id: string;
          /** Whether the app is distributed through Apple Arcade. */
          arcadeEnabled?: boolean | null;
          /** Game Center group the app belongs to, or null when the app is not grouped or App Store Connect did not return the relationship. */
          gameCenterGroupId: string | null;
          /** Leaderboard shown by default in the Game Center dashboard, or null when there is none. */
          defaultLeaderboardId: string | null;
          /** Group leaderboard shown by default when the app belongs to a group, or null when there is none. */
          defaultGroupLeaderboardId: string | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one app info record with its review state, derived age ratings, and App Store category identifiers. */
    "app_store_connect.get_app_info": {
      input: {
        /**
         * App Store Connect identifier of the app info record.
         * @minLength 1
         * @pattern \S
         */
        appInfoId: string;
      };
      output: {
        /** App-level App Store information shared by every version of the app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appInfo: {
          /** App Store Connect identifier for the app info record. */
          id: string;
          /** Review state of this app info record. */
          state?: "ACCEPTED" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "PENDING_RELEASE" | "PREPARE_FOR_SUBMISSION" | "READY_FOR_DISTRIBUTION" | "READY_FOR_REVIEW" | "REJECTED" | "REPLACED_WITH_NEW_INFO" | "WAITING_FOR_REVIEW" | null;
          /** App Store state of the app the record belongs to. */
          appStoreState?: "ACCEPTED" | "DEVELOPER_REMOVED_FROM_SALE" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "INVALID_BINARY" | "METADATA_REJECTED" | "PENDING_APPLE_RELEASE" | "PENDING_CONTRACT" | "PENDING_DEVELOPER_RELEASE" | "PREPARE_FOR_SUBMISSION" | "PREORDER_READY_FOR_SALE" | "PROCESSING_FOR_APP_STORE" | "READY_FOR_REVIEW" | "READY_FOR_SALE" | "REJECTED" | "REMOVED_FROM_SALE" | "WAITING_FOR_EXPORT_COMPLIANCE" | "WAITING_FOR_REVIEW" | "REPLACED_WITH_NEW_VERSION" | "NOT_APPLICABLE" | null;
          /** Age rating App Store Connect derived from the age rating declaration. */
          appStoreAgeRating?: "L" | "ALL" | "ZERO_ZERO" | "ONE_PLUS" | "TWO_PLUS" | "THREE_PLUS" | "FOUR_PLUS" | "FIVE_PLUS" | "SIX_PLUS" | "SEVEN_PLUS" | "EIGHT_PLUS" | "NINE_PLUS" | "TEN_PLUS" | "ELEVEN_PLUS" | "TWELVE_PLUS" | "THIRTEEN_PLUS" | "FOURTEEN_PLUS" | "FIFTEEN_PLUS" | "SIXTEEN_PLUS" | "SEVENTEEN_PLUS" | "EIGHTEEN_PLUS" | "NINETEEN_PLUS" | "TWENTY_PLUS" | "TWENTY_ONE_PLUS" | "UNRATED" | null;
          /** Age rating shown on the Brazil storefront. */
          brazilAgeRating?: "L" | "TEN" | "TWELVE" | "FOURTEEN" | "SIXTEEN" | "EIGHTEEN" | null;
          /** Kids age band when the app is in the Kids category, otherwise null. */
          kidsAgeBand?: "FIVE_AND_UNDER" | "SIX_TO_EIGHT" | "NINE_TO_ELEVEN" | null;
          /** Identifier of the primary App Store category, such as GAMES, or null when none is set. */
          primaryCategoryId: string | null;
          /** Identifier of the first primary subcategory, used when the primary category is Games or Stickers. */
          primarySubcategoryOneId: string | null;
          /** Identifier of the second primary subcategory, or null when none is set. */
          primarySubcategoryTwoId: string | null;
          /** Identifier of the secondary App Store category, or null when none is set. */
          secondaryCategoryId: string | null;
          /** Identifier of the first secondary subcategory, or null when none is set. */
          secondarySubcategoryOneId: string | null;
          /** Identifier of the second secondary subcategory, or null when none is set. */
          secondarySubcategoryTwoId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one app info localization by its identifier. */
    "app_store_connect.get_app_info_localization": {
      input: {
        /**
         * App Store Connect identifier of the app info localization.
         * @minLength 1
         * @pattern \S
         */
        appInfoLocalizationId: string;
      };
      output: {
        /** Localized app-level App Store information for one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appInfoLocalization: {
          /** App Store Connect identifier for the app info localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** App name shown on the App Store in this locale. */
          name?: string | null;
          /** Subtitle shown under the app name in this locale. */
          subtitle?: string | null;
          /** Privacy policy URL published for this locale. */
          privacyPolicyUrl?: string | null;
          /** URL where users can manage their privacy choices, published for this locale. */
          privacyChoicesUrl?: string | null;
          /** Privacy policy text shown on Apple TV, where a URL cannot be opened. */
          privacyPolicyText?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Fetch the performance and power metrics of the most recent versions of an app, such as launch time, hang rate, memory, and battery use, in the xcodeMetrics document format. Use get_build_perf_power_metrics for one specific build. */
    "app_store_connect.get_app_perf_power_metrics": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Return only metrics for this platform. */
        platform?: "IOS";
        /**
         * Return only these metric categories.
         * @minItems 1
         */
        metricTypes?: Array<"DISK" | "HANG" | "BATTERY" | "LAUNCH" | "MEMORY" | "ANIMATION" | "TERMINATION" | "STORAGE">;
        /**
         * Return only metrics for these device types, such as iPhone14,2. Use all_iphones for every iPhone model and all_ipads for every iPad model.
         * @minItems 1
         */
        deviceTypes?: Array<string>;
      };
      output: {
        /** The xcodeMetrics document exactly as App Store Connect returned it. */
        metrics: {
          /** Version of the xcodeMetrics document format. */
          version?: string | null;
          /** Automatically generated trends comparing the latest version with earlier ones. */
          insights?: {
            /** Metrics that got worse in the latest version. */
            trendingUp?: Array<Record<string, unknown>> | null;
            /** Metrics that regressed against reference versions. */
            regressions?: Array<Record<string, unknown>> | null;
            [key: string]: unknown;
          } | null;
          /** Metric categories and datasets, one entry per platform. */
          productData?: Array<{
            /** Platform the metrics were collected on. */
            platform?: string | null;
            /** Metric categories with their metrics, units, goals, and datasets. */
            metricCategories?: Array<Record<string, unknown>> | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one price point by its App Store Connect identifier, including the territory it belongs to. */
    "app_store_connect.get_app_price_point": {
      input: {
        /**
         * App Store Connect identifier of the price point, from list_app_price_points.
         * @minLength 1
         * @pattern \S
         */
        appPricePointId: string;
      };
      output: {
        /** A price point an app can be sold at in one territory. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appPricePoint: {
          /** App Store Connect identifier for the price point. It encodes the app, the territory and the price tier, so it is what create_app_price_schedule takes. */
          id: string;
          /** Price customers pay in the territory currency, as a decimal string. */
          customerPrice?: string | null;
          /** Developer proceeds in the territory currency, as a decimal string. */
          proceeds?: string | null;
          /** Territory the price point belongs to, such as USA. */
          territoryId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the price schedule of an app, including the base territory its automatic prices derive from. Returns null when App Store Connect has no price schedule for the app yet. Use list_app_price_schedule_manual_prices and list_app_price_schedule_automatic_prices for the individual prices. */
    "app_store_connect.get_app_price_schedule": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
      };
      output: {
        /** The price schedule of an app: the base territory plus the manual and automatic prices App Store Connect derives from it. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appPriceSchedule: {
          /** App Store Connect identifier for the price schedule. */
          id: string;
          /** Territory whose price the automatic prices of every other territory are derived from, such as USA. */
          baseTerritoryId: string | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read the base territory of a price schedule, the territory whose price the automatic prices of the other territories are derived from. */
    "app_store_connect.get_app_price_schedule_base_territory": {
      input: {
        /**
         * App Store Connect identifier of the price schedule, from get_app_price_schedule.
         * @minLength 1
         * @pattern \S
         */
        appPriceScheduleId: string;
      };
      output: {
        /** An App Store territory. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        territory: {
          /** ISO 3166-1 alpha-3 territory code that App Store Connect uses as the territory identifier, such as USA. */
          id: string;
          /** ISO 4217 code of the currency prices in the territory are expressed in, such as USD. */
          currency?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the App Review contact details, demo account, and notes attached to an App Store version, or null when none were entered yet. */
    "app_store_connect.get_app_store_review_detail": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** The App Review contact and demo account information of an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreReviewDetail: {
          /** App Store Connect identifier for the review detail. */
          id: string;
          /** First name of the person App Review may contact. */
          contactFirstName?: string | null;
          /** Last name of the person App Review may contact. */
          contactLastName?: string | null;
          /** Phone number App Review may call. */
          contactPhone?: string | null;
          /** Email address App Review may write to. */
          contactEmail?: string | null;
          /** Username of the demo account provided to App Review. */
          demoAccountName?: string | null;
          /** Password of the demo account provided to App Review. */
          demoAccountPassword?: string | null;
          /** Whether App Review needs a demo account to sign in. */
          demoAccountRequired?: boolean | null;
          /** Additional notes for the App Review team. */
          notes?: string | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one App Store version by its App Store Connect identifier. */
    "app_store_connect.get_app_store_version": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** An App Store version of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersion: {
          /** App Store Connect identifier for the version. */
          id: string;
          /** Content platform the version targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Version string shown on the App Store, such as 1.4.0. */
          versionString?: string | null;
          /** Current review and release state. */
          appVersionState?: "ACCEPTED" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "INVALID_BINARY" | "METADATA_REJECTED" | "PENDING_APPLE_RELEASE" | "PENDING_DEVELOPER_RELEASE" | "PREPARE_FOR_SUBMISSION" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "READY_FOR_REVIEW" | "REJECTED" | "REPLACED_WITH_NEW_VERSION" | "WAITING_FOR_EXPORT_COMPLIANCE" | "WAITING_FOR_REVIEW" | null;
          /** Copyright line published with the version. */
          copyright?: string | null;
          /** Review track the version goes through. */
          reviewType?: "APP_STORE" | "NOTARIZATION" | null;
          /** Release behavior after approval. */
          releaseType?: "MANUAL" | "AFTER_APPROVAL" | "SCHEDULED" | null;
          /** Earliest scheduled release time, as an ISO 8601 timestamp. */
          earliestReleaseDate?: string | null;
          /** Whether the version is downloadable. */
          downloadable?: boolean | null;
          /** When the version record was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the alternative distribution package generated for one App Store version, or null when none exists yet. */
    "app_store_connect.get_app_store_version_alternative_distribution_package": {
      input: {
        /**
         * App Store Connect identifier of the App Store version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** An alternative distribution package generated for an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        alternativeDistributionPackage: {
          /** App Store Connect identifier of the alternative distribution package. */
          id: string;
          /** Checksums of the source file the package was built from. */
          sourceFileChecksum?: {
            /** Checksum of the source file. */
            file?: {
              /** Hex-encoded checksum value. */
              hash?: string | null;
              /** Algorithm the checksum was computed with. */
              algorithm?: "MD5" | "SHA_256" | null;
              [key: string]: unknown;
            } | null;
            /** Composite checksum of the source file. */
            composite?: {
              /** Hex-encoded checksum value. */
              hash?: string | null;
              /** Algorithm the checksum was computed with. */
              algorithm?: "MD5" | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read the build currently attached to an App Store version, or null when no build has been selected. */
    "app_store_connect.get_app_store_version_build": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** The build attached to an App Store version, or null when no build has been selected yet. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        build: {
          /** App Store Connect identifier for the build. */
          id: string;
          /** Build number, such as 42. */
          version?: string | null;
          /** When the build finished uploading, as an ISO 8601 timestamp. */
          uploadedDate?: string | null;
          /** When the build stops being installable by testers. */
          expirationDate?: string | null;
          /** Whether the build has expired for TestFlight. */
          expired?: boolean | null;
          /** Processing state of the uploaded build. */
          processingState?: "PROCESSING" | "FAILED" | "INVALID" | "VALID" | null;
          /** Distribution audience the build was uploaded for. */
          buildAudienceType?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE" | null;
          /** Minimum OS version the build supports. */
          minOsVersion?: string | null;
          /** Minimum macOS system version declared by the build. */
          lsMinimumSystemVersion?: string | null;
          /** Minimum macOS version App Store Connect computed for the build. */
          computedMinMacOsVersion?: string | null;
          /** Minimum visionOS version App Store Connect computed for the build. */
          computedMinVisionOsVersion?: string | null;
          /** Whether the build declares non-exempt encryption. */
          usesNonExemptEncryption?: boolean | null;
          /** Template URL and pixel size of the build icon asset. */
          iconAssetToken?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Icon width in pixels. */
            width?: number;
            /** Icon height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one product page optimization test, including the App Store version that serves as its control. */
    "app_store_connect.get_app_store_version_experiment": {
      input: {
        /**
         * App Store Connect identifier of the product page optimization test.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentId: string;
      };
      output: {
        /** A product page optimization test (App Store version experiment v2) that splits App Store traffic between the default product page and its treatments. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionExperiment: {
          /** App Store Connect identifier for the experiment. */
          id: string;
          /** Test name shown in App Store Connect. */
          name?: string | null;
          /** Content platform the test runs on. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Percentage of App Store traffic directed to the treatments instead of the default product page. */
          trafficProportion?: number | null;
          /** Review and run state of the test. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REJECTED" | "COMPLETED" | "STOPPED" | null;
          /** Whether the treatments must pass App Review before the test can start. */
          reviewRequired?: boolean | null;
          /** When the test started, as an ISO 8601 timestamp. */
          startDate?: string | null;
          /** When the test ended or was stopped, as an ISO 8601 timestamp. */
          endDate?: string | null;
          /** App Store Connect identifier of the App Store version currently serving as the control (the default product page), or null when App Store Connect did not return it. */
          latestControlVersionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one treatment of a product page optimization test. */
    "app_store_connect.get_app_store_version_experiment_treatment": {
      input: {
        /**
         * App Store Connect identifier of the treatment.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentTreatmentId: string;
      };
      output: {
        /** One treatment of a product page optimization test: an alternative product page shown to part of the traffic. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionExperimentTreatment: {
          /** App Store Connect identifier for the treatment. */
          id: string;
          /** Treatment name shown in App Store Connect. */
          name?: string | null;
          /** Name of the alternate app icon asset the treatment uses, or null for the default icon. */
          appIconName?: string | null;
          /** Template URL and pixel size of the treatment app icon. */
          appIcon?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Icon width in pixels. */
            width?: number;
            /** Icon height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** When the treatment was promoted to the default product page, as an ISO 8601 timestamp, or null. */
          promotedDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one treatment localization by its App Store Connect identifier. */
    "app_store_connect.get_app_store_version_experiment_treatment_localization": {
      input: {
        /**
         * App Store Connect identifier of the treatment localization.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentTreatmentLocalizationId: string;
      };
      output: {
        /** A locale of a treatment. Screenshots and app previews for the treatment are attached to this localization. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionExperimentTreatmentLocalization: {
          /** App Store Connect identifier for the treatment localization. */
          id: string;
          /** Locale the treatment metadata is shown in, such as en-US. */
          locale?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the Game Center app version of one App Store version. Returns null when Game Center was never enabled for that version. */
    "app_store_connect.get_app_store_version_game_center_app_version": {
      input: {
        /**
         * App Store Connect identifier of the App Store version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** The Game Center enablement of one App Store version, plus the earlier versions it stays multiplayer compatible with. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAppVersion: {
          /** App Store Connect identifier for the Game Center app version. */
          id: string;
          /** Whether Game Center is enabled for this App Store version. */
          enabled?: boolean | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read the localized App Store metadata of one version in one locale. */
    "app_store_connect.get_app_store_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the version localization.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionLocalizationId: string;
      };
      output: {
        /** The App Store metadata of one version in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionLocalization: {
          /** App Store Connect identifier for the version localization. */
          id: string;
          /** Locale the metadata is written in, such as en-US. */
          locale?: string | null;
          /** App description shown on the product page. */
          description?: string | null;
          /** Comma-separated App Store search keywords. */
          keywords?: string | null;
          /** Marketing website URL shown on the product page. */
          marketingUrl?: string | null;
          /** Promotional text shown above the description, editable without a new version. */
          promotionalText?: string | null;
          /** Support website URL shown on the product page. */
          supportUrl?: string | null;
          /** Release notes shown for this version. */
          whatsNew?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the phased release configuration of an App Store version, or null when phased release is not configured for it. */
    "app_store_connect.get_app_store_version_phased_release": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** The phased release configuration of an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionPhasedRelease: {
          /** App Store Connect identifier for the phased release. */
          id: string;
          /** Whether the seven day phased rollout is inactive, active, paused, or complete. */
          phasedReleaseState?: "INACTIVE" | "ACTIVE" | "PAUSED" | "COMPLETE" | null;
          /** When the phased release started, as an ISO 8601 timestamp. */
          startDate?: string | null;
          /** Total number of days the rollout has been paused. */
          totalPauseDuration?: number | null;
          /** Current day of the seven day rollout, starting at 1. */
          currentDayNumber?: number | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one background asset pack, with the version currently in each of the App Store, internal TestFlight, and external TestFlight slots. */
    "app_store_connect.get_background_asset": {
      input: {
        /**
         * App Store Connect identifier of the background asset pack.
         * @minLength 1
         * @pattern \S
         */
        backgroundAssetId: string;
      };
      output: {
        /** An Apple-hosted background asset pack of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        backgroundAsset: {
          /** App Store Connect identifier of the background asset pack. */
          id: string;
          /** Asset pack identifier the app uses to request the pack, chosen when the pack was created. */
          assetPackIdentifier?: string | null;
          /** Whether the asset pack has been archived. */
          archived?: boolean | null;
          /** When the asset pack record was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Storage in bytes the asset pack occupies. */
          usedBytes?: number | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          appStoreVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          internalBetaVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          externalBetaVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one background asset version with its processing state, validation messages, parent asset pack, and release records. */
    "app_store_connect.get_background_asset_version": {
      input: {
        /**
         * App Store Connect identifier of the background asset version.
         * @minLength 1
         * @pattern \S
         */
        backgroundAssetVersionId: string;
      };
      output: {
        /** One version of an Apple-hosted background asset pack. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        backgroundAssetVersion: {
          /** App Store Connect identifier of the background asset version. */
          id: string;
          /** Version number App Store Connect assigned when the version was created; it increases automatically. */
          version?: string | null;
          /** Upload and processing state of the version. AWAITING_UPLOAD until the asset pack archive is uploaded and committed. */
          state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
          /** Validation errors, warnings, and informational messages from processing. */
          stateDetails?: {
            /** Errors that stopped processing. */
            errors?: Array<{
              /** Machine-readable code of the message. */
              code?: string | null;
              /** Human-readable text of the message. */
              description?: string | null;
              [key: string]: unknown;
            }>;
            /** Warnings raised during processing. */
            warnings?: Array<{
              /** Machine-readable code of the message. */
              code?: string | null;
              /** Human-readable text of the message. */
              description?: string | null;
              [key: string]: unknown;
            }>;
            /** Informational messages from processing. */
            infos?: Array<{
              /** Machine-readable code of the message. */
              code?: string | null;
              /** Human-readable text of the message. */
              description?: string | null;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          } | null;
          /** Locale declared in the asset pack manifest, when the pack is localized. */
          locale?: string | null;
          /** Platforms the asset pack version targets, taken from the uploaded manifest. */
          platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
          /** When the version was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** The asset pack the version belongs to, or null when App Store Connect did not return it. */
          backgroundAsset: {
            /** App Store Connect identifier of the background asset pack. */
            id: string;
            /** Asset pack identifier the app uses to request the pack. */
            assetPackIdentifier?: string | null;
            [key: string]: unknown;
          } | null;
          /** The internal TestFlight release of this version. Null when the version has no such release yet or App Store Connect did not return it. */
          internalBetaRelease: {
            /** App Store Connect identifier of the release record. */
            id: string;
            /** State of the release. */
            state?: "READY_FOR_TESTING" | "SUPERSEDED" | null;
            [key: string]: unknown;
          } | null;
          /** The external TestFlight release of this version. Null when the version has no such release yet or App Store Connect did not return it. */
          externalBetaRelease: {
            /** App Store Connect identifier of the release record. */
            id: string;
            /** State of the release. */
            state?: "READY_FOR_BETA_SUBMISSION" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "PROCESSING_FOR_TESTING" | "READY_FOR_TESTING" | "SUPERSEDED" | null;
            [key: string]: unknown;
          } | null;
          /** The App Store release of this version. Null when the version has no such release yet or App Store Connect did not return it. */
          appStoreRelease: {
            /** App Store Connect identifier of the release record. */
            id: string;
            /** State of the release. */
            state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "REJECTED" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "SUPERSEDED" | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the App Store release record of a background asset version, which tracks its App Review and distribution state. */
    "app_store_connect.get_background_asset_version_app_store_release": {
      input: {
        /**
         * App Store Connect identifier of the App Store release record, as returned in appStoreRelease of a background asset version.
         * @minLength 1
         * @pattern \S
         */
        backgroundAssetVersionAppStoreReleaseId: string;
      };
      output: {
        /** App Store Connect identifier of the release record. */
        id: string;
        /** State of the release. */
        state: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "REJECTED" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "SUPERSEDED" | null;
        /** Identifier of the background asset version the release belongs to, or null when App Store Connect did not return it. */
        backgroundAssetVersionId: string | null;
      };
    };
    /** Read the external TestFlight release record of a background asset version, which tracks its beta review state. */
    "app_store_connect.get_background_asset_version_external_beta_release": {
      input: {
        /**
         * App Store Connect identifier of the external beta release record, as returned in externalBetaRelease of a background asset version.
         * @minLength 1
         * @pattern \S
         */
        backgroundAssetVersionExternalBetaReleaseId: string;
      };
      output: {
        /** App Store Connect identifier of the release record. */
        id: string;
        /** State of the release. */
        state: "READY_FOR_BETA_SUBMISSION" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "PROCESSING_FOR_TESTING" | "READY_FOR_TESTING" | "SUPERSEDED" | null;
        /** Identifier of the background asset version the release belongs to, or null when App Store Connect did not return it. */
        backgroundAssetVersionId: string | null;
      };
    };
    /** Read the internal TestFlight release record of a background asset version. App Store Connect creates it once the version finishes processing. */
    "app_store_connect.get_background_asset_version_internal_beta_release": {
      input: {
        /**
         * App Store Connect identifier of the internal beta release record, as returned in internalBetaRelease of a background asset version.
         * @minLength 1
         * @pattern \S
         */
        backgroundAssetVersionInternalBetaReleaseId: string;
      };
      output: {
        /** App Store Connect identifier of the release record. */
        id: string;
        /** State of the release. */
        state: "READY_FOR_TESTING" | "SUPERSEDED" | null;
        /** Identifier of the background asset version the release belongs to, or null when App Store Connect did not return it. */
        backgroundAssetVersionId: string | null;
      };
    };
    /** Read one TestFlight App Clip invocation together with its localized titles. */
    "app_store_connect.get_beta_app_clip_invocation": {
      input: {
        /**
         * App Store Connect identifier of the App Clip invocation.
         * @minLength 1
         * @pattern \S
         */
        betaAppClipInvocationId: string;
      };
      output: {
        /** An App Clip experience testers can launch from the TestFlight app, with its localized titles. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppClipInvocation: {
          /** App Store Connect identifier for the App Clip invocation. */
          id: string;
          /** Invocation URL the App Clip is launched with from TestFlight. */
          url?: string | null;
          /** Localized titles App Store Connect returned alongside the invocation. */
          betaAppClipInvocationLocalizations: Array<{
            /** App Store Connect identifier for the localization. */
            id: string;
            /** Title testers see for the App Clip invocation in this locale. */
            title?: string | null;
            /** Locale the title is written in, such as en-US. */
            locale?: string | null;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        };
      };
    };
    /** Read the TestFlight test information of one app and locale by its identifier. */
    "app_store_connect.get_beta_app_localization": {
      input: {
        /**
         * App Store Connect identifier of the beta app localization.
         * @minLength 1
         * @pattern \S
         */
        betaAppLocalizationId: string;
      };
      output: {
        /** TestFlight test information of an app in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppLocalization: {
          /** App Store Connect identifier for the beta app localization. */
          id: string;
          /** Locale the test information is written in, such as en-US. */
          locale?: string | null;
          /** Description of the app shown to testers in TestFlight. */
          description?: string | null;
          /** Email address testers send feedback to. */
          feedbackEmail?: string | null;
          /** Marketing URL shown to testers. */
          marketingUrl?: string | null;
          /** Privacy policy URL shown to testers. */
          privacyPolicyUrl?: string | null;
          /** Privacy policy text shown to tvOS testers. */
          tvOsPrivacyPolicy?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the contact person, demo account, and notes Apple uses when reviewing an app for TestFlight external testing. */
    "app_store_connect.get_beta_app_review_detail": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
      };
      output: {
        /** Contact and demo account information Apple uses when reviewing the app for TestFlight. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppReviewDetail: {
          /** App Store Connect identifier for the beta app review detail. */
          id: string;
          /** First name of the person Apple contacts about the review. */
          contactFirstName?: string | null;
          /** Last name of the review contact. */
          contactLastName?: string | null;
          /** Phone number of the review contact. */
          contactPhone?: string | null;
          /** Email address of the review contact. */
          contactEmail?: string | null;
          /** Username of the demo account reviewers sign in with. */
          demoAccountName?: string | null;
          /** Password of the demo account reviewers sign in with. */
          demoAccountPassword?: string | null;
          /** Whether reviewers need a demo account to test the app. */
          demoAccountRequired?: boolean | null;
          /** Additional notes for the beta review team. */
          notes?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one TestFlight beta review submission by its identifier. */
    "app_store_connect.get_beta_app_review_submission": {
      input: {
        /**
         * App Store Connect identifier of the beta app review submission.
         * @minLength 1
         * @pattern \S
         */
        betaAppReviewSubmissionId: string;
      };
      output: {
        /** A TestFlight beta review submission of one build. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppReviewSubmission: {
          /** App Store Connect identifier for the beta app review submission. */
          id: string;
          /** State of the TestFlight beta review. */
          betaReviewState?: "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "APPROVED" | null;
          /** When the build was submitted for beta review, as an ISO 8601 timestamp. */
          submittedDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the "What to Test" notes record of one build and locale by its identifier. */
    "app_store_connect.get_beta_build_localization": {
      input: {
        /**
         * App Store Connect identifier of the beta build localization.
         * @minLength 1
         * @pattern \S
         */
        betaBuildLocalizationId: string;
      };
      output: {
        /** The "What to Test" notes of one build in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaBuildLocalization: {
          /** App Store Connect identifier for the beta build localization. */
          id: string;
          /** Locale the test notes belong to, such as en-US. */
          locale?: string | null;
          /** Test notes shown to testers for this locale. */
          whatsNew?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the full crash log text attached to one crash feedback submission. */
    "app_store_connect.get_beta_feedback_crash_log": {
      input: {
        /**
         * App Store Connect identifier of the crash feedback submission whose crash log to read.
         * @minLength 1
         * @pattern \S
         */
        betaFeedbackCrashSubmissionId: string;
      };
      output: {
        /** The crash log attached to a TestFlight crash feedback submission. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        crashLog: {
          /** App Store Connect identifier for the crash log. */
          id: string;
          /** Full text of the crash log. */
          logText?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one crash feedback submission with its device details and the build and tester it came from. */
    "app_store_connect.get_beta_feedback_crash_submission": {
      input: {
        /**
         * App Store Connect identifier of the crash feedback submission.
         * @minLength 1
         * @pattern \S
         */
        betaFeedbackCrashSubmissionId: string;
      };
      output: {
        /** A crash feedback submission a tester sent from TestFlight. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaFeedbackCrashSubmission: {
          /** App Store Connect identifier for the crash feedback submission. */
          id: string;
          /** When the tester sent the feedback, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Free-text comment the tester wrote. */
          comment?: string | null;
          /** Email address of the tester who sent the feedback. */
          email?: string | null;
          /** Device model identifier, such as iPhone16,2. */
          deviceModel?: string | null;
          /** Operating system version on the device. */
          osVersion?: string | null;
          /** Locale the device was set to. */
          locale?: string | null;
          /** Time zone the device was set to. */
          timeZone?: string | null;
          /** CPU architecture of the device. */
          architecture?: string | null;
          /** Network connection the device was using. */
          connectionType?: "WIFI" | "MOBILE_DATA" | "WIRE" | "UNKNOWN" | "NONE" | null;
          /** Model of the Apple Watch paired with the device, if any. */
          pairedAppleWatch?: string | null;
          /** How long the app had been running when the feedback was sent, in milliseconds. */
          appUptimeInMilliseconds?: number | null;
          /** Free disk space on the device, in bytes. */
          diskBytesAvailable?: number | null;
          /** Total disk space on the device, in bytes. */
          diskBytesTotal?: number | null;
          /** Battery level of the device, as a percentage. */
          batteryPercentage?: number | null;
          /** Screen width of the device, in points. */
          screenWidthInPoints?: number | null;
          /** Screen height of the device, in points. */
          screenHeightInPoints?: number | null;
          /** Platform the app was built for. */
          appPlatform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Platform of the device the app ran on. */
          devicePlatform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Device family the feedback came from. */
          deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION" | null;
          /** App Store Connect identifier of the build bundle the feedback was sent from. */
          buildBundleId?: string | null;
          /** The build the feedback was sent from, or null when App Store Connect did not return it. */
          build: {
            /** App Store Connect identifier for the build. */
            id: string;
            /** Build number, such as 42. */
            version?: string | null;
            [key: string]: unknown;
          } | null;
          /** The TestFlight tester the record belongs to, or null when App Store Connect did not return it. */
          tester: {
            /** App Store Connect identifier for the beta tester. */
            id: string;
            /** Email address the tester was invited with. */
            email?: string | null;
            /** Tester first name. */
            firstName?: string | null;
            /** Tester last name. */
            lastName?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one screenshot feedback submission with its device details, attached screenshots, and the build and tester it came from. */
    "app_store_connect.get_beta_feedback_screenshot_submission": {
      input: {
        /**
         * App Store Connect identifier of the screenshot feedback submission.
         * @minLength 1
         * @pattern \S
         */
        betaFeedbackScreenshotSubmissionId: string;
      };
      output: {
        /** A screenshot feedback submission a tester sent from TestFlight. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaFeedbackScreenshotSubmission: {
          /** App Store Connect identifier for the screenshot feedback submission. */
          id: string;
          /** When the tester sent the feedback, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Free-text comment the tester wrote. */
          comment?: string | null;
          /** Email address of the tester who sent the feedback. */
          email?: string | null;
          /** Device model identifier, such as iPhone16,2. */
          deviceModel?: string | null;
          /** Operating system version on the device. */
          osVersion?: string | null;
          /** Locale the device was set to. */
          locale?: string | null;
          /** Time zone the device was set to. */
          timeZone?: string | null;
          /** CPU architecture of the device. */
          architecture?: string | null;
          /** Network connection the device was using. */
          connectionType?: "WIFI" | "MOBILE_DATA" | "WIRE" | "UNKNOWN" | "NONE" | null;
          /** Model of the Apple Watch paired with the device, if any. */
          pairedAppleWatch?: string | null;
          /** How long the app had been running when the feedback was sent, in milliseconds. */
          appUptimeInMilliseconds?: number | null;
          /** Free disk space on the device, in bytes. */
          diskBytesAvailable?: number | null;
          /** Total disk space on the device, in bytes. */
          diskBytesTotal?: number | null;
          /** Battery level of the device, as a percentage. */
          batteryPercentage?: number | null;
          /** Screen width of the device, in points. */
          screenWidthInPoints?: number | null;
          /** Screen height of the device, in points. */
          screenHeightInPoints?: number | null;
          /** Platform the app was built for. */
          appPlatform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Platform of the device the app ran on. */
          devicePlatform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Device family the feedback came from. */
          deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION" | null;
          /** App Store Connect identifier of the build bundle the feedback was sent from. */
          buildBundleId?: string | null;
          /** The build the feedback was sent from, or null when App Store Connect did not return it. */
          build: {
            /** App Store Connect identifier for the build. */
            id: string;
            /** Build number, such as 42. */
            version?: string | null;
            [key: string]: unknown;
          } | null;
          /** The TestFlight tester the record belongs to, or null when App Store Connect did not return it. */
          tester: {
            /** App Store Connect identifier for the beta tester. */
            id: string;
            /** Email address the tester was invited with. */
            email?: string | null;
            /** Tester first name. */
            firstName?: string | null;
            /** Tester last name. */
            lastName?: string | null;
            [key: string]: unknown;
          } | null;
          /** Screenshots attached to the feedback, each with a time-limited download URL. */
          screenshots?: Array<{
            /** Download URL for the screenshot; it expires at expirationDate. */
            url?: string;
            /** Screenshot width in pixels. */
            width?: number;
            /** Screenshot height in pixels. */
            height?: number;
            /** When the download URL stops working, as an ISO 8601 timestamp. */
            expirationDate?: string;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one TestFlight group by its identifier, including its public link settings. */
    "app_store_connect.get_beta_group": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
      };
      output: {
        /** A TestFlight beta group. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaGroup: {
          /** App Store Connect identifier for the beta group. */
          id: string;
          /** Group name shown in TestFlight. */
          name?: string | null;
          /** When the group was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the group is an internal group of team members. */
          isInternalGroup?: boolean | null;
          /** Whether the group automatically receives every new build. */
          hasAccessToAllBuilds?: boolean | null;
          /** Whether a public TestFlight link is enabled for the group. */
          publicLinkEnabled?: boolean | null;
          /** Identifier segment of the public TestFlight link. */
          publicLinkId?: string | null;
          /** Full public TestFlight invitation link. */
          publicLink?: string | null;
          /** Whether the public link enforces a tester limit. */
          publicLinkLimitEnabled?: boolean | null;
          /** Maximum number of testers who may join through the public link. */
          publicLinkLimit?: number | null;
          /** Whether testers can send feedback from TestFlight. */
          feedbackEnabled?: boolean | null;
          /** Whether iOS builds are offered to Apple silicon Macs. */
          iosBuildsAvailableForAppleSiliconMac?: boolean | null;
          /** Whether iOS builds are offered to Apple Vision Pro. */
          iosBuildsAvailableForAppleVision?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read TestFlight usage metrics for one group: sessions, crashes, and feedback over the chosen period, either as one aggregate row or as one row per tester. */
    "app_store_connect.get_beta_group_beta_tester_usages": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
        /** Length of the reporting window ending now: 7, 30, 90, or 365 days. */
        period?: "P7D" | "P30D" | "P90D" | "P365D";
        /** Return one row per tester instead of a single aggregate row. */
        groupByBetaTester?: boolean;
        /**
         * Return only the usage of this tester.
         * @minLength 1
         * @pattern \S
         */
        betaTesterId?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Usage rows returned for this page. */
        usages: Array<{
          /** App Store Connect identifier of the tester this row covers, or null for the aggregate row. */
          betaTesterId: string | null;
          /** The TestFlight tester the record belongs to, or null when App Store Connect did not return it. */
          betaTester: {
            /** App Store Connect identifier for the beta tester. */
            id: string;
            /** Email address the tester was invited with. */
            email?: string | null;
            /** Tester first name. */
            firstName?: string | null;
            /** Tester last name. */
            lastName?: string | null;
            [key: string]: unknown;
          } | null;
          /** Usage counters per reporting interval. */
          dataPoints: Array<{
            /** Start of the reporting interval, as an ISO 8601 timestamp. */
            start: string | null;
            /** End of the reporting interval, as an ISO 8601 timestamp. */
            end: string | null;
            /** Crashes reported during the interval. */
            crashCount: number | null;
            /** App sessions during the interval. */
            sessionCount: number | null;
            /** Feedback submissions sent during the interval. */
            feedbackCount: number | null;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read the license agreement TestFlight testers of an app must accept. */
    "app_store_connect.get_beta_license_agreement": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
      };
      output: {
        /** The license agreement TestFlight testers accept before installing the app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaLicenseAgreement: {
          /** App Store Connect identifier for the beta license agreement. */
          id: string;
          /** Full text of the beta license agreement. */
          agreementText?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the recruitment criteria of a TestFlight group, which restrict who can join through its public link by device family and OS version. Returns null when the group has no criteria. */
    "app_store_connect.get_beta_recruitment_criterion": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
      };
      output: {
        /** Recruitment criteria that limit who can join a TestFlight group through its public link. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaRecruitmentCriterion: {
          /** App Store Connect identifier for the recruitment criteria. */
          id: string;
          /** When the criteria were last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** Device families and OS version ranges a tester must match. */
          deviceFamilyOsVersionFilters?: Array<{
            /** Device family the tester must use. */
            deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION";
            /** Lowest OS version allowed, inclusive. */
            minimumOsInclusive?: string | null;
            /** Highest OS version allowed, inclusive. */
            maximumOsInclusive?: string | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Check whether a TestFlight group has a build that testers matching its recruitment criteria could install. */
    "app_store_connect.get_beta_recruitment_criterion_compatible_build_check": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
      };
      output: {
        /** Whether a TestFlight group has a build that satisfies its recruitment criteria. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        compatibleBuildCheck: {
          /** App Store Connect identifier for the check. */
          id: string;
          /** True when at least one build available to the group can run on the devices the criteria allow. */
          hasCompatibleBuild?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one TestFlight tester by its identifier, including the invitation state and the devices the app is installed on. */
    "app_store_connect.get_beta_tester": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight tester.
         * @minLength 1
         * @pattern \S
         */
        betaTesterId: string;
      };
      output: {
        /** A TestFlight beta tester. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaTester: {
          /** App Store Connect identifier for the beta tester. */
          id: string;
          /** Email address the invitation was sent to. */
          email?: string | null;
          /** Tester first name. */
          firstName?: string | null;
          /** Tester last name. */
          lastName?: string | null;
          /** How the tester was invited. */
          inviteType?: "EMAIL" | "PUBLIC_LINK" | null;
          /** Where the tester stands in the invitation flow. */
          state?: "NOT_INVITED" | "INVITED" | "ACCEPTED" | "INSTALLED" | "REVOKED" | null;
          /** Devices the tester has installed the app on. */
          appDevices?: Array<{
            /** Device model name. */
            model?: string;
            /** Platform of the device. */
            platform?: "IOS" | "MAC_OS" | "TV_OS" | "WATCH_OS" | "VISION_OS";
            /** Operating system version on the device. */
            osVersion?: string;
            /** Build number installed on the device. */
            appBuildVersion?: string;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read TestFlight usage metrics of one tester in one app: sessions, crashes, and feedback over the chosen period. */
    "app_store_connect.get_beta_tester_usages": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight tester.
         * @minLength 1
         * @pattern \S
         */
        betaTesterId: string;
        /**
         * App Store Connect identifier of the app to report usage for.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Length of the reporting window ending now: 7, 30, 90, or 365 days. */
        period?: "P7D" | "P30D" | "P90D" | "P365D";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Usage rows returned for this page. */
        usages: Array<{
          /** App Store Connect identifier of the app this row covers, or null when App Store Connect did not report it. */
          appId: string | null;
          /** Usage counters per reporting interval. */
          dataPoints: Array<{
            /** Start of the reporting interval, as an ISO 8601 timestamp. */
            start: string | null;
            /** End of the reporting interval, as an ISO 8601 timestamp. */
            end: string | null;
            /** Crashes reported during the interval. */
            crashCount: number | null;
            /** App sessions during the interval. */
            sessionCount: number | null;
            /** Feedback submissions sent during the interval. */
            feedbackCount: number | null;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read one build together with its prerelease version, its TestFlight review submission, and the app it belongs to. */
    "app_store_connect.get_build": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
      };
      output: {
        /** A build with the related records requested alongside it. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        build: {
          /** App Store Connect identifier for the build. */
          id: string;
          /** Build number, such as 42. */
          version?: string | null;
          /** When the build finished uploading, as an ISO 8601 timestamp. */
          uploadedDate?: string | null;
          /** When the build stops being installable by testers. */
          expirationDate?: string | null;
          /** Whether the build has expired for TestFlight. */
          expired?: boolean | null;
          /** Processing state of the uploaded build. */
          processingState?: "PROCESSING" | "FAILED" | "INVALID" | "VALID" | null;
          /** Distribution audience the build was uploaded for. */
          buildAudienceType?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE" | null;
          /** The prerelease version a build belongs to, or null when it was not returned. */
          preReleaseVersion: {
            /** App Store Connect identifier for the prerelease version. */
            id: string;
            /** Marketing version string, such as 1.4.0. */
            version?: string | null;
            /** Content platform the version targets. */
            platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
            [key: string]: unknown;
          } | null;
          /** The TestFlight beta review submission for a build, or null when there is none. */
          betaAppReviewSubmission: {
            /** App Store Connect identifier for the beta app review submission. */
            id: string;
            /** State of the TestFlight beta review. */
            betaReviewState?: "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "APPROVED" | null;
            /** When the build was submitted for beta review, as an ISO 8601 timestamp. */
            submittedDate?: string | null;
            [key: string]: unknown;
          } | null;
          /** The app a record belongs to, or null when App Store Connect did not return it. */
          app: {
            /** App Store Connect identifier for the app. */
            id: string;
            /** App name shown on the App Store. */
            name?: string | null;
            /** Bundle identifier registered for the app. */
            bundleId?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read TestFlight usage metrics of one build: invitations, installs, sessions, crashes, and feedback since the build became available. */
    "app_store_connect.get_build_beta_build_usages": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
      };
      output: {
        /** The build the usage belongs to. */
        buildId: string;
        /** Usage counters per reporting interval. */
        dataPoints: Array<{
          /** Start of the reporting interval, as an ISO 8601 timestamp. */
          start: string | null;
          /** End of the reporting interval, as an ISO 8601 timestamp. */
          end: string | null;
          /** Crashes reported during the interval. */
          crashCount: number | null;
          /** Installs of the build during the interval. */
          installCount: number | null;
          /** App sessions during the interval. */
          sessionCount: number | null;
          /** Feedback submissions sent during the interval. */
          feedbackCount: number | null;
          /** Invitations sent for the build during the interval. */
          inviteCount: number | null;
          [key: string]: unknown;
        }>;
      };
    };
    /** Read the TestFlight distribution details of one build: its internal and external testing states and whether testers are notified automatically. */
    "app_store_connect.get_build_beta_detail": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
      };
      output: {
        /** TestFlight distribution details of one build. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        buildBetaDetail: {
          /** App Store Connect identifier for the build beta detail. */
          id: string;
          /** Whether testers are notified automatically when the build becomes available. */
          autoNotifyEnabled?: boolean | null;
          /** Where the build stands for internal TestFlight testers. */
          internalBuildState?: "PROCESSING" | "PROCESSING_EXCEPTION" | "MISSING_EXPORT_COMPLIANCE" | "READY_FOR_BETA_TESTING" | "IN_BETA_TESTING" | "EXPIRED" | "IN_EXPORT_COMPLIANCE_REVIEW" | null;
          /** Where the build stands for external TestFlight testers, including beta review. */
          externalBuildState?: "PROCESSING" | "PROCESSING_EXCEPTION" | "MISSING_EXPORT_COMPLIANCE" | "READY_FOR_BETA_TESTING" | "IN_BETA_TESTING" | "EXPIRED" | "IN_EXPORT_COMPLIANCE_REVIEW" | "READY_FOR_BETA_SUBMISSION" | "WAITING_FOR_BETA_REVIEW" | "IN_BETA_REVIEW" | "BETA_REJECTED" | "BETA_APPROVED" | "NOT_APPLICABLE" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the cached validation status of the associated domains an App Clip build bundle declares, as Apple's CDN last recorded it. Returns null when App Store Connect has no status for the bundle. */
    "app_store_connect.get_build_bundle_app_clip_domain_cache_status": {
      input: {
        /**
         * App Store Connect identifier of the build bundle.
         * @minLength 1
         * @pattern \S
         */
        buildBundleId: string;
      };
      output: {
        /** Validation status of the associated domains an App Clip bundle declares. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipDomainCacheStatus: {
          /** App Store Connect identifier for the domain status record. */
          id: string;
          /** Validation result for each associated domain. */
          domains?: Array<{
            /** Domain name that was checked. */
            domain?: string;
            /** Whether the apple-app-site-association file validated. */
            isValid?: boolean;
            /** When this domain was last checked, as an ISO 8601 timestamp. */
            lastUpdatedDate?: string | null;
            /** Why validation failed, or null when the domain is valid. */
            errorCode?: "BAD_HTTP_RESPONSE" | "BAD_JSON_CONTENT" | "BAD_PKCS7_SIGNATURE" | "CANNOT_REACH_AASA_FILE" | "CROSS_SITE_REDIRECTS_FORBIDDEN" | "DNS_ERROR" | "INSECURE_REDIRECTS_FORBIDDEN" | "INVALID_ENTITLEMENT_MISSING_SECTION" | "INVALID_ENTITLEMENT_SYNTAX_ERROR" | "INVALID_ENTITLEMENT_UNHANDLED_SECTION" | "INVALID_ENTITLEMENT_UNKNOWN_ID" | "NETWORK_ERROR" | "NETWORK_ERROR_TEMPORARY" | "OTHER_ERROR" | "TIMEOUT" | "TLS_ERROR" | "TOO_MANY_REDIRECTS" | "UNEXPECTED_ERROR" | null;
            [key: string]: unknown;
          }> | null;
          /** When the status was last refreshed, as an ISO 8601 timestamp. */
          lastUpdatedDate?: string | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read the debug-mode validation status of the associated domains an App Clip build bundle declares, which checks the domains directly instead of Apple's CDN cache. Returns null when App Store Connect has no status for the bundle. */
    "app_store_connect.get_build_bundle_app_clip_domain_debug_status": {
      input: {
        /**
         * App Store Connect identifier of the build bundle.
         * @minLength 1
         * @pattern \S
         */
        buildBundleId: string;
      };
      output: {
        /** Validation status of the associated domains an App Clip bundle declares. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipDomainDebugStatus: {
          /** App Store Connect identifier for the domain status record. */
          id: string;
          /** Validation result for each associated domain. */
          domains?: Array<{
            /** Domain name that was checked. */
            domain?: string;
            /** Whether the apple-app-site-association file validated. */
            isValid?: boolean;
            /** When this domain was last checked, as an ISO 8601 timestamp. */
            lastUpdatedDate?: string | null;
            /** Why validation failed, or null when the domain is valid. */
            errorCode?: "BAD_HTTP_RESPONSE" | "BAD_JSON_CONTENT" | "BAD_PKCS7_SIGNATURE" | "CANNOT_REACH_AASA_FILE" | "CROSS_SITE_REDIRECTS_FORBIDDEN" | "DNS_ERROR" | "INSECURE_REDIRECTS_FORBIDDEN" | "INVALID_ENTITLEMENT_MISSING_SECTION" | "INVALID_ENTITLEMENT_SYNTAX_ERROR" | "INVALID_ENTITLEMENT_UNHANDLED_SECTION" | "INVALID_ENTITLEMENT_UNKNOWN_ID" | "NETWORK_ERROR" | "NETWORK_ERROR_TEMPORARY" | "OTHER_ERROR" | "TIMEOUT" | "TLS_ERROR" | "TOO_MANY_REDIRECTS" | "UNEXPECTED_ERROR" | null;
            [key: string]: unknown;
          }> | null;
          /** When the status was last refreshed, as an ISO 8601 timestamp. */
          lastUpdatedDate?: string | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Fetch the performance and power metrics collected for one build in the xcodeMetrics document format. */
    "app_store_connect.get_build_perf_power_metrics": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /** Return only metrics for this platform. */
        platform?: "IOS";
        /**
         * Return only these metric categories.
         * @minItems 1
         */
        metricTypes?: Array<"DISK" | "HANG" | "BATTERY" | "LAUNCH" | "MEMORY" | "ANIMATION" | "TERMINATION" | "STORAGE">;
        /**
         * Return only metrics for these device types, such as iPhone14,2. Use all_iphones for every iPhone model and all_ipads for every iPad model.
         * @minItems 1
         */
        deviceTypes?: Array<string>;
      };
      output: {
        /** The xcodeMetrics document exactly as App Store Connect returned it. */
        metrics: {
          /** Version of the xcodeMetrics document format. */
          version?: string | null;
          /** Automatically generated trends comparing the latest version with earlier ones. */
          insights?: {
            /** Metrics that got worse in the latest version. */
            trendingUp?: Array<Record<string, unknown>> | null;
            /** Metrics that regressed against reference versions. */
            regressions?: Array<Record<string, unknown>> | null;
            [key: string]: unknown;
          } | null;
          /** Metric categories and datasets, one entry per platform. */
          productData?: Array<{
            /** Platform the metrics were collected on. */
            platform?: string | null;
            /** Metric categories with their metrics, units, goals, and datasets. */
            metricCategories?: Array<Record<string, unknown>> | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one bundle ID record by its App Store Connect identifier. */
    "app_store_connect.get_bundle_id": {
      input: {
        /**
         * App Store Connect identifier of the bundle ID record (the opaque resource id, not the reverse-DNS identifier string).
         * @minLength 1
         * @pattern \S
         */
        bundleIdId: string;
      };
      output: {
        /** A bundle ID (App ID) registered with the team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        bundleId: {
          /** App Store Connect identifier of the bundle ID record. */
          id: string;
          /** Display name given to the bundle ID when it was registered. */
          name?: string | null;
          /** Platform the bundle ID is registered for. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Reverse-DNS bundle identifier string, such as com.example.app, or a wildcard ending in *. */
          identifier?: string | null;
          /** Team ID prefix (seed ID) associated with the bundle ID. */
          seedId?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the App Store Connect app that uses one bundle ID. Returns null when no app has been created for the bundle ID yet. */
    "app_store_connect.get_bundle_id_app": {
      input: {
        /**
         * App Store Connect identifier of the bundle ID record (the opaque resource id, not the reverse-DNS identifier string).
         * @minLength 1
         * @pattern \S
         */
        bundleIdId: string;
      };
      output: {
        /** An app registered in App Store Connect. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        app: {
          /** App Store Connect identifier for the app. */
          id: string;
          /** App name shown on the App Store. */
          name?: string | null;
          /** Bundle identifier registered for the app. */
          bundleId?: string | null;
          /** SKU chosen when the app record was created. */
          sku?: string | null;
          /** Primary App Store locale, such as en-US. */
          primaryLocale?: string | null;
          /** Whether the app is or has ever been part of the Kids category. */
          isOrEverWasMadeForKids?: boolean | null;
          /** Third-party content rights declared for the app. */
          contentRightsDeclaration?: "DOES_NOT_USE_THIRD_PARTY_CONTENT" | "USES_THIRD_PARTY_CONTENT" | null;
          /** Whether streamlined purchasing is enabled for the app. */
          streamlinedPurchasingEnabled?: boolean | null;
          /** Accessibility information URL published with the app. */
          accessibilityUrl?: string | null;
          /** Production server-to-server subscription status URL. */
          subscriptionStatusUrl?: string | null;
          /** Version of the production subscription status URL. */
          subscriptionStatusUrlVersion?: "V1" | "V2" | null;
          /** Sandbox server-to-server subscription status URL. */
          subscriptionStatusUrlForSandbox?: string | null;
          /** Version of the sandbox subscription status URL. */
          subscriptionStatusUrlVersionForSandbox?: "V1" | "V2" | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one certificate by its App Store Connect identifier, including its base64 DER content. */
    "app_store_connect.get_certificate": {
      input: {
        /**
         * App Store Connect identifier of the certificate.
         * @minLength 1
         * @pattern \S
         */
        certificateId: string;
      };
      output: {
        /** A signing certificate issued by Apple. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        certificate: {
          /** App Store Connect identifier of the certificate. */
          id: string;
          /** Certificate name. */
          name?: string | null;
          /** Kind of certificate. */
          certificateType?: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC" | null;
          /** Certificate display name shown in the developer portal. */
          displayName?: string | null;
          /** Serial number of the certificate. */
          serialNumber?: string | null;
          /** Platform the certificate applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** When the certificate expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          /** Base64-encoded DER certificate. Decode it to a .cer file to install it in a keychain. */
          certificateContent?: string | null;
          /** Whether the certificate is activated. Only Apple Pay payment processing certificates carry an activation state. */
          activated?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one build artifact, including a fresh time-limited download URL for its file. */
    "app_store_connect.get_ci_artifact": {
      input: {
        /**
         * App Store Connect identifier of the artifact.
         * @minLength 1
         * @pattern \S
         */
        ciArtifactId: string;
      };
      output: {
        /** A file produced by a build action, such as an archive or a log bundle. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciArtifact: {
          /** App Store Connect identifier for the artifact. */
          id: string;
          /** Kind of artifact. */
          fileType?: "ARCHIVE" | "ARCHIVE_EXPORT" | "LOG_BUNDLE" | "RESULT_BUNDLE" | "TEST_PRODUCTS" | "XCODEBUILD_PRODUCTS" | "STAPLED_NOTARIZED_ARCHIVE" | null;
          /** File name of the artifact. */
          fileName?: string | null;
          /** File size in bytes. */
          fileSize?: number | null;
          /** Time-limited URL to download the artifact from. */
          downloadUrl?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one action of a build run, with its progress, outcome, and issue counts. */
    "app_store_connect.get_ci_build_action": {
      input: {
        /**
         * App Store Connect identifier of the build action.
         * @minLength 1
         * @pattern \S
         */
        ciBuildActionId: string;
      };
      output: {
        /** One action (build, analyze, test, or archive) inside a build run. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciBuildAction: {
          /** App Store Connect identifier for the build action. */
          id: string;
          /** Action name shown in Xcode Cloud. */
          name?: string | null;
          /** What the action does. */
          actionType?: "BUILD" | "ANALYZE" | "TEST" | "ARCHIVE" | null;
          /** When the action started, as an ISO 8601 timestamp. */
          startedDate?: string | null;
          /** When the action finished, as an ISO 8601 timestamp. */
          finishedDate?: string | null;
          /** Number of issues by type. */
          issueCounts?: {
            /** Number of analyzer warnings. */
            analyzerWarnings?: number;
            /** Number of errors. */
            errors?: number;
            /** Number of test failures. */
            testFailures?: number;
            /** Number of warnings. */
            warnings?: number;
            [key: string]: unknown;
          } | null;
          /** Whether the action is pending, running, or complete. */
          executionProgress?: "PENDING" | "RUNNING" | "COMPLETE" | null;
          /** Outcome of the action once it is complete. */
          completionStatus?: "SUCCEEDED" | "FAILED" | "ERRORED" | "CANCELED" | "SKIPPED" | null;
          /** Whether the whole build run fails when this action fails. */
          isRequiredToPass?: boolean | null;
          /** Identifier of the build run the action belongs to. */
          ciBuildRunId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the build run a build action belongs to. */
    "app_store_connect.get_ci_build_action_build_run": {
      input: {
        /**
         * App Store Connect identifier of the build action.
         * @minLength 1
         * @pattern \S
         */
        ciBuildActionId: string;
      };
      output: {
        /** One run of an Xcode Cloud workflow. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciBuildRun: {
          /** App Store Connect identifier for the build run. */
          id: string;
          /** Sequential build number within the product. */
          number?: number | null;
          /** When the run was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the run started, as an ISO 8601 timestamp. */
          startedDate?: string | null;
          /** When the run finished, as an ISO 8601 timestamp. */
          finishedDate?: string | null;
          /** A commit Xcode Cloud built. */
          sourceCommit?: {
            /** Full commit SHA. */
            commitSha?: string;
            /** Commit message. */
            message?: string;
            /** Who authored the commit. */
            author?: {
              /** Display name of the author. */
              displayName?: string;
              /** Avatar image URL of the author. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** Who committed the change. */
            committer?: {
              /** Display name of the committer. */
              displayName?: string;
              /** Avatar image URL of the committer. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** URL of the commit on the source control provider. */
            webUrl?: string;
            [key: string]: unknown;
          } | null;
          /** A commit Xcode Cloud built. */
          destinationCommit?: {
            /** Full commit SHA. */
            commitSha?: string;
            /** Commit message. */
            message?: string;
            /** Who authored the commit. */
            author?: {
              /** Display name of the author. */
              displayName?: string;
              /** Avatar image URL of the author. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** Who committed the change. */
            committer?: {
              /** Display name of the committer. */
              displayName?: string;
              /** Avatar image URL of the committer. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** URL of the commit on the source control provider. */
            webUrl?: string;
            [key: string]: unknown;
          } | null;
          /** Whether the run was started for a pull request. */
          isPullRequestBuild?: boolean | null;
          /** Number of issues by type. */
          issueCounts?: {
            /** Number of analyzer warnings. */
            analyzerWarnings?: number;
            /** Number of errors. */
            errors?: number;
            /** Number of test failures. */
            testFailures?: number;
            /** Number of warnings. */
            warnings?: number;
            [key: string]: unknown;
          } | null;
          /** Whether the run is pending, running, or complete. */
          executionProgress?: "PENDING" | "RUNNING" | "COMPLETE" | null;
          /** Outcome of the run once it is complete. */
          completionStatus?: "SUCCEEDED" | "FAILED" | "ERRORED" | "CANCELED" | "SKIPPED" | null;
          /** What started the run. */
          startReason?: "GIT_REF_CHANGE" | "MANUAL" | "MANUAL_REBUILD" | "PULL_REQUEST_OPEN" | "PULL_REQUEST_UPDATE" | "SCHEDULE" | null;
          /** Why the run was canceled, when it was. */
          cancelReason?: "AUTOMATICALLY_BY_NEWER_BUILD" | "MANUALLY_BY_USER" | null;
          /** Identifier of the workflow that ran. */
          ciWorkflowId: string | null;
          /** Identifier of the Xcode Cloud product the run belongs to. */
          ciProductId: string | null;
          /** Identifier of the Git reference (branch or tag) the run built. */
          sourceBranchOrTagId: string | null;
          /** Identifier of the destination branch for a pull request run. */
          destinationBranchId: string | null;
          /** Identifier of the pull request the run was started for. */
          scmPullRequestId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Xcode Cloud build run, including its progress, completion status, issue counts, and the commit it built. Poll it to follow a run started with start_ci_build_run. */
    "app_store_connect.get_ci_build_run": {
      input: {
        /**
         * App Store Connect identifier of the build run.
         * @minLength 1
         * @pattern \S
         */
        ciBuildRunId: string;
      };
      output: {
        /** One run of an Xcode Cloud workflow. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciBuildRun: {
          /** App Store Connect identifier for the build run. */
          id: string;
          /** Sequential build number within the product. */
          number?: number | null;
          /** When the run was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the run started, as an ISO 8601 timestamp. */
          startedDate?: string | null;
          /** When the run finished, as an ISO 8601 timestamp. */
          finishedDate?: string | null;
          /** A commit Xcode Cloud built. */
          sourceCommit?: {
            /** Full commit SHA. */
            commitSha?: string;
            /** Commit message. */
            message?: string;
            /** Who authored the commit. */
            author?: {
              /** Display name of the author. */
              displayName?: string;
              /** Avatar image URL of the author. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** Who committed the change. */
            committer?: {
              /** Display name of the committer. */
              displayName?: string;
              /** Avatar image URL of the committer. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** URL of the commit on the source control provider. */
            webUrl?: string;
            [key: string]: unknown;
          } | null;
          /** A commit Xcode Cloud built. */
          destinationCommit?: {
            /** Full commit SHA. */
            commitSha?: string;
            /** Commit message. */
            message?: string;
            /** Who authored the commit. */
            author?: {
              /** Display name of the author. */
              displayName?: string;
              /** Avatar image URL of the author. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** Who committed the change. */
            committer?: {
              /** Display name of the committer. */
              displayName?: string;
              /** Avatar image URL of the committer. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** URL of the commit on the source control provider. */
            webUrl?: string;
            [key: string]: unknown;
          } | null;
          /** Whether the run was started for a pull request. */
          isPullRequestBuild?: boolean | null;
          /** Number of issues by type. */
          issueCounts?: {
            /** Number of analyzer warnings. */
            analyzerWarnings?: number;
            /** Number of errors. */
            errors?: number;
            /** Number of test failures. */
            testFailures?: number;
            /** Number of warnings. */
            warnings?: number;
            [key: string]: unknown;
          } | null;
          /** Whether the run is pending, running, or complete. */
          executionProgress?: "PENDING" | "RUNNING" | "COMPLETE" | null;
          /** Outcome of the run once it is complete. */
          completionStatus?: "SUCCEEDED" | "FAILED" | "ERRORED" | "CANCELED" | "SKIPPED" | null;
          /** What started the run. */
          startReason?: "GIT_REF_CHANGE" | "MANUAL" | "MANUAL_REBUILD" | "PULL_REQUEST_OPEN" | "PULL_REQUEST_UPDATE" | "SCHEDULE" | null;
          /** Why the run was canceled, when it was. */
          cancelReason?: "AUTOMATICALLY_BY_NEWER_BUILD" | "MANUALLY_BY_USER" | null;
          /** Identifier of the workflow that ran. */
          ciWorkflowId: string | null;
          /** Identifier of the Xcode Cloud product the run belongs to. */
          ciProductId: string | null;
          /** Identifier of the Git reference (branch or tag) the run built. */
          sourceBranchOrTagId: string | null;
          /** Identifier of the destination branch for a pull request run. */
          destinationBranchId: string | null;
          /** Identifier of the pull request the run was started for. */
          scmPullRequestId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one issue a build action reported, with the file and line it points at. */
    "app_store_connect.get_ci_issue": {
      input: {
        /**
         * App Store Connect identifier of the issue.
         * @minLength 1
         * @pattern \S
         */
        ciIssueId: string;
      };
      output: {
        /** An error, warning, analyzer warning, or test failure reported by a build action. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciIssue: {
          /** App Store Connect identifier for the issue. */
          id: string;
          /** Kind of issue. */
          issueType?: "ANALYZER_WARNING" | "ERROR" | "TEST_FAILURE" | "WARNING" | null;
          /** Issue message. */
          message?: string | null;
          /** Source file the record points at. */
          fileSource?: {
            /** File path relative to the repository root. */
            path?: string;
            /** Line number in the file. */
            lineNumber?: number;
            [key: string]: unknown;
          } | null;
          /** Issue category, such as Swift Compiler Error. */
          category?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one macOS version available in Xcode Cloud. */
    "app_store_connect.get_ci_mac_os_version": {
      input: {
        /**
         * App Store Connect identifier of the macOS version.
         * @minLength 1
         * @pattern \S
         */
        ciMacOsVersionId: string;
      };
      output: {
        /** A macOS version Xcode Cloud can build on. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciMacOsVersion: {
          /** App Store Connect identifier for the macOS version. */
          id: string;
          /** macOS version number, such as 14.4. */
          version?: string | null;
          /** macOS version name, such as macOS Sonoma 14.4. */
          name?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Xcode Cloud product by its App Store Connect identifier. */
    "app_store_connect.get_ci_product": {
      input: {
        /**
         * App Store Connect identifier of the Xcode Cloud product.
         * @minLength 1
         * @pattern \S
         */
        ciProductId: string;
      };
      output: {
        /** An Xcode Cloud product, the container that holds the workflows of one app or framework. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciProduct: {
          /** App Store Connect identifier for the Xcode Cloud product. */
          id: string;
          /** Product name. */
          name?: string | null;
          /** When the product was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the product builds an app or a framework. */
          productType?: "APP" | "FRAMEWORK" | null;
          /** Identifier of the app the product belongs to. */
          appId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the result of one test method across the destinations it ran on. */
    "app_store_connect.get_ci_test_result": {
      input: {
        /**
         * App Store Connect identifier of the test result.
         * @minLength 1
         * @pattern \S
         */
        ciTestResultId: string;
      };
      output: {
        /** The result of one test method across the destinations it ran on. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciTestResult: {
          /** App Store Connect identifier for the test result. */
          id: string;
          /** Name of the test class. */
          className?: string | null;
          /** Name of the test method. */
          name?: string | null;
          /** Overall test status across all destinations. */
          status?: "SUCCESS" | "FAILURE" | "MIXED" | "SKIPPED" | "EXPECTED_FAILURE" | null;
          /** Source file the record points at. */
          fileSource?: {
            /** File path relative to the repository root. */
            path?: string;
            /** Line number in the file. */
            lineNumber?: number;
            [key: string]: unknown;
          } | null;
          /** Failure message, when the test did not succeed. */
          message?: string | null;
          /** Per-destination results. */
          destinationTestResults?: Array<{
            /** Identifier of the destination. */
            uuid?: string;
            /** Device name, such as iPhone 15. */
            deviceName?: string;
            /** Operating system version on the destination. */
            osVersion?: string;
            /** Test status on this destination. */
            status?: "SUCCESS" | "FAILURE" | "MIXED" | "SKIPPED" | "EXPECTED_FAILURE";
            /** Test duration in seconds. */
            duration?: number;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Xcode Cloud workflow with its start conditions, actions, and the product, repository, Xcode version, and macOS version it uses. */
    "app_store_connect.get_ci_workflow": {
      input: {
        /**
         * App Store Connect identifier of the workflow.
         * @minLength 1
         * @pattern \S
         */
        ciWorkflowId: string;
      };
      output: {
        /** An Xcode Cloud workflow. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciWorkflow: {
          /** App Store Connect identifier for the workflow. */
          id: string;
          /** Workflow name shown in App Store Connect and Xcode. */
          name?: string | null;
          /** Description shown under the workflow name. */
          description?: string | null;
          /** Start a build when a matching branch changes. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchstartcondition */
          branchStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start a build when a matching tag is created. See https://developer.apple.com/documentation/appstoreconnectapi/citagstartcondition */
          tagStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start a build when a matching pull request is opened or updated. See https://developer.apple.com/documentation/appstoreconnectapi/cipullrequeststartcondition */
          pullRequestStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            destination?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start builds on a schedule. See https://developer.apple.com/documentation/appstoreconnectapi/cischeduledstartcondition */
          scheduledStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** When the schedule fires. */
            schedule?: {
              /** How often the build starts. */
              frequency?: "WEEKLY" | "DAILY" | "HOURLY";
              /** Days of the week for a WEEKLY schedule. */
              days?: Array<"SUNDAY" | "MONDAY" | "TUESDAY" | "WEDNESDAY" | "THURSDAY" | "FRIDAY" | "SATURDAY">;
              /** Hour of the day the build starts. */
              hour?: number;
              /** Minute of the hour the build starts. */
              minute?: number;
              /** IANA time zone name, such as America/Los_Angeles. */
              timezone?: string;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Branches a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualbranchstartcondition */
          manualBranchStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Tags a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualtagstartcondition */
          manualTagStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Pull requests a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualpullrequeststartcondition */
          manualPullRequestStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            destination?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Actions the workflow runs, in order. */
          actions?: Array<{
            /** Action name shown in Xcode Cloud. */
            name?: string;
            /** What the action does. */
            actionType?: "BUILD" | "ANALYZE" | "TEST" | "ARCHIVE";
            /** Device or simulator family the action targets. */
            destination?: "ANY_IOS_DEVICE" | "ANY_IOS_SIMULATOR" | "ANY_TVOS_DEVICE" | "ANY_TVOS_SIMULATOR" | "ANY_WATCHOS_DEVICE" | "ANY_WATCHOS_SIMULATOR" | "ANY_MAC" | "ANY_MAC_CATALYST" | "ANY_VISIONOS_DEVICE" | "ANY_VISIONOS_SIMULATOR";
            /** Who may install the build an ARCHIVE action produces. */
            buildDistributionAudience?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE";
            /** Test plan configuration for a TEST action. */
            testConfiguration?: {
              /** Use the scheme test settings or a specific test plan. */
              kind?: "USE_SCHEME_SETTINGS" | "SPECIFIC_TEST_PLANS";
              /** Name of the test plan to run when kind is SPECIFIC_TEST_PLANS. */
              testPlanName?: string;
              /** Simulators and Macs the tests run on. */
              testDestinations?: Array<{
                /** Device type name, such as iPhone 15. */
                deviceTypeName?: string;
                /** Device type identifier. */
                deviceTypeIdentifier?: string;
                /** Runtime name, such as iOS 17.4. */
                runtimeName?: string;
                /** Runtime identifier. */
                runtimeIdentifier?: string;
                /** Whether the destination is a simulator or a Mac. */
                kind?: "SIMULATOR" | "MAC";
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Xcode scheme the action builds. */
            scheme?: string;
            /** Platform the action builds for. */
            platform?: "MACOS" | "IOS" | "TVOS" | "WATCHOS" | "VISIONOS";
            /** Whether the whole build run fails when this action fails. */
            isRequiredToPass?: boolean;
            [key: string]: unknown;
          }> | null;
          /** Whether the workflow is enabled and can start builds. */
          isEnabled?: boolean | null;
          /** Whether the workflow is locked against editing in Xcode. */
          isLockedForEditing?: boolean | null;
          /** Whether every build starts from a clean environment without cached derived data. */
          clean?: boolean | null;
          /** Path of the Xcode project, workspace, or Swift package the workflow builds, relative to the repository root. */
          containerFilePath?: string | null;
          /** When the workflow was last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** Identifier of the Xcode Cloud product the workflow belongs to. */
          ciProductId: string | null;
          /** Identifier of the repository the workflow builds from. */
          scmRepositoryId: string | null;
          /** Identifier of the Xcode version the workflow builds with. */
          ciXcodeVersionId: string | null;
          /** Identifier of the macOS version the workflow builds on. */
          ciMacOsVersionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the source repository an Xcode Cloud workflow builds from. */
    "app_store_connect.get_ci_workflow_repository": {
      input: {
        /**
         * App Store Connect identifier of the workflow.
         * @minLength 1
         * @pattern \S
         */
        ciWorkflowId: string;
      };
      output: {
        /** A source repository Xcode Cloud can build. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        scmRepository: {
          /** App Store Connect identifier for the repository. */
          id: string;
          /** When Xcode Cloud last accessed the repository, as an ISO 8601 timestamp. */
          lastAccessedDate?: string | null;
          /** HTTPS clone URL. */
          httpCloneUrl?: string | null;
          /** SSH clone URL. */
          sshCloneUrl?: string | null;
          /** Owner or organization the repository belongs to. */
          ownerName?: string | null;
          /** Repository name. */
          repositoryName?: string | null;
          /** Identifier of the source control provider. */
          scmProviderId: string | null;
          /** Identifier of the Git reference for the default branch. */
          defaultBranchId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Xcode version available in Xcode Cloud, with the simulators and Macs available for tests. */
    "app_store_connect.get_ci_xcode_version": {
      input: {
        /**
         * App Store Connect identifier of the Xcode version.
         * @minLength 1
         * @pattern \S
         */
        ciXcodeVersionId: string;
      };
      output: {
        /** An Xcode version Xcode Cloud can build with. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciXcodeVersion: {
          /** App Store Connect identifier for the Xcode version. */
          id: string;
          /** Xcode version number, such as 15.3. */
          version?: string | null;
          /** Xcode version name, such as Xcode 15.3. */
          name?: string | null;
          /** Simulators and Macs available for tests with this Xcode version. */
          testDestinations?: Array<{
            /** Device type name, such as iPhone 15. */
            deviceTypeName?: string;
            /** Device type identifier. */
            deviceTypeIdentifier?: string;
            /** Runtimes available for the device type. */
            availableRuntimes?: Array<{
              /** Runtime name, such as iOS 17.4. */
              runtimeName?: string;
              /** Runtime identifier. */
              runtimeIdentifier?: string;
              [key: string]: unknown;
            }>;
            /** Whether the destination is a simulator or a Mac. */
            kind?: "SIMULATOR" | "MAC";
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one App Store review together with the developer response published for it. */
    "app_store_connect.get_customer_review": {
      input: {
        /**
         * App Store Connect identifier of the review.
         * @minLength 1
         * @pattern \S
         */
        customerReviewId: string;
      };
      output: {
        /** A customer review left on the App Store. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        customerReview: {
          /** App Store Connect identifier for the review. */
          id: string;
          /** Star rating from 1 to 5. */
          rating?: number | null;
          /** Review title. */
          title?: string | null;
          /** Review text. */
          body?: string | null;
          /** Nickname the reviewer publishes under. */
          reviewerNickname?: string | null;
          /** When the review was written, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** ISO 3166-1 alpha-3 storefront the review was written in, such as USA. */
          territory?: string | null;
          /** The developer response published for a review, or null when there is none. */
          response: {
            /** App Store Connect identifier for the response. */
            id: string;
            /** Text of the developer response. */
            responseBody?: string | null;
            /** When the response was last changed, as an ISO 8601 timestamp. */
            lastModifiedDate?: string | null;
            /** Publication state of the response. */
            state?: "PUBLISHED" | "PENDING_PUBLISH" | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one registered device by its App Store Connect identifier. */
    "app_store_connect.get_device": {
      input: {
        /**
         * App Store Connect identifier of the device.
         * @minLength 1
         * @pattern \S
         */
        deviceId: string;
      };
      output: {
        /** A device registered with the team for development. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        device: {
          /** App Store Connect identifier of the device. */
          id: string;
          /** Device name given when it was registered. */
          name?: string | null;
          /** Platform the device runs. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Unique device identifier (UDID) of the device. */
          udid?: string | null;
          /** Hardware class of the device. */
          deviceClass?: "APPLE_VISION_PRO" | "APPLE_WATCH" | "IPAD" | "IPHONE" | "IPOD" | "APPLE_TV" | "MAC" | null;
          /** Whether the device is enabled for development. Disabled devices are excluded from new profiles. */
          status?: "ENABLED" | "DISABLED" | null;
          /** Device model name. */
          model?: string | null;
          /** When the device was registered, as an ISO 8601 timestamp. */
          addedDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Fetch the anonymized backtrace logs App Store Connect recorded for one diagnostic signature, including call stack trees and device metadata. */
    "app_store_connect.get_diagnostic_signature_logs": {
      input: {
        /**
         * App Store Connect identifier of the diagnostic signature.
         * @minLength 1
         * @pattern \S
         */
        diagnosticSignatureId: string;
        /**
         * Maximum number of diagnostic logs to return.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
      };
      output: {
        /** Version of the diagnostic logs document format. */
        version: string | null;
        /** Diagnostic insights and logs, one entry per signature. */
        productData: Array<{
          /** Identifier of the diagnostic signature. */
          signatureId?: string | null;
          /** Insights App Store Connect derived from the logs. */
          diagnosticInsights?: Array<{
            /** Insight category. */
            insightsCategory?: string | null;
            /** Human readable insight text. */
            insightsString?: string | null;
            /** Documentation link for the insight. */
            insightsURL?: string | null;
            [key: string]: unknown;
          }> | null;
          /** Anonymized logs with their call stack trees and metadata. */
          diagnosticLogs?: Array<{
            /** Device and app details of the log. */
            diagnosticMetaData?: {
              /** App version the log was recorded on. */
              appVersion?: string | null;
              /** Build number the log was recorded on. */
              buildVersion?: string | null;
              /** Bundle identifier of the app. */
              bundleId?: string | null;
              /** Device model the log came from. */
              deviceType?: string | null;
              /** Operating system version on the device. */
              osVersion?: string | null;
              /** CPU architecture of the device. */
              platformArchitecture?: string | null;
              /** Event that produced the log. */
              event?: string | null;
              /** Additional detail about the event. */
              eventDetail?: string | null;
              /** Amount of disk writes for disk write logs. */
              writesCaused?: string | null;
              [key: string]: unknown;
            } | null;
            /** Call stack trees captured in the log. */
            callStackTree?: Array<Record<string, unknown>> | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
      };
    };
    /** Read the custom end user license agreement of an app together with the territories it applies in. Returns null when the app uses Apple's standard EULA everywhere. */
    "app_store_connect.get_end_user_license_agreement": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
      };
      output: {
        /** A custom end user license agreement that replaces Apple's standard EULA in some territories. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        endUserLicenseAgreement: {
          /** App Store Connect identifier for the end user license agreement. */
          id: string;
          /** Full text of the custom license agreement. */
          agreementText?: string | null;
          /** ISO 3166-1 alpha-3 territories the custom agreement applies in. */
          territoryIds: Array<string> | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one Game Center achievement by its App Store Connect identifier. */
    "app_store_connect.get_game_center_achievement": {
      input: {
        /**
         * App Store Connect identifier of the achievement.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementId: string;
      };
      output: {
        /** A Game Center achievement. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAchievement: {
          /** App Store Connect identifier for the achievement. */
          id: string;
          /** Internal achievement name shown in App Store Connect. */
          referenceName?: string | null;
          /** Achievement identifier the game reports scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** Points the achievement is worth, from 0 to 100. */
          points?: number | null;
          /** Whether players can see the achievement before they earn it. */
          showBeforeEarned?: boolean | null;
          /** Whether players can earn the achievement more than once. */
          repeatable?: boolean | null;
          /** Whether the achievement is archived and no longer offered to players. */
          archived?: boolean | null;
          /** Key-value properties the achievement contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Game Center achievement localization by its App Store Connect identifier. */
    "app_store_connect.get_game_center_achievement_localization": {
      input: {
        /**
         * App Store Connect identifier of the achievement localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementLocalizationId: string;
      };
      output: {
        /** The achievement text in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAchievementLocalization: {
          /** App Store Connect identifier for the achievement localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Achievement name shown to players in this locale. */
          name?: string | null;
          /** Description shown before the player earns the achievement. */
          beforeEarnedDescription?: string | null;
          /** Description shown after the player earns the achievement. */
          afterEarnedDescription?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the achievement image uploaded for one Game Center achievement localization, including its delivery state, or null when no image has been uploaded. Uploading an image is not covered by this connector; only the already uploaded image can be read. */
    "app_store_connect.get_game_center_achievement_localization_image": {
      input: {
        /**
         * App Store Connect identifier of the achievement localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementLocalizationId: string;
      };
      output: {
        /** The image shown with a Game Center achievement localization. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAchievementImage: {
          /** App Store Connect identifier for the achievement image. */
          id: string;
          /** Size of the uploaded image file in bytes. */
          fileSize?: number | null;
          /** File name the image was uploaded under. */
          fileName?: string | null;
          /** Template URL and pixel size of the delivered image. */
          imageAsset?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Image width in pixels. */
            width?: number;
            /** Image height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** Upload and delivery state of the image. */
          assetDeliveryState?: {
            /** Delivery state of the image asset. */
            state?: "AWAITING_UPLOAD" | "UPLOAD_COMPLETE" | "COMPLETE" | "FAILED" | null;
            /** Delivery errors App Store Connect reported for the image. */
            errors?: Array<{
              /** Error code. */
              code?: string | null;
              /** Human readable error description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            /** Delivery warnings App Store Connect reported for the image. */
            warnings?: Array<{
              /** Warning code. */
              code?: string | null;
              /** Human readable warning description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one Game Center achievement version by its App Store Connect identifier. */
    "app_store_connect.get_game_center_achievement_version": {
      input: {
        /**
         * App Store Connect identifier of the achievement version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementVersionId: string;
      };
      output: {
        /** One version of a Game Center achievement. A new version carries the localizations through App Review. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAchievementVersion: {
          /** App Store Connect identifier for the achievement version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Game Center activity by its App Store Connect identifier. */
    "app_store_connect.get_game_center_activity": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
      };
      output: {
        /** A Game Center activity: a play session other players can be invited into, with its own achievements and leaderboards. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivity: {
          /** App Store Connect identifier for the activity. */
          id: string;
          /** Internal activity name shown in App Store Connect. */
          referenceName?: string | null;
          /** Activity identifier the game starts sessions with, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** Whether players take turns or play at the same time. */
          playStyle?: "ASYNCHRONOUS" | "SYNCHRONOUS" | null;
          /** Fewest players an activity session needs. */
          minimumPlayersCount?: number | null;
          /** Most players an activity session accepts. */
          maximumPlayersCount?: number | null;
          /** Whether players can join the activity with a party code. */
          supportsPartyCode?: boolean | null;
          /** Whether the activity is archived and no longer offered. */
          archived?: boolean | null;
          /** Key-value properties the game reads when it starts the activity. */
          properties?: Record<string, unknown> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Game Center activity localization by its App Store Connect identifier. */
    "app_store_connect.get_game_center_activity_localization": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityLocalizationId: string;
      };
      output: {
        /** The activity text in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivityLocalization: {
          /** App Store Connect identifier for the activity localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Activity name shown to players in this locale. */
          name?: string | null;
          /** Activity description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the image of one Game Center activity localization. Returns null when the locale has no image of its own and falls back to the default image of the version; uploading an image is not covered by this connector. */
    "app_store_connect.get_game_center_activity_localization_image": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityLocalizationId: string;
      };
      output: {
        /** The image shown with a Game Center activity. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivityImage: {
          /** App Store Connect identifier for the activity image. */
          id: string;
          /** Size of the uploaded image file in bytes. */
          fileSize?: number | null;
          /** File name the image was uploaded under. */
          fileName?: string | null;
          /** Template URL and pixel size of the delivered image. */
          imageAsset?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Image width in pixels. */
            width?: number;
            /** Image height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** Upload and delivery state of the image. */
          assetDeliveryState?: {
            /** Delivery state of the image asset. */
            state?: "AWAITING_UPLOAD" | "UPLOAD_COMPLETE" | "COMPLETE" | "FAILED" | null;
            /** Delivery errors App Store Connect reported for the image. */
            errors?: Array<{
              /** Error code. */
              code?: string | null;
              /** Human readable error description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            /** Delivery warnings App Store Connect reported for the image. */
            warnings?: Array<{
              /** Warning code. */
              code?: string | null;
              /** Human readable warning description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one Game Center activity version by its App Store Connect identifier. */
    "app_store_connect.get_game_center_activity_version": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityVersionId: string;
      };
      output: {
        /** One version of a Game Center activity. A new version carries the localizations and the default image through App Review. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivityVersion: {
          /** App Store Connect identifier for the activity version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          /** Web page opened when a player without the game follows an activity invitation. */
          fallbackUrl?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the default image of a Game Center activity version, shown wherever a locale has no image of its own. Returns null when no default image has been uploaded yet; uploading one is not covered by this connector. */
    "app_store_connect.get_game_center_activity_version_default_image": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityVersionId: string;
      };
      output: {
        /** The image shown with a Game Center activity. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivityImage: {
          /** App Store Connect identifier for the activity image. */
          id: string;
          /** Size of the uploaded image file in bytes. */
          fileSize?: number | null;
          /** File name the image was uploaded under. */
          fileName?: string | null;
          /** Template URL and pixel size of the delivered image. */
          imageAsset?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Image width in pixels. */
            width?: number;
            /** Image height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** Upload and delivery state of the image. */
          assetDeliveryState?: {
            /** Delivery state of the image asset. */
            state?: "AWAITING_UPLOAD" | "UPLOAD_COMPLETE" | "COMPLETE" | "FAILED" | null;
            /** Delivery errors App Store Connect reported for the image. */
            errors?: Array<{
              /** Error code. */
              code?: string | null;
              /** Human readable error description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            /** Delivery warnings App Store Connect reported for the image. */
            warnings?: Array<{
              /** Warning code. */
              code?: string | null;
              /** Human readable warning description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one Game Center app version by its App Store Connect identifier. */
    "app_store_connect.get_game_center_app_version": {
      input: {
        /**
         * App Store Connect identifier of the Game Center app version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAppVersionId: string;
      };
      output: {
        /** The Game Center enablement of one App Store version, plus the earlier versions it stays multiplayer compatible with. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAppVersion: {
          /** App Store Connect identifier for the Game Center app version. */
          id: string;
          /** Whether Game Center is enabled for this App Store version. */
          enabled?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the App Store version a Game Center app version belongs to, for example to learn its version string and review state. */
    "app_store_connect.get_game_center_app_version_app_store_version": {
      input: {
        /**
         * App Store Connect identifier of the Game Center app version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAppVersionId: string;
      };
      output: {
        /** An App Store version of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersion: {
          /** App Store Connect identifier for the version. */
          id: string;
          /** Content platform the version targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Version string shown on the App Store, such as 1.4.0. */
          versionString?: string | null;
          /** Current review and release state. */
          appVersionState?: "ACCEPTED" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "INVALID_BINARY" | "METADATA_REJECTED" | "PENDING_APPLE_RELEASE" | "PENDING_DEVELOPER_RELEASE" | "PREPARE_FOR_SUBMISSION" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "READY_FOR_REVIEW" | "REJECTED" | "REPLACED_WITH_NEW_VERSION" | "WAITING_FOR_EXPORT_COMPLIANCE" | "WAITING_FOR_REVIEW" | null;
          /** Copyright line published with the version. */
          copyright?: string | null;
          /** Review track the version goes through. */
          reviewType?: "APP_STORE" | "NOTARIZATION" | null;
          /** Release behavior after approval. */
          releaseType?: "MANUAL" | "AFTER_APPROVAL" | "SCHEDULED" | null;
          /** Earliest scheduled release time, as an ISO 8601 timestamp. */
          earliestReleaseDate?: string | null;
          /** Whether the version is downloadable. */
          downloadable?: boolean | null;
          /** When the version record was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Game Center challenge by its App Store Connect identifier. */
    "app_store_connect.get_game_center_challenge": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeId: string;
      };
      output: {
        /** A Game Center challenge: an invitation to beat a score on one leaderboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterChallenge: {
          /** App Store Connect identifier for the challenge. */
          id: string;
          /** Internal challenge name shown in App Store Connect. */
          referenceName?: string | null;
          /** Challenge identifier the game refers to, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** What the challenge is based on. */
          challengeType?: "LEADERBOARD" | null;
          /** Whether a player can take the challenge more than once. */
          repeatable?: boolean | null;
          /** Whether the challenge is archived and no longer offered. */
          archived?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Game Center challenge localization by its App Store Connect identifier. */
    "app_store_connect.get_game_center_challenge_localization": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeLocalizationId: string;
      };
      output: {
        /** The challenge text in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterChallengeLocalization: {
          /** App Store Connect identifier for the challenge localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Challenge name shown to players in this locale. */
          name?: string | null;
          /** Challenge description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the image of one Game Center challenge localization, or null when the locale has no image of its own and falls back to the default image of the version. Uploading an image is not covered by this connector. */
    "app_store_connect.get_game_center_challenge_localization_image": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeLocalizationId: string;
      };
      output: {
        /** The image shown with a Game Center challenge. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterChallengeImage: {
          /** App Store Connect identifier for the challenge image. */
          id: string;
          /** Size of the uploaded image file in bytes. */
          fileSize?: number | null;
          /** File name the image was uploaded under. */
          fileName?: string | null;
          /** Template URL and pixel size of the delivered image. */
          imageAsset?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Image width in pixels. */
            width?: number;
            /** Image height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** Upload and delivery state of the image. */
          assetDeliveryState?: {
            /** Delivery state of the image asset. */
            state?: "AWAITING_UPLOAD" | "UPLOAD_COMPLETE" | "COMPLETE" | "FAILED" | null;
            /** Delivery errors App Store Connect reported for the image. */
            errors?: Array<{
              /** Error code. */
              code?: string | null;
              /** Human readable error description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            /** Delivery warnings App Store Connect reported for the image. */
            warnings?: Array<{
              /** Warning code. */
              code?: string | null;
              /** Human readable warning description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one Game Center challenge version by its App Store Connect identifier. */
    "app_store_connect.get_game_center_challenge_version": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeVersionId: string;
      };
      output: {
        /** One version of a Game Center challenge. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterChallengeVersion: {
          /** App Store Connect identifier for the challenge version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the default image of a Game Center challenge version, the one shown for locales without their own image. Returns null when the version has no default image. Uploading an image is not covered by this connector. */
    "app_store_connect.get_game_center_challenge_version_default_image": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeVersionId: string;
      };
      output: {
        /** The image shown with a Game Center challenge. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterChallengeImage: {
          /** App Store Connect identifier for the challenge image. */
          id: string;
          /** Size of the uploaded image file in bytes. */
          fileSize?: number | null;
          /** File name the image was uploaded under. */
          fileName?: string | null;
          /** Template URL and pixel size of the delivered image. */
          imageAsset?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Image width in pixels. */
            width?: number;
            /** Image height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** Upload and delivery state of the image. */
          assetDeliveryState?: {
            /** Delivery state of the image asset. */
            state?: "AWAITING_UPLOAD" | "UPLOAD_COMPLETE" | "COMPLETE" | "FAILED" | null;
            /** Delivery errors App Store Connect reported for the image. */
            errors?: Array<{
              /** Error code. */
              code?: string | null;
              /** Human readable error description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            /** Delivery warnings App Store Connect reported for the image. */
            warnings?: Array<{
              /** Warning code. */
              code?: string | null;
              /** Human readable warning description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read the matchmaking requests an app made through classic (non rule based) matchmaking, as time series of request counts and queue wait times. Each data point covers the granularity you pass. */
    "app_store_connect.get_game_center_classic_matchmaking_request_metrics": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail of the app.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Split the returned series by these dimensions, such as the request result.
         * @minItems 1
         */
        groupBy?: Array<"result">;
        /** Return only series for matchmaking requests that ended with this result. */
        result?: "MATCHED" | "CANCELED" | "EXPIRED";
        /** Time span each returned data point covers. */
        granularity: "P1D" | "PT1H" | "PT15M";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Metric series returned for this page. */
        metrics: Array<{
          /** Time span each data point covers. */
          granularity?: "P1D" | "PT1H" | "PT15M" | null;
          /** Dimension values this series is grouped by, keyed by dimension name. */
          dimensions?: Record<string, string | null> | null;
          /** Measured periods of this series, oldest first. */
          dataPoints?: Array<{
            /** Start of the period, as an ISO 8601 timestamp. */
            start?: string | null;
            /** End of the period, as an ISO 8601 timestamp. */
            end?: string | null;
            /** Values measured in the period. */
            values?: {
              /** Number of matchmaking requests in the period. */
              count?: number | null;
              /** Average time a request waited in the queue, in seconds. */
              averageSecondsInQueue?: number | null;
              /** Median time a request waited in the queue, in seconds. */
              p50SecondsInQueue?: number | null;
              /** Time the slowest 5 percent of requests waited in the queue, in seconds. */
              p95SecondsInQueue?: number | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read one Game Center detail by its App Store Connect identifier, together with the group and the default leaderboards it links to. */
    "app_store_connect.get_game_center_detail": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
      };
      output: {
        /** The Game Center configuration of one app. It owns the app's achievements, leaderboards, leaderboard sets, activities and challenges, and links the app to a Game Center group when it shares them with other apps. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterDetail: {
          /** App Store Connect identifier for the Game Center detail. */
          id: string;
          /** Whether the app is distributed through Apple Arcade. */
          arcadeEnabled?: boolean | null;
          /** Game Center group the app belongs to, or null when the app is not grouped or App Store Connect did not return the relationship. */
          gameCenterGroupId: string | null;
          /** Leaderboard shown by default in the Game Center dashboard, or null when there is none. */
          defaultLeaderboardId: string | null;
          /** Group leaderboard shown by default when the app belongs to a group, or null when there is none. */
          defaultGroupLeaderboardId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the Game Center group a Game Center detail belongs to. Returns null when the app does not share its achievements and leaderboards with other apps. */
    "app_store_connect.get_game_center_detail_group": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
      };
      output: {
        /** A Game Center group. Apps in a group share achievements, leaderboards and leaderboard sets. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterGroup: {
          /** App Store Connect identifier for the Game Center group. */
          id: string;
          /** Internal group name shown in App Store Connect. */
          referenceName?: string | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one Game Center group by its App Store Connect identifier. */
    "app_store_connect.get_game_center_group": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
      };
      output: {
        /** A Game Center group. Apps in a group share achievements, leaderboards and leaderboard sets. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterGroup: {
          /** App Store Connect identifier for the Game Center group. */
          id: string;
          /** Internal group name shown in App Store Connect. */
          referenceName?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Game Center leaderboard by its App Store Connect identifier. */
    "app_store_connect.get_game_center_leaderboard": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId: string;
      };
      output: {
        /** A Game Center leaderboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboard: {
          /** App Store Connect identifier for the leaderboard. */
          id: string;
          /** Internal leaderboard name shown in App Store Connect. */
          referenceName?: string | null;
          /** Leaderboard identifier the game submits scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** How scores are formatted unless a localization overrides it. */
          defaultFormatter?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
          /** Which submitted score is kept for a player. */
          submissionType?: "BEST_SCORE" | "MOST_RECENT_SCORE" | null;
          /** Whether a higher or a lower score ranks first. */
          scoreSortType?: "ASC" | "DESC" | null;
          /** Lowest accepted score, as a decimal string. */
          scoreRangeStart?: string | null;
          /** Highest accepted score, as a decimal string. */
          scoreRangeEnd?: string | null;
          /** When the first recurring run of the leaderboard starts, as an ISO 8601 timestamp. */
          recurrenceStartDate?: string | null;
          /** How long each recurring run lasts, as an ISO 8601 duration such as P1D. */
          recurrenceDuration?: string | null;
          /** ICS recurrence rule that schedules the recurring runs. */
          recurrenceRule?: string | null;
          /** Whether the leaderboard is archived and no longer accepting scores. */
          archived?: boolean | null;
          /** Key-value properties the leaderboard contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          /** Whether the leaderboard is shown to players. */
          visibility?: "SHOW_FOR_ALL" | "HIDE_FOR_ALL" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Game Center leaderboard localization by its App Store Connect identifier. */
    "app_store_connect.get_game_center_leaderboard_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardLocalizationId: string;
      };
      output: {
        /** The leaderboard text and score formatting in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardLocalization: {
          /** App Store Connect identifier for the leaderboard localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Leaderboard name shown to players in this locale. */
          name?: string | null;
          /** Score formatter used in this locale instead of the leaderboard default. */
          formatterOverride?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
          /** Suffix appended to plural score values, such as points. */
          formatterSuffix?: string | null;
          /** Suffix appended to a score value of one, such as point. */
          formatterSuffixSingular?: string | null;
          /** Score description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the image shown with a Game Center leaderboard localization, including its delivery state. Returns null when no image has been uploaded for the locale. Uploading an image is not covered by this connector, only reading the one already uploaded. */
    "app_store_connect.get_game_center_leaderboard_localization_image": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardLocalizationId: string;
      };
      output: {
        /** The image shown with a Game Center leaderboard localization. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardImage: {
          /** App Store Connect identifier for the leaderboard image. */
          id: string;
          /** Size of the uploaded image file in bytes. */
          fileSize?: number | null;
          /** File name the image was uploaded under. */
          fileName?: string | null;
          /** Template URL and pixel size of the delivered image. */
          imageAsset?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Image width in pixels. */
            width?: number;
            /** Image height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** Upload and delivery state of the image. */
          assetDeliveryState?: {
            /** Delivery state of the image asset. */
            state?: "AWAITING_UPLOAD" | "UPLOAD_COMPLETE" | "COMPLETE" | "FAILED" | null;
            /** Delivery errors App Store Connect reported for the image. */
            errors?: Array<{
              /** Error code. */
              code?: string | null;
              /** Human readable error description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            /** Delivery warnings App Store Connect reported for the image. */
            warnings?: Array<{
              /** Warning code. */
              code?: string | null;
              /** Human readable warning description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one Game Center leaderboard set by its App Store Connect identifier. */
    "app_store_connect.get_game_center_leaderboard_set": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
      };
      output: {
        /** A Game Center leaderboard set that groups related leaderboards under one entry in the Game Center dashboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSet: {
          /** App Store Connect identifier for the leaderboard set. */
          id: string;
          /** Internal set name shown in App Store Connect. */
          referenceName?: string | null;
          /** Set identifier the game refers to, unique within the app or group. */
          vendorIdentifier?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Game Center leaderboard set localization by its App Store Connect identifier. */
    "app_store_connect.get_game_center_leaderboard_set_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetLocalizationId: string;
      };
      output: {
        /** The leaderboard set name in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSetLocalization: {
          /** App Store Connect identifier for the leaderboard set localization. */
          id: string;
          /** Locale the name is written in, such as en-US. */
          locale?: string | null;
          /** Set name shown to players in this locale. */
          name?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the image attached to one Game Center leaderboard set localization, with its delivery URL and upload state. Uploading an image is not covered by this connector; only an image that was already uploaded can be read. */
    "app_store_connect.get_game_center_leaderboard_set_localization_image": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetLocalizationId: string;
      };
      output: {
        /** The image shown with a Game Center leaderboard set localization. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSetImage: {
          /** App Store Connect identifier for the leaderboard set image. */
          id: string;
          /** Size of the uploaded image file in bytes. */
          fileSize?: number | null;
          /** File name the image was uploaded under. */
          fileName?: string | null;
          /** Template URL and pixel size of the delivered image. */
          imageAsset?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Image width in pixels. */
            width?: number;
            /** Image height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** Upload and delivery state of the image. */
          assetDeliveryState?: {
            /** Delivery state of the image asset. */
            state?: "AWAITING_UPLOAD" | "UPLOAD_COMPLETE" | "COMPLETE" | "FAILED" | null;
            /** Delivery errors App Store Connect reported for the image. */
            errors?: Array<{
              /** Error code. */
              code?: string | null;
              /** Human readable error description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            /** Delivery warnings App Store Connect reported for the image. */
            warnings?: Array<{
              /** Warning code. */
              code?: string | null;
              /** Human readable warning description. */
              description?: string | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one Game Center leaderboard set version by its App Store Connect identifier, including its review state. */
    "app_store_connect.get_game_center_leaderboard_set_version": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetVersionId: string;
      };
      output: {
        /** One version of a Game Center leaderboard set. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSetVersion: {
          /** App Store Connect identifier for the leaderboard set version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Game Center leaderboard version by its App Store Connect identifier. */
    "app_store_connect.get_game_center_leaderboard_version": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardVersionId: string;
      };
      output: {
        /** One version of a Game Center leaderboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardVersion: {
          /** App Store Connect identifier for the leaderboard version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Game Center matchmaking queue by its App Store Connect identifier, including the rule set it matches requests with and the rule set of a running experiment. */
    "app_store_connect.get_game_center_matchmaking_queue": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking queue.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId: string;
      };
      output: {
        /** A Game Center matchmaking queue. Requests that reach the queue are matched with the rule set attached to it. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingQueue: {
          /** App Store Connect identifier for the matchmaking queue. */
          id: string;
          /** Internal queue name shown in App Store Connect. */
          referenceName?: string | null;
          /** Bundle identifiers whose classic matchmaking requests use this queue. */
          classicMatchmakingBundleIds?: Array<string> | null;
          /** Rule set the queue matches requests with. */
          ruleSetId: string | null;
          /** Rule set a share of the queue traffic is matched with while an experiment runs, or null when no experiment is running. */
          experimentRuleSetId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the share of a queue's traffic that was matched with the experiment rule set, as time series of request counts and queue wait times. Compare it with get_game_center_matchmaking_queue_request_metrics to judge whether the experiment rule set is an improvement. */
    "app_store_connect.get_game_center_matchmaking_queue_experiment_request_metrics": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking queue.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId: string;
        /**
         * Split the returned series by these dimensions, such as the request result or the app that sent the request.
         * @minItems 1
         */
        groupBy?: Array<"result" | "gameCenterDetail">;
        /** Return only series for matchmaking requests that ended with this result. */
        result?: "MATCHED" | "CANCELED" | "EXPIRED";
        /**
         * Return only series for the app with this Game Center detail identifier.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId?: string;
        /** Time span each returned data point covers. */
        granularity: "P1D" | "PT1H" | "PT15M";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Metric series returned for this page. */
        metrics: Array<{
          /** Time span each data point covers. */
          granularity?: "P1D" | "PT1H" | "PT15M" | null;
          /** Dimension values this series is grouped by, keyed by dimension name. */
          dimensions?: Record<string, string | null> | null;
          /** Measured periods of this series, oldest first. */
          dataPoints?: Array<{
            /** Start of the period, as an ISO 8601 timestamp. */
            start?: string | null;
            /** End of the period, as an ISO 8601 timestamp. */
            end?: string | null;
            /** Values measured in the period. */
            values?: {
              /** Number of matchmaking requests in the period. */
              count?: number | null;
              /** Average time a request waited in the queue, in seconds. */
              averageSecondsInQueue?: number | null;
              /** Median time a request waited in the queue, in seconds. */
              p50SecondsInQueue?: number | null;
              /** Time the slowest 5 percent of requests waited in the queue, in seconds. */
              p95SecondsInQueue?: number | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read how many requests were waiting in the part of a matchmaking queue served by the experiment rule set, as time series of average and percentile queue sizes. */
    "app_store_connect.get_game_center_matchmaking_queue_experiment_size_metrics": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking queue.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId: string;
        /** Time span each returned data point covers. */
        granularity: "P1D" | "PT1H" | "PT15M";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Metric series returned for this page. */
        metrics: Array<{
          /** Time span each data point covers. */
          granularity?: "P1D" | "PT1H" | "PT15M" | null;
          /** Dimension values this series is grouped by, keyed by dimension name. */
          dimensions?: Record<string, string | null> | null;
          /** Measured periods of this series, oldest first. */
          dataPoints?: Array<{
            /** Start of the period, as an ISO 8601 timestamp. */
            start?: string | null;
            /** End of the period, as an ISO 8601 timestamp. */
            end?: string | null;
            /** Values measured in the period. */
            values?: {
              /** Number of queue size samples in the period. */
              count?: number | null;
              /** Average number of requests waiting in the queue. */
              averageNumberOfRequests?: number | null;
              /** Median number of requests waiting in the queue. */
              p50NumberOfRequests?: number | null;
              /** Number of requests waiting in the queue at the 95th percentile. */
              p95NumberOfRequests?: number | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read the matchmaking requests one queue handled, as time series of request counts and queue wait times, optionally split by result or by the app that sent the requests. */
    "app_store_connect.get_game_center_matchmaking_queue_request_metrics": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking queue.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId: string;
        /**
         * Split the returned series by these dimensions, such as the request result or the app that sent the request.
         * @minItems 1
         */
        groupBy?: Array<"result" | "gameCenterDetail">;
        /** Return only series for matchmaking requests that ended with this result. */
        result?: "MATCHED" | "CANCELED" | "EXPIRED";
        /**
         * Return only series for the app with this Game Center detail identifier.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId?: string;
        /** Time span each returned data point covers. */
        granularity: "P1D" | "PT1H" | "PT15M";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Metric series returned for this page. */
        metrics: Array<{
          /** Time span each data point covers. */
          granularity?: "P1D" | "PT1H" | "PT15M" | null;
          /** Dimension values this series is grouped by, keyed by dimension name. */
          dimensions?: Record<string, string | null> | null;
          /** Measured periods of this series, oldest first. */
          dataPoints?: Array<{
            /** Start of the period, as an ISO 8601 timestamp. */
            start?: string | null;
            /** End of the period, as an ISO 8601 timestamp. */
            end?: string | null;
            /** Values measured in the period. */
            values?: {
              /** Number of matchmaking requests in the period. */
              count?: number | null;
              /** Average time a request waited in the queue, in seconds. */
              averageSecondsInQueue?: number | null;
              /** Median time a request waited in the queue, in seconds. */
              p50SecondsInQueue?: number | null;
              /** Time the slowest 5 percent of requests waited in the queue, in seconds. */
              p95SecondsInQueue?: number | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read the matchmaking sessions one queue produced, as time series of session counts and of how many players each session held. */
    "app_store_connect.get_game_center_matchmaking_queue_session_metrics": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking queue.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId: string;
        /** Time span each returned data point covers. */
        granularity: "P1D" | "PT1H" | "PT15M";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Metric series returned for this page. */
        metrics: Array<{
          /** Time span each data point covers. */
          granularity?: "P1D" | "PT1H" | "PT15M" | null;
          /** Dimension values this series is grouped by, keyed by dimension name. */
          dimensions?: Record<string, string | null> | null;
          /** Measured periods of this series, oldest first. */
          dataPoints?: Array<{
            /** Start of the period, as an ISO 8601 timestamp. */
            start?: string | null;
            /** End of the period, as an ISO 8601 timestamp. */
            end?: string | null;
            /** Values measured in the period. */
            values?: {
              /** Number of matchmaking sessions in the period. */
              count?: number | null;
              /** Average number of players in a session. */
              averagePlayerCount?: number | null;
              /** Median number of players in a session. */
              p50PlayerCount?: number | null;
              /** Number of players in a session at the 95th percentile. */
              p95PlayerCount?: number | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read how many requests were waiting in one matchmaking queue, as time series of average and percentile queue sizes. */
    "app_store_connect.get_game_center_matchmaking_queue_size_metrics": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking queue.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId: string;
        /** Time span each returned data point covers. */
        granularity: "P1D" | "PT1H" | "PT15M";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Metric series returned for this page. */
        metrics: Array<{
          /** Time span each data point covers. */
          granularity?: "P1D" | "PT1H" | "PT15M" | null;
          /** Dimension values this series is grouped by, keyed by dimension name. */
          dimensions?: Record<string, string | null> | null;
          /** Measured periods of this series, oldest first. */
          dataPoints?: Array<{
            /** Start of the period, as an ISO 8601 timestamp. */
            start?: string | null;
            /** End of the period, as an ISO 8601 timestamp. */
            end?: string | null;
            /** Values measured in the period. */
            values?: {
              /** Number of queue size samples in the period. */
              count?: number | null;
              /** Average number of requests waiting in the queue. */
              averageNumberOfRequests?: number | null;
              /** Median number of requests waiting in the queue. */
              p50NumberOfRequests?: number | null;
              /** Number of requests waiting in the queue at the 95th percentile. */
              p95NumberOfRequests?: number | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read how often one matchmaking rule returned each boolean result, as time series of evaluation counts. */
    "app_store_connect.get_game_center_matchmaking_rule_boolean_result_metrics": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleId: string;
        /**
         * Split the returned series by these dimensions, such as the rule result or the queue that evaluated the rule.
         * @minItems 1
         */
        groupBy?: Array<"result" | "gameCenterMatchmakingQueue">;
        /**
         * Return only series for this rule result value, as App Store Connect reports it.
         * @minLength 1
         * @pattern \S
         */
        result?: string;
        /**
         * Return only series for the matchmaking queue with this identifier.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId?: string;
        /** Time span each returned data point covers. */
        granularity: "P1D" | "PT1H" | "PT15M";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Metric series returned for this page. */
        metrics: Array<{
          /** Time span each data point covers. */
          granularity?: "P1D" | "PT1H" | "PT15M" | null;
          /** Dimension values this series is grouped by, keyed by dimension name. */
          dimensions?: Record<string, string | null> | null;
          /** Measured periods of this series, oldest first. */
          dataPoints?: Array<{
            /** Start of the period, as an ISO 8601 timestamp. */
            start?: string | null;
            /** End of the period, as an ISO 8601 timestamp. */
            end?: string | null;
            /** Values measured in the period. */
            values?: {
              /** Number of rule evaluations in the period. */
              count?: number | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read how often one matchmaking rule failed to evaluate, as time series of error counts. */
    "app_store_connect.get_game_center_matchmaking_rule_error_metrics": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleId: string;
        /**
         * Split the returned series by the queue that evaluated the rule.
         * @minItems 1
         */
        groupBy?: Array<"gameCenterMatchmakingQueue">;
        /**
         * Return only series for the matchmaking queue with this identifier.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId?: string;
        /** Time span each returned data point covers. */
        granularity: "P1D" | "PT1H" | "PT15M";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Metric series returned for this page. */
        metrics: Array<{
          /** Time span each data point covers. */
          granularity?: "P1D" | "PT1H" | "PT15M" | null;
          /** Dimension values this series is grouped by, keyed by dimension name. */
          dimensions?: Record<string, string | null> | null;
          /** Measured periods of this series, oldest first. */
          dataPoints?: Array<{
            /** Start of the period, as an ISO 8601 timestamp. */
            start?: string | null;
            /** End of the period, as an ISO 8601 timestamp. */
            end?: string | null;
            /** Values measured in the period. */
            values?: {
              /** Number of rule evaluations in the period. */
              count?: number | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read the values one matchmaking rule expression returned, as time series of evaluation counts with average and percentile results. */
    "app_store_connect.get_game_center_matchmaking_rule_number_result_metrics": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleId: string;
        /**
         * Split the returned series by the queue that evaluated the rule.
         * @minItems 1
         */
        groupBy?: Array<"gameCenterMatchmakingQueue">;
        /**
         * Return only series for the matchmaking queue with this identifier.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId?: string;
        /** Time span each returned data point covers. */
        granularity: "P1D" | "PT1H" | "PT15M";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Metric series returned for this page. */
        metrics: Array<{
          /** Time span each data point covers. */
          granularity?: "P1D" | "PT1H" | "PT15M" | null;
          /** Dimension values this series is grouped by, keyed by dimension name. */
          dimensions?: Record<string, string | null> | null;
          /** Measured periods of this series, oldest first. */
          dataPoints?: Array<{
            /** Start of the period, as an ISO 8601 timestamp. */
            start?: string | null;
            /** End of the period, as an ISO 8601 timestamp. */
            end?: string | null;
            /** Values measured in the period. */
            values?: {
              /** Number of rule evaluations in the period. */
              count?: number | null;
              /** Average value the rule expression returned. */
              averageResult?: number | null;
              /** Median value the rule expression returned. */
              p50Result?: number | null;
              /** Value the rule expression returned at the 95th percentile. */
              p95Result?: number | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read one Game Center matchmaking rule set by its App Store Connect identifier. */
    "app_store_connect.get_game_center_matchmaking_rule_set": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId: string;
      };
      output: {
        /** A Game Center matchmaking rule set: the team layout, player limits and rules a queue matches requests with. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingRuleSet: {
          /** App Store Connect identifier for the rule set. */
          id: string;
          /** Internal rule set name shown in App Store Connect. */
          referenceName?: string | null;
          /** Version of the rule expression language the rules use. */
          ruleLanguageVersion?: number | null;
          /** Fewest players a match created from this rule set may have. */
          minPlayers?: number | null;
          /** Most players a match created from this rule set may have. */
          maxPlayers?: number | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the matchmaking requests an app made through rule based matchmaking, as time series of request counts and queue wait times. */
    "app_store_connect.get_game_center_rule_based_matchmaking_request_metrics": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail of the app.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Split the returned series by these dimensions, such as the request result.
         * @minItems 1
         */
        groupBy?: Array<"result">;
        /** Return only series for matchmaking requests that ended with this result. */
        result?: "MATCHED" | "CANCELED" | "EXPIRED";
        /** Time span each returned data point covers. */
        granularity: "P1D" | "PT1H" | "PT15M";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Metric series returned for this page. */
        metrics: Array<{
          /** Time span each data point covers. */
          granularity?: "P1D" | "PT1H" | "PT15M" | null;
          /** Dimension values this series is grouped by, keyed by dimension name. */
          dimensions?: Record<string, string | null> | null;
          /** Measured periods of this series, oldest first. */
          dataPoints?: Array<{
            /** Start of the period, as an ISO 8601 timestamp. */
            start?: string | null;
            /** End of the period, as an ISO 8601 timestamp. */
            end?: string | null;
            /** Values measured in the period. */
            values?: {
              /** Number of matchmaking requests in the period. */
              count?: number | null;
              /** Average time a request waited in the queue, in seconds. */
              averageSecondsInQueue?: number | null;
              /** Median time a request waited in the queue, in seconds. */
              p50SecondsInQueue?: number | null;
              /** Time the slowest 5 percent of requests waited in the queue, in seconds. */
              p95SecondsInQueue?: number | null;
              [key: string]: unknown;
            } | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Read one in-app purchase by its App Store Connect identifier. */
    "app_store_connect.get_in_app_purchase": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
      };
      output: {
        /** An in-app purchase of an app: a consumable, non-consumable, or non-renewing subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        inAppPurchase: {
          /** App Store Connect identifier for the in-app purchase. */
          id: string;
          /** Reference name shown in App Store Connect. */
          name?: string | null;
          /** Product identifier the app passes to StoreKit to request the purchase. */
          productId?: string | null;
          /** Kind of in-app purchase. */
          inAppPurchaseType?: "CONSUMABLE" | "NON_CONSUMABLE" | "NON_RENEWING_SUBSCRIPTION" | null;
          /** Where the in-app purchase stands in metadata preparation, review, and sale. */
          state?: "MISSING_METADATA" | "WAITING_FOR_UPLOAD" | "PROCESSING_CONTENT" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED" | null;
          /** Note shown to App Review alongside the purchase. */
          reviewNote?: string | null;
          /** Whether the purchase can be shared through Family Sharing. */
          familySharable?: boolean | null;
          /** Whether Apple hosts the downloadable content of the purchase. */
          contentHosting?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the availability record of an in-app purchase, which says whether it is offered in new territories automatically. Returns null when no availability has been set. Use list_in_app_purchase_available_territories for the territory list. */
    "app_store_connect.get_in_app_purchase_availability": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
      };
      output: {
        /** The availability record, or null when the in-app purchase has none. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        availability: {
          /** App Store Connect identifier for the availability record. */
          id: string;
          /** Whether the purchase automatically becomes available in territories Apple adds later. */
          availableInNewTerritories?: boolean | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read the metadata and temporary download URL of the hosted content package of an in-app purchase. Returns null when no content has been uploaded. */
    "app_store_connect.get_in_app_purchase_content": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
      };
      output: {
        /** The hosted content record, or null when the in-app purchase has none. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        content: {
          /** App Store Connect identifier for the content record. */
          id: string;
          /** File name of the uploaded content package. */
          fileName?: string | null;
          /** Size of the content package in bytes. */
          fileSize?: number | null;
          /** Temporary download URL for the content package. */
          url?: string | null;
          /** When the content was last uploaded, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one in-app purchase localization by its identifier. */
    "app_store_connect.get_in_app_purchase_localization": {
      input: {
        /**
         * App Store Connect identifier of the localization.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseLocalizationId: string;
      };
      output: {
        /** A localized display name and description of an in-app purchase. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        localization: {
          /** App Store Connect identifier for the localization. */
          id: string;
          /** Display name customers see in this locale. */
          name?: string | null;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Description customers see in this locale. */
          description?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one in-app purchase offer code configuration by its identifier. */
    "app_store_connect.get_in_app_purchase_offer_code": {
      input: {
        /**
         * App Store Connect identifier of the offer code.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseOfferCodeId: string;
      };
      output: {
        /** An offer code configuration that lets customers redeem an in-app purchase at a discounted price. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        offerCode: {
          /** App Store Connect identifier for the offer code. */
          id: string;
          /** Reference name of the offer. */
          name?: string | null;
          /** Customer groups allowed to redeem the offer. */
          customerEligibilities?: Array<"NON_SPENDER" | "ACTIVE_SPENDER" | "CHURNED_SPENDER"> | null;
          /** Number of one-time-use codes generated for the production environment. */
          productionCodeCount?: number | null;
          /** Number of one-time-use codes generated for the sandbox environment. */
          sandboxCodeCount?: number | null;
          /** Whether the offer can currently be redeemed. */
          active?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the price schedule of an in-app purchase together with its base territory. Returns null when the purchase has no price schedule yet. */
    "app_store_connect.get_in_app_purchase_price_schedule": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
      };
      output: {
        /** The price schedule, or null when the in-app purchase has none. */
        priceSchedule: {
          /** App Store Connect identifier for the price schedule. */
          id: string;
          /** Territory whose price the automatic prices of the other territories are derived from, as an ISO 3166-1 alpha-3 territory code, such as USA. */
          baseTerritory: string | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read the base territory of an in-app purchase price schedule, including its currency. */
    "app_store_connect.get_in_app_purchase_price_schedule_base_territory": {
      input: {
        /**
         * App Store Connect identifier of the price schedule.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchasePriceScheduleId: string;
      };
      output: {
        /** An App Store territory. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        territory: {
          /** ISO 3166-1 alpha-3 territory code that App Store Connect uses as the territory identifier, such as USA. */
          id: string;
          /** ISO 4217 code of the currency prices in the territory are expressed in, such as USD. */
          currency?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one in-app purchase version by its identifier. */
    "app_store_connect.get_in_app_purchase_version": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase version.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseVersionId: string;
      };
      output: {
        /** A metadata version of an in-app purchase that App Review evaluates as a unit. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        version: {
          /** App Store Connect identifier for the in-app purchase version. */
          id: string;
          /** Sequential version number assigned by App Store Connect. */
          version?: number | null;
          /** Review state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one localization of an in-app purchase version by its identifier. */
    "app_store_connect.get_in_app_purchase_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the version localization.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseVersionLocalizationId: string;
      };
      output: {
        /** A localized display name and description attached to one in-app purchase version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        localization: {
          /** App Store Connect identifier for the localization. */
          id: string;
          /** Display name customers see in this locale. */
          name?: string | null;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Description customers see in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the marketplace search detail of an alternative marketplace app, or null when no catalog URL has been configured. */
    "app_store_connect.get_marketplace_search_detail": {
      input: {
        /**
         * App Store Connect identifier of the marketplace app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
      };
      output: {
        /** The search catalog configuration of an alternative marketplace app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        marketplaceSearchDetail: {
          /** App Store Connect identifier of the marketplace search detail. */
          id: string;
          /** URL of the marketplace sitemap Apple crawls to include its apps in Spotlight search. */
          catalogUrl?: string | null;
          [key: string]: unknown;
        } | null;
      };
    };
    /** Read one Apple Pay merchant ID record by its App Store Connect identifier. */
    "app_store_connect.get_merchant_id": {
      input: {
        /**
         * App Store Connect identifier of the merchant ID record (the opaque resource id, not the merchant.com.example identifier string).
         * @minLength 1
         * @pattern \S
         */
        merchantIdId: string;
      };
      output: {
        /** An Apple Pay merchant ID registered with the team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        merchantId: {
          /** App Store Connect identifier of the merchant ID record. */
          id: string;
          /** Display name of the merchant ID. */
          name?: string | null;
          /** Merchant identifier string, such as merchant.com.example.shop. */
          identifier?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one featuring nomination by its identifier. */
    "app_store_connect.get_nomination": {
      input: {
        /**
         * App Store Connect identifier of the nomination.
         * @minLength 1
         * @pattern \S
         */
        nominationId: string;
      };
      output: {
        /** A featuring nomination that tells Apple's editorial team about an upcoming launch, enhancement, or content drop. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        nomination: {
          /** App Store Connect identifier for the nomination. */
          id: string;
          /** Name of the nomination. */
          name?: string | null;
          /** What the nomination is about. */
          type?: "APP_LAUNCH" | "APP_ENHANCEMENTS" | "NEW_CONTENT" | null;
          /** Description of the launch or content for the editorial team. */
          description?: string | null;
          /** When the nomination was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the nomination was last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** When the nomination was submitted, as an ISO 8601 timestamp, or null while it is a draft. */
          submittedDate?: string | null;
          /** Whether the nomination is a draft, submitted, or archived. */
          state?: "DRAFT" | "SUBMITTED" | "ARCHIVED" | null;
          /** When the launch or content becomes available, as an ISO 8601 timestamp. */
          publishStartDate?: string | null;
          /** When the content stops being available, as an ISO 8601 timestamp, or null. */
          publishEndDate?: string | null;
          /** Device families the nomination applies to. */
          deviceFamilies?: Array<"IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION"> | null;
          /** Locales the launch or content is available in. */
          locales?: Array<string> | null;
          /** URLs of supplemental materials for the editorial team. */
          supplementalMaterialsUris?: Array<string> | null;
          /** Whether in-app events accompany the launch. */
          hasInAppEvents?: boolean | null;
          /** Whether the launch happens in selected markets before the rest. */
          launchInSelectMarketsFirst?: boolean | null;
          /** Additional notes for the editorial team. */
          notes?: string | null;
          /** Whether the app is available for pre-order. */
          preOrderEnabled?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one Wallet pass type ID record by its App Store Connect identifier. */
    "app_store_connect.get_pass_type_id": {
      input: {
        /**
         * App Store Connect identifier of the pass type ID record (the opaque resource id, not the pass.com.example identifier string).
         * @minLength 1
         * @pattern \S
         */
        passTypeIdId: string;
      };
      output: {
        /** A Wallet pass type ID registered with the team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        passTypeId: {
          /** App Store Connect identifier of the pass type ID record. */
          id: string;
          /** Display name of the pass type ID. */
          name?: string | null;
          /** Pass type identifier string, such as pass.com.example.ticket. */
          identifier?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one prerelease version by its identifier. */
    "app_store_connect.get_pre_release_version": {
      input: {
        /**
         * App Store Connect identifier of the prerelease version.
         * @minLength 1
         * @pattern \S
         */
        preReleaseVersionId: string;
      };
      output: {
        /** A prerelease version that groups TestFlight builds. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        preReleaseVersion: {
          /** App Store Connect identifier for the prerelease version. */
          id: string;
          /** Marketing version string, such as 1.4.0. */
          version?: string | null;
          /** Content platform the version targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one provisioning profile by its App Store Connect identifier, including its base64 content. */
    "app_store_connect.get_profile": {
      input: {
        /**
         * App Store Connect identifier of the provisioning profile.
         * @minLength 1
         * @pattern \S
         */
        profileId: string;
      };
      output: {
        /** A provisioning profile. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        profile: {
          /** App Store Connect identifier of the provisioning profile. */
          id: string;
          /** Profile name. */
          name?: string | null;
          /** Platform the profile applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Distribution method and platform of the profile. */
          profileType?: "IOS_APP_DEVELOPMENT" | "IOS_APP_STORE" | "IOS_APP_ADHOC" | "IOS_APP_INHOUSE" | "MAC_APP_DEVELOPMENT" | "MAC_APP_STORE" | "MAC_APP_DIRECT" | "TVOS_APP_DEVELOPMENT" | "TVOS_APP_STORE" | "TVOS_APP_ADHOC" | "TVOS_APP_INHOUSE" | "MAC_CATALYST_APP_DEVELOPMENT" | "MAC_CATALYST_APP_STORE" | "MAC_CATALYST_APP_DIRECT" | null;
          /** Whether the profile is still valid. A profile becomes INVALID when a certificate it contains is revoked or a device it contains is disabled. */
          profileState?: "ACTIVE" | "INVALID" | null;
          /** Base64-encoded provisioning profile. Decode it to a .mobileprovision or .provisionprofile file to install it. */
          profileContent?: string | null;
          /** UUID embedded in the profile. */
          uuid?: string | null;
          /** When the profile was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the profile expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the bundle ID a provisioning profile was created for. */
    "app_store_connect.get_profile_bundle_id": {
      input: {
        /**
         * App Store Connect identifier of the provisioning profile.
         * @minLength 1
         * @pattern \S
         */
        profileId: string;
      };
      output: {
        /** A bundle ID (App ID) registered with the team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        bundleId: {
          /** App Store Connect identifier of the bundle ID record. */
          id: string;
          /** Display name given to the bundle ID when it was registered. */
          name?: string | null;
          /** Platform the bundle ID is registered for. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Reverse-DNS bundle identifier string, such as com.example.app, or a wildcard ending in *. */
          identifier?: string | null;
          /** Team ID prefix (seed ID) associated with the bundle ID. */
          seedId?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one promoted purchase by its identifier. */
    "app_store_connect.get_promoted_purchase": {
      input: {
        /**
         * App Store Connect identifier of the promoted purchase.
         * @minLength 1
         * @pattern \S
         */
        promotedPurchaseId: string;
      };
      output: {
        /** An in-app purchase or subscription promoted on the App Store product page of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        promotedPurchase: {
          /** App Store Connect identifier for the promoted purchase. */
          id: string;
          /** Whether every customer sees the promotion, or only those the app selects through StoreKit. */
          visibleForAllUsers?: boolean | null;
          /** Whether the promotion is enabled. */
          enabled?: boolean | null;
          /** Review state of the promotion. */
          state?: "APPROVED" | "IN_REVIEW" | "PREPARE_FOR_SUBMISSION" | "REJECTED" | null;
          /** The promoted in-app purchase, or null when the promotion is for a subscription or the link was not returned. */
          inAppPurchaseId: string | null;
          /** The promoted subscription, or null when the promotion is for an in-app purchase or the link was not returned. */
          subscriptionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one review submission with its state and the App Store version it carries. */
    "app_store_connect.get_review_submission": {
      input: {
        /**
         * App Store Connect identifier of the review submission.
         * @minLength 1
         * @pattern \S
         */
        reviewSubmissionId: string;
      };
      output: {
        /** A review submission that bundles the items sent to App Review together. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        reviewSubmission: {
          /** App Store Connect identifier for the review submission. */
          id: string;
          /** Content platform the submission targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** When the submission was sent to App Review, as an ISO 8601 timestamp, or null while it is still being prepared. */
          submittedDate?: string | null;
          /** Where the submission stands in App Review. */
          state?: "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "UNRESOLVED_ISSUES" | "CANCELING" | "COMPLETING" | "COMPLETE" | null;
          /** App Store Connect identifier of the App Store version under review, or null when the submission carries no version. */
          appStoreVersionForReviewId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one branch or tag of a repository as Xcode Cloud sees it. */
    "app_store_connect.get_scm_git_reference": {
      input: {
        /**
         * App Store Connect identifier of the Git reference.
         * @minLength 1
         * @pattern \S
         */
        scmGitReferenceId: string;
      };
      output: {
        /** A branch or tag in a source repository. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        scmGitReference: {
          /** App Store Connect identifier for the Git reference. */
          id: string;
          /** Short name, such as main or v1.2.0. */
          name?: string | null;
          /** Full reference name, such as refs/heads/main. */
          canonicalName?: string | null;
          /** Whether the reference has been deleted from the repository. */
          isDeleted?: boolean | null;
          /** Whether the reference is a branch or a tag. */
          kind?: "BRANCH" | "TAG" | null;
          /** Identifier of the repository the reference belongs to. */
          scmRepositoryId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one source control provider connected to Xcode Cloud. */
    "app_store_connect.get_scm_provider": {
      input: {
        /**
         * App Store Connect identifier of the source control provider.
         * @minLength 1
         * @pattern \S
         */
        scmProviderId: string;
      };
      output: {
        /** A source control provider connected to Xcode Cloud. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        scmProvider: {
          /** App Store Connect identifier for the source control provider. */
          id: string;
          /** Kind of source control provider. */
          scmProviderType?: {
            /** Provider product. */
            kind?: "BITBUCKET_CLOUD" | "BITBUCKET_SERVER" | "GITHUB_CLOUD" | "GITHUB_ENTERPRISE" | "GITLAB_CLOUD" | "GITLAB_SELF_MANAGED";
            /** Display name of the provider. */
            displayName?: string;
            /** Whether the provider is self-hosted. */
            isOnPremise?: boolean;
            [key: string]: unknown;
          } | null;
          /** Base URL of the provider. */
          url?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one pull request of a repository as Xcode Cloud sees it. */
    "app_store_connect.get_scm_pull_request": {
      input: {
        /**
         * App Store Connect identifier of the pull request.
         * @minLength 1
         * @pattern \S
         */
        scmPullRequestId: string;
      };
      output: {
        /** A pull request in a source repository. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        scmPullRequest: {
          /** App Store Connect identifier for the pull request. */
          id: string;
          /** Pull request title. */
          title?: string | null;
          /** Pull request number on the source control provider. */
          number?: number | null;
          /** URL of the pull request on the source control provider. */
          webUrl?: string | null;
          /** Owner of the repository the changes come from. */
          sourceRepositoryOwner?: string | null;
          /** Name of the repository the changes come from. */
          sourceRepositoryName?: string | null;
          /** Branch the changes come from. */
          sourceBranchName?: string | null;
          /** Owner of the repository the changes go to. */
          destinationRepositoryOwner?: string | null;
          /** Name of the repository the changes go to. */
          destinationRepositoryName?: string | null;
          /** Branch the changes go to. */
          destinationBranchName?: string | null;
          /** Whether the pull request is closed. */
          isClosed?: boolean | null;
          /** Whether the source and destination repositories differ. */
          isCrossRepository?: boolean | null;
          /** Identifier of the repository the pull request belongs to. */
          scmRepositoryId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one source repository, with its provider and default branch identifiers. */
    "app_store_connect.get_scm_repository": {
      input: {
        /**
         * App Store Connect identifier of the repository.
         * @minLength 1
         * @pattern \S
         */
        scmRepositoryId: string;
      };
      output: {
        /** A source repository Xcode Cloud can build. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        scmRepository: {
          /** App Store Connect identifier for the repository. */
          id: string;
          /** When Xcode Cloud last accessed the repository, as an ISO 8601 timestamp. */
          lastAccessedDate?: string | null;
          /** HTTPS clone URL. */
          httpCloneUrl?: string | null;
          /** SSH clone URL. */
          sshCloneUrl?: string | null;
          /** Owner or organization the repository belongs to. */
          ownerName?: string | null;
          /** Repository name. */
          repositoryName?: string | null;
          /** Identifier of the source control provider. */
          scmProviderId: string | null;
          /** Identifier of the Git reference for the default branch. */
          defaultBranchId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one auto-renewable subscription by its App Store Connect identifier. */
    "app_store_connect.get_subscription": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
      };
      output: {
        /** An auto-renewable subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscription: {
          /** App Store Connect identifier for the subscription. */
          id: string;
          /** Internal reference name of the subscription. */
          name?: string | null;
          /** Product identifier used by the app to purchase the subscription. */
          productId?: string | null;
          /** Whether the subscription can be shared with Family Sharing. */
          familySharable?: boolean | null;
          /** Review and sale state of the subscription. */
          state?: "MISSING_METADATA" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED" | null;
          /** Length of one subscription period. */
          subscriptionPeriod?: "ONE_WEEK" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** Note shown to App Review alongside the subscription. */
          reviewNote?: string | null;
          /** Rank of the subscription within its group, where 1 is the highest level of service. */
          groupLevel?: number | null;
          /** App Store Connect identifier of the group the subscription belongs to. */
          subscriptionGroupId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read the billing grace period settings of an app, either by app or by the grace period record identifier. Pass exactly one of appId or subscriptionGracePeriodId. */
    "app_store_connect.get_subscription_grace_period": {
      input: {
        /**
         * App Store Connect identifier of the app whose grace period settings to read.
         * @minLength 1
         * @pattern \S
         */
        appId?: string;
        /**
         * App Store Connect identifier of the grace period record, as returned by an earlier read.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGracePeriodId?: string;
      };
      output: {
        /** Billing grace period settings of an app, which keep subscribers on service while a failed payment is retried. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGracePeriod: {
          /** App Store Connect identifier for the subscription grace period record. */
          id: string;
          /** Whether the grace period is enabled in production. */
          optIn?: boolean | null;
          /** Whether the grace period is enabled in the sandbox. */
          sandboxOptIn?: boolean | null;
          /** Length of the grace period. */
          duration?: "THREE_DAYS" | "SIXTEEN_DAYS" | "TWENTY_EIGHT_DAYS" | null;
          /** Which renewals get a grace period: every renewal, or only paid-to-paid renewals. */
          renewalType?: "ALL_RENEWALS" | "PAID_TO_PAID_ONLY" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one subscription group by its App Store Connect identifier. */
    "app_store_connect.get_subscription_group": {
      input: {
        /**
         * App Store Connect identifier of the subscription group.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupId: string;
      };
      output: {
        /** A subscription group that holds the auto-renewable subscriptions a customer can switch between. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroup: {
          /** App Store Connect identifier for the subscription group. */
          id: string;
          /** Internal reference name of the group, not shown to customers. */
          referenceName?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one subscription group localization by its App Store Connect identifier. */
    "app_store_connect.get_subscription_group_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription group localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupLocalizationId: string;
      };
      output: {
        /** Customer-facing name of a subscription group in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroupLocalization: {
          /** App Store Connect identifier for the subscription group localization. */
          id: string;
          /** Group name shown to customers in this locale. */
          name?: string | null;
          /** Alternative app name shown with the group in this locale, when one is set. */
          customAppName?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one subscription group version by its App Store Connect identifier. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.get_subscription_group_version": {
      input: {
        /**
         * App Store Connect identifier of the subscription group version.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupVersionId: string;
      };
      output: {
        /** A draft version of a subscription group that carries its localized names through App Review. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroupVersion: {
          /** App Store Connect identifier for the subscription group version. */
          id: string;
          /** Sequential version number assigned by App Store Connect. */
          version?: number | null;
          /** Review state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one subscription group version localization by its App Store Connect identifier. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.get_subscription_group_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription group version localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupLocalizationId: string;
      };
      output: {
        /** Customer-facing name of a subscription group in one locale, attached to a subscription group version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroupLocalization: {
          /** App Store Connect identifier for the subscription group version localization. */
          id: string;
          /** Group name shown to customers in this locale. */
          name?: string | null;
          /** Alternative app name shown with the group in this locale, when one is set. */
          customAppName?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one subscription localization by its App Store Connect identifier. */
    "app_store_connect.get_subscription_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionLocalizationId: string;
      };
      output: {
        /** Customer-facing name and description of a subscription in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionLocalization: {
          /** App Store Connect identifier for the subscription localization. */
          id: string;
          /** Subscription name shown to customers in this locale. */
          name?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Subscription description shown to customers in this locale. */
          description?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one offer code configuration by its App Store Connect identifier. */
    "app_store_connect.get_subscription_offer_code": {
      input: {
        /**
         * App Store Connect identifier of the offer code configuration.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeId: string;
      };
      output: {
        /** An offer code configuration of an auto-renewable subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionOfferCode: {
          /** App Store Connect identifier for the offer code configuration. */
          id: string;
          /** Reference name of the offer code configuration. */
          name?: string | null;
          /** Customer groups eligible to redeem the codes. */
          customerEligibilities?: Array<"NEW" | "EXISTING" | "EXPIRED"> | null;
          /** Whether the offer stacks with or replaces introductory offers. */
          offerEligibility?: "STACK_WITH_INTRO_OFFERS" | "REPLACE_INTRO_OFFERS" | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          /** Total number of codes generated so far. */
          totalNumberOfCodes?: number | null;
          /** Number of codes generated for production. */
          productionCodeCount?: number | null;
          /** Number of codes generated for the sandbox. */
          sandboxCodeCount?: number | null;
          /** Whether the configuration is active and its codes redeemable. */
          active?: boolean | null;
          /** Whether the subscription renews automatically after the offer ends. */
          autoRenewEnabled?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one custom code batch by its App Store Connect identifier. */
    "app_store_connect.get_subscription_offer_code_custom_code": {
      input: {
        /**
         * App Store Connect identifier of the custom code batch.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeCustomCodeId: string;
      };
      output: {
        /** A custom code batch of an offer code configuration. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionOfferCodeCustomCode: {
          /** App Store Connect identifier for the custom code batch. */
          id: string;
          /** The custom code customers redeem. */
          customCode?: string | null;
          /** Number of redemptions the custom code allows. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the code can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the custom code is active and redeemable. */
          active?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one one-time use code batch by its App Store Connect identifier. */
    "app_store_connect.get_subscription_offer_code_one_time_use_code": {
      input: {
        /**
         * App Store Connect identifier of the one-time use code batch.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeOneTimeUseCodeId: string;
      };
      output: {
        /** A batch of one-time use codes of an offer code configuration. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionOfferCodeOneTimeUseCode: {
          /** App Store Connect identifier for the one-time use code batch. */
          id: string;
          /** Number of single-use codes in the batch. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the codes can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the batch is active and its codes redeemable. */
          active?: boolean | null;
          /** Environment the codes redeem in. */
          environment?: "PRODUCTION" | "SANDBOX" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one subscription plan availability by its App Store Connect identifier. */
    "app_store_connect.get_subscription_plan_availability": {
      input: {
        /**
         * App Store Connect identifier of the subscription plan availability.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPlanAvailabilityId: string;
      };
      output: {
        /** Territory availability of one payment plan of a subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionPlanAvailability: {
          /** App Store Connect identifier for the subscription plan availability. */
          id: string;
          /** Whether the plan becomes available automatically in territories the App Store adds later. */
          availableInNewTerritories?: boolean | null;
          /** Payment plan of the subscription: UPFRONT charges the full price at the start of each period, MONTHLY is a 12-month commitment billed monthly (App Store Connect API 4.4). */
          planType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one subscription price point by its App Store Connect identifier. */
    "app_store_connect.get_subscription_price_point": {
      input: {
        /**
         * App Store Connect identifier of the subscription price point.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPricePointId: string;
      };
      output: {
        /** A standard price tier available to a subscription in one territory. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionPricePoint: {
          /** App Store Connect identifier for the subscription price point. */
          id: string;
          /** Price the customer pays, as a decimal string in the territory currency. */
          customerPrice?: string | null;
          /** Developer proceeds during the first year of a subscription, as a decimal string. */
          proceeds?: string | null;
          /** Developer proceeds after the first year of a subscription, as a decimal string. */
          proceedsYear2?: string | null;
          /** Territory the price point belongs to. ISO 3166-1 alpha-3 App Store territory code, such as USA. */
          territoryId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one promotional offer by its App Store Connect identifier. */
    "app_store_connect.get_subscription_promotional_offer": {
      input: {
        /**
         * App Store Connect identifier of the promotional offer.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPromotionalOfferId: string;
      };
      output: {
        /** A promotional offer of an auto-renewable subscription for existing or lapsed subscribers. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionPromotionalOffer: {
          /** App Store Connect identifier for the promotional offer. */
          id: string;
          /** Reference name shown in App Store Connect. */
          name?: string | null;
          /** Offer identifier your app passes to StoreKit when presenting the offer. */
          offerCode?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one subscription version by its App Store Connect identifier. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.get_subscription_version": {
      input: {
        /**
         * App Store Connect identifier of the subscription version.
         * @minLength 1
         * @pattern \S
         */
        subscriptionVersionId: string;
      };
      output: {
        /** A draft version of a subscription that carries its localized metadata through App Review. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionVersion: {
          /** App Store Connect identifier for the subscription version. */
          id: string;
          /** Sequential version number assigned by App Store Connect. */
          version?: number | null;
          /** Review state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one subscription version localization by its App Store Connect identifier. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.get_subscription_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription version localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionLocalizationId: string;
      };
      output: {
        /** Customer-facing name and description of a subscription in one locale, attached to a subscription version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionLocalization: {
          /** App Store Connect identifier for the subscription version localization. */
          id: string;
          /** Subscription name shown to customers in this locale. */
          name?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Subscription description shown to customers in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one App Store Connect team member by identifier. */
    "app_store_connect.get_user": {
      input: {
        /**
         * App Store Connect identifier of the team member.
         * @minLength 1
         * @pattern \S
         */
        userId: string;
      };
      output: {
        /** A member of the App Store Connect team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        user: {
          /** App Store Connect identifier for the user. */
          id: string;
          /** Apple Account email the user signs in with. */
          username?: string | null;
          /** User first name. */
          firstName?: string | null;
          /** User last name. */
          lastName?: string | null;
          /** Roles granted to the user. */
          roles?: Array<"ADMIN" | "FINANCE" | "ACCOUNT_HOLDER" | "SALES" | "MARKETING" | "APP_MANAGER" | "DEVELOPER" | "ACCESS_TO_REPORTS" | "CUSTOMER_SUPPORT" | "CREATE_APPS" | "CLOUD_MANAGED_DEVELOPER_ID" | "CLOUD_MANAGED_APP_DISTRIBUTION" | "GENERATE_INDIVIDUAL_KEYS"> | null;
          /** Whether the user can see every app on the team. */
          allAppsVisible?: boolean | null;
          /** Whether the user may manage certificates, identifiers, and profiles. */
          provisioningAllowed?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one pending team invitation by identifier. */
    "app_store_connect.get_user_invitation": {
      input: {
        /**
         * App Store Connect identifier of the invitation.
         * @minLength 1
         * @pattern \S
         */
        userInvitationId: string;
      };
      output: {
        /** A pending invitation to join the App Store Connect team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        userInvitation: {
          /** App Store Connect identifier for the invitation. */
          id: string;
          /** Email address the invitation was sent to. */
          email?: string | null;
          /** First name of the invited user. */
          firstName?: string | null;
          /** Last name of the invited user. */
          lastName?: string | null;
          /** When the invitation expires, as an ISO 8601 timestamp. Invitations last three days. */
          expirationDate?: string | null;
          /** Roles the user receives on accepting. */
          roles?: Array<"ADMIN" | "FINANCE" | "ACCOUNT_HOLDER" | "SALES" | "MARKETING" | "APP_MANAGER" | "DEVELOPER" | "ACCESS_TO_REPORTS" | "CUSTOMER_SUPPORT" | "CREATE_APPS" | "CLOUD_MANAGED_DEVELOPER_ID" | "CLOUD_MANAGED_APP_DISTRIBUTION" | "GENERATE_INDIVIDUAL_KEYS"> | null;
          /** Whether the user will see every app on the team. */
          allAppsVisible?: boolean | null;
          /** Whether the user may manage certificates, identifiers, and profiles. */
          provisioningAllowed?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one webhook by its App Store Connect identifier. */
    "app_store_connect.get_webhook": {
      input: {
        /**
         * App Store Connect identifier of the webhook.
         * @minLength 1
         * @pattern \S
         */
        webhookId: string;
      };
      output: {
        /** A webhook registered on an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        webhook: {
          /** App Store Connect identifier for the webhook. */
          id: string;
          /** Display name of the webhook. */
          name?: string | null;
          /** HTTPS endpoint App Store Connect posts notifications to. */
          url?: string | null;
          /** Whether App Store Connect currently sends notifications to the webhook. */
          enabled?: boolean | null;
          /** Event types the webhook is subscribed to. */
          eventTypes?: Array<"ALTERNATIVE_DISTRIBUTION_PACKAGE_AVAILABLE_UPDATED" | "ALTERNATIVE_DISTRIBUTION_PACKAGE_VERSION_CREATED" | "ALTERNATIVE_DISTRIBUTION_TERRITORY_AVAILABILITY_UPDATED" | "APP_STORE_VERSION_APP_VERSION_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_APP_STORE_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_EXTERNAL_BETA_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_INTERNAL_BETA_RELEASE_CREATED" | "BACKGROUND_ASSET_VERSION_STATE_UPDATED" | "BETA_FEEDBACK_CRASH_SUBMISSION_CREATED" | "BETA_FEEDBACK_SCREENSHOT_SUBMISSION_CREATED" | "BUILD_BETA_DETAIL_EXTERNAL_BUILD_STATE_UPDATED" | "BUILD_UPLOAD_STATE_UPDATED"> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Read one win-back offer by its App Store Connect identifier. */
    "app_store_connect.get_win_back_offer": {
      input: {
        /**
         * App Store Connect identifier of the win-back offer.
         * @minLength 1
         * @pattern \S
         */
        winBackOfferId: string;
      };
      output: {
        /** A win-back offer that targets lapsed subscribers of an auto-renewable subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        winBackOffer: {
          /** App Store Connect identifier for the win-back offer. */
          id: string;
          /** Reference name shown in App Store Connect. */
          referenceName?: string | null;
          /** Offer identifier your app passes to StoreKit. */
          offerId?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          periodCount?: number | null;
          /** Minimum number of months the customer must have paid for the subscription before lapsing. */
          customerEligibilityPaidSubscriptionDurationInMonths?: number | null;
          /** Inclusive range of months since the customer last subscribed. */
          customerEligibilityTimeSinceLastSubscribedInMonths?: {
            /** Lower bound in months. */
            minimum?: number | null;
            /** Upper bound in months. */
            maximum?: number | null;
            [key: string]: unknown;
          } | null;
          /** Months a customer must wait after redeeming a win-back offer before another one is offered. */
          customerEligibilityWaitBetweenOffersInMonths?: number | null;
          /** First day the offer is available, as YYYY-MM-DD. */
          startDate?: string | null;
          /** Last day the offer is available, as YYYY-MM-DD. */
          endDate?: string | null;
          /** Priority the App Store uses when several win-back offers apply. */
          priority?: "HIGH" | "NORMAL" | null;
          /** Whether Apple may promote the offer on the App Store with auto-generated assets. */
          promotionIntent?: "NOT_PROMOTED" | "USE_AUTO_GENERATED_ASSETS" | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Invite someone to join the App Store Connect team with the given roles. Apple emails them an activation link that expires after three days; the invitation can be cancelled until it is accepted. */
    "app_store_connect.invite_user": {
      input: {
        /**
         * Email address the invitation is sent to.
         * @format email
         */
        email: string;
        /**
         * First name of the invited user.
         * @minLength 1
         * @pattern \S
         */
        firstName: string;
        /**
         * Last name of the invited user.
         * @minLength 1
         * @pattern \S
         */
        lastName: string;
        /**
         * Roles the user holds after accepting.
         * @minItems 1
         */
        roles: Array<"ADMIN" | "FINANCE" | "ACCOUNT_HOLDER" | "SALES" | "MARKETING" | "APP_MANAGER" | "DEVELOPER" | "ACCESS_TO_REPORTS" | "CUSTOMER_SUPPORT" | "CREATE_APPS" | "CLOUD_MANAGED_DEVELOPER_ID" | "CLOUD_MANAGED_APP_DISTRIBUTION" | "GENERATE_INDIVIDUAL_KEYS">;
        /** Give the user access to every app on the team; when false, only visibleAppIds are shown. */
        allAppsVisible?: boolean;
        /** Let the user manage certificates, identifiers, and profiles. */
        provisioningAllowed?: boolean;
        /**
         * Apps the user may see when allAppsVisible is false.
         * @minItems 1
         */
        visibleAppIds?: Array<string>;
      };
      output: {
        /** A pending invitation to join the App Store Connect team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        userInvitation: {
          /** App Store Connect identifier for the invitation. */
          id: string;
          /** Email address the invitation was sent to. */
          email?: string | null;
          /** First name of the invited user. */
          firstName?: string | null;
          /** Last name of the invited user. */
          lastName?: string | null;
          /** When the invitation expires, as an ISO 8601 timestamp. Invitations last three days. */
          expirationDate?: string | null;
          /** Roles the user receives on accepting. */
          roles?: Array<"ADMIN" | "FINANCE" | "ACCOUNT_HOLDER" | "SALES" | "MARKETING" | "APP_MANAGER" | "DEVELOPER" | "ACCESS_TO_REPORTS" | "CUSTOMER_SUPPORT" | "CREATE_APPS" | "CLOUD_MANAGED_DEVELOPER_ID" | "CLOUD_MANAGED_APP_DISTRIBUTION" | "GENERATE_INDIVIDUAL_KEYS"> | null;
          /** Whether the user will see every app on the team. */
          allAppsVisible?: boolean | null;
          /** Whether the user may manage certificates, identifiers, and profiles. */
          provisioningAllowed?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** List the accessibility declarations (Accessibility Nutrition Labels) of an app, optionally filtered by device family or publication state. */
    "app_store_connect.list_accessibility_declarations": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only declarations for these device families.
         * @minItems 1
         */
        deviceFamilies?: Array<"IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION">;
        /**
         * Return only declarations in these states.
         * @minItems 1
         */
        states?: Array<"DRAFT" | "PUBLISHED" | "REPLACED">;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Accessibility declarations returned for this page. */
        accessibilityDeclarations: Array<{
          /** App Store Connect identifier for the accessibility declaration. */
          id: string;
          /** Device family the declaration describes. */
          deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION" | null;
          /** Whether the declaration is a draft, published, or replaced by a newer one. */
          state?: "DRAFT" | "PUBLISHED" | "REPLACED" | null;
          /** Whether the app supports audio descriptions. */
          supportsAudioDescriptions?: boolean | null;
          /** Whether the app supports captions. */
          supportsCaptions?: boolean | null;
          /** Whether the app supports a dark interface. */
          supportsDarkInterface?: boolean | null;
          /** Whether the app conveys information without relying on color alone. */
          supportsDifferentiateWithoutColorAlone?: boolean | null;
          /** Whether the app supports larger text. */
          supportsLargerText?: boolean | null;
          /** Whether the app supports reduced motion. */
          supportsReducedMotion?: boolean | null;
          /** Whether the app supports sufficient contrast. */
          supportsSufficientContrast?: boolean | null;
          /** Whether the app supports Voice Control. */
          supportsVoiceControl?: boolean | null;
          /** Whether the app supports VoiceOver. */
          supportsVoiceover?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Resolve audit-log actor identifiers, such as the submitter of a review submission, to the team member, API key, or system that performed the action. App Store Connect requires at least one identifier. */
    "app_store_connect.list_actors": {
      input: {
        /**
         * Identifiers of the actors to return.
         * @minItems 1
         */
        actorIds: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Actors returned for this page. */
        actors: Array<{
          /** App Store Connect identifier for the actor. */
          id: string;
          /** Kind of actor. */
          actorType?: "USER" | "API_KEY" | "XCODE_CLOUD" | "APPLE" | null;
          /** First name when the actor is a team member. */
          userFirstName?: string | null;
          /** Last name when the actor is a team member. */
          userLastName?: string | null;
          /** Email address when the actor is a team member. */
          userEmail?: string | null;
          /** Key ID when the actor is an App Store Connect API key. */
          apiKeyId?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the base web domains registered for alternative distribution on the account. */
    "app_store_connect.list_alternative_distribution_domains": {
      input: {
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Alternative distribution domains returned for this page. */
        alternativeDistributionDomains: Array<{
          /** App Store Connect identifier of the alternative distribution domain. */
          id: string;
          /** Base domain that serves the marketplace or web distribution app. */
          domain?: string | null;
          /** Display name chosen for the domain in App Store Connect. */
          referenceName?: string | null;
          /** When the domain was added, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the alternative distribution public keys of the team. A key without an app applies to every alternative distribution app on the account. */
    "app_store_connect.list_alternative_distribution_keys": {
      input: {
        /** Return only keys tied to a single app when true, or only account-wide keys when false. */
        hasApp?: boolean;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Alternative distribution keys returned for this page. */
        alternativeDistributionKeys: Array<{
          /** App Store Connect identifier of the alternative distribution key. */
          id: string;
          /** PEM-encoded public key that was uploaded. */
          publicKey?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the delta updates of an alternative distribution package version, each with its download URL and encrypted key blob. */
    "app_store_connect.list_alternative_distribution_package_version_deltas": {
      input: {
        /**
         * App Store Connect identifier of the alternative distribution package version.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionPackageVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Package deltas returned for this page. */
        deltas: Array<{
          /** App Store Connect identifier of the package delta. */
          id: string;
          /** Time-limited download URL of the file. */
          url?: string | null;
          /** When the download URL stops working, as an ISO 8601 timestamp. */
          urlExpirationDate?: string | null;
          /** Encrypted key blob the marketplace or web server needs to process the file. Treat it as a secret. */
          alternativeDistributionKeyBlob?: string | null;
          /** SHA-256 checksum of the file. */
          fileChecksum?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the device-specific variants of an alternative distribution package version, each with its download URL and encrypted key blob. */
    "app_store_connect.list_alternative_distribution_package_version_variants": {
      input: {
        /**
         * App Store Connect identifier of the alternative distribution package version.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionPackageVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Package variants returned for this page. */
        variants: Array<{
          /** App Store Connect identifier of the package variant. */
          id: string;
          /** Time-limited download URL of the file. */
          url?: string | null;
          /** When the download URL stops working, as an ISO 8601 timestamp. */
          urlExpirationDate?: string | null;
          /** Encrypted key blob the marketplace or web server needs to process the file. Treat it as a secret. */
          alternativeDistributionKeyBlob?: string | null;
          /** SHA-256 checksum of the file. */
          fileChecksum?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the versions of an alternative distribution package with their time-limited download URLs. */
    "app_store_connect.list_alternative_distribution_package_versions": {
      input: {
        /**
         * App Store Connect identifier of the alternative distribution package.
         * @minLength 1
         * @pattern \S
         */
        alternativeDistributionPackageId: string;
        /** Return only versions in this state. */
        state?: "COMPLETED" | "REPLACED";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Package versions returned for this page. */
        alternativeDistributionPackageVersions: Array<{
          /** App Store Connect identifier of the alternative distribution package version. */
          id: string;
          /** Version string of the packaged app. */
          version?: string | null;
          /** Whether this is the current package version or has been replaced by a newer one. */
          state?: "COMPLETED" | "REPLACED" | null;
          /** Time-limited download URL of the package version manifest. */
          url?: string | null;
          /** When the download URL stops working, as an ISO 8601 timestamp. */
          urlExpirationDate?: string | null;
          /** Checksum of the package version file. */
          fileChecksum?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the processed instances of one analytics report, each covering a daily, weekly, or monthly period. */
    "app_store_connect.list_analytics_report_instances": {
      input: {
        /**
         * App Store Connect identifier of the report.
         * @minLength 1
         * @pattern \S
         */
        analyticsReportId: string;
        /** Return only instances with this granularity. */
        granularity?: "DAILY" | "WEEKLY" | "MONTHLY";
        /**
         * Return only instances processed on this date, as YYYY-MM-DD.
         * @format date
         */
        processingDate?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Report instances returned for this page. */
        analyticsReportInstances: Array<{
          /** App Store Connect identifier for the report instance. */
          id: string;
          /** Reporting period covered by the instance. */
          granularity?: "DAILY" | "WEEKLY" | "MONTHLY" | null;
          /** Date the instance was processed, as YYYY-MM-DD. */
          processingDate?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the analytics report requests of one app, which are the entry point to its analytics reports. */
    "app_store_connect.list_analytics_report_requests": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Return only requests with this access type. */
        accessType?: "ONE_TIME_SNAPSHOT" | "ONGOING";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Report requests returned for this page. */
        analyticsReportRequests: Array<{
          /** App Store Connect identifier for the report request. */
          id: string;
          /** ONGOING keeps producing daily, weekly, and monthly reports; ONE_TIME_SNAPSHOT delivers historical data once. */
          accessType?: "ONE_TIME_SNAPSHOT" | "ONGOING" | null;
          /** True once App Store Connect stopped the request because its reports were not read for a long time; create a new request to resume. */
          stoppedDueToInactivity?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the downloadable segments of one analytics report instance. Each url points at a gzip-compressed file that the caller downloads itself; the links expire five minutes after they are issued, so call this action right before downloading. */
    "app_store_connect.list_analytics_report_segments": {
      input: {
        /**
         * App Store Connect identifier of the report instance.
         * @minLength 1
         * @pattern \S
         */
        analyticsReportInstanceId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Segments returned for this page. */
        analyticsReportSegments: Array<{
          /** App Store Connect identifier for the segment. */
          id: string;
          /** Download URL of the gzip-compressed report segment. It expires five minutes after this response, so download it right away. */
          url?: string | null;
          /** MD5 checksum of the compressed segment file. */
          checksum?: string | null;
          /** Size of the compressed segment file in bytes. */
          sizeInBytes?: number | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the report types available under one analytics report request, optionally narrowed by category or exact name. */
    "app_store_connect.list_analytics_reports": {
      input: {
        /**
         * App Store Connect identifier of the report request.
         * @minLength 1
         * @pattern \S
         */
        analyticsReportRequestId: string;
        /** Return only reports in this category. */
        category?: "APP_USAGE" | "APP_STORE_ENGAGEMENT" | "COMMERCE" | "FRAMEWORK_USAGE" | "PERFORMANCE";
        /**
         * Return only the report with this exact name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Reports returned for this page. */
        analyticsReports: Array<{
          /** App Store Connect identifier for the report. */
          id: string;
          /** Report name, such as App Store Downloads Standard. */
          name?: string | null;
          /** Report category. */
          category?: "APP_USAGE" | "APP_STORE_ENGAGEMENT" | "COMMERCE" | "FRAMEWORK_USAGE" | "PERFORMANCE" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Android app mappings of an app, which link an Android package to this app for Android to iPhone migration. */
    "app_store_connect.list_android_to_ios_app_mapping_details": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Android app mappings returned for this page. */
        androidToIosAppMappingDetails: Array<{
          /** App Store Connect identifier for the mapping detail. */
          id: string;
          /** Android package name of the mapped app. */
          packageName?: string | null;
          /** SHA-256 fingerprints of the Android app signing key certificates. */
          appSigningKeyPublicCertificateSha256Fingerprints?: Array<string> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List App Store categories with their platforms, parent, and subcategory identifiers. Filter by platform or list only top-level categories or only subcategories. */
    "app_store_connect.list_app_categories": {
      input: {
        /**
         * Return only categories offered on any of these platforms.
         * @minItems 1
         */
        platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS">;
        /** Return only subcategories when true, or only top-level categories when false. */
        hasParent?: boolean;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Categories returned for this page. */
        appCategories: Array<{
          /** Category identifier used in category relationships, such as GAMES or GAMES_ACTION. */
          id: string;
          /** Content platforms the category is offered on. */
          platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
          /** Identifier of the parent category, or null for a top-level category. */
          parentId: string | null;
          /** Identifiers of the subcategories under this category; empty for a subcategory. */
          subcategoryIds: Array<string> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the advanced experiences of an App Clip with their localized card text. Filter by status, place match status, or action. */
    "app_store_connect.list_app_clip_advanced_experiences": {
      input: {
        /**
         * App Store Connect identifier of the App Clip.
         * @minLength 1
         * @pattern \S
         */
        appClipId: string;
        /**
         * Return only experiences in any of these statuses.
         * @minItems 1
         */
        statuses?: Array<"RECEIVED" | "DEACTIVATED" | "APP_TRANSFER_IN_PROGRESS">;
        /**
         * Return only experiences whose place has any of these match statuses.
         * @minItems 1
         */
        placeStatuses?: Array<"PENDING" | "MATCHED" | "NO_MATCH">;
        /**
         * Return only experiences with any of these call-to-action verbs.
         * @minItems 1
         */
        actions?: Array<"OPEN" | "VIEW" | "PLAY">;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Advanced App Clip experiences returned for this page. */
        appClipAdvancedExperiences: Array<{
          /** App Store Connect identifier for the advanced App Clip experience. */
          id: string;
          /** Invocation URL of the experience. */
          link?: string | null;
          /** Build version of the App Clip the experience belongs to. */
          version?: number | null;
          /** Whether users can invoke the experience (RECEIVED), it is deactivated, or its app is being transferred. */
          status?: "RECEIVED" | "DEACTIVATED" | "APP_TRANSFER_IN_PROGRESS" | null;
          /** Call-to-action verb shown on the App Clip card. */
          action?: "OPEN" | "VIEW" | "PLAY" | null;
          /** Whether the experience was submitted by a platform provider that serves multiple businesses. */
          isPoweredBy?: boolean | null;
          /** Physical place the experience is tied to, or null. */
          place?: {
            /** Apple Maps place identifier when the place is already known to Apple Maps. */
            placeId?: string;
            /** Names of the place. */
            names?: Array<string>;
            /** Main address of the place. */
            mainAddress?: {
              /** Full address as a single line. */
              fullAddress?: string;
              /** Address split into its parts. */
              structuredAddress?: {
                /** Street address lines. */
                streetAddress?: Array<string>;
                /** Floor within the building. */
                floor?: string;
                /** Neighborhood of the place. */
                neighborhood?: string;
                /** City or locality. */
                locality?: string;
                /** State or province. */
                stateProvince?: string;
                /** Postal code. */
                postalCode?: string;
                /** ISO 3166-1 alpha-2 country code, such as US. */
                countryCode?: string;
                [key: string]: unknown;
              };
              [key: string]: unknown;
            };
            /** Where the pin is placed on the map. */
            displayPoint?: {
              /** Geographic coordinates of the pin. */
              coordinates?: {
                /** Latitude in decimal degrees. */
                latitude?: number;
                /** Longitude in decimal degrees. */
                longitude?: number;
                [key: string]: unknown;
              };
              /** Whether the pin was calculated from the address or placed manually. */
              source?: "CALCULATED" | "MANUALLY_PLACED";
              [key: string]: unknown;
            };
            /** Action offered on the place card in Apple Maps. */
            mapAction?: "BUY_TICKETS" | "VIEW_AVAILABILITY" | "VIEW_PRICING" | "HOTEL_BOOK_ROOM" | "PARKING_RESERVE_PARKING" | "RESTAURANT_JOIN_WAITLIST" | "RESTAURANT_ORDER_DELIVERY" | "RESTAURANT_ORDER_FOOD" | "RESTAURANT_ORDER_TAKEOUT" | "RESTAURANT_RESERVATION" | "SCHEDULE_APPOINTMENT" | "RESTAURANT_VIEW_MENU" | "THEATER_NOW_PLAYING" | "AIRLINE_BOOK_TRAVEL" | "AIRLINE_CHECK_IN" | "AIRLINE_FLIGHT_STATUS" | "APPLY" | "BOOK" | "BOOK_ACTIVITIES" | "BOOK_RIDES" | "BOOK_TEETIMES" | "BOOK_TOURS" | "CAREERS" | "CHARGE_EV" | "COUPONS" | "DONATE" | "EVENTS" | "EVENTS_SHOWS" | "EVENTS_SPORTS" | "GIFT_CARD" | "HOTEL_AMENITIES" | "JOIN" | "PARKING_AVAILABLE" | "RESTAURANT_PICKUP" | "RETAIL_SERVICE_QUOTE" | "RETAIL_STORE_DELIVERY" | "RETAIL_STORE_PICKUP" | "RETAIL_STORE_SHOP" | "SERVICES" | "SUPPORT" | "PAY_TO_PARK";
            /** Relationship between the submitter and the place. */
            relationship?: "OWNER" | "AUTHORIZED" | "OTHER";
            /** Phone number of the place. */
            phoneNumber?: {
              /** Phone number of the place. */
              number?: string;
              /** Kind of phone line. */
              type?: "FAX" | "LANDLINE" | "MOBILE" | "TOLLFREE";
              /** What the number is for, such as reservations. */
              intent?: string;
              [key: string]: unknown;
            };
            /** Website of the place. */
            homePage?: string;
            /** Apple Maps categories of the place. */
            categories?: Array<string>;
            [key: string]: unknown;
          } | null;
          /** Whether Apple Maps matched the place to a point of interest. */
          placeStatus?: "PENDING" | "MATCHED" | "NO_MATCH" | null;
          /** Business category of the experience. */
          businessCategory?: "AUTOMOTIVE" | "BEAUTY" | "BIKES" | "BOOKS" | "CASINO" | "EDUCATION" | "EDUCATION_JAPAN" | "ENTERTAINMENT" | "EV_CHARGER" | "FINANCIAL_USD" | "FINANCIAL_CNY" | "FINANCIAL_GBP" | "FINANCIAL_JPY" | "FINANCIAL_EUR" | "FITNESS" | "FOOD_AND_DRINK" | "GAS" | "GROCERY" | "HEALTH_AND_MEDICAL" | "HOTEL_AND_TRAVEL" | "MUSIC" | "PARKING" | "PET_SERVICES" | "PROFESSIONAL_SERVICES" | "SHOPPING" | "TICKETING" | "TRANSIT" | null;
          /** Default language of the experience. */
          defaultLanguage?: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH" | null;
          /** Localized card text of the experience, empty when none was returned. */
          localizations: Array<{
            /** App Store Connect identifier for the localization. */
            id: string;
            /** Language of the localization. */
            language?: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH" | null;
            /** Title shown on the App Clip card. */
            title?: string | null;
            /** Subtitle shown on the App Clip card. */
            subtitle?: string | null;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the localized subtitles of a default App Clip experience, optionally narrowed to some locales. */
    "app_store_connect.list_app_clip_default_experience_localizations": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceId: string;
        /**
         * Return only the localizations for these locales.
         * @minItems 1
         */
        locales?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Localizations returned for this page. */
        appClipDefaultExperienceLocalizations: Array<{
          /** App Store Connect identifier for the default App Clip experience localization. */
          id: string;
          /** Locale the subtitle is written in, such as en-US. */
          locale?: string | null;
          /** Subtitle shown on the App Clip card in this locale. */
          subtitle?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the default experiences of an App Clip with the App Store version each one is released with. Optionally keep only experiences that are, or are not, attached to a version. */
    "app_store_connect.list_app_clip_default_experiences": {
      input: {
        /**
         * App Store Connect identifier of the App Clip.
         * @minLength 1
         * @pattern \S
         */
        appClipId: string;
        /** Return only experiences attached to an App Store version when true, or only unattached ones when false. */
        hasReleaseVersion?: boolean;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Default App Clip experiences returned for this page. */
        appClipDefaultExperiences: Array<{
          /** App Store Connect identifier for the default App Clip experience. */
          id: string;
          /** Call-to-action verb shown on the App Clip card. */
          action?: "OPEN" | "VIEW" | "PLAY" | null;
          /** App Store Connect identifier of the App Store version the experience is released with, or null when none is attached. */
          releaseWithAppStoreVersionId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the App Clips of an app, optionally filtered by bundle identifier. */
    "app_store_connect.list_app_clips": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only App Clips with any of these bundle identifiers.
         * @minItems 1
         */
        bundleIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** App Clips returned for this page. */
        appClips: Array<{
          /** App Store Connect identifier for the App Clip. */
          id: string;
          /** Bundle identifier of the App Clip. */
          bundleId?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the localizations of one custom product page version, optionally narrowed to some locales. */
    "app_store_connect.list_app_custom_product_page_localizations": {
      input: {
        /**
         * App Store Connect identifier of the custom product page version.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageVersionId: string;
        /**
         * Return only localizations for these locales.
         * @minItems 1
         */
        locales?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Custom product page localizations returned for this page. */
        appCustomProductPageLocalizations: Array<{
          /** App Store Connect identifier for the custom product page localization. */
          id: string;
          /** Locale the metadata is written in, such as en-US. */
          locale?: string | null;
          /** Promotional text shown above the description on this custom product page. */
          promotionalText?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the versions of one custom product page, optionally narrowed to some review states. */
    "app_store_connect.list_app_custom_product_page_versions": {
      input: {
        /**
         * App Store Connect identifier of the custom product page.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageId: string;
        /**
         * Return only versions in these review states.
         * @minItems 1
         */
        states?: Array<"PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED">;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Custom product page versions returned for this page. */
        appCustomProductPageVersions: Array<{
          /** App Store Connect identifier for the custom product page version. */
          id: string;
          /** Version number App Store Connect assigned to this page version. */
          version?: string | null;
          /** Review state of the page version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | null;
          /** Deep link opened when a user taps Get or Open on this custom product page, or null. */
          deepLink?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the custom product pages of one app, optionally only the visible or only the hidden ones. */
    "app_store_connect.list_app_custom_product_pages": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Return only pages visible on the App Store when true, or only hidden pages when false. */
        visible?: boolean;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Custom product pages returned for this page. */
        appCustomProductPages: Array<{
          /** App Store Connect identifier for the custom product page. */
          id: string;
          /** Internal name of the custom product page shown in App Store Connect. */
          name?: string | null;
          /** App Store URL that opens this custom product page. */
          url?: string | null;
          /** Whether the page is visible on the App Store. A hidden page keeps its URL but is not shown. */
          visible?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the export compliance (encryption) declarations of an app with their review state and compliance code, optionally filtered by platform or by the builds they cover. */
    "app_store_connect.list_app_encryption_declarations": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only declarations for these platforms.
         * @minItems 1
         */
        platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS">;
        /**
         * Return only declarations attached to these builds.
         * @minItems 1
         */
        buildIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** App encryption declarations returned for this page. */
        appEncryptionDeclarations: Array<{
          /** App Store Connect identifier for the app encryption declaration. */
          id: string;
          /** Description of how the app uses encryption. */
          appDescription?: string | null;
          /** When the declaration was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the encryption qualifies for an export exemption. */
          exempt?: boolean | null;
          /** Whether the app contains proprietary or non-standard cryptography. */
          containsProprietaryCryptography?: boolean | null;
          /** Whether the app contains third-party cryptography. */
          containsThirdPartyCryptography?: boolean | null;
          /** Whether the app may be distributed on the France storefront. */
          availableOnFrenchStore?: boolean | null;
          /** Content platform the declaration applies to. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Review state of the declaration. */
          appEncryptionDeclarationState?: "CREATED" | "IN_REVIEW" | "APPROVED" | "REJECTED" | "INVALID" | "EXPIRED" | null;
          /** Export compliance code App Store Connect assigned, to reuse in Info.plist or on upload. */
          codeValue?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the localized name and descriptions of an in-app event for every locale. */
    "app_store_connect.list_app_event_localizations": {
      input: {
        /**
         * App Store Connect identifier of the in-app event.
         * @minLength 1
         * @pattern \S
         */
        appEventId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** In-app event localizations returned for this page. */
        appEventLocalizations: Array<{
          /** App Store Connect identifier for the in-app event localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Event name shown on the App Store in this locale. */
          name?: string | null;
          /** Short description shown on the event card. */
          shortDescription?: string | null;
          /** Long description shown on the event details page. */
          longDescription?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the in-app events of an app, optionally narrowed to some review states or identifiers. */
    "app_store_connect.list_app_events": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only events in any of these states.
         * @minItems 1
         */
        eventStates?: Array<"DRAFT" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "ACCEPTED" | "APPROVED" | "PUBLISHED" | "PAST" | "ARCHIVED">;
        /**
         * Return only the events with these identifiers.
         * @minItems 1
         */
        appEventIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** In-app events returned for this page. */
        appEvents: Array<{
          /** App Store Connect identifier for the in-app event. */
          id: string;
          /** Internal reference name of the event, not shown on the App Store. */
          referenceName?: string | null;
          /** Badge that labels the kind of event. */
          badge?: "LIVE_EVENT" | "PREMIERE" | "CHALLENGE" | "COMPETITION" | "NEW_SEASON" | "MAJOR_UPDATE" | "SPECIAL_EVENT" | null;
          /** Review and publication state of the event. */
          eventState?: "DRAFT" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "ACCEPTED" | "APPROVED" | "PUBLISHED" | "PAST" | "ARCHIVED" | null;
          /** Deep link opened in the app when a user taps the event. */
          deepLink?: string | null;
          /** Purchase required to take part, such as NO_COST_ASSOCIATED or IN_APP_PURCHASE. */
          purchaseRequirement?: string | null;
          /** Primary locale of the event metadata, such as en-US. */
          primaryLocale?: string | null;
          /** Priority relative to the other events of the app. */
          priority?: "HIGH" | "NORMAL" | null;
          /** Audience the event is meant for. */
          purpose?: "APPROPRIATE_FOR_ALL_USERS" | "ATTRACT_NEW_USERS" | "KEEP_ACTIVE_USERS_INFORMED" | "BRING_BACK_LAPSED_USERS" | null;
          /** Territory schedules of the event, or null when none are set. */
          territorySchedules?: Array<{
            /** ISO 3166-1 alpha-3 territories this schedule applies to. */
            territories?: Array<string> | null;
            /** When the event becomes visible on the App Store, as an ISO 8601 timestamp. */
            publishStart?: string | null;
            /** When the event starts, as an ISO 8601 timestamp. */
            eventStart?: string | null;
            /** When the event ends, as an ISO 8601 timestamp. */
            eventEnd?: string | null;
            [key: string]: unknown;
          }> | null;
          /** Schedules of past occurrences App Store Connect archived, or null when there are none. */
          archivedTerritorySchedules?: Array<{
            /** ISO 3166-1 alpha-3 territories this schedule applies to. */
            territories?: Array<string> | null;
            /** When the event becomes visible on the App Store, as an ISO 8601 timestamp. */
            publishStart?: string | null;
            /** When the event starts, as an ISO 8601 timestamp. */
            eventStart?: string | null;
            /** When the event ends, as an ISO 8601 timestamp. */
            eventEnd?: string | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the localized names, subtitles, and privacy URLs of an app info record, optionally narrowed to some locales. */
    "app_store_connect.list_app_info_localizations": {
      input: {
        /**
         * App Store Connect identifier of the app info record.
         * @minLength 1
         * @pattern \S
         */
        appInfoId: string;
        /**
         * Return only the localizations for these locales.
         * @minItems 1
         */
        locales?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** App info localizations returned for this page. */
        appInfoLocalizations: Array<{
          /** App Store Connect identifier for the app info localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** App name shown on the App Store in this locale. */
          name?: string | null;
          /** Subtitle shown under the app name in this locale. */
          subtitle?: string | null;
          /** Privacy policy URL published for this locale. */
          privacyPolicyUrl?: string | null;
          /** URL where users can manage their privacy choices, published for this locale. */
          privacyChoicesUrl?: string | null;
          /** Privacy policy text shown on Apple TV, where a URL cannot be opened. */
          privacyPolicyText?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the app info records of an app, each with its review state, derived age ratings, and the identifiers of its App Store categories. An app usually has one editable record plus the record of the version on the App Store. */
    "app_store_connect.list_app_infos": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** App info records returned for this page. */
        appInfos: Array<{
          /** App Store Connect identifier for the app info record. */
          id: string;
          /** Review state of this app info record. */
          state?: "ACCEPTED" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "PENDING_RELEASE" | "PREPARE_FOR_SUBMISSION" | "READY_FOR_DISTRIBUTION" | "READY_FOR_REVIEW" | "REJECTED" | "REPLACED_WITH_NEW_INFO" | "WAITING_FOR_REVIEW" | null;
          /** App Store state of the app the record belongs to. */
          appStoreState?: "ACCEPTED" | "DEVELOPER_REMOVED_FROM_SALE" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "INVALID_BINARY" | "METADATA_REJECTED" | "PENDING_APPLE_RELEASE" | "PENDING_CONTRACT" | "PENDING_DEVELOPER_RELEASE" | "PREPARE_FOR_SUBMISSION" | "PREORDER_READY_FOR_SALE" | "PROCESSING_FOR_APP_STORE" | "READY_FOR_REVIEW" | "READY_FOR_SALE" | "REJECTED" | "REMOVED_FROM_SALE" | "WAITING_FOR_EXPORT_COMPLIANCE" | "WAITING_FOR_REVIEW" | "REPLACED_WITH_NEW_VERSION" | "NOT_APPLICABLE" | null;
          /** Age rating App Store Connect derived from the age rating declaration. */
          appStoreAgeRating?: "L" | "ALL" | "ZERO_ZERO" | "ONE_PLUS" | "TWO_PLUS" | "THREE_PLUS" | "FOUR_PLUS" | "FIVE_PLUS" | "SIX_PLUS" | "SEVEN_PLUS" | "EIGHT_PLUS" | "NINE_PLUS" | "TEN_PLUS" | "ELEVEN_PLUS" | "TWELVE_PLUS" | "THIRTEEN_PLUS" | "FOURTEEN_PLUS" | "FIFTEEN_PLUS" | "SIXTEEN_PLUS" | "SEVENTEEN_PLUS" | "EIGHTEEN_PLUS" | "NINETEEN_PLUS" | "TWENTY_PLUS" | "TWENTY_ONE_PLUS" | "UNRATED" | null;
          /** Age rating shown on the Brazil storefront. */
          brazilAgeRating?: "L" | "TEN" | "TWELVE" | "FOURTEEN" | "SIXTEEN" | "EIGHTEEN" | null;
          /** Kids age band when the app is in the Kids category, otherwise null. */
          kidsAgeBand?: "FIVE_AND_UNDER" | "SIX_TO_EIGHT" | "NINE_TO_ELEVEN" | null;
          /** Identifier of the primary App Store category, such as GAMES, or null when none is set. */
          primaryCategoryId: string | null;
          /** Identifier of the first primary subcategory, used when the primary category is Games or Stickers. */
          primarySubcategoryOneId: string | null;
          /** Identifier of the second primary subcategory, or null when none is set. */
          primarySubcategoryTwoId: string | null;
          /** Identifier of the secondary App Store category, or null when none is set. */
          secondaryCategoryId: string | null;
          /** Identifier of the first secondary subcategory, or null when none is set. */
          secondarySubcategoryOneId: string | null;
          /** Identifier of the second secondary subcategory, or null when none is set. */
          secondarySubcategoryTwoId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the price points in other territories that App Store Connect considers equivalent to one price point, optionally narrowed to some territories. Use it to pick matching manual prices across territories. */
    "app_store_connect.list_app_price_point_equalizations": {
      input: {
        /**
         * App Store Connect identifier of the price point, from list_app_price_points.
         * @minLength 1
         * @pattern \S
         */
        appPricePointId: string;
        /**
         * Return only equalized price points for these territories.
         * @minItems 1
         */
        territoryIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Equalized price points returned for this page. */
        appPricePoints: Array<{
          /** App Store Connect identifier for the price point. It encodes the app, the territory and the price tier, so it is what create_app_price_schedule takes. */
          id: string;
          /** Price customers pay in the territory currency, as a decimal string. */
          customerPrice?: string | null;
          /** Developer proceeds in the territory currency, as a decimal string. */
          proceeds?: string | null;
          /** Territory the price point belongs to, such as USA. */
          territoryId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the price points an app can be sold at, optionally narrowed to some territories. Each price point carries the customer price and developer proceeds in the territory currency; pass its identifier to create_app_price_schedule. */
    "app_store_connect.list_app_price_points": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only price points for these territories.
         * @minItems 1
         */
        territoryIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Price points returned for this page. */
        appPricePoints: Array<{
          /** App Store Connect identifier for the price point. It encodes the app, the territory and the price tier, so it is what create_app_price_schedule takes. */
          id: string;
          /** Price customers pay in the territory currency, as a decimal string. */
          customerPrice?: string | null;
          /** Developer proceeds in the territory currency, as a decimal string. */
          proceeds?: string | null;
          /** Territory the price point belongs to, such as USA. */
          territoryId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the automatic prices of a price schedule, which App Store Connect derives from the base territory price for every territory without a manual price. Each entry carries its price point, territory, customer price and proceeds. */
    "app_store_connect.list_app_price_schedule_automatic_prices": {
      input: {
        /**
         * App Store Connect identifier of the price schedule, from get_app_price_schedule.
         * @minLength 1
         * @pattern \S
         */
        appPriceScheduleId: string;
        /**
         * Return only prices for these territories.
         * @minItems 1
         */
        territoryIds?: Array<string>;
        /**
         * Return only prices that start on this day, as YYYY-MM-DD.
         * @format date
         */
        startDate?: string;
        /**
         * Return only prices that end on this day, as YYYY-MM-DD.
         * @format date
         */
        endDate?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Automatic prices returned for this page. */
        automaticPrices: Array<{
          /** App Store Connect identifier for the app price. */
          id: string;
          /** True for a price set manually, false for one derived automatically from the base territory price. */
          manual?: boolean | null;
          /** Day the price starts applying, as YYYY-MM-DD, or null for the price in effect since the schedule was created. */
          startDate?: string | null;
          /** Day the price stops applying, as YYYY-MM-DD (the next scheduled price starts that day), or null when it applies indefinitely. */
          endDate?: string | null;
          /** App Store Connect identifier of the price point the price uses. */
          appPricePointId: string | null;
          /** Territory the price applies to, such as USA. */
          territoryId: string | null;
          /** Price customers pay in the territory currency, taken from the price point, as a decimal string. */
          customerPrice: string | null;
          /** Developer proceeds in the territory currency, taken from the price point, as a decimal string. */
          proceeds: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the manual prices of a price schedule, each with the price point it uses, the territory it applies to and the customer price and proceeds of that price point. */
    "app_store_connect.list_app_price_schedule_manual_prices": {
      input: {
        /**
         * App Store Connect identifier of the price schedule, from get_app_price_schedule.
         * @minLength 1
         * @pattern \S
         */
        appPriceScheduleId: string;
        /**
         * Return only prices for these territories.
         * @minItems 1
         */
        territoryIds?: Array<string>;
        /**
         * Return only prices that start on this day, as YYYY-MM-DD.
         * @format date
         */
        startDate?: string;
        /**
         * Return only prices that end on this day, as YYYY-MM-DD.
         * @format date
         */
        endDate?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Manual prices returned for this page. */
        manualPrices: Array<{
          /** App Store Connect identifier for the app price. */
          id: string;
          /** True for a price set manually, false for one derived automatically from the base territory price. */
          manual?: boolean | null;
          /** Day the price starts applying, as YYYY-MM-DD, or null for the price in effect since the schedule was created. */
          startDate?: string | null;
          /** Day the price stops applying, as YYYY-MM-DD (the next scheduled price starts that day), or null when it applies indefinitely. */
          endDate?: string | null;
          /** App Store Connect identifier of the price point the price uses. */
          appPricePointId: string | null;
          /** Territory the price applies to, such as USA. */
          territoryId: string | null;
          /** Price customers pay in the territory currency, taken from the price point, as a decimal string. */
          customerPrice: string | null;
          /** Developer proceeds in the territory currency, taken from the price point, as a decimal string. */
          proceeds: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the locales of one treatment, optionally narrowed to some locales. Screenshots and previews of a treatment are attached to these localizations. */
    "app_store_connect.list_app_store_version_experiment_treatment_localizations": {
      input: {
        /**
         * App Store Connect identifier of the treatment.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentTreatmentId: string;
        /**
         * Return only localizations for these locales.
         * @minItems 1
         */
        locales?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Treatment localizations returned for this page. */
        appStoreVersionExperimentTreatmentLocalizations: Array<{
          /** App Store Connect identifier for the treatment localization. */
          id: string;
          /** Locale the treatment metadata is shown in, such as en-US. */
          locale?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the treatments of one product page optimization test. */
    "app_store_connect.list_app_store_version_experiment_treatments": {
      input: {
        /**
         * App Store Connect identifier of the product page optimization test.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Treatments returned for this page. */
        appStoreVersionExperimentTreatments: Array<{
          /** App Store Connect identifier for the treatment. */
          id: string;
          /** Treatment name shown in App Store Connect. */
          name?: string | null;
          /** Name of the alternate app icon asset the treatment uses, or null for the default icon. */
          appIconName?: string | null;
          /** Template URL and pixel size of the treatment app icon. */
          appIcon?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Icon width in pixels. */
            width?: number;
            /** Icon height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** When the treatment was promoted to the default product page, as an ISO 8601 timestamp, or null. */
          promotedDate?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the product page optimization tests of one app, optionally narrowed to some states, with the App Store version each test uses as its control. */
    "app_store_connect.list_app_store_version_experiments": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only tests in these states.
         * @minItems 1
         */
        states?: Array<"PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REJECTED" | "COMPLETED" | "STOPPED">;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Product page optimization tests returned for this page. */
        appStoreVersionExperiments: Array<{
          /** App Store Connect identifier for the experiment. */
          id: string;
          /** Test name shown in App Store Connect. */
          name?: string | null;
          /** Content platform the test runs on. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Percentage of App Store traffic directed to the treatments instead of the default product page. */
          trafficProportion?: number | null;
          /** Review and run state of the test. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REJECTED" | "COMPLETED" | "STOPPED" | null;
          /** Whether the treatments must pass App Review before the test can start. */
          reviewRequired?: boolean | null;
          /** When the test started, as an ISO 8601 timestamp. */
          startDate?: string | null;
          /** When the test ended or was stopped, as an ISO 8601 timestamp. */
          endDate?: string | null;
          /** App Store Connect identifier of the App Store version currently serving as the control (the default product page), or null when App Store Connect did not return it. */
          latestControlVersionId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the localized App Store metadata (description, keywords, release notes, URLs) of one App Store version, optionally narrowed to specific locales. */
    "app_store_connect.list_app_store_version_localizations": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
        /**
         * Return only the localizations for these locales.
         * @minItems 1
         */
        locales?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Version localizations returned for this page. */
        appStoreVersionLocalizations: Array<{
          /** App Store Connect identifier for the version localization. */
          id: string;
          /** Locale the metadata is written in, such as en-US. */
          locale?: string | null;
          /** App description shown on the product page. */
          description?: string | null;
          /** Comma-separated App Store search keywords. */
          keywords?: string | null;
          /** Marketing website URL shown on the product page. */
          marketingUrl?: string | null;
          /** Promotional text shown above the description, editable without a new version. */
          promotionalText?: string | null;
          /** Support website URL shown on the product page. */
          supportUrl?: string | null;
          /** Release notes shown for this version. */
          whatsNew?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the App Store versions of one app, with the review and release state of each version. */
    "app_store_connect.list_app_store_versions": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Return only versions for this content platform. */
        platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
        /**
         * Return only the version with this exact version string.
         * @minLength 1
         * @pattern \S
         */
        versionString?: string;
        /** Return only versions in this review and release state. */
        appVersionState?: "ACCEPTED" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "INVALID_BINARY" | "METADATA_REJECTED" | "PENDING_APPLE_RELEASE" | "PENDING_DEVELOPER_RELEASE" | "PREPARE_FOR_SUBMISSION" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "READY_FOR_REVIEW" | "REJECTED" | "REPLACED_WITH_NEW_VERSION" | "WAITING_FOR_EXPORT_COMPLIANCE" | "WAITING_FOR_REVIEW";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** App Store versions returned for this page. */
        appStoreVersions: Array<{
          /** App Store Connect identifier for the version. */
          id: string;
          /** Content platform the version targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Version string shown on the App Store, such as 1.4.0. */
          versionString?: string | null;
          /** Current review and release state. */
          appVersionState?: "ACCEPTED" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "INVALID_BINARY" | "METADATA_REJECTED" | "PENDING_APPLE_RELEASE" | "PENDING_DEVELOPER_RELEASE" | "PREPARE_FOR_SUBMISSION" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "READY_FOR_REVIEW" | "REJECTED" | "REPLACED_WITH_NEW_VERSION" | "WAITING_FOR_EXPORT_COMPLIANCE" | "WAITING_FOR_REVIEW" | null;
          /** Copyright line published with the version. */
          copyright?: string | null;
          /** Review track the version goes through. */
          reviewType?: "APP_STORE" | "NOTARIZATION" | null;
          /** Release behavior after approval. */
          releaseType?: "MANUAL" | "AFTER_APPROVAL" | "SCHEDULED" | null;
          /** Earliest scheduled release time, as an ISO 8601 timestamp. */
          earliestReleaseDate?: string | null;
          /** Whether the version is downloadable. */
          downloadable?: boolean | null;
          /** When the version record was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the tags App Store Connect assigned to an app for App Store discovery, optionally only those shown or hidden on the App Store. */
    "app_store_connect.list_app_tags": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Return only tags shown on the App Store when true, or only hidden tags when false. */
        visibleInAppStore?: boolean;
        /** Sort order for the returned tags. */
        sort?: "name" | "-name";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** App tags returned for this page. */
        appTags: Array<{
          /** App Store Connect identifier for the app tag. */
          id: string;
          /** Tag name. */
          name?: string | null;
          /** Whether the tag is shown on the App Store. */
          visibleInAppStore?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the apps the API key can see, optionally filtered by bundle identifier, name, or SKU. */
    "app_store_connect.list_apps": {
      input: {
        /**
         * Return only the app with this exact bundle identifier.
         * @minLength 1
         * @pattern \S
         */
        bundleId?: string;
        /**
         * Return only apps with this exact name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * Return only the app with this exact SKU.
         * @minLength 1
         * @pattern \S
         */
        sku?: string;
        /** Sort order for the returned apps. */
        sort?: "name" | "-name" | "bundleId" | "-bundleId" | "sku" | "-sku";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Apps returned for this page. */
        apps: Array<{
          /** App Store Connect identifier for the app. */
          id: string;
          /** App name shown on the App Store. */
          name?: string | null;
          /** Bundle identifier registered for the app. */
          bundleId?: string | null;
          /** SKU chosen when the app record was created. */
          sku?: string | null;
          /** Primary App Store locale, such as en-US. */
          primaryLocale?: string | null;
          /** Whether the app is or has ever been part of the Kids category. */
          isOrEverWasMadeForKids?: boolean | null;
          /** Third-party content rights declared for the app. */
          contentRightsDeclaration?: "DOES_NOT_USE_THIRD_PARTY_CONTENT" | "USES_THIRD_PARTY_CONTENT" | null;
          /** Whether streamlined purchasing is enabled for the app. */
          streamlinedPurchasingEnabled?: boolean | null;
          /** Accessibility information URL published with the app. */
          accessibilityUrl?: string | null;
          /** Production server-to-server subscription status URL. */
          subscriptionStatusUrl?: string | null;
          /** Version of the production subscription status URL. */
          subscriptionStatusUrlVersion?: "V1" | "V2" | null;
          /** Sandbox server-to-server subscription status URL. */
          subscriptionStatusUrlForSandbox?: string | null;
          /** Version of the sandbox subscription status URL. */
          subscriptionStatusUrlVersionForSandbox?: "V1" | "V2" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the versions of one background asset pack with their processing state and their internal TestFlight, external TestFlight, and App Store release records. */
    "app_store_connect.list_background_asset_versions": {
      input: {
        /**
         * App Store Connect identifier of the background asset pack.
         * @minLength 1
         * @pattern \S
         */
        backgroundAssetId: string;
        /**
         * Return only the version with this exact version number.
         * @minLength 1
         * @pattern \S
         */
        version?: string;
        /** Return only versions in this processing state. */
        state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE";
        /**
         * Return only versions declared for this locale.
         * @minLength 1
         * @pattern \S
         */
        locale?: string;
        /** Return only versions that target this platform. */
        platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
        /** Return only versions whose internal TestFlight release is in this state. */
        internalBetaReleaseState?: "READY_FOR_TESTING" | "SUPERSEDED";
        /** Return only versions whose external TestFlight release is in this state. */
        externalBetaReleaseState?: "READY_FOR_BETA_SUBMISSION" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "PROCESSING_FOR_TESTING" | "READY_FOR_TESTING" | "SUPERSEDED";
        /** Return only versions whose App Store release is in this state. */
        appStoreReleaseState?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "REJECTED" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "SUPERSEDED";
        /** Sort order for the returned versions. */
        sort?: "version" | "-version";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Background asset versions returned for this page. */
        backgroundAssetVersions: Array<{
          /** App Store Connect identifier of the background asset version. */
          id: string;
          /** Version number App Store Connect assigned when the version was created; it increases automatically. */
          version?: string | null;
          /** Upload and processing state of the version. AWAITING_UPLOAD until the asset pack archive is uploaded and committed. */
          state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
          /** Validation errors, warnings, and informational messages from processing. */
          stateDetails?: {
            /** Errors that stopped processing. */
            errors?: Array<{
              /** Machine-readable code of the message. */
              code?: string | null;
              /** Human-readable text of the message. */
              description?: string | null;
              [key: string]: unknown;
            }>;
            /** Warnings raised during processing. */
            warnings?: Array<{
              /** Machine-readable code of the message. */
              code?: string | null;
              /** Human-readable text of the message. */
              description?: string | null;
              [key: string]: unknown;
            }>;
            /** Informational messages from processing. */
            infos?: Array<{
              /** Machine-readable code of the message. */
              code?: string | null;
              /** Human-readable text of the message. */
              description?: string | null;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          } | null;
          /** Locale declared in the asset pack manifest, when the pack is localized. */
          locale?: string | null;
          /** Platforms the asset pack version targets, taken from the uploaded manifest. */
          platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
          /** When the version was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** The asset pack the version belongs to, or null when App Store Connect did not return it. */
          backgroundAsset: {
            /** App Store Connect identifier of the background asset pack. */
            id: string;
            /** Asset pack identifier the app uses to request the pack. */
            assetPackIdentifier?: string | null;
            [key: string]: unknown;
          } | null;
          /** The internal TestFlight release of this version. Null when the version has no such release yet or App Store Connect did not return it. */
          internalBetaRelease: {
            /** App Store Connect identifier of the release record. */
            id: string;
            /** State of the release. */
            state?: "READY_FOR_TESTING" | "SUPERSEDED" | null;
            [key: string]: unknown;
          } | null;
          /** The external TestFlight release of this version. Null when the version has no such release yet or App Store Connect did not return it. */
          externalBetaRelease: {
            /** App Store Connect identifier of the release record. */
            id: string;
            /** State of the release. */
            state?: "READY_FOR_BETA_SUBMISSION" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "PROCESSING_FOR_TESTING" | "READY_FOR_TESTING" | "SUPERSEDED" | null;
            [key: string]: unknown;
          } | null;
          /** The App Store release of this version. Null when the version has no such release yet or App Store Connect did not return it. */
          appStoreRelease: {
            /** App Store Connect identifier of the release record. */
            id: string;
            /** State of the release. */
            state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "REJECTED" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "SUPERSEDED" | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Apple-hosted background asset packs of one app, with the version currently in each of the App Store, internal TestFlight, and external TestFlight slots. */
    "app_store_connect.list_background_assets": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only the asset pack with this exact asset pack identifier.
         * @minLength 1
         * @pattern \S
         */
        assetPackIdentifier?: string;
        /** Return only archived asset packs when true, or only active ones when false. */
        archived?: boolean;
        /**
         * Return only asset packs that have a version declared for this locale.
         * @minLength 1
         * @pattern \S
         */
        versionLocale?: string;
        /** Return only asset packs that have a version targeting this platform. */
        versionPlatform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
        /** Sort order for the returned asset packs. */
        sort?: "assetPackIdentifier" | "-assetPackIdentifier" | "createdDate" | "-createdDate";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Background asset packs returned for this page. */
        backgroundAssets: Array<{
          /** App Store Connect identifier of the background asset pack. */
          id: string;
          /** Asset pack identifier the app uses to request the pack, chosen when the pack was created. */
          assetPackIdentifier?: string | null;
          /** Whether the asset pack has been archived. */
          archived?: boolean | null;
          /** When the asset pack record was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Storage in bytes the asset pack occupies. */
          usedBytes?: number | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          appStoreVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          internalBetaVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          externalBetaVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the App Clip invocations testers can launch from TestFlight for one App Clip build bundle, with the localized titles of each. Find build bundle identifiers with list_build_bundles. */
    "app_store_connect.list_beta_app_clip_invocations": {
      input: {
        /**
         * App Store Connect identifier of the App Clip build bundle.
         * @minLength 1
         * @pattern \S
         */
        buildBundleId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** App Clip invocations returned for this page. */
        betaAppClipInvocations: Array<{
          /** App Store Connect identifier for the App Clip invocation. */
          id: string;
          /** Invocation URL the App Clip is launched with from TestFlight. */
          url?: string | null;
          /** Localized titles App Store Connect returned alongside the invocation. */
          betaAppClipInvocationLocalizations: Array<{
            /** App Store Connect identifier for the localization. */
            id: string;
            /** Title testers see for the App Clip invocation in this locale. */
            title?: string | null;
            /** Locale the title is written in, such as en-US. */
            locale?: string | null;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the TestFlight test information of one app, one record per locale, optionally narrowed to a single locale. */
    "app_store_connect.list_beta_app_localizations": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only the test information written in this locale, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Test information records returned for this page. */
        betaAppLocalizations: Array<{
          /** App Store Connect identifier for the beta app localization. */
          id: string;
          /** Locale the test information is written in, such as en-US. */
          locale?: string | null;
          /** Description of the app shown to testers in TestFlight. */
          description?: string | null;
          /** Email address testers send feedback to. */
          feedbackEmail?: string | null;
          /** Marketing URL shown to testers. */
          marketingUrl?: string | null;
          /** Privacy policy URL shown to testers. */
          privacyPolicyUrl?: string | null;
          /** Privacy policy text shown to tvOS testers. */
          tvOsPrivacyPolicy?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the TestFlight beta review submissions of one build, optionally narrowed to one review state. */
    "app_store_connect.list_beta_app_review_submissions": {
      input: {
        /**
         * App Store Connect identifier of the build whose submissions to list.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /** Return only submissions in this review state. */
        betaReviewState?: "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "APPROVED";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Beta review submissions returned for this page. */
        betaAppReviewSubmissions: Array<{
          /** App Store Connect identifier for the beta app review submission. */
          id: string;
          /** State of the TestFlight beta review. */
          betaReviewState?: "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "APPROVED" | null;
          /** When the build was submitted for beta review, as an ISO 8601 timestamp. */
          submittedDate?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the "What to Test" notes of one build, one record per locale, optionally narrowed to a single locale. */
    "app_store_connect.list_beta_build_localizations": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /**
         * Return only the notes written in this locale, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Test notes returned for this page. */
        betaBuildLocalizations: Array<{
          /** App Store Connect identifier for the beta build localization. */
          id: string;
          /** Locale the test notes belong to, such as en-US. */
          locale?: string | null;
          /** Test notes shown to testers for this locale. */
          whatsNew?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the crash feedback testers sent from TestFlight for one app, with the build and tester each submission came from. Use get_beta_feedback_crash_log to read the crash log text of one submission. */
    "app_store_connect.list_beta_feedback_crash_submissions": {
      input: {
        /**
         * App Store Connect identifier of the app whose feedback to list.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only feedback sent from these builds.
         * @minItems 1
         */
        buildIds?: Array<string>;
        /**
         * Return only feedback sent from builds under these prerelease versions.
         * @minItems 1
         */
        preReleaseVersionIds?: Array<string>;
        /**
         * Return only feedback sent by these testers.
         * @minItems 1
         */
        betaTesterIds?: Array<string>;
        /**
         * Return only feedback sent from these device models.
         * @minItems 1
         */
        deviceModels?: Array<string>;
        /**
         * Return only feedback sent from these OS versions.
         * @minItems 1
         */
        osVersions?: Array<string>;
        /**
         * Return only feedback for apps built for these platforms.
         * @minItems 1
         */
        appPlatforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS">;
        /**
         * Return only feedback sent from devices running these platforms.
         * @minItems 1
         */
        devicePlatforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS">;
        /** Sort order for the returned feedback. */
        sort?: "createdDate" | "-createdDate";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Crash feedback submissions returned for this page. */
        betaFeedbackCrashSubmissions: Array<{
          /** App Store Connect identifier for the crash feedback submission. */
          id: string;
          /** When the tester sent the feedback, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Free-text comment the tester wrote. */
          comment?: string | null;
          /** Email address of the tester who sent the feedback. */
          email?: string | null;
          /** Device model identifier, such as iPhone16,2. */
          deviceModel?: string | null;
          /** Operating system version on the device. */
          osVersion?: string | null;
          /** Locale the device was set to. */
          locale?: string | null;
          /** Time zone the device was set to. */
          timeZone?: string | null;
          /** CPU architecture of the device. */
          architecture?: string | null;
          /** Network connection the device was using. */
          connectionType?: "WIFI" | "MOBILE_DATA" | "WIRE" | "UNKNOWN" | "NONE" | null;
          /** Model of the Apple Watch paired with the device, if any. */
          pairedAppleWatch?: string | null;
          /** How long the app had been running when the feedback was sent, in milliseconds. */
          appUptimeInMilliseconds?: number | null;
          /** Free disk space on the device, in bytes. */
          diskBytesAvailable?: number | null;
          /** Total disk space on the device, in bytes. */
          diskBytesTotal?: number | null;
          /** Battery level of the device, as a percentage. */
          batteryPercentage?: number | null;
          /** Screen width of the device, in points. */
          screenWidthInPoints?: number | null;
          /** Screen height of the device, in points. */
          screenHeightInPoints?: number | null;
          /** Platform the app was built for. */
          appPlatform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Platform of the device the app ran on. */
          devicePlatform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Device family the feedback came from. */
          deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION" | null;
          /** App Store Connect identifier of the build bundle the feedback was sent from. */
          buildBundleId?: string | null;
          /** The build the feedback was sent from, or null when App Store Connect did not return it. */
          build: {
            /** App Store Connect identifier for the build. */
            id: string;
            /** Build number, such as 42. */
            version?: string | null;
            [key: string]: unknown;
          } | null;
          /** The TestFlight tester the record belongs to, or null when App Store Connect did not return it. */
          tester: {
            /** App Store Connect identifier for the beta tester. */
            id: string;
            /** Email address the tester was invited with. */
            email?: string | null;
            /** Tester first name. */
            firstName?: string | null;
            /** Tester last name. */
            lastName?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the screenshot feedback testers sent from TestFlight for one app, with the build and tester each submission came from. Screenshot download URLs expire, so fetch them soon after listing. */
    "app_store_connect.list_beta_feedback_screenshot_submissions": {
      input: {
        /**
         * App Store Connect identifier of the app whose feedback to list.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only feedback sent from these builds.
         * @minItems 1
         */
        buildIds?: Array<string>;
        /**
         * Return only feedback sent from builds under these prerelease versions.
         * @minItems 1
         */
        preReleaseVersionIds?: Array<string>;
        /**
         * Return only feedback sent by these testers.
         * @minItems 1
         */
        betaTesterIds?: Array<string>;
        /**
         * Return only feedback sent from these device models.
         * @minItems 1
         */
        deviceModels?: Array<string>;
        /**
         * Return only feedback sent from these OS versions.
         * @minItems 1
         */
        osVersions?: Array<string>;
        /**
         * Return only feedback for apps built for these platforms.
         * @minItems 1
         */
        appPlatforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS">;
        /**
         * Return only feedback sent from devices running these platforms.
         * @minItems 1
         */
        devicePlatforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS">;
        /** Sort order for the returned feedback. */
        sort?: "createdDate" | "-createdDate";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Screenshot feedback submissions returned for this page. */
        betaFeedbackScreenshotSubmissions: Array<{
          /** App Store Connect identifier for the screenshot feedback submission. */
          id: string;
          /** When the tester sent the feedback, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Free-text comment the tester wrote. */
          comment?: string | null;
          /** Email address of the tester who sent the feedback. */
          email?: string | null;
          /** Device model identifier, such as iPhone16,2. */
          deviceModel?: string | null;
          /** Operating system version on the device. */
          osVersion?: string | null;
          /** Locale the device was set to. */
          locale?: string | null;
          /** Time zone the device was set to. */
          timeZone?: string | null;
          /** CPU architecture of the device. */
          architecture?: string | null;
          /** Network connection the device was using. */
          connectionType?: "WIFI" | "MOBILE_DATA" | "WIRE" | "UNKNOWN" | "NONE" | null;
          /** Model of the Apple Watch paired with the device, if any. */
          pairedAppleWatch?: string | null;
          /** How long the app had been running when the feedback was sent, in milliseconds. */
          appUptimeInMilliseconds?: number | null;
          /** Free disk space on the device, in bytes. */
          diskBytesAvailable?: number | null;
          /** Total disk space on the device, in bytes. */
          diskBytesTotal?: number | null;
          /** Battery level of the device, as a percentage. */
          batteryPercentage?: number | null;
          /** Screen width of the device, in points. */
          screenWidthInPoints?: number | null;
          /** Screen height of the device, in points. */
          screenHeightInPoints?: number | null;
          /** Platform the app was built for. */
          appPlatform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Platform of the device the app ran on. */
          devicePlatform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Device family the feedback came from. */
          deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION" | null;
          /** App Store Connect identifier of the build bundle the feedback was sent from. */
          buildBundleId?: string | null;
          /** The build the feedback was sent from, or null when App Store Connect did not return it. */
          build: {
            /** App Store Connect identifier for the build. */
            id: string;
            /** Build number, such as 42. */
            version?: string | null;
            [key: string]: unknown;
          } | null;
          /** The TestFlight tester the record belongs to, or null when App Store Connect did not return it. */
          tester: {
            /** App Store Connect identifier for the beta tester. */
            id: string;
            /** Email address the tester was invited with. */
            email?: string | null;
            /** Tester first name. */
            firstName?: string | null;
            /** Tester last name. */
            lastName?: string | null;
            [key: string]: unknown;
          } | null;
          /** Screenshots attached to the feedback, each with a time-limited download URL. */
          screenshots?: Array<{
            /** Download URL for the screenshot; it expires at expirationDate. */
            url?: string;
            /** Screenshot width in pixels. */
            width?: number;
            /** Screenshot height in pixels. */
            height?: number;
            /** When the download URL stops working, as an ISO 8601 timestamp. */
            expirationDate?: string;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the builds one TestFlight group can install, with the prerelease version each build belongs to. */
    "app_store_connect.list_beta_group_builds": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Builds returned for this page. */
        builds: Array<{
          /** App Store Connect identifier for the build. */
          id: string;
          /** Build number, such as 42. */
          version?: string | null;
          /** When the build finished uploading, as an ISO 8601 timestamp. */
          uploadedDate?: string | null;
          /** When the build stops being installable by testers. */
          expirationDate?: string | null;
          /** Whether the build has expired for TestFlight. */
          expired?: boolean | null;
          /** Processing state of the uploaded build. */
          processingState?: "PROCESSING" | "FAILED" | "INVALID" | "VALID" | null;
          /** Distribution audience the build was uploaded for. */
          buildAudienceType?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE" | null;
          /** The prerelease version a build belongs to, or null when it was not returned. */
          preReleaseVersion: {
            /** App Store Connect identifier for the prerelease version. */
            id: string;
            /** Marketing version string, such as 1.4.0. */
            version?: string | null;
            /** Content platform the version targets. */
            platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
            [key: string]: unknown;
          } | null;
          /** Minimum OS version the build supports. */
          minOsVersion?: string | null;
          /** Minimum macOS system version declared by the build. */
          lsMinimumSystemVersion?: string | null;
          /** Minimum macOS version App Store Connect computed for the build. */
          computedMinMacOsVersion?: string | null;
          /** Minimum visionOS version App Store Connect computed for the build. */
          computedMinVisionOsVersion?: string | null;
          /** Whether the build declares non-exempt encryption. */
          usesNonExemptEncryption?: boolean | null;
          /** Template URL and pixel size of the build icon asset. */
          iconAssetToken?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Icon width in pixels. */
            width?: number;
            /** Icon height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the TestFlight groups of one app, including the public invitation link of each group. */
    "app_store_connect.list_beta_groups": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only the group with this exact name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** Return only internal groups when true, or only external groups when false. */
        isInternalGroup?: boolean;
        /** Return only groups whose public link is enabled or disabled. */
        publicLinkEnabled?: boolean;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** TestFlight groups returned for this page. */
        betaGroups: Array<{
          /** App Store Connect identifier for the beta group. */
          id: string;
          /** Group name shown in TestFlight. */
          name?: string | null;
          /** When the group was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the group is an internal group of team members. */
          isInternalGroup?: boolean | null;
          /** Whether the group automatically receives every new build. */
          hasAccessToAllBuilds?: boolean | null;
          /** Whether a public TestFlight link is enabled for the group. */
          publicLinkEnabled?: boolean | null;
          /** Identifier segment of the public TestFlight link. */
          publicLinkId?: string | null;
          /** Full public TestFlight invitation link. */
          publicLink?: string | null;
          /** Whether the public link enforces a tester limit. */
          publicLinkLimitEnabled?: boolean | null;
          /** Maximum number of testers who may join through the public link. */
          publicLinkLimit?: number | null;
          /** Whether testers can send feedback from TestFlight. */
          feedbackEnabled?: boolean | null;
          /** Whether iOS builds are offered to Apple silicon Macs. */
          iosBuildsAvailableForAppleSiliconMac?: boolean | null;
          /** Whether iOS builds are offered to Apple Vision Pro. */
          iosBuildsAvailableForAppleVision?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the device families and OS versions App Store Connect accepts in recruitment criteria, to pick valid minimumOsInclusive and maximumOsInclusive values. */
    "app_store_connect.list_beta_recruitment_criterion_options": {
      input: {
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Option sets returned for this page. */
        betaRecruitmentCriterionOptions: Array<{
          /** App Store Connect identifier for the option set. */
          id: string;
          /** Selectable OS versions per device family. */
          deviceFamilyOsVersions?: Array<{
            /** Device family. */
            deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION";
            /** OS versions that can be used as a minimum or maximum for this family. */
            osVersions?: Array<string>;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the apps one TestFlight tester has access to. */
    "app_store_connect.list_beta_tester_apps": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight tester.
         * @minLength 1
         * @pattern \S
         */
        betaTesterId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Apps returned for this page. */
        apps: Array<{
          /** App Store Connect identifier for the app. */
          id: string;
          /** App name shown on the App Store. */
          name?: string | null;
          /** Bundle identifier registered for the app. */
          bundleId?: string | null;
          /** SKU chosen when the app record was created. */
          sku?: string | null;
          /** Primary App Store locale, such as en-US. */
          primaryLocale?: string | null;
          /** Whether the app is or has ever been part of the Kids category. */
          isOrEverWasMadeForKids?: boolean | null;
          /** Third-party content rights declared for the app. */
          contentRightsDeclaration?: "DOES_NOT_USE_THIRD_PARTY_CONTENT" | "USES_THIRD_PARTY_CONTENT" | null;
          /** Whether streamlined purchasing is enabled for the app. */
          streamlinedPurchasingEnabled?: boolean | null;
          /** Accessibility information URL published with the app. */
          accessibilityUrl?: string | null;
          /** Production server-to-server subscription status URL. */
          subscriptionStatusUrl?: string | null;
          /** Version of the production subscription status URL. */
          subscriptionStatusUrlVersion?: "V1" | "V2" | null;
          /** Sandbox server-to-server subscription status URL. */
          subscriptionStatusUrlForSandbox?: string | null;
          /** Version of the sandbox subscription status URL. */
          subscriptionStatusUrlVersionForSandbox?: "V1" | "V2" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the TestFlight groups one tester belongs to. */
    "app_store_connect.list_beta_tester_beta_groups": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight tester.
         * @minLength 1
         * @pattern \S
         */
        betaTesterId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** TestFlight groups returned for this page. */
        betaGroups: Array<{
          /** App Store Connect identifier for the beta group. */
          id: string;
          /** Group name shown in TestFlight. */
          name?: string | null;
          /** When the group was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the group is an internal group of team members. */
          isInternalGroup?: boolean | null;
          /** Whether the group automatically receives every new build. */
          hasAccessToAllBuilds?: boolean | null;
          /** Whether a public TestFlight link is enabled for the group. */
          publicLinkEnabled?: boolean | null;
          /** Identifier segment of the public TestFlight link. */
          publicLinkId?: string | null;
          /** Full public TestFlight invitation link. */
          publicLink?: string | null;
          /** Whether the public link enforces a tester limit. */
          publicLinkLimitEnabled?: boolean | null;
          /** Maximum number of testers who may join through the public link. */
          publicLinkLimit?: number | null;
          /** Whether testers can send feedback from TestFlight. */
          feedbackEnabled?: boolean | null;
          /** Whether iOS builds are offered to Apple silicon Macs. */
          iosBuildsAvailableForAppleSiliconMac?: boolean | null;
          /** Whether iOS builds are offered to Apple Vision Pro. */
          iosBuildsAvailableForAppleVision?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List TestFlight testers, optionally narrowed to one app, group, or build. */
    "app_store_connect.list_beta_testers": {
      input: {
        /**
         * Return only the tester with this exact email address.
         * @format email
         */
        email?: string;
        /**
         * Return only testers with this exact first name.
         * @minLength 1
         * @pattern \S
         */
        firstName?: string;
        /**
         * Return only testers with this exact last name.
         * @minLength 1
         * @pattern \S
         */
        lastName?: string;
        /** Return only testers invited this way. */
        inviteType?: "EMAIL" | "PUBLIC_LINK";
        /**
         * Return only testers who have access to this app.
         * @minLength 1
         * @pattern \S
         */
        appId?: string;
        /**
         * Return only testers who belong to this TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId?: string;
        /**
         * Return only testers who were assigned this build individually.
         * @minLength 1
         * @pattern \S
         */
        buildId?: string;
        /** Sort order for the returned testers. */
        sort?: "firstName" | "-firstName" | "lastName" | "-lastName" | "email" | "-email" | "inviteType" | "-inviteType" | "state" | "-state";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** TestFlight testers returned for this page. */
        betaTesters: Array<{
          /** App Store Connect identifier for the beta tester. */
          id: string;
          /** Email address the invitation was sent to. */
          email?: string | null;
          /** Tester first name. */
          firstName?: string | null;
          /** Tester last name. */
          lastName?: string | null;
          /** How the tester was invited. */
          inviteType?: "EMAIL" | "PUBLIC_LINK" | null;
          /** Where the tester stands in the invitation flow. */
          state?: "NOT_INVITED" | "INVITED" | "ACCEPTED" | "INSTALLED" | "REVOKED" | null;
          /** Devices the tester has installed the app on. */
          appDevices?: Array<{
            /** Device model name. */
            model?: string;
            /** Platform of the device. */
            platform?: "IOS" | "MAC_OS" | "TV_OS" | "WATCH_OS" | "VISION_OS";
            /** Operating system version on the device. */
            osVersion?: string;
            /** Build number installed on the device. */
            appBuildVersion?: string;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the TestFlight groups that can install one build. */
    "app_store_connect.list_build_beta_groups": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** TestFlight groups returned for this page. */
        betaGroups: Array<{
          /** App Store Connect identifier for the beta group. */
          id: string;
          /** Group name shown in TestFlight. */
          name?: string | null;
          /** When the group was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the group is an internal group of team members. */
          isInternalGroup?: boolean | null;
          /** Whether the group automatically receives every new build. */
          hasAccessToAllBuilds?: boolean | null;
          /** Whether a public TestFlight link is enabled for the group. */
          publicLinkEnabled?: boolean | null;
          /** Identifier segment of the public TestFlight link. */
          publicLinkId?: string | null;
          /** Full public TestFlight invitation link. */
          publicLink?: string | null;
          /** Whether the public link enforces a tester limit. */
          publicLinkLimitEnabled?: boolean | null;
          /** Maximum number of testers who may join through the public link. */
          publicLinkLimit?: number | null;
          /** Whether testers can send feedback from TestFlight. */
          feedbackEnabled?: boolean | null;
          /** Whether iOS builds are offered to Apple silicon Macs. */
          iosBuildsAvailableForAppleSiliconMac?: boolean | null;
          /** Whether iOS builds are offered to Apple Vision Pro. */
          iosBuildsAvailableForAppleVision?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the download and install sizes of one build bundle per device model and OS version, as App Store Connect computed them after processing. */
    "app_store_connect.list_build_bundle_file_sizes": {
      input: {
        /**
         * App Store Connect identifier of the build bundle.
         * @minLength 1
         * @pattern \S
         */
        buildBundleId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** File size records returned for this page. */
        buildBundleFileSizes: Array<{
          /** App Store Connect identifier for the file size record. */
          id: string;
          /** Device model the sizes were computed for. */
          deviceModel?: string | null;
          /** OS version the sizes were computed for. */
          osVersion?: string | null;
          /** Download size in bytes. */
          downloadBytes?: number | null;
          /** Installed size in bytes. */
          installBytes?: number | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the bundles (app, App Clip, and watch app) contained in one uploaded build, with their bundle identifiers, architectures, entitlements, and dSYM download URL. Every bundle of the build is returned in one call: App Store Connect only exposes bundles as an include of the build, capped at 50, and offers no cursor. Build bundle identifiers are needed for the App Clip and file size actions. */
    "app_store_connect.list_build_bundles": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
      };
      output: {
        /** The build the bundles belong to. */
        buildId: string;
        /** Bundles contained in the build. */
        buildBundles: Array<{
          /** App Store Connect identifier for the build bundle. */
          id: string;
          /** Bundle identifier of the bundle. */
          bundleId?: string | null;
          /** What kind of bundle this is. */
          bundleType?: "APP" | "APP_CLIP" | "WATCH_APP" | null;
          /** SDK build the bundle was compiled with. */
          sdkBuild?: string | null;
          /** Platform build the bundle was compiled against. */
          platformBuild?: string | null;
          /** File name of the bundle inside the build. */
          fileName?: string | null;
          /** Whether the bundle uses SiriKit. */
          hasSirikit?: boolean | null;
          /** Whether the bundle uses on-demand resources. */
          hasOnDemandResources?: boolean | null;
          /** Whether the bundle ships a prerendered icon. */
          hasPrerenderedIcon?: boolean | null;
          /** Whether the bundle uses location services. */
          usesLocationServices?: boolean | null;
          /** Whether the iOS bundle can be offered on the Mac App Store. */
          isIosBuildMacAppStoreCompatible?: boolean | null;
          /** Whether symbols were uploaded with the bundle. */
          includesSymbols?: boolean | null;
          /** Download URL for the dSYM archive, when symbols were uploaded. */
          dSYMUrl?: string | null;
          /** CPU architectures the bundle supports. */
          supportedArchitectures?: Array<string> | null;
          /** Device capabilities the bundle requires. */
          requiredCapabilities?: Array<string> | null;
          /** External accessory protocols the bundle declares. */
          deviceProtocols?: Array<string> | null;
          /** Locales the bundle is localized for. */
          locales?: Array<string> | null;
          /** Entitlements declared by the bundle, keyed by entitlement name. */
          entitlements?: Record<string, unknown> | null;
          /** Background asset download allowance declared by the bundle, in bytes. */
          baDownloadAllowance?: number | null;
          /** Maximum background asset install size declared by the bundle, in bytes. */
          baMaxInstallSize?: number | null;
          /** Minimum OS version the bundle supports. */
          minimumOsVersion?: string | null;
          [key: string]: unknown;
        }>;
      };
    };
    /** List the TestFlight testers who were assigned one build individually, outside of any group. */
    "app_store_connect.list_build_individual_testers": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Individual testers returned for this page. */
        betaTesters: Array<{
          /** App Store Connect identifier for the beta tester. */
          id: string;
          /** Email address the invitation was sent to. */
          email?: string | null;
          /** Tester first name. */
          firstName?: string | null;
          /** Tester last name. */
          lastName?: string | null;
          /** How the tester was invited. */
          inviteType?: "EMAIL" | "PUBLIC_LINK" | null;
          /** Where the tester stands in the invitation flow. */
          state?: "NOT_INVITED" | "INVITED" | "ACCEPTED" | "INSTALLED" | "REVOKED" | null;
          /** Devices the tester has installed the app on. */
          appDevices?: Array<{
            /** Device model name. */
            model?: string;
            /** Platform of the device. */
            platform?: "IOS" | "MAC_OS" | "TV_OS" | "WATCH_OS" | "VISION_OS";
            /** Operating system version on the device. */
            osVersion?: string;
            /** Build number installed on the device. */
            appBuildVersion?: string;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List builds uploaded for one app, with the prerelease version each build belongs to. Filter by version, platform, processing state, or TestFlight review state. */
    "app_store_connect.list_builds": {
      input: {
        /**
         * App Store Connect identifier of the app whose builds to list.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only builds with this build number, such as 42.
         * @minLength 1
         * @pattern \S
         */
        version?: string;
        /**
         * Return only builds under this marketing version, such as 1.4.0.
         * @minLength 1
         * @pattern \S
         */
        preReleaseVersion?: string;
        /** Return only builds for this content platform. */
        platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
        /** Return only builds in this processing state. */
        processingState?: "PROCESSING" | "FAILED" | "INVALID" | "VALID";
        /** Return only builds whose beta review submission is in this state. */
        betaReviewState?: "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "APPROVED";
        /** Return only expired builds when true, or only unexpired builds when false. */
        expired?: boolean;
        /** Sort order for the returned builds. */
        sort?: "version" | "-version" | "uploadedDate" | "-uploadedDate" | "preReleaseVersion" | "-preReleaseVersion";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Builds returned for this page. */
        builds: Array<{
          /** App Store Connect identifier for the build. */
          id: string;
          /** Build number, such as 42. */
          version?: string | null;
          /** When the build finished uploading, as an ISO 8601 timestamp. */
          uploadedDate?: string | null;
          /** When the build stops being installable by testers. */
          expirationDate?: string | null;
          /** Whether the build has expired for TestFlight. */
          expired?: boolean | null;
          /** Processing state of the uploaded build. */
          processingState?: "PROCESSING" | "FAILED" | "INVALID" | "VALID" | null;
          /** Distribution audience the build was uploaded for. */
          buildAudienceType?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE" | null;
          /** The prerelease version a build belongs to, or null when it was not returned. */
          preReleaseVersion: {
            /** App Store Connect identifier for the prerelease version. */
            id: string;
            /** Marketing version string, such as 1.4.0. */
            version?: string | null;
            /** Content platform the version targets. */
            platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
            [key: string]: unknown;
          } | null;
          /** Minimum OS version the build supports. */
          minOsVersion?: string | null;
          /** Minimum macOS system version declared by the build. */
          lsMinimumSystemVersion?: string | null;
          /** Minimum macOS version App Store Connect computed for the build. */
          computedMinMacOsVersion?: string | null;
          /** Minimum visionOS version App Store Connect computed for the build. */
          computedMinVisionOsVersion?: string | null;
          /** Whether the build declares non-exempt encryption. */
          usesNonExemptEncryption?: boolean | null;
          /** Template URL and pixel size of the build icon asset. */
          iconAssetToken?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Icon width in pixels. */
            width?: number;
            /** Icon height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the capabilities enabled on one bundle ID, with their configured settings. */
    "app_store_connect.list_bundle_id_capabilities": {
      input: {
        /**
         * App Store Connect identifier of the bundle ID record (the opaque resource id, not the reverse-DNS identifier string).
         * @minLength 1
         * @pattern \S
         */
        bundleIdId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Capabilities returned for this page. */
        bundleIdCapabilities: Array<{
          /** App Store Connect identifier of the bundle ID capability. */
          id: string;
          /** Capability that is enabled. */
          capabilityType?: "ICLOUD" | "IN_APP_PURCHASE" | "GAME_CENTER" | "PUSH_NOTIFICATIONS" | "WALLET" | "INTER_APP_AUDIO" | "MAPS" | "ASSOCIATED_DOMAINS" | "PERSONAL_VPN" | "APP_GROUPS" | "HEALTHKIT" | "HOMEKIT" | "WIRELESS_ACCESSORY_CONFIGURATION" | "APPLE_PAY" | "DATA_PROTECTION" | "SIRIKIT" | "NETWORK_EXTENSIONS" | "MULTIPATH" | "HOT_SPOT" | "NFC_TAG_READING" | "CLASSKIT" | "AUTOFILL_CREDENTIAL_PROVIDER" | "ACCESS_WIFI_INFORMATION" | "NETWORK_CUSTOM_PROTOCOL" | "COREMEDIA_HLS_LOW_LATENCY" | "SYSTEM_EXTENSION_INSTALL" | "USER_MANAGEMENT" | "APPLE_ID_AUTH" | null;
          /** Configured settings of the capability. */
          settings?: Array<{
            /** Setting key. */
            key?: "ICLOUD_VERSION" | "DATA_PROTECTION_PERMISSION_LEVEL" | "APPLE_ID_AUTH_APP_CONSENT" | null;
            /** Setting name shown in the developer portal. */
            name?: string | null;
            /** Setting description shown in the developer portal. */
            description?: string | null;
            /** Whether the setting is enabled unless configured otherwise. */
            enabledByDefault?: boolean | null;
            /** Whether the setting is shown in the developer portal. */
            visible?: boolean | null;
            /** How many options of the setting may be enabled at once. */
            allowedInstances?: "ENTRY" | "SINGLE" | "MULTIPLE" | null;
            /** Minimum number of options that must be enabled. */
            minInstances?: number | null;
            /** Options available for the setting. */
            options?: Array<{
              /** Option key. */
              key?: "XCODE_5" | "XCODE_6" | "COMPLETE_PROTECTION" | "PROTECTED_UNLESS_OPEN" | "PROTECTED_UNTIL_FIRST_USER_AUTH" | "PRIMARY_APP_CONSENT" | null;
              /** Option name shown in the developer portal. */
              name?: string | null;
              /** Option description shown in the developer portal. */
              description?: string | null;
              /** Whether the option is enabled unless configured otherwise. */
              enabledByDefault?: boolean | null;
              /** Whether the option is currently enabled. */
              enabled?: boolean | null;
              /** Whether the option applies to wildcard bundle IDs. */
              supportsWildcard?: boolean | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the provisioning profiles that were created for one bundle ID. */
    "app_store_connect.list_bundle_id_profiles": {
      input: {
        /**
         * App Store Connect identifier of the bundle ID record (the opaque resource id, not the reverse-DNS identifier string).
         * @minLength 1
         * @pattern \S
         */
        bundleIdId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Provisioning profiles returned for this page. */
        profiles: Array<{
          /** App Store Connect identifier of the provisioning profile. */
          id: string;
          /** Profile name. */
          name?: string | null;
          /** Platform the profile applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Distribution method and platform of the profile. */
          profileType?: "IOS_APP_DEVELOPMENT" | "IOS_APP_STORE" | "IOS_APP_ADHOC" | "IOS_APP_INHOUSE" | "MAC_APP_DEVELOPMENT" | "MAC_APP_STORE" | "MAC_APP_DIRECT" | "TVOS_APP_DEVELOPMENT" | "TVOS_APP_STORE" | "TVOS_APP_ADHOC" | "TVOS_APP_INHOUSE" | "MAC_CATALYST_APP_DEVELOPMENT" | "MAC_CATALYST_APP_STORE" | "MAC_CATALYST_APP_DIRECT" | null;
          /** Whether the profile is still valid. A profile becomes INVALID when a certificate it contains is revoked or a device it contains is disabled. */
          profileState?: "ACTIVE" | "INVALID" | null;
          /** Base64-encoded provisioning profile. Decode it to a .mobileprovision or .provisionprofile file to install it. */
          profileContent?: string | null;
          /** UUID embedded in the profile. */
          uuid?: string | null;
          /** When the profile was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the profile expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the bundle IDs (App IDs) registered with the team, optionally filtered by identifier, name, platform, or seed ID. */
    "app_store_connect.list_bundle_ids": {
      input: {
        /**
         * Return only the bundle ID with this exact reverse-DNS identifier, such as com.example.app.
         * @minLength 1
         * @pattern \S
         */
        identifier?: string;
        /**
         * Return only bundle IDs with this exact name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** Return only bundle IDs for this platform. */
        platform?: "IOS" | "MAC_OS" | "UNIVERSAL";
        /**
         * Return only bundle IDs with this seed ID (team ID prefix).
         * @minLength 1
         * @pattern \S
         */
        seedId?: string;
        /** Sort order for the returned bundle IDs. */
        sort?: "name" | "-name" | "platform" | "-platform" | "identifier" | "-identifier" | "seedId" | "-seedId" | "id" | "-id";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Bundle IDs returned for this page. */
        bundleIds: Array<{
          /** App Store Connect identifier of the bundle ID record. */
          id: string;
          /** Display name given to the bundle ID when it was registered. */
          name?: string | null;
          /** Platform the bundle ID is registered for. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Reverse-DNS bundle identifier string, such as com.example.app, or a wildcard ending in *. */
          identifier?: string | null;
          /** Team ID prefix (seed ID) associated with the bundle ID. */
          seedId?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the signing certificates of the team, including their base64 DER content, optionally filtered by type, display name, or serial number. */
    "app_store_connect.list_certificates": {
      input: {
        /**
         * Return only certificates with this exact display name.
         * @minLength 1
         * @pattern \S
         */
        displayName?: string;
        /** Return only certificates of this type. */
        certificateType?: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC";
        /**
         * Return only the certificate with this serial number.
         * @minLength 1
         * @pattern \S
         */
        serialNumber?: string;
        /** Sort order for the returned certificates. */
        sort?: "displayName" | "-displayName" | "certificateType" | "-certificateType" | "serialNumber" | "-serialNumber" | "id" | "-id";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Certificates returned for this page. */
        certificates: Array<{
          /** App Store Connect identifier of the certificate. */
          id: string;
          /** Certificate name. */
          name?: string | null;
          /** Kind of certificate. */
          certificateType?: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC" | null;
          /** Certificate display name shown in the developer portal. */
          displayName?: string | null;
          /** Serial number of the certificate. */
          serialNumber?: string | null;
          /** Platform the certificate applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** When the certificate expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          /** Base64-encoded DER certificate. Decode it to a .cer file to install it in a keychain. */
          certificateContent?: string | null;
          /** Whether the certificate is activated. Only Apple Pay payment processing certificates carry an activation state. */
          activated?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the artifacts a build action produced, such as archives, log bundles, and result bundles, each with a time-limited download URL. */
    "app_store_connect.list_ci_build_action_artifacts": {
      input: {
        /**
         * App Store Connect identifier of the build action.
         * @minLength 1
         * @pattern \S
         */
        ciBuildActionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Artifacts returned for this page. */
        ciArtifacts: Array<{
          /** App Store Connect identifier for the artifact. */
          id: string;
          /** Kind of artifact. */
          fileType?: "ARCHIVE" | "ARCHIVE_EXPORT" | "LOG_BUNDLE" | "RESULT_BUNDLE" | "TEST_PRODUCTS" | "XCODEBUILD_PRODUCTS" | "STAPLED_NOTARIZED_ARCHIVE" | null;
          /** File name of the artifact. */
          fileName?: string | null;
          /** File size in bytes. */
          fileSize?: number | null;
          /** Time-limited URL to download the artifact from. */
          downloadUrl?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the errors, warnings, analyzer warnings, and test failures a build action reported. */
    "app_store_connect.list_ci_build_action_issues": {
      input: {
        /**
         * App Store Connect identifier of the build action.
         * @minLength 1
         * @pattern \S
         */
        ciBuildActionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Issues returned for this page. */
        ciIssues: Array<{
          /** App Store Connect identifier for the issue. */
          id: string;
          /** Kind of issue. */
          issueType?: "ANALYZER_WARNING" | "ERROR" | "TEST_FAILURE" | "WARNING" | null;
          /** Issue message. */
          message?: string | null;
          /** Source file the record points at. */
          fileSource?: {
            /** File path relative to the repository root. */
            path?: string;
            /** Line number in the file. */
            lineNumber?: number;
            [key: string]: unknown;
          } | null;
          /** Issue category, such as Swift Compiler Error. */
          category?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the per-test results of a test action, with the outcome on each destination. */
    "app_store_connect.list_ci_build_action_test_results": {
      input: {
        /**
         * App Store Connect identifier of the build action.
         * @minLength 1
         * @pattern \S
         */
        ciBuildActionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Test results returned for this page. */
        ciTestResults: Array<{
          /** App Store Connect identifier for the test result. */
          id: string;
          /** Name of the test class. */
          className?: string | null;
          /** Name of the test method. */
          name?: string | null;
          /** Overall test status across all destinations. */
          status?: "SUCCESS" | "FAILURE" | "MIXED" | "SKIPPED" | "EXPECTED_FAILURE" | null;
          /** Source file the record points at. */
          fileSource?: {
            /** File path relative to the repository root. */
            path?: string;
            /** Line number in the file. */
            lineNumber?: number;
            [key: string]: unknown;
          } | null;
          /** Failure message, when the test did not succeed. */
          message?: string | null;
          /** Per-destination results. */
          destinationTestResults?: Array<{
            /** Identifier of the destination. */
            uuid?: string;
            /** Device name, such as iPhone 15. */
            deviceName?: string;
            /** Operating system version on the destination. */
            osVersion?: string;
            /** Test status on this destination. */
            status?: "SUCCESS" | "FAILURE" | "MIXED" | "SKIPPED" | "EXPECTED_FAILURE";
            /** Test duration in seconds. */
            duration?: number;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the actions (build, analyze, test, archive) of one build run with the progress and outcome of each. */
    "app_store_connect.list_ci_build_run_actions": {
      input: {
        /**
         * App Store Connect identifier of the build run.
         * @minLength 1
         * @pattern \S
         */
        ciBuildRunId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Build actions returned for this page. */
        ciBuildActions: Array<{
          /** App Store Connect identifier for the build action. */
          id: string;
          /** Action name shown in Xcode Cloud. */
          name?: string | null;
          /** What the action does. */
          actionType?: "BUILD" | "ANALYZE" | "TEST" | "ARCHIVE" | null;
          /** When the action started, as an ISO 8601 timestamp. */
          startedDate?: string | null;
          /** When the action finished, as an ISO 8601 timestamp. */
          finishedDate?: string | null;
          /** Number of issues by type. */
          issueCounts?: {
            /** Number of analyzer warnings. */
            analyzerWarnings?: number;
            /** Number of errors. */
            errors?: number;
            /** Number of test failures. */
            testFailures?: number;
            /** Number of warnings. */
            warnings?: number;
            [key: string]: unknown;
          } | null;
          /** Whether the action is pending, running, or complete. */
          executionProgress?: "PENDING" | "RUNNING" | "COMPLETE" | null;
          /** Outcome of the action once it is complete. */
          completionStatus?: "SUCCEEDED" | "FAILED" | "ERRORED" | "CANCELED" | "SKIPPED" | null;
          /** Whether the whole build run fails when this action fails. */
          isRequiredToPass?: boolean | null;
          /** Identifier of the build run the action belongs to. */
          ciBuildRunId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the App Store Connect builds an Xcode Cloud build run delivered, with the prerelease version each build belongs to. Use it to find the build to distribute after an archive action. */
    "app_store_connect.list_ci_build_run_builds": {
      input: {
        /**
         * App Store Connect identifier of the build run.
         * @minLength 1
         * @pattern \S
         */
        ciBuildRunId: string;
        /**
         * Return only builds with this build number, such as 42.
         * @minLength 1
         * @pattern \S
         */
        version?: string;
        /**
         * Return only builds under this marketing version, such as 1.4.0.
         * @minLength 1
         * @pattern \S
         */
        preReleaseVersion?: string;
        /** Return only builds for this content platform. */
        platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
        /** Return only builds in this processing state. */
        processingState?: "PROCESSING" | "FAILED" | "INVALID" | "VALID";
        /** Return only builds whose beta review submission is in this state. */
        betaReviewState?: "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "APPROVED";
        /** Return only expired builds when true, or only unexpired builds when false. */
        expired?: boolean;
        /** Sort order for the returned builds. */
        sort?: "version" | "-version" | "uploadedDate" | "-uploadedDate" | "preReleaseVersion" | "-preReleaseVersion";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Builds returned for this page. */
        builds: Array<{
          /** App Store Connect identifier for the build. */
          id: string;
          /** Build number, such as 42. */
          version?: string | null;
          /** When the build finished uploading, as an ISO 8601 timestamp. */
          uploadedDate?: string | null;
          /** When the build stops being installable by testers. */
          expirationDate?: string | null;
          /** Whether the build has expired for TestFlight. */
          expired?: boolean | null;
          /** Processing state of the uploaded build. */
          processingState?: "PROCESSING" | "FAILED" | "INVALID" | "VALID" | null;
          /** Distribution audience the build was uploaded for. */
          buildAudienceType?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE" | null;
          /** The prerelease version a build belongs to, or null when it was not returned. */
          preReleaseVersion: {
            /** App Store Connect identifier for the prerelease version. */
            id: string;
            /** Marketing version string, such as 1.4.0. */
            version?: string | null;
            /** Content platform the version targets. */
            platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
            [key: string]: unknown;
          } | null;
          /** Minimum OS version the build supports. */
          minOsVersion?: string | null;
          /** Minimum macOS system version declared by the build. */
          lsMinimumSystemVersion?: string | null;
          /** Minimum macOS version App Store Connect computed for the build. */
          computedMinMacOsVersion?: string | null;
          /** Minimum visionOS version App Store Connect computed for the build. */
          computedMinVisionOsVersion?: string | null;
          /** Whether the build declares non-exempt encryption. */
          usesNonExemptEncryption?: boolean | null;
          /** Template URL and pixel size of the build icon asset. */
          iconAssetToken?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Icon width in pixels. */
            width?: number;
            /** Icon height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Xcode versions that can run on one macOS version in Xcode Cloud. */
    "app_store_connect.list_ci_mac_os_version_xcode_versions": {
      input: {
        /**
         * App Store Connect identifier of the macOS version.
         * @minLength 1
         * @pattern \S
         */
        ciMacOsVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Xcode versions returned for this page. */
        ciXcodeVersions: Array<{
          /** App Store Connect identifier for the Xcode version. */
          id: string;
          /** Xcode version number, such as 15.3. */
          version?: string | null;
          /** Xcode version name, such as Xcode 15.3. */
          name?: string | null;
          /** Simulators and Macs available for tests with this Xcode version. */
          testDestinations?: Array<{
            /** Device type name, such as iPhone 15. */
            deviceTypeName?: string;
            /** Device type identifier. */
            deviceTypeIdentifier?: string;
            /** Runtimes available for the device type. */
            availableRuntimes?: Array<{
              /** Runtime name, such as iOS 17.4. */
              runtimeName?: string;
              /** Runtime identifier. */
              runtimeIdentifier?: string;
              [key: string]: unknown;
            }>;
            /** Whether the destination is a simulator or a Mac. */
            kind?: "SIMULATOR" | "MAC";
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the macOS versions Xcode Cloud can build on. */
    "app_store_connect.list_ci_mac_os_versions": {
      input: {
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** macOS versions returned for this page. */
        ciMacOsVersions: Array<{
          /** App Store Connect identifier for the macOS version. */
          id: string;
          /** macOS version number, such as 14.4. */
          version?: string | null;
          /** macOS version name, such as macOS Sonoma 14.4. */
          name?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the additional repositories of one Xcode Cloud product, such as Swift package dependencies Xcode Cloud has been granted access to. */
    "app_store_connect.list_ci_product_additional_repositories": {
      input: {
        /**
         * App Store Connect identifier of the Xcode Cloud product.
         * @minLength 1
         * @pattern \S
         */
        ciProductId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Repositories returned for this page. */
        scmRepositories: Array<{
          /** App Store Connect identifier for the repository. */
          id: string;
          /** When Xcode Cloud last accessed the repository, as an ISO 8601 timestamp. */
          lastAccessedDate?: string | null;
          /** HTTPS clone URL. */
          httpCloneUrl?: string | null;
          /** SSH clone URL. */
          sshCloneUrl?: string | null;
          /** Owner or organization the repository belongs to. */
          ownerName?: string | null;
          /** Repository name. */
          repositoryName?: string | null;
          /** Identifier of the source control provider. */
          scmProviderId: string | null;
          /** Identifier of the Git reference for the default branch. */
          defaultBranchId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the build runs of one Xcode Cloud product across all of its workflows, optionally narrowed to the run that produced one build. */
    "app_store_connect.list_ci_product_build_runs": {
      input: {
        /**
         * App Store Connect identifier of the Xcode Cloud product.
         * @minLength 1
         * @pattern \S
         */
        ciProductId: string;
        /**
         * Return only build runs that produced this App Store Connect build.
         * @minLength 1
         * @pattern \S
         */
        buildId?: string;
        /** Sort order for the returned build runs. */
        sort?: "number" | "-number";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Build runs returned for this page. */
        ciBuildRuns: Array<{
          /** App Store Connect identifier for the build run. */
          id: string;
          /** Sequential build number within the product. */
          number?: number | null;
          /** When the run was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the run started, as an ISO 8601 timestamp. */
          startedDate?: string | null;
          /** When the run finished, as an ISO 8601 timestamp. */
          finishedDate?: string | null;
          /** A commit Xcode Cloud built. */
          sourceCommit?: {
            /** Full commit SHA. */
            commitSha?: string;
            /** Commit message. */
            message?: string;
            /** Who authored the commit. */
            author?: {
              /** Display name of the author. */
              displayName?: string;
              /** Avatar image URL of the author. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** Who committed the change. */
            committer?: {
              /** Display name of the committer. */
              displayName?: string;
              /** Avatar image URL of the committer. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** URL of the commit on the source control provider. */
            webUrl?: string;
            [key: string]: unknown;
          } | null;
          /** A commit Xcode Cloud built. */
          destinationCommit?: {
            /** Full commit SHA. */
            commitSha?: string;
            /** Commit message. */
            message?: string;
            /** Who authored the commit. */
            author?: {
              /** Display name of the author. */
              displayName?: string;
              /** Avatar image URL of the author. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** Who committed the change. */
            committer?: {
              /** Display name of the committer. */
              displayName?: string;
              /** Avatar image URL of the committer. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** URL of the commit on the source control provider. */
            webUrl?: string;
            [key: string]: unknown;
          } | null;
          /** Whether the run was started for a pull request. */
          isPullRequestBuild?: boolean | null;
          /** Number of issues by type. */
          issueCounts?: {
            /** Number of analyzer warnings. */
            analyzerWarnings?: number;
            /** Number of errors. */
            errors?: number;
            /** Number of test failures. */
            testFailures?: number;
            /** Number of warnings. */
            warnings?: number;
            [key: string]: unknown;
          } | null;
          /** Whether the run is pending, running, or complete. */
          executionProgress?: "PENDING" | "RUNNING" | "COMPLETE" | null;
          /** Outcome of the run once it is complete. */
          completionStatus?: "SUCCEEDED" | "FAILED" | "ERRORED" | "CANCELED" | "SKIPPED" | null;
          /** What started the run. */
          startReason?: "GIT_REF_CHANGE" | "MANUAL" | "MANUAL_REBUILD" | "PULL_REQUEST_OPEN" | "PULL_REQUEST_UPDATE" | "SCHEDULE" | null;
          /** Why the run was canceled, when it was. */
          cancelReason?: "AUTOMATICALLY_BY_NEWER_BUILD" | "MANUALLY_BY_USER" | null;
          /** Identifier of the workflow that ran. */
          ciWorkflowId: string | null;
          /** Identifier of the Xcode Cloud product the run belongs to. */
          ciProductId: string | null;
          /** Identifier of the Git reference (branch or tag) the run built. */
          sourceBranchOrTagId: string | null;
          /** Identifier of the destination branch for a pull request run. */
          destinationBranchId: string | null;
          /** Identifier of the pull request the run was started for. */
          scmPullRequestId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the primary repositories of one Xcode Cloud product, the repositories that hold the project or package its workflows build. */
    "app_store_connect.list_ci_product_primary_repositories": {
      input: {
        /**
         * App Store Connect identifier of the Xcode Cloud product.
         * @minLength 1
         * @pattern \S
         */
        ciProductId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Repositories returned for this page. */
        scmRepositories: Array<{
          /** App Store Connect identifier for the repository. */
          id: string;
          /** When Xcode Cloud last accessed the repository, as an ISO 8601 timestamp. */
          lastAccessedDate?: string | null;
          /** HTTPS clone URL. */
          httpCloneUrl?: string | null;
          /** SSH clone URL. */
          sshCloneUrl?: string | null;
          /** Owner or organization the repository belongs to. */
          ownerName?: string | null;
          /** Repository name. */
          repositoryName?: string | null;
          /** Identifier of the source control provider. */
          scmProviderId: string | null;
          /** Identifier of the Git reference for the default branch. */
          defaultBranchId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the workflows of one Xcode Cloud product. */
    "app_store_connect.list_ci_product_workflows": {
      input: {
        /**
         * App Store Connect identifier of the Xcode Cloud product.
         * @minLength 1
         * @pattern \S
         */
        ciProductId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Workflows returned for this page. */
        ciWorkflows: Array<{
          /** App Store Connect identifier for the workflow. */
          id: string;
          /** Workflow name shown in App Store Connect and Xcode. */
          name?: string | null;
          /** Description shown under the workflow name. */
          description?: string | null;
          /** Start a build when a matching branch changes. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchstartcondition */
          branchStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start a build when a matching tag is created. See https://developer.apple.com/documentation/appstoreconnectapi/citagstartcondition */
          tagStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start a build when a matching pull request is opened or updated. See https://developer.apple.com/documentation/appstoreconnectapi/cipullrequeststartcondition */
          pullRequestStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            destination?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start builds on a schedule. See https://developer.apple.com/documentation/appstoreconnectapi/cischeduledstartcondition */
          scheduledStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** When the schedule fires. */
            schedule?: {
              /** How often the build starts. */
              frequency?: "WEEKLY" | "DAILY" | "HOURLY";
              /** Days of the week for a WEEKLY schedule. */
              days?: Array<"SUNDAY" | "MONDAY" | "TUESDAY" | "WEDNESDAY" | "THURSDAY" | "FRIDAY" | "SATURDAY">;
              /** Hour of the day the build starts. */
              hour?: number;
              /** Minute of the hour the build starts. */
              minute?: number;
              /** IANA time zone name, such as America/Los_Angeles. */
              timezone?: string;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Branches a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualbranchstartcondition */
          manualBranchStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Tags a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualtagstartcondition */
          manualTagStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Pull requests a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualpullrequeststartcondition */
          manualPullRequestStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            destination?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Actions the workflow runs, in order. */
          actions?: Array<{
            /** Action name shown in Xcode Cloud. */
            name?: string;
            /** What the action does. */
            actionType?: "BUILD" | "ANALYZE" | "TEST" | "ARCHIVE";
            /** Device or simulator family the action targets. */
            destination?: "ANY_IOS_DEVICE" | "ANY_IOS_SIMULATOR" | "ANY_TVOS_DEVICE" | "ANY_TVOS_SIMULATOR" | "ANY_WATCHOS_DEVICE" | "ANY_WATCHOS_SIMULATOR" | "ANY_MAC" | "ANY_MAC_CATALYST" | "ANY_VISIONOS_DEVICE" | "ANY_VISIONOS_SIMULATOR";
            /** Who may install the build an ARCHIVE action produces. */
            buildDistributionAudience?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE";
            /** Test plan configuration for a TEST action. */
            testConfiguration?: {
              /** Use the scheme test settings or a specific test plan. */
              kind?: "USE_SCHEME_SETTINGS" | "SPECIFIC_TEST_PLANS";
              /** Name of the test plan to run when kind is SPECIFIC_TEST_PLANS. */
              testPlanName?: string;
              /** Simulators and Macs the tests run on. */
              testDestinations?: Array<{
                /** Device type name, such as iPhone 15. */
                deviceTypeName?: string;
                /** Device type identifier. */
                deviceTypeIdentifier?: string;
                /** Runtime name, such as iOS 17.4. */
                runtimeName?: string;
                /** Runtime identifier. */
                runtimeIdentifier?: string;
                /** Whether the destination is a simulator or a Mac. */
                kind?: "SIMULATOR" | "MAC";
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Xcode scheme the action builds. */
            scheme?: string;
            /** Platform the action builds for. */
            platform?: "MACOS" | "IOS" | "TVOS" | "WATCHOS" | "VISIONOS";
            /** Whether the whole build run fails when this action fails. */
            isRequiredToPass?: boolean;
            [key: string]: unknown;
          }> | null;
          /** Whether the workflow is enabled and can start builds. */
          isEnabled?: boolean | null;
          /** Whether the workflow is locked against editing in Xcode. */
          isLockedForEditing?: boolean | null;
          /** Whether every build starts from a clean environment without cached derived data. */
          clean?: boolean | null;
          /** Path of the Xcode project, workspace, or Swift package the workflow builds, relative to the repository root. */
          containerFilePath?: string | null;
          /** When the workflow was last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** Identifier of the Xcode Cloud product the workflow belongs to. */
          ciProductId: string | null;
          /** Identifier of the repository the workflow builds from. */
          scmRepositoryId: string | null;
          /** Identifier of the Xcode version the workflow builds with. */
          ciXcodeVersionId: string | null;
          /** Identifier of the macOS version the workflow builds on. */
          ciMacOsVersionId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Xcode Cloud products of the team, optionally narrowed to one app or to app or framework products. A product holds the workflows and build runs of one app or framework. */
    "app_store_connect.list_ci_products": {
      input: {
        /**
         * Return only the product of this app.
         * @minLength 1
         * @pattern \S
         */
        appId?: string;
        /** Return only products of this type. */
        productType?: "APP" | "FRAMEWORK";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Xcode Cloud products returned for this page. */
        ciProducts: Array<{
          /** App Store Connect identifier for the Xcode Cloud product. */
          id: string;
          /** Product name. */
          name?: string | null;
          /** When the product was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the product builds an app or a framework. */
          productType?: "APP" | "FRAMEWORK" | null;
          /** Identifier of the app the product belongs to. */
          appId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the build runs of one Xcode Cloud workflow, optionally narrowed to the run that produced one build. */
    "app_store_connect.list_ci_workflow_build_runs": {
      input: {
        /**
         * App Store Connect identifier of the workflow.
         * @minLength 1
         * @pattern \S
         */
        ciWorkflowId: string;
        /**
         * Return only build runs that produced this App Store Connect build.
         * @minLength 1
         * @pattern \S
         */
        buildId?: string;
        /** Sort order for the returned build runs. */
        sort?: "number" | "-number";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Build runs returned for this page. */
        ciBuildRuns: Array<{
          /** App Store Connect identifier for the build run. */
          id: string;
          /** Sequential build number within the product. */
          number?: number | null;
          /** When the run was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the run started, as an ISO 8601 timestamp. */
          startedDate?: string | null;
          /** When the run finished, as an ISO 8601 timestamp. */
          finishedDate?: string | null;
          /** A commit Xcode Cloud built. */
          sourceCommit?: {
            /** Full commit SHA. */
            commitSha?: string;
            /** Commit message. */
            message?: string;
            /** Who authored the commit. */
            author?: {
              /** Display name of the author. */
              displayName?: string;
              /** Avatar image URL of the author. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** Who committed the change. */
            committer?: {
              /** Display name of the committer. */
              displayName?: string;
              /** Avatar image URL of the committer. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** URL of the commit on the source control provider. */
            webUrl?: string;
            [key: string]: unknown;
          } | null;
          /** A commit Xcode Cloud built. */
          destinationCommit?: {
            /** Full commit SHA. */
            commitSha?: string;
            /** Commit message. */
            message?: string;
            /** Who authored the commit. */
            author?: {
              /** Display name of the author. */
              displayName?: string;
              /** Avatar image URL of the author. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** Who committed the change. */
            committer?: {
              /** Display name of the committer. */
              displayName?: string;
              /** Avatar image URL of the committer. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** URL of the commit on the source control provider. */
            webUrl?: string;
            [key: string]: unknown;
          } | null;
          /** Whether the run was started for a pull request. */
          isPullRequestBuild?: boolean | null;
          /** Number of issues by type. */
          issueCounts?: {
            /** Number of analyzer warnings. */
            analyzerWarnings?: number;
            /** Number of errors. */
            errors?: number;
            /** Number of test failures. */
            testFailures?: number;
            /** Number of warnings. */
            warnings?: number;
            [key: string]: unknown;
          } | null;
          /** Whether the run is pending, running, or complete. */
          executionProgress?: "PENDING" | "RUNNING" | "COMPLETE" | null;
          /** Outcome of the run once it is complete. */
          completionStatus?: "SUCCEEDED" | "FAILED" | "ERRORED" | "CANCELED" | "SKIPPED" | null;
          /** What started the run. */
          startReason?: "GIT_REF_CHANGE" | "MANUAL" | "MANUAL_REBUILD" | "PULL_REQUEST_OPEN" | "PULL_REQUEST_UPDATE" | "SCHEDULE" | null;
          /** Why the run was canceled, when it was. */
          cancelReason?: "AUTOMATICALLY_BY_NEWER_BUILD" | "MANUALLY_BY_USER" | null;
          /** Identifier of the workflow that ran. */
          ciWorkflowId: string | null;
          /** Identifier of the Xcode Cloud product the run belongs to. */
          ciProductId: string | null;
          /** Identifier of the Git reference (branch or tag) the run built. */
          sourceBranchOrTagId: string | null;
          /** Identifier of the destination branch for a pull request run. */
          destinationBranchId: string | null;
          /** Identifier of the pull request the run was started for. */
          scmPullRequestId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the macOS versions one Xcode version can run on in Xcode Cloud. */
    "app_store_connect.list_ci_xcode_version_mac_os_versions": {
      input: {
        /**
         * App Store Connect identifier of the Xcode version.
         * @minLength 1
         * @pattern \S
         */
        ciXcodeVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** macOS versions returned for this page. */
        ciMacOsVersions: Array<{
          /** App Store Connect identifier for the macOS version. */
          id: string;
          /** macOS version number, such as 14.4. */
          version?: string | null;
          /** macOS version name, such as macOS Sonoma 14.4. */
          name?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Xcode versions Xcode Cloud can build with, each with the simulators and Macs available for tests. */
    "app_store_connect.list_ci_xcode_versions": {
      input: {
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Xcode versions returned for this page. */
        ciXcodeVersions: Array<{
          /** App Store Connect identifier for the Xcode version. */
          id: string;
          /** Xcode version number, such as 15.3. */
          version?: string | null;
          /** Xcode version name, such as Xcode 15.3. */
          name?: string | null;
          /** Simulators and Macs available for tests with this Xcode version. */
          testDestinations?: Array<{
            /** Device type name, such as iPhone 15. */
            deviceTypeName?: string;
            /** Device type identifier. */
            deviceTypeIdentifier?: string;
            /** Runtimes available for the device type. */
            availableRuntimes?: Array<{
              /** Runtime name, such as iOS 17.4. */
              runtimeName?: string;
              /** Runtime identifier. */
              runtimeIdentifier?: string;
              [key: string]: unknown;
            }>;
            /** Whether the destination is a simulator or a Mac. */
            kind?: "SIMULATOR" | "MAC";
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the App Store reviews of one app together with the developer response published for each review. */
    "app_store_connect.list_customer_reviews": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only reviews with this star rating.
         * @minimum 1
         * @maximum 5
         */
        rating?: number;
        /**
         * Return only reviews written in this storefront, as an ISO 3166-1 alpha-3 code such as USA or DEU.
         * @minLength 1
         * @pattern ^[A-Z]{3}$
         */
        territory?: string;
        /** Return only reviews that already have a developer response, or only those without. */
        hasResponse?: boolean;
        /** Sort order for the returned reviews. */
        sort?: "rating" | "-rating" | "createdDate" | "-createdDate";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Reviews returned for this page. */
        customerReviews: Array<{
          /** App Store Connect identifier for the review. */
          id: string;
          /** Star rating from 1 to 5. */
          rating?: number | null;
          /** Review title. */
          title?: string | null;
          /** Review text. */
          body?: string | null;
          /** Nickname the reviewer publishes under. */
          reviewerNickname?: string | null;
          /** When the review was written, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** ISO 3166-1 alpha-3 storefront the review was written in, such as USA. */
          territory?: string | null;
          /** The developer response published for a review, or null when there is none. */
          response: {
            /** App Store Connect identifier for the response. */
            id: string;
            /** Text of the developer response. */
            responseBody?: string | null;
            /** When the response was last changed, as an ISO 8601 timestamp. */
            lastModifiedDate?: string | null;
            /** Publication state of the response. */
            state?: "PUBLISHED" | "PENDING_PUBLISH" | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the devices registered with the team, optionally filtered by name, platform, UDID, or status. */
    "app_store_connect.list_devices": {
      input: {
        /**
         * Return only devices with this exact name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** Return only devices for this platform. */
        platform?: "IOS" | "MAC_OS" | "UNIVERSAL";
        /**
         * Return only the device with this UDID.
         * @minLength 1
         * @pattern \S
         */
        udid?: string;
        /** Return only enabled or only disabled devices. */
        status?: "ENABLED" | "DISABLED";
        /** Sort order for the returned devices. */
        sort?: "name" | "-name" | "platform" | "-platform" | "udid" | "-udid" | "status" | "-status" | "id" | "-id";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Devices returned for this page. */
        devices: Array<{
          /** App Store Connect identifier of the device. */
          id: string;
          /** Device name given when it was registered. */
          name?: string | null;
          /** Platform the device runs. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Unique device identifier (UDID) of the device. */
          udid?: string | null;
          /** Hardware class of the device. */
          deviceClass?: "APPLE_VISION_PRO" | "APPLE_WATCH" | "IPAD" | "IPHONE" | "IPOD" | "APPLE_TV" | "MAC" | null;
          /** Whether the device is enabled for development. Disabled devices are excluded from new profiles. */
          status?: "ENABLED" | "DISABLED" | null;
          /** Device model name. */
          model?: string | null;
          /** When the device was registered, as an ISO 8601 timestamp. */
          addedDate?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the diagnostic signatures App Store Connect collected for one build: recurring call patterns behind disk writes, hangs, or slow launches, weighted by how critical they are. */
    "app_store_connect.list_diagnostic_signatures": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /** Return only signatures of this diagnostic type. */
        diagnosticType?: "DISK_WRITES" | "HANGS" | "LAUNCHES";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Diagnostic signatures returned for this page. */
        diagnosticSignatures: Array<{
          /** App Store Connect identifier for the diagnostic signature. */
          id: string;
          /** Diagnostic category the signature belongs to. */
          diagnosticType?: "DISK_WRITES" | "HANGS" | "LAUNCHES" | null;
          /** Name of the signature, generated by the system. */
          signature?: string | null;
          /** How critical the issue is, between 0 and 1; higher values matter more. */
          weight?: number | null;
          /** Trend App Store Connect derived for the signature across app versions. */
          insight?: {
            /** Kind of insight. */
            insightType?: "TREND" | null;
            /** Whether the signature is trending up or down. */
            direction?: "UP" | "DOWN" | "UNDEFINED" | null;
            /** Earlier app versions the trend was computed against. */
            referenceVersions?: Array<{
              /** App version string. */
              version?: string | null;
              /** Signature weight measured for that version. */
              value?: number | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the localizations of one Game Center achievement version, one per locale the achievement is shown in. */
    "app_store_connect.list_game_center_achievement_version_localizations": {
      input: {
        /**
         * App Store Connect identifier of the achievement version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center achievement localizations returned for this page. */
        gameCenterAchievementLocalizations: Array<{
          /** App Store Connect identifier for the achievement localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Achievement name shown to players in this locale. */
          name?: string | null;
          /** Description shown before the player earns the achievement. */
          beforeEarnedDescription?: string | null;
          /** Description shown after the player earns the achievement. */
          afterEarnedDescription?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the versions of one Game Center achievement, each with the review and release state of the localizations it carries. */
    "app_store_connect.list_game_center_achievement_versions": {
      input: {
        /**
         * App Store Connect identifier of the achievement.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center achievement versions returned for this page. */
        gameCenterAchievementVersions: Array<{
          /** App Store Connect identifier for the achievement version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the locales one Game Center activity version has text for. */
    "app_store_connect.list_game_center_activity_version_localizations": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center activity localizations returned for this page. */
        gameCenterActivityLocalizations: Array<{
          /** App Store Connect identifier for the activity localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Activity name shown to players in this locale. */
          name?: string | null;
          /** Activity description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the versions of one Game Center activity, each with its review and release state. */
    "app_store_connect.list_game_center_activity_versions": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center activity versions returned for this page. */
        gameCenterActivityVersions: Array<{
          /** App Store Connect identifier for the activity version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          /** Web page opened when a player without the game follows an activity invitation. */
          fallbackUrl?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the earlier app versions that stay multiplayer compatible with this one, so players who have not updated yet can still be matched with players who have. Optionally narrowed to the compatible versions that have Game Center enabled. */
    "app_store_connect.list_game_center_app_version_compatibility_versions": {
      input: {
        /**
         * App Store Connect identifier of the Game Center app version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAppVersionId: string;
        /** Return only compatible versions that have Game Center enabled when true, or only the disabled ones when false. */
        enabled?: boolean;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Compatible Game Center app versions returned for this page. */
        gameCenterAppVersions: Array<{
          /** App Store Connect identifier for the Game Center app version. */
          id: string;
          /** Whether Game Center is enabled for this App Store version. */
          enabled?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the locales one Game Center challenge version carries text for. */
    "app_store_connect.list_game_center_challenge_version_localizations": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center challenge localizations returned for this page. */
        gameCenterChallengeLocalizations: Array<{
          /** App Store Connect identifier for the challenge localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Challenge name shown to players in this locale. */
          name?: string | null;
          /** Challenge description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the versions of one Game Center challenge, each with the review and release state it is in. */
    "app_store_connect.list_game_center_challenge_versions": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center challenge versions returned for this page. */
        gameCenterChallengeVersions: Array<{
          /** App Store Connect identifier for the challenge version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the achievements of one Game Center detail, optionally narrowed by reference name, archived state or identifier. */
    "app_store_connect.list_game_center_detail_achievements": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Return only achievements with these internal reference names.
         * @minItems 1
         */
        referenceNames?: Array<string>;
        /** Return only archived achievements when true, or only achievements still offered to players when false. */
        archived?: boolean;
        /**
         * Return only the achievements with these identifiers.
         * @minItems 1
         */
        gameCenterAchievementIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center achievements returned for this page. */
        gameCenterAchievements: Array<{
          /** App Store Connect identifier for the achievement. */
          id: string;
          /** Internal achievement name shown in App Store Connect. */
          referenceName?: string | null;
          /** Achievement identifier the game reports scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** Points the achievement is worth, from 0 to 100. */
          points?: number | null;
          /** Whether players can see the achievement before they earn it. */
          showBeforeEarned?: boolean | null;
          /** Whether players can earn the achievement more than once. */
          repeatable?: boolean | null;
          /** Whether the achievement is archived and no longer offered to players. */
          archived?: boolean | null;
          /** Key-value properties the achievement contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Game Center activities of one app, reached through its Game Center detail record. App Store Connect offers no filters here, so page through the results with limit and cursor. */
    "app_store_connect.list_game_center_detail_activities": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center activities returned for this page. */
        gameCenterActivities: Array<{
          /** App Store Connect identifier for the activity. */
          id: string;
          /** Internal activity name shown in App Store Connect. */
          referenceName?: string | null;
          /** Activity identifier the game starts sessions with, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** Whether players take turns or play at the same time. */
          playStyle?: "ASYNCHRONOUS" | "SYNCHRONOUS" | null;
          /** Fewest players an activity session needs. */
          minimumPlayersCount?: number | null;
          /** Most players an activity session accepts. */
          maximumPlayersCount?: number | null;
          /** Whether players can join the activity with a party code. */
          supportsPartyCode?: boolean | null;
          /** Whether the activity is archived and no longer offered. */
          archived?: boolean | null;
          /** Key-value properties the game reads when it starts the activity. */
          properties?: Record<string, unknown> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the App Store versions of the app together with their Game Center enablement, optionally narrowed to the enabled or the disabled ones. */
    "app_store_connect.list_game_center_detail_app_versions": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /** Return only versions with Game Center enabled when true, or only versions with Game Center disabled when false. */
        enabled?: boolean;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center app versions returned for this page. */
        gameCenterAppVersions: Array<{
          /** App Store Connect identifier for the Game Center app version. */
          id: string;
          /** Whether Game Center is enabled for this App Store version. */
          enabled?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the challenges configured on the Game Center detail of one app, optionally narrowed by reference name, archived state or identifier. */
    "app_store_connect.list_game_center_detail_challenges": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Return only challenges with these internal reference names.
         * @minItems 1
         */
        referenceNames?: Array<string>;
        /** Return only archived challenges when true, or only challenges still offered to players when false. */
        archived?: boolean;
        /**
         * Return only the challenges with these App Store Connect identifiers.
         * @minItems 1
         */
        gameCenterChallengeIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center challenges returned for this page. */
        gameCenterChallenges: Array<{
          /** App Store Connect identifier for the challenge. */
          id: string;
          /** Internal challenge name shown in App Store Connect. */
          referenceName?: string | null;
          /** Challenge identifier the game refers to, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** What the challenge is based on. */
          challengeType?: "LEADERBOARD" | null;
          /** Whether a player can take the challenge more than once. */
          repeatable?: boolean | null;
          /** Whether the challenge is archived and no longer offered. */
          archived?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the leaderboard sets of one Game Center detail, optionally narrowed by reference name or identifier. */
    "app_store_connect.list_game_center_detail_leaderboard_sets": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Return only leaderboard sets with these internal reference names.
         * @minItems 1
         */
        referenceNames?: Array<string>;
        /**
         * Return only the leaderboard sets with these identifiers.
         * @minItems 1
         */
        gameCenterLeaderboardSetIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center leaderboard sets returned for this page. */
        gameCenterLeaderboardSets: Array<{
          /** App Store Connect identifier for the leaderboard set. */
          id: string;
          /** Internal set name shown in App Store Connect. */
          referenceName?: string | null;
          /** Set identifier the game refers to, unique within the app or group. */
          vendorIdentifier?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the leaderboards of one Game Center detail, optionally narrowed by reference name, archived state or identifier. */
    "app_store_connect.list_game_center_detail_leaderboards": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Return only leaderboards with these internal reference names.
         * @minItems 1
         */
        referenceNames?: Array<string>;
        /** Return only archived leaderboards when true, or only leaderboards still accepting scores when false. */
        archived?: boolean;
        /**
         * Return only the leaderboards with these identifiers.
         * @minItems 1
         */
        gameCenterLeaderboardIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center leaderboards returned for this page. */
        gameCenterLeaderboards: Array<{
          /** App Store Connect identifier for the leaderboard. */
          id: string;
          /** Internal leaderboard name shown in App Store Connect. */
          referenceName?: string | null;
          /** Leaderboard identifier the game submits scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** How scores are formatted unless a localization overrides it. */
          defaultFormatter?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
          /** Which submitted score is kept for a player. */
          submissionType?: "BEST_SCORE" | "MOST_RECENT_SCORE" | null;
          /** Whether a higher or a lower score ranks first. */
          scoreSortType?: "ASC" | "DESC" | null;
          /** Lowest accepted score, as a decimal string. */
          scoreRangeStart?: string | null;
          /** Highest accepted score, as a decimal string. */
          scoreRangeEnd?: string | null;
          /** When the first recurring run of the leaderboard starts, as an ISO 8601 timestamp. */
          recurrenceStartDate?: string | null;
          /** How long each recurring run lasts, as an ISO 8601 duration such as P1D. */
          recurrenceDuration?: string | null;
          /** ICS recurrence rule that schedules the recurring runs. */
          recurrenceRule?: string | null;
          /** Whether the leaderboard is archived and no longer accepting scores. */
          archived?: boolean | null;
          /** Key-value properties the leaderboard contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          /** Whether the leaderboard is shown to players. */
          visibility?: "SHOW_FOR_ALL" | "HIDE_FOR_ALL" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the achievements shared by one Game Center group, optionally narrowed by reference name, archived state or identifier. */
    "app_store_connect.list_game_center_group_achievements": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
        /**
         * Return only achievements with these reference names.
         * @minItems 1
         */
        referenceNames?: Array<string>;
        /** Return only archived achievements when true, or only achievements still offered to players when false. */
        archived?: boolean;
        /**
         * Return only the achievements with these identifiers.
         * @minItems 1
         */
        gameCenterAchievementIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Achievements returned for this page. */
        gameCenterAchievements: Array<{
          /** App Store Connect identifier for the achievement. */
          id: string;
          /** Internal achievement name shown in App Store Connect. */
          referenceName?: string | null;
          /** Achievement identifier the game reports scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** Points the achievement is worth, from 0 to 100. */
          points?: number | null;
          /** Whether players can see the achievement before they earn it. */
          showBeforeEarned?: boolean | null;
          /** Whether players can earn the achievement more than once. */
          repeatable?: boolean | null;
          /** Whether the achievement is archived and no longer offered to players. */
          archived?: boolean | null;
          /** Key-value properties the achievement contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Game Center activities shared by the apps of one Game Center group. App Store Connect offers no filters here, so page through the results with limit and cursor. */
    "app_store_connect.list_game_center_group_activities": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center activities returned for this page. */
        gameCenterActivities: Array<{
          /** App Store Connect identifier for the activity. */
          id: string;
          /** Internal activity name shown in App Store Connect. */
          referenceName?: string | null;
          /** Activity identifier the game starts sessions with, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** Whether players take turns or play at the same time. */
          playStyle?: "ASYNCHRONOUS" | "SYNCHRONOUS" | null;
          /** Fewest players an activity session needs. */
          minimumPlayersCount?: number | null;
          /** Most players an activity session accepts. */
          maximumPlayersCount?: number | null;
          /** Whether players can join the activity with a party code. */
          supportsPartyCode?: boolean | null;
          /** Whether the activity is archived and no longer offered. */
          archived?: boolean | null;
          /** Key-value properties the game reads when it starts the activity. */
          properties?: Record<string, unknown> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the challenges shared by the games of one Game Center group, optionally narrowed by reference name, archived state or identifier. */
    "app_store_connect.list_game_center_group_challenges": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
        /**
         * Return only challenges with these internal reference names.
         * @minItems 1
         */
        referenceNames?: Array<string>;
        /** Return only archived challenges when true, or only challenges still offered to players when false. */
        archived?: boolean;
        /**
         * Return only the challenges with these App Store Connect identifiers.
         * @minItems 1
         */
        gameCenterChallengeIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center challenges returned for this page. */
        gameCenterChallenges: Array<{
          /** App Store Connect identifier for the challenge. */
          id: string;
          /** Internal challenge name shown in App Store Connect. */
          referenceName?: string | null;
          /** Challenge identifier the game refers to, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** What the challenge is based on. */
          challengeType?: "LEADERBOARD" | null;
          /** Whether a player can take the challenge more than once. */
          repeatable?: boolean | null;
          /** Whether the challenge is archived and no longer offered. */
          archived?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Game Center details of the apps that belong to one group, that is the apps sharing the achievements, leaderboards and leaderboard sets of the group. */
    "app_store_connect.list_game_center_group_details": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
        /** Return only details that have App Store versions with Game Center enabled when true, or with Game Center disabled when false. */
        appVersionsEnabled?: boolean;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center details returned for this page. */
        gameCenterDetails: Array<{
          /** App Store Connect identifier for the Game Center detail. */
          id: string;
          /** Whether the app is distributed through Apple Arcade. */
          arcadeEnabled?: boolean | null;
          /** Game Center group the app belongs to, or null when the app is not grouped or App Store Connect did not return the relationship. */
          gameCenterGroupId: string | null;
          /** Leaderboard shown by default in the Game Center dashboard, or null when there is none. */
          defaultLeaderboardId: string | null;
          /** Group leaderboard shown by default when the app belongs to a group, or null when there is none. */
          defaultGroupLeaderboardId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the leaderboard sets shared by one Game Center group, optionally narrowed by reference name or identifier. */
    "app_store_connect.list_game_center_group_leaderboard_sets": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
        /**
         * Return only leaderboard sets with these reference names.
         * @minItems 1
         */
        referenceNames?: Array<string>;
        /**
         * Return only the leaderboard sets with these identifiers.
         * @minItems 1
         */
        gameCenterLeaderboardSetIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Leaderboard sets returned for this page. */
        gameCenterLeaderboardSets: Array<{
          /** App Store Connect identifier for the leaderboard set. */
          id: string;
          /** Internal set name shown in App Store Connect. */
          referenceName?: string | null;
          /** Set identifier the game refers to, unique within the app or group. */
          vendorIdentifier?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the leaderboards shared by one Game Center group, optionally narrowed by reference name, archived state or identifier. */
    "app_store_connect.list_game_center_group_leaderboards": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
        /**
         * Return only leaderboards with these reference names.
         * @minItems 1
         */
        referenceNames?: Array<string>;
        /** Return only archived leaderboards when true, or only leaderboards still accepting scores when false. */
        archived?: boolean;
        /**
         * Return only the leaderboards with these identifiers.
         * @minItems 1
         */
        gameCenterLeaderboardIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Leaderboards returned for this page. */
        gameCenterLeaderboards: Array<{
          /** App Store Connect identifier for the leaderboard. */
          id: string;
          /** Internal leaderboard name shown in App Store Connect. */
          referenceName?: string | null;
          /** Leaderboard identifier the game submits scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** How scores are formatted unless a localization overrides it. */
          defaultFormatter?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
          /** Which submitted score is kept for a player. */
          submissionType?: "BEST_SCORE" | "MOST_RECENT_SCORE" | null;
          /** Whether a higher or a lower score ranks first. */
          scoreSortType?: "ASC" | "DESC" | null;
          /** Lowest accepted score, as a decimal string. */
          scoreRangeStart?: string | null;
          /** Highest accepted score, as a decimal string. */
          scoreRangeEnd?: string | null;
          /** When the first recurring run of the leaderboard starts, as an ISO 8601 timestamp. */
          recurrenceStartDate?: string | null;
          /** How long each recurring run lasts, as an ISO 8601 duration such as P1D. */
          recurrenceDuration?: string | null;
          /** ICS recurrence rule that schedules the recurring runs. */
          recurrenceRule?: string | null;
          /** Whether the leaderboard is archived and no longer accepting scores. */
          archived?: boolean | null;
          /** Key-value properties the leaderboard contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          /** Whether the leaderboard is shown to players. */
          visibility?: "SHOW_FOR_ALL" | "HIDE_FOR_ALL" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Game Center groups of the team. Apps in a group share their achievements, leaderboards and leaderboard sets, so players keep one progress across the apps. */
    "app_store_connect.list_game_center_groups": {
      input: {
        /**
         * Return only the groups these Game Center details belong to.
         * @minItems 1
         */
        gameCenterDetailIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Game Center groups returned for this page. */
        gameCenterGroups: Array<{
          /** App Store Connect identifier for the Game Center group. */
          id: string;
          /** Internal group name shown in App Store Connect. */
          referenceName?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the leaderboards that belong to one Game Center leaderboard set, optionally narrowed by reference name, by archived state, or to specific leaderboards. */
    "app_store_connect.list_game_center_leaderboard_set_leaderboards": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
        /**
         * Return only leaderboards with these reference names.
         * @minItems 1
         */
        referenceNames?: Array<string>;
        /** Return only archived leaderboards when true, or only leaderboards that still accept scores when false. */
        archived?: boolean;
        /**
         * Return only the leaderboards with these identifiers.
         * @minItems 1
         */
        gameCenterLeaderboardIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Leaderboards returned for this page. */
        gameCenterLeaderboards: Array<{
          /** App Store Connect identifier for the leaderboard. */
          id: string;
          /** Internal leaderboard name shown in App Store Connect. */
          referenceName?: string | null;
          /** Leaderboard identifier the game submits scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** How scores are formatted unless a localization overrides it. */
          defaultFormatter?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
          /** Which submitted score is kept for a player. */
          submissionType?: "BEST_SCORE" | "MOST_RECENT_SCORE" | null;
          /** Whether a higher or a lower score ranks first. */
          scoreSortType?: "ASC" | "DESC" | null;
          /** Lowest accepted score, as a decimal string. */
          scoreRangeStart?: string | null;
          /** Highest accepted score, as a decimal string. */
          scoreRangeEnd?: string | null;
          /** When the first recurring run of the leaderboard starts, as an ISO 8601 timestamp. */
          recurrenceStartDate?: string | null;
          /** How long each recurring run lasts, as an ISO 8601 duration such as P1D. */
          recurrenceDuration?: string | null;
          /** ICS recurrence rule that schedules the recurring runs. */
          recurrenceRule?: string | null;
          /** Whether the leaderboard is archived and no longer accepting scores. */
          archived?: boolean | null;
          /** Key-value properties the leaderboard contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          /** Whether the leaderboard is shown to players. */
          visibility?: "SHOW_FOR_ALL" | "HIDE_FOR_ALL" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the member localizations of one leaderboard inside one leaderboard set. A member localization is the name a leaderboard is shown under while a player browses the set, which can differ from the name of the leaderboard on its own. App Store Connect requires both the set and the leaderboard. */
    "app_store_connect.list_game_center_leaderboard_set_member_localizations": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
        /**
         * App Store Connect identifier of the leaderboard inside that set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Member localizations returned for this page. */
        gameCenterLeaderboardSetMemberLocalizations: Array<{
          /** App Store Connect identifier for the leaderboard set member localization. */
          id: string;
          /** Locale the name is written in, such as en-US. */
          locale?: string | null;
          /** Name the leaderboard is shown under inside the set. */
          name?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the locales one Game Center leaderboard set version is translated into. */
    "app_store_connect.list_game_center_leaderboard_set_version_localizations": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Leaderboard set localizations returned for this page. */
        gameCenterLeaderboardSetLocalizations: Array<{
          /** App Store Connect identifier for the leaderboard set localization. */
          id: string;
          /** Locale the name is written in, such as en-US. */
          locale?: string | null;
          /** Set name shown to players in this locale. */
          name?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the versions of one Game Center leaderboard set. Each version carries its own localized names through App Review. */
    "app_store_connect.list_game_center_leaderboard_set_versions": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Leaderboard set versions returned for this page. */
        gameCenterLeaderboardSetVersions: Array<{
          /** App Store Connect identifier for the leaderboard set version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the locales of one Game Center leaderboard version, each with the name, score description and score formatting shown to players. */
    "app_store_connect.list_game_center_leaderboard_version_localizations": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Leaderboard localizations returned for this page. */
        gameCenterLeaderboardLocalizations: Array<{
          /** App Store Connect identifier for the leaderboard localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Leaderboard name shown to players in this locale. */
          name?: string | null;
          /** Score formatter used in this locale instead of the leaderboard default. */
          formatterOverride?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
          /** Suffix appended to plural score values, such as points. */
          formatterSuffix?: string | null;
          /** Suffix appended to a score value of one, such as point. */
          formatterSuffixSingular?: string | null;
          /** Score description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the versions of one Game Center leaderboard. Each version carries its own localizations through App Review. */
    "app_store_connect.list_game_center_leaderboard_versions": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Leaderboard versions returned for this page. */
        gameCenterLeaderboardVersions: Array<{
          /** App Store Connect identifier for the leaderboard version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List every Game Center matchmaking queue of the team, each with the rule set it matches requests with and the rule set of a running experiment. */
    "app_store_connect.list_game_center_matchmaking_queues": {
      input: {
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Matchmaking queues returned for this page. */
        gameCenterMatchmakingQueues: Array<{
          /** App Store Connect identifier for the matchmaking queue. */
          id: string;
          /** Internal queue name shown in App Store Connect. */
          referenceName?: string | null;
          /** Bundle identifiers whose classic matchmaking requests use this queue. */
          classicMatchmakingBundleIds?: Array<string> | null;
          /** Rule set the queue matches requests with. */
          ruleSetId: string | null;
          /** Rule set a share of the queue traffic is matched with while an experiment runs, or null when no experiment is running. */
          experimentRuleSetId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the matchmaking queues that match their requests with one rule set, either as their rule set or as their experiment rule set. */
    "app_store_connect.list_game_center_matchmaking_rule_set_queues": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Matchmaking queues returned for this page. */
        gameCenterMatchmakingQueues: Array<{
          /** App Store Connect identifier for the matchmaking queue. */
          id: string;
          /** Internal queue name shown in App Store Connect. */
          referenceName?: string | null;
          /** Bundle identifiers whose classic matchmaking requests use this queue. */
          classicMatchmakingBundleIds?: Array<string> | null;
          /** Rule set the queue matches requests with. */
          ruleSetId: string | null;
          /** Rule set a share of the queue traffic is matched with while an experiment runs, or null when no experiment is running. */
          experimentRuleSetId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the rules of one Game Center matchmaking rule set, with the expression each rule evaluates for a candidate match. */
    "app_store_connect.list_game_center_matchmaking_rule_set_rules": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Matchmaking rules returned for this page. */
        gameCenterMatchmakingRules: Array<{
          /** App Store Connect identifier for the rule. */
          id: string;
          /** Internal rule name shown in App Store Connect. */
          referenceName?: string | null;
          /** What the rule checks, for the team reading the rule set. */
          description?: string | null;
          /** How the rule result is used. */
          type?: "COMPATIBLE" | "DISTANCE" | "MATCH" | "TEAM" | null;
          /** Rule expression evaluated for every candidate match. */
          expression?: string | null;
          /** How much the rule counts against the other rules of the same type when Game Center scores a candidate match. */
          weight?: number | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the teams of one Game Center matchmaking rule set, with the player limits of each team. */
    "app_store_connect.list_game_center_matchmaking_rule_set_teams": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Matchmaking teams returned for this page. */
        gameCenterMatchmakingTeams: Array<{
          /** App Store Connect identifier for the team. */
          id: string;
          /** Internal team name the rule expressions refer to. */
          referenceName?: string | null;
          /** Fewest players the team needs. */
          minPlayers?: number | null;
          /** Most players the team accepts. */
          maxPlayers?: number | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List every Game Center matchmaking rule set of the team, with the player limits and the rule language version each one uses. */
    "app_store_connect.list_game_center_matchmaking_rule_sets": {
      input: {
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Matchmaking rule sets returned for this page. */
        gameCenterMatchmakingRuleSets: Array<{
          /** App Store Connect identifier for the rule set. */
          id: string;
          /** Internal rule set name shown in App Store Connect. */
          referenceName?: string | null;
          /** Version of the rule expression language the rules use. */
          ruleLanguageVersion?: number | null;
          /** Fewest players a match created from this rule set may have. */
          minPlayers?: number | null;
          /** Most players a match created from this rule set may have. */
          maxPlayers?: number | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the territories an in-app purchase is currently sold in, using the availability record identifier from get_in_app_purchase_availability. */
    "app_store_connect.list_in_app_purchase_available_territories": {
      input: {
        /**
         * App Store Connect identifier of the availability record.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseAvailabilityId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Territories returned for this page. */
        territories: Array<{
          /** ISO 3166-1 alpha-3 territory code that App Store Connect uses as the territory identifier, such as USA. */
          id: string;
          /** ISO 4217 code of the currency prices in the territory are expressed in, such as USD. */
          currency?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the localized display names and descriptions of one in-app purchase, one record per locale. */
    "app_store_connect.list_in_app_purchase_localizations": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Localizations returned for this page. */
        localizations: Array<{
          /** App Store Connect identifier for the localization. */
          id: string;
          /** Display name customers see in this locale. */
          name?: string | null;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Description customers see in this locale. */
          description?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the custom codes generated for an in-app purchase offer code. */
    "app_store_connect.list_in_app_purchase_offer_code_custom_codes": {
      input: {
        /**
         * App Store Connect identifier of the offer code.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseOfferCodeId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Custom codes returned for this page. */
        customCodes: Array<{
          /** App Store Connect identifier for the custom code. */
          id: string;
          /** The code customers enter, such as SPRING2026. */
          customCode?: string | null;
          /** How many redemptions the custom code allows. */
          numberOfCodes?: number | null;
          /** When the code was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the code can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the code can currently be redeemed. */
          active?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the one-time-use code batches generated for an in-app purchase offer code. The code values are only available as a CSV download in App Store Connect. */
    "app_store_connect.list_in_app_purchase_offer_code_one_time_use_codes": {
      input: {
        /**
         * App Store Connect identifier of the offer code.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseOfferCodeId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** One-time-use code batches returned for this page. */
        oneTimeUseCodes: Array<{
          /** App Store Connect identifier for the batch. */
          id: string;
          /** Number of codes in the batch. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the codes can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the codes can currently be redeemed. */
          active?: boolean | null;
          /** Environment the codes redeem in. */
          environment?: "PRODUCTION" | "SANDBOX" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the discounted prices of an in-app purchase offer code per territory, with the price point, customer price, and proceeds of each. */
    "app_store_connect.list_in_app_purchase_offer_code_prices": {
      input: {
        /**
         * App Store Connect identifier of the offer code.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseOfferCodeId: string;
        /**
         * Return only records for this territory, as an ISO 3166-1 alpha-3 territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territory?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Offer prices returned for this page. */
        prices: Array<{
          /** App Store Connect identifier for the offer price. */
          id: string;
          /** Territory the price applies to, as an ISO 3166-1 alpha-3 territory code, such as USA. */
          territory: string | null;
          /** Price point the discounted price uses. */
          inAppPurchasePricePointId: string | null;
          /** Price the customer pays, as a decimal string in the territory currency. */
          customerPrice: string | null;
          /** Amount the developer receives per sale, as a decimal string in the territory currency. */
          proceeds: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the offer code configurations of an in-app purchase, optionally narrowed to offers priced in one territory. */
    "app_store_connect.list_in_app_purchase_offer_codes": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
        /**
         * Return only records for this territory, as an ISO 3166-1 alpha-3 territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territory?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Offer codes returned for this page. */
        offerCodes: Array<{
          /** App Store Connect identifier for the offer code. */
          id: string;
          /** Reference name of the offer. */
          name?: string | null;
          /** Customer groups allowed to redeem the offer. */
          customerEligibilities?: Array<"NON_SPENDER" | "ACTIVE_SPENDER" | "CHURNED_SPENDER"> | null;
          /** Number of one-time-use codes generated for the production environment. */
          productionCodeCount?: number | null;
          /** Number of one-time-use codes generated for the sandbox environment. */
          sandboxCodeCount?: number | null;
          /** Whether the offer can currently be redeemed. */
          active?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the price points in other territories that Apple considers equivalent to one price point, which is how automatic prices for a base territory are derived. */
    "app_store_connect.list_in_app_purchase_price_point_equalizations": {
      input: {
        /**
         * App Store Connect identifier of the price point to equalize from.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchasePricePointId: string;
        /**
         * Return only equalizations that belong to this in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId?: string;
        /**
         * Return only records for this territory, as an ISO 3166-1 alpha-3 territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territory?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Equalized price points returned for this page. */
        pricePoints: Array<{
          /** App Store Connect identifier for the price point. */
          id: string;
          /** Price the customer pays, as a decimal string in the territory currency. */
          customerPrice?: string | null;
          /** Amount the developer receives per sale, as a decimal string in the territory currency. */
          proceeds?: string | null;
          /** Territory the price applies to, as an ISO 3166-1 alpha-3 territory code, such as USA. */
          territory: string | null;
          /** ISO 4217 currency of the territory, such as USD. */
          currency: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the price points available to an in-app purchase, with the customer price and proceeds per territory. Use a price point identifier from here when building a price schedule or offer code price. */
    "app_store_connect.list_in_app_purchase_price_points": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
        /**
         * Return only records for this territory, as an ISO 3166-1 alpha-3 territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territory?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Price points returned for this page. */
        pricePoints: Array<{
          /** App Store Connect identifier for the price point. */
          id: string;
          /** Price the customer pays, as a decimal string in the territory currency. */
          customerPrice?: string | null;
          /** Amount the developer receives per sale, as a decimal string in the territory currency. */
          proceeds?: string | null;
          /** Territory the price applies to, as an ISO 3166-1 alpha-3 territory code, such as USA. */
          territory: string | null;
          /** ISO 4217 currency of the territory, such as USD. */
          currency: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the prices App Store Connect derived automatically from the base territory of an in-app purchase price schedule. */
    "app_store_connect.list_in_app_purchase_price_schedule_automatic_prices": {
      input: {
        /**
         * App Store Connect identifier of the price schedule, from get_in_app_purchase_price_schedule.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchasePriceScheduleId: string;
        /**
         * Return only records for this territory, as an ISO 3166-1 alpha-3 territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territory?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Automatic prices returned for this page. */
        prices: Array<{
          /** App Store Connect identifier for the scheduled price. */
          id: string;
          /** First day the price applies, as YYYY-MM-DD, or null when it applies from the start of the schedule. */
          startDate?: string | null;
          /** Last day the price applies, as YYYY-MM-DD, or null when it stays in effect until replaced. */
          endDate?: string | null;
          /** Whether the price was set manually rather than derived from the base territory. */
          manual?: boolean | null;
          /** Territory the price applies to, as an ISO 3166-1 alpha-3 territory code, such as USA. */
          territory: string | null;
          /** Price point the price uses. */
          inAppPurchasePricePointId: string | null;
          /** Price the customer pays, as a decimal string in the territory currency. */
          customerPrice: string | null;
          /** Amount the developer receives per sale, as a decimal string in the territory currency. */
          proceeds: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the manually set prices of an in-app purchase price schedule, with the price point, customer price, and proceeds of each. */
    "app_store_connect.list_in_app_purchase_price_schedule_manual_prices": {
      input: {
        /**
         * App Store Connect identifier of the price schedule, from get_in_app_purchase_price_schedule.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchasePriceScheduleId: string;
        /**
         * Return only records for this territory, as an ISO 3166-1 alpha-3 territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territory?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Manual prices returned for this page. */
        prices: Array<{
          /** App Store Connect identifier for the scheduled price. */
          id: string;
          /** First day the price applies, as YYYY-MM-DD, or null when it applies from the start of the schedule. */
          startDate?: string | null;
          /** Last day the price applies, as YYYY-MM-DD, or null when it stays in effect until replaced. */
          endDate?: string | null;
          /** Whether the price was set manually rather than derived from the base territory. */
          manual?: boolean | null;
          /** Territory the price applies to, as an ISO 3166-1 alpha-3 territory code, such as USA. */
          territory: string | null;
          /** Price point the price uses. */
          inAppPurchasePricePointId: string | null;
          /** Price the customer pays, as a decimal string in the territory currency. */
          customerPrice: string | null;
          /** Amount the developer receives per sale, as a decimal string in the territory currency. */
          proceeds: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the localized display names and descriptions attached to one in-app purchase version. */
    "app_store_connect.list_in_app_purchase_version_localizations": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase version.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Localizations returned for this page. */
        localizations: Array<{
          /** App Store Connect identifier for the localization. */
          id: string;
          /** Display name customers see in this locale. */
          name?: string | null;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Description customers see in this locale. */
          description?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the metadata versions of an in-app purchase, optionally narrowed to one review state. */
    "app_store_connect.list_in_app_purchase_versions": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
        /** Return only versions in this state. */
        state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Versions returned for this page. */
        versions: Array<{
          /** App Store Connect identifier for the in-app purchase version. */
          id: string;
          /** Sequential version number assigned by App Store Connect. */
          version?: number | null;
          /** Review state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the in-app purchases of one app (consumables, non-consumables, and non-renewing subscriptions; auto-renewable subscriptions are a separate family). Filter by type, state, name, or product identifier. */
    "app_store_connect.list_in_app_purchases": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Return only in-app purchases of this type. */
        inAppPurchaseType?: "CONSUMABLE" | "NON_CONSUMABLE" | "NON_RENEWING_SUBSCRIPTION";
        /** Return only in-app purchases in this state. */
        state?: "MISSING_METADATA" | "WAITING_FOR_UPLOAD" | "PROCESSING_CONTENT" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED";
        /**
         * Return only in-app purchases with this exact reference name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * Return only the in-app purchase with this exact product identifier.
         * @minLength 1
         * @pattern \S
         */
        productId?: string;
        /** Sort order for the returned in-app purchases. */
        sort?: "name" | "-name" | "inAppPurchaseType" | "-inAppPurchaseType";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** In-app purchases returned for this page. */
        inAppPurchases: Array<{
          /** App Store Connect identifier for the in-app purchase. */
          id: string;
          /** Reference name shown in App Store Connect. */
          name?: string | null;
          /** Product identifier the app passes to StoreKit to request the purchase. */
          productId?: string | null;
          /** Kind of in-app purchase. */
          inAppPurchaseType?: "CONSUMABLE" | "NON_CONSUMABLE" | "NON_RENEWING_SUBSCRIPTION" | null;
          /** Where the in-app purchase stands in metadata preparation, review, and sale. */
          state?: "MISSING_METADATA" | "WAITING_FOR_UPLOAD" | "PROCESSING_CONTENT" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED" | null;
          /** Note shown to App Review alongside the purchase. */
          reviewNote?: string | null;
          /** Whether the purchase can be shared through Family Sharing. */
          familySharable?: boolean | null;
          /** Whether Apple hosts the downloadable content of the purchase. */
          contentHosting?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Apple Pay certificates issued for one merchant ID, optionally filtered by type, display name, or serial number. */
    "app_store_connect.list_merchant_id_certificates": {
      input: {
        /**
         * App Store Connect identifier of the merchant ID record (the opaque resource id, not the merchant.com.example identifier string).
         * @minLength 1
         * @pattern \S
         */
        merchantIdId: string;
        /**
         * Return only certificates with this exact display name.
         * @minLength 1
         * @pattern \S
         */
        displayName?: string;
        /** Return only certificates of this type. */
        certificateType?: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC";
        /**
         * Return only the certificate with this serial number.
         * @minLength 1
         * @pattern \S
         */
        serialNumber?: string;
        /** Sort order for the returned certificates. */
        sort?: "displayName" | "-displayName" | "certificateType" | "-certificateType" | "serialNumber" | "-serialNumber" | "id" | "-id";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Certificates returned for this page. */
        certificates: Array<{
          /** App Store Connect identifier of the certificate. */
          id: string;
          /** Certificate name. */
          name?: string | null;
          /** Kind of certificate. */
          certificateType?: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC" | null;
          /** Certificate display name shown in the developer portal. */
          displayName?: string | null;
          /** Serial number of the certificate. */
          serialNumber?: string | null;
          /** Platform the certificate applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** When the certificate expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          /** Base64-encoded DER certificate. Decode it to a .cer file to install it in a keychain. */
          certificateContent?: string | null;
          /** Whether the certificate is activated. Only Apple Pay payment processing certificates carry an activation state. */
          activated?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Apple Pay merchant IDs registered with the team, optionally filtered by name or identifier. */
    "app_store_connect.list_merchant_ids": {
      input: {
        /**
         * Return only merchant IDs with this exact name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * Return only the merchant ID with this exact identifier, such as merchant.com.example.shop.
         * @minLength 1
         * @pattern \S
         */
        identifier?: string;
        /** Sort order for the returned merchant IDs. */
        sort?: "name" | "-name" | "identifier" | "-identifier";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Merchant IDs returned for this page. */
        merchantIds: Array<{
          /** App Store Connect identifier of the merchant ID record. */
          id: string;
          /** Display name of the merchant ID. */
          name?: string | null;
          /** Merchant identifier string, such as merchant.com.example.shop. */
          identifier?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the featuring nominations of the team in the given states, optionally narrowed by type, related app, or whether they include in-app events. */
    "app_store_connect.list_nominations": {
      input: {
        /**
         * Return only nominations in any of these states.
         * @minItems 1
         */
        states: Array<"DRAFT" | "SUBMITTED" | "ARCHIVED">;
        /**
         * Return only nominations of any of these types.
         * @minItems 1
         */
        types?: Array<"APP_LAUNCH" | "APP_ENHANCEMENTS" | "NEW_CONTENT">;
        /**
         * Return only nominations related to any of these apps.
         * @minItems 1
         */
        relatedAppIds?: Array<string>;
        /** Return only nominations that include in-app events when true, or only those without when false. */
        hasInAppEvents?: boolean;
        /** Sort order for the returned nominations. */
        sort?: "lastModifiedDate" | "-lastModifiedDate" | "publishStartDate" | "-publishStartDate" | "publishEndDate" | "-publishEndDate" | "name" | "-name" | "type" | "-type";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Nominations returned for this page. */
        nominations: Array<{
          /** App Store Connect identifier for the nomination. */
          id: string;
          /** Name of the nomination. */
          name?: string | null;
          /** What the nomination is about. */
          type?: "APP_LAUNCH" | "APP_ENHANCEMENTS" | "NEW_CONTENT" | null;
          /** Description of the launch or content for the editorial team. */
          description?: string | null;
          /** When the nomination was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the nomination was last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** When the nomination was submitted, as an ISO 8601 timestamp, or null while it is a draft. */
          submittedDate?: string | null;
          /** Whether the nomination is a draft, submitted, or archived. */
          state?: "DRAFT" | "SUBMITTED" | "ARCHIVED" | null;
          /** When the launch or content becomes available, as an ISO 8601 timestamp. */
          publishStartDate?: string | null;
          /** When the content stops being available, as an ISO 8601 timestamp, or null. */
          publishEndDate?: string | null;
          /** Device families the nomination applies to. */
          deviceFamilies?: Array<"IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION"> | null;
          /** Locales the launch or content is available in. */
          locales?: Array<string> | null;
          /** URLs of supplemental materials for the editorial team. */
          supplementalMaterialsUris?: Array<string> | null;
          /** Whether in-app events accompany the launch. */
          hasInAppEvents?: boolean | null;
          /** Whether the launch happens in selected markets before the rest. */
          launchInSelectMarketsFirst?: boolean | null;
          /** Additional notes for the editorial team. */
          notes?: string | null;
          /** Whether the app is available for pre-order. */
          preOrderEnabled?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the pass signing certificates issued for one pass type ID, optionally filtered by type, display name, or serial number. */
    "app_store_connect.list_pass_type_id_certificates": {
      input: {
        /**
         * App Store Connect identifier of the pass type ID record (the opaque resource id, not the pass.com.example identifier string).
         * @minLength 1
         * @pattern \S
         */
        passTypeIdId: string;
        /**
         * Return only certificates with this exact display name.
         * @minLength 1
         * @pattern \S
         */
        displayName?: string;
        /** Return only certificates of this type. */
        certificateType?: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC";
        /**
         * Return only the certificate with this serial number.
         * @minLength 1
         * @pattern \S
         */
        serialNumber?: string;
        /** Sort order for the returned certificates. */
        sort?: "displayName" | "-displayName" | "certificateType" | "-certificateType" | "serialNumber" | "-serialNumber" | "id" | "-id";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Certificates returned for this page. */
        certificates: Array<{
          /** App Store Connect identifier of the certificate. */
          id: string;
          /** Certificate name. */
          name?: string | null;
          /** Kind of certificate. */
          certificateType?: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC" | null;
          /** Certificate display name shown in the developer portal. */
          displayName?: string | null;
          /** Serial number of the certificate. */
          serialNumber?: string | null;
          /** Platform the certificate applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** When the certificate expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          /** Base64-encoded DER certificate. Decode it to a .cer file to install it in a keychain. */
          certificateContent?: string | null;
          /** Whether the certificate is activated. Only Apple Pay payment processing certificates carry an activation state. */
          activated?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Wallet pass type IDs registered with the team, optionally filtered by name or identifier. */
    "app_store_connect.list_pass_type_ids": {
      input: {
        /**
         * Return only pass type IDs with this exact name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * Return only the pass type ID with this exact identifier, such as pass.com.example.ticket.
         * @minLength 1
         * @pattern \S
         */
        identifier?: string;
        /** Sort order for the returned pass type IDs. */
        sort?: "name" | "-name" | "identifier" | "-identifier" | "id" | "-id";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Pass type IDs returned for this page. */
        passTypeIds: Array<{
          /** App Store Connect identifier of the pass type ID record. */
          id: string;
          /** Display name of the pass type ID. */
          name?: string | null;
          /** Pass type identifier string, such as pass.com.example.ticket. */
          identifier?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the prerelease versions of one app, which group its TestFlight builds by marketing version. */
    "app_store_connect.list_pre_release_versions": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Return only prerelease versions for this content platform. */
        platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
        /**
         * Return only the prerelease version with this exact version string.
         * @minLength 1
         * @pattern \S
         */
        version?: string;
        /** Sort order for the returned prerelease versions. */
        sort?: "version" | "-version";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Prerelease versions returned for this page. */
        preReleaseVersions: Array<{
          /** App Store Connect identifier for the prerelease version. */
          id: string;
          /** Marketing version string, such as 1.4.0. */
          version?: string | null;
          /** Content platform the version targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the certificates embedded in one provisioning profile. */
    "app_store_connect.list_profile_certificates": {
      input: {
        /**
         * App Store Connect identifier of the provisioning profile.
         * @minLength 1
         * @pattern \S
         */
        profileId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Certificates returned for this page. */
        certificates: Array<{
          /** App Store Connect identifier of the certificate. */
          id: string;
          /** Certificate name. */
          name?: string | null;
          /** Kind of certificate. */
          certificateType?: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC" | null;
          /** Certificate display name shown in the developer portal. */
          displayName?: string | null;
          /** Serial number of the certificate. */
          serialNumber?: string | null;
          /** Platform the certificate applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** When the certificate expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          /** Base64-encoded DER certificate. Decode it to a .cer file to install it in a keychain. */
          certificateContent?: string | null;
          /** Whether the certificate is activated. Only Apple Pay payment processing certificates carry an activation state. */
          activated?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the devices a development or ad hoc provisioning profile allows. */
    "app_store_connect.list_profile_devices": {
      input: {
        /**
         * App Store Connect identifier of the provisioning profile.
         * @minLength 1
         * @pattern \S
         */
        profileId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Devices returned for this page. */
        devices: Array<{
          /** App Store Connect identifier of the device. */
          id: string;
          /** Device name given when it was registered. */
          name?: string | null;
          /** Platform the device runs. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Unique device identifier (UDID) of the device. */
          udid?: string | null;
          /** Hardware class of the device. */
          deviceClass?: "APPLE_VISION_PRO" | "APPLE_WATCH" | "IPAD" | "IPHONE" | "IPOD" | "APPLE_TV" | "MAC" | null;
          /** Whether the device is enabled for development. Disabled devices are excluded from new profiles. */
          status?: "ENABLED" | "DISABLED" | null;
          /** Device model name. */
          model?: string | null;
          /** When the device was registered, as an ISO 8601 timestamp. */
          addedDate?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the provisioning profiles of the team, including their base64 content, optionally filtered by name, profile type, or validity state. */
    "app_store_connect.list_profiles": {
      input: {
        /**
         * Return only profiles with this exact name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** Return only profiles of this type. */
        profileType?: "IOS_APP_DEVELOPMENT" | "IOS_APP_STORE" | "IOS_APP_ADHOC" | "IOS_APP_INHOUSE" | "MAC_APP_DEVELOPMENT" | "MAC_APP_STORE" | "MAC_APP_DIRECT" | "TVOS_APP_DEVELOPMENT" | "TVOS_APP_STORE" | "TVOS_APP_ADHOC" | "TVOS_APP_INHOUSE" | "MAC_CATALYST_APP_DEVELOPMENT" | "MAC_CATALYST_APP_STORE" | "MAC_CATALYST_APP_DIRECT";
        /** Return only profiles in this state. */
        profileState?: "ACTIVE" | "INVALID";
        /** Sort order for the returned profiles. */
        sort?: "name" | "-name" | "profileType" | "-profileType" | "profileState" | "-profileState" | "id" | "-id";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Provisioning profiles returned for this page. */
        profiles: Array<{
          /** App Store Connect identifier of the provisioning profile. */
          id: string;
          /** Profile name. */
          name?: string | null;
          /** Platform the profile applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Distribution method and platform of the profile. */
          profileType?: "IOS_APP_DEVELOPMENT" | "IOS_APP_STORE" | "IOS_APP_ADHOC" | "IOS_APP_INHOUSE" | "MAC_APP_DEVELOPMENT" | "MAC_APP_STORE" | "MAC_APP_DIRECT" | "TVOS_APP_DEVELOPMENT" | "TVOS_APP_STORE" | "TVOS_APP_ADHOC" | "TVOS_APP_INHOUSE" | "MAC_CATALYST_APP_DEVELOPMENT" | "MAC_CATALYST_APP_STORE" | "MAC_CATALYST_APP_DIRECT" | null;
          /** Whether the profile is still valid. A profile becomes INVALID when a certificate it contains is revoked or a device it contains is disabled. */
          profileState?: "ACTIVE" | "INVALID" | null;
          /** Base64-encoded provisioning profile. Decode it to a .mobileprovision or .provisionprofile file to install it. */
          profileContent?: string | null;
          /** UUID embedded in the profile. */
          uuid?: string | null;
          /** When the profile was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the profile expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the in-app purchases and subscriptions promoted on the App Store product page of one app, in display order. */
    "app_store_connect.list_promoted_purchases": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Promoted purchases returned for this page. */
        promotedPurchases: Array<{
          /** App Store Connect identifier for the promoted purchase. */
          id: string;
          /** Whether every customer sees the promotion, or only those the app selects through StoreKit. */
          visibleForAllUsers?: boolean | null;
          /** Whether the promotion is enabled. */
          enabled?: boolean | null;
          /** Review state of the promotion. */
          state?: "APPROVED" | "IN_REVIEW" | "PREPARE_FOR_SUBMISSION" | "REJECTED" | null;
          /** The promoted in-app purchase, or null when the promotion is for a subscription or the link was not returned. */
          inAppPurchaseId: string | null;
          /** The promoted subscription, or null when the promotion is for an in-app purchase or the link was not returned. */
          subscriptionId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the items in a review submission, each naming the App Store version, custom product page version, experiment, or in-app event it reviews. */
    "app_store_connect.list_review_submission_items": {
      input: {
        /**
         * App Store Connect identifier of the review submission.
         * @minLength 1
         * @pattern \S
         */
        reviewSubmissionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Review submission items returned for this page. */
        reviewSubmissionItems: Array<{
          /** App Store Connect identifier for the review submission item. */
          id: string;
          /** Review state of this item. */
          state?: "READY_FOR_REVIEW" | "ACCEPTED" | "APPROVED" | "REJECTED" | "REMOVED" | null;
          /** App Store Connect identifier of the App Store version this item reviews, or null. */
          appStoreVersionId: string | null;
          /** App Store Connect identifier of the custom product page version this item reviews, or null. */
          appCustomProductPageVersionId: string | null;
          /** App Store Connect identifier of the product page optimization experiment (v1) this item reviews, or null. */
          appStoreVersionExperimentId: string | null;
          /** App Store Connect identifier of the product page optimization experiment (v2) this item reviews, or null. */
          appStoreVersionExperimentV2Id: string | null;
          /** App Store Connect identifier of the in-app event this item reviews, or null. */
          appEventId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the review submissions of one app, with the App Store version each submission carries. Filter by platform or review state. */
    "app_store_connect.list_review_submissions": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /** Return only submissions for this content platform. */
        platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
        /**
         * Return only submissions in these review states.
         * @minItems 1
         */
        states?: Array<"READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "UNRESOLVED_ISSUES" | "CANCELING" | "COMPLETING" | "COMPLETE">;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Review submissions returned for this page. */
        reviewSubmissions: Array<{
          /** App Store Connect identifier for the review submission. */
          id: string;
          /** Content platform the submission targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** When the submission was sent to App Review, as an ISO 8601 timestamp, or null while it is still being prepared. */
          submittedDate?: string | null;
          /** Where the submission stands in App Review. */
          state?: "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "UNRESOLVED_ISSUES" | "CANCELING" | "COMPLETING" | "COMPLETE" | null;
          /** App Store Connect identifier of the App Store version under review, or null when the submission carries no version. */
          appStoreVersionForReviewId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the Sandbox Apple Accounts of the team used to test in-app purchases. Sandbox accounts are created and deleted in App Store Connect, not through the API. */
    "app_store_connect.list_sandbox_testers": {
      input: {
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Sandbox testers returned for this page. */
        sandboxTesters: Array<{
          /** App Store Connect identifier for the sandbox tester. */
          id: string;
          /** Tester first name. */
          firstName?: string | null;
          /** Tester last name. */
          lastName?: string | null;
          /** Sandbox Apple Account email the tester signs in with. */
          acAccountName?: string | null;
          /** App Store territory the tester purchases in, as an ISO 3166-1 alpha-3 code. */
          territory?: "ABW" | "AFG" | "AGO" | "AIA" | "ALB" | "AND" | "ANT" | "ARE" | "ARG" | "ARM" | "ASM" | "ATG" | "AUS" | "AUT" | "AZE" | "BDI" | "BEL" | "BEN" | "BES" | "BFA" | "BGD" | "BGR" | "BHR" | "BHS" | "BIH" | "BLR" | "BLZ" | "BMU" | "BOL" | "BRA" | "BRB" | "BRN" | "BTN" | "BWA" | "CAF" | "CAN" | "CHE" | "CHL" | "CHN" | "CIV" | "CMR" | "COD" | "COG" | "COK" | "COL" | "COM" | "CPV" | "CRI" | "CUB" | "CUW" | "CXR" | "CYM" | "CYP" | "CZE" | "DEU" | "DJI" | "DMA" | "DNK" | "DOM" | "DZA" | "ECU" | "EGY" | "ERI" | "ESP" | "EST" | "ETH" | "FIN" | "FJI" | "FLK" | "FRA" | "FRO" | "FSM" | "GAB" | "GBR" | "GEO" | "GGY" | "GHA" | "GIB" | "GIN" | "GLP" | "GMB" | "GNB" | "GNQ" | "GRC" | "GRD" | "GRL" | "GTM" | "GUF" | "GUM" | "GUY" | "HKG" | "HND" | "HRV" | "HTI" | "HUN" | "IDN" | "IMN" | "IND" | "IRL" | "IRQ" | "ISL" | "ISR" | "ITA" | "JAM" | "JEY" | "JOR" | "JPN" | "KAZ" | "KEN" | "KGZ" | "KHM" | "KIR" | "KNA" | "KOR" | "KWT" | "LAO" | "LBN" | "LBR" | "LBY" | "LCA" | "LIE" | "LKA" | "LSO" | "LTU" | "LUX" | "LVA" | "MAC" | "MAR" | "MCO" | "MDA" | "MDG" | "MDV" | "MEX" | "MHL" | "MKD" | "MLI" | "MLT" | "MMR" | "MNE" | "MNG" | "MNP" | "MOZ" | "MRT" | "MSR" | "MTQ" | "MUS" | "MWI" | "MYS" | "MYT" | "NAM" | "NCL" | "NER" | "NFK" | "NGA" | "NIC" | "NIU" | "NLD" | "NOR" | "NPL" | "NRU" | "NZL" | "OMN" | "PAK" | "PAN" | "PER" | "PHL" | "PLW" | "PNG" | "POL" | "PRI" | "PRT" | "PRY" | "PSE" | "PYF" | "QAT" | "REU" | "ROU" | "RUS" | "RWA" | "SAU" | "SEN" | "SGP" | "SHN" | "SLB" | "SLE" | "SLV" | "SMR" | "SOM" | "SPM" | "SRB" | "SSD" | "STP" | "SUR" | "SVK" | "SVN" | "SWE" | "SWZ" | "SXM" | "SYC" | "TCA" | "TCD" | "TGO" | "THA" | "TJK" | "TKM" | "TLS" | "TON" | "TTO" | "TUN" | "TUR" | "TUV" | "TWN" | "TZA" | "UGA" | "UKR" | "UMI" | "URY" | "USA" | "UZB" | "VAT" | "VCT" | "VEN" | "VGB" | "VIR" | "VNM" | "VUT" | "WLF" | "WSM" | "XKS" | "YEM" | "ZAF" | "ZMB" | "ZWE" | null;
          /** Whether the account can test Apple Pay. */
          applePayCompatible?: boolean | null;
          /** Whether purchases are interrupted so the tester must resolve a billing issue first. */
          interruptPurchases?: boolean | null;
          /** How fast a one-month subscription renews in the sandbox. */
          subscriptionRenewalRate?: "MONTHLY_RENEWAL_EVERY_ONE_HOUR" | "MONTHLY_RENEWAL_EVERY_THIRTY_MINUTES" | "MONTHLY_RENEWAL_EVERY_FIFTEEN_MINUTES" | "MONTHLY_RENEWAL_EVERY_FIVE_MINUTES" | "MONTHLY_RENEWAL_EVERY_THREE_MINUTES" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the repositories Xcode Cloud can access on one source control provider. */
    "app_store_connect.list_scm_provider_repositories": {
      input: {
        /**
         * App Store Connect identifier of the source control provider.
         * @minLength 1
         * @pattern \S
         */
        scmProviderId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Repositories returned for this page. */
        scmRepositories: Array<{
          /** App Store Connect identifier for the repository. */
          id: string;
          /** When Xcode Cloud last accessed the repository, as an ISO 8601 timestamp. */
          lastAccessedDate?: string | null;
          /** HTTPS clone URL. */
          httpCloneUrl?: string | null;
          /** SSH clone URL. */
          sshCloneUrl?: string | null;
          /** Owner or organization the repository belongs to. */
          ownerName?: string | null;
          /** Repository name. */
          repositoryName?: string | null;
          /** Identifier of the source control provider. */
          scmProviderId: string | null;
          /** Identifier of the Git reference for the default branch. */
          defaultBranchId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the source control providers (GitHub, GitLab, Bitbucket) connected to Xcode Cloud for the team. */
    "app_store_connect.list_scm_providers": {
      input: {
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Source control providers returned for this page. */
        scmProviders: Array<{
          /** App Store Connect identifier for the source control provider. */
          id: string;
          /** Kind of source control provider. */
          scmProviderType?: {
            /** Provider product. */
            kind?: "BITBUCKET_CLOUD" | "BITBUCKET_SERVER" | "GITHUB_CLOUD" | "GITHUB_ENTERPRISE" | "GITLAB_CLOUD" | "GITLAB_SELF_MANAGED";
            /** Display name of the provider. */
            displayName?: string;
            /** Whether the provider is self-hosted. */
            isOnPremise?: boolean;
            [key: string]: unknown;
          } | null;
          /** Base URL of the provider. */
          url?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List every source repository Xcode Cloud can access across all connected providers, optionally narrowed to specific repository identifiers. */
    "app_store_connect.list_scm_repositories": {
      input: {
        /**
         * Return only the repositories with these identifiers.
         * @minItems 1
         */
        scmRepositoryIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Repositories returned for this page. */
        scmRepositories: Array<{
          /** App Store Connect identifier for the repository. */
          id: string;
          /** When Xcode Cloud last accessed the repository, as an ISO 8601 timestamp. */
          lastAccessedDate?: string | null;
          /** HTTPS clone URL. */
          httpCloneUrl?: string | null;
          /** SSH clone URL. */
          sshCloneUrl?: string | null;
          /** Owner or organization the repository belongs to. */
          ownerName?: string | null;
          /** Repository name. */
          repositoryName?: string | null;
          /** Identifier of the source control provider. */
          scmProviderId: string | null;
          /** Identifier of the Git reference for the default branch. */
          defaultBranchId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the branches and tags of one repository as Xcode Cloud sees them. Their identifiers are what start_ci_build_run takes as sourceBranchOrTagId. */
    "app_store_connect.list_scm_repository_git_references": {
      input: {
        /**
         * App Store Connect identifier of the repository.
         * @minLength 1
         * @pattern \S
         */
        scmRepositoryId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Branches and tags returned for this page. */
        scmGitReferences: Array<{
          /** App Store Connect identifier for the Git reference. */
          id: string;
          /** Short name, such as main or v1.2.0. */
          name?: string | null;
          /** Full reference name, such as refs/heads/main. */
          canonicalName?: string | null;
          /** Whether the reference has been deleted from the repository. */
          isDeleted?: boolean | null;
          /** Whether the reference is a branch or a tag. */
          kind?: "BRANCH" | "TAG" | null;
          /** Identifier of the repository the reference belongs to. */
          scmRepositoryId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the pull requests of one repository as Xcode Cloud sees them. Their identifiers are what start_ci_build_run takes as scmPullRequestId. */
    "app_store_connect.list_scm_repository_pull_requests": {
      input: {
        /**
         * App Store Connect identifier of the repository.
         * @minLength 1
         * @pattern \S
         */
        scmRepositoryId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Pull requests returned for this page. */
        scmPullRequests: Array<{
          /** App Store Connect identifier for the pull request. */
          id: string;
          /** Pull request title. */
          title?: string | null;
          /** Pull request number on the source control provider. */
          number?: number | null;
          /** URL of the pull request on the source control provider. */
          webUrl?: string | null;
          /** Owner of the repository the changes come from. */
          sourceRepositoryOwner?: string | null;
          /** Name of the repository the changes come from. */
          sourceRepositoryName?: string | null;
          /** Branch the changes come from. */
          sourceBranchName?: string | null;
          /** Owner of the repository the changes go to. */
          destinationRepositoryOwner?: string | null;
          /** Name of the repository the changes go to. */
          destinationRepositoryName?: string | null;
          /** Branch the changes go to. */
          destinationBranchName?: string | null;
          /** Whether the pull request is closed. */
          isClosed?: boolean | null;
          /** Whether the source and destination repositories differ. */
          isCrossRepository?: boolean | null;
          /** Identifier of the repository the pull request belongs to. */
          scmRepositoryId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the customer-facing localized names of one subscription group. */
    "app_store_connect.list_subscription_group_localizations": {
      input: {
        /**
         * App Store Connect identifier of the subscription group.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscription group localizations returned for this page. */
        subscriptionGroupLocalizations: Array<{
          /** App Store Connect identifier for the subscription group localization. */
          id: string;
          /** Group name shown to customers in this locale. */
          name?: string | null;
          /** Alternative app name shown with the group in this locale, when one is set. */
          customAppName?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the localized names attached to one subscription group version. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.list_subscription_group_version_localizations": {
      input: {
        /**
         * App Store Connect identifier of the subscription group version.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscription group version localizations returned for this page. */
        subscriptionGroupLocalizations: Array<{
          /** App Store Connect identifier for the subscription group version localization. */
          id: string;
          /** Group name shown to customers in this locale. */
          name?: string | null;
          /** Alternative app name shown with the group in this locale, when one is set. */
          customAppName?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the versions of one subscription group, optionally narrowed by review state. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.list_subscription_group_versions": {
      input: {
        /**
         * App Store Connect identifier of the subscription group.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupId: string;
        /**
         * Return only versions in one of these review states.
         * @minItems 1
         */
        states?: Array<"PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED">;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscription group versions returned for this page. */
        subscriptionGroupVersions: Array<{
          /** App Store Connect identifier for the subscription group version. */
          id: string;
          /** Sequential version number assigned by App Store Connect. */
          version?: number | null;
          /** Review state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the subscription groups of one app, optionally narrowed by reference name or by the state of the subscriptions they contain. */
    "app_store_connect.list_subscription_groups": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Return only the group with this exact internal reference name.
         * @minLength 1
         * @pattern \S
         */
        referenceName?: string;
        /**
         * Return only groups that contain a subscription in one of these states.
         * @minItems 1
         */
        subscriptionStates?: Array<"MISSING_METADATA" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED">;
        /** Sort order for the returned groups. */
        sort?: "referenceName" | "-referenceName";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscription groups returned for this page. */
        subscriptionGroups: Array<{
          /** App Store Connect identifier for the subscription group. */
          id: string;
          /** Internal reference name of the group, not shown to customers. */
          referenceName?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the introductory offers of one auto-renewable subscription, with the territory and price point of each offer. Filter by territory to inspect one storefront. */
    "app_store_connect.list_subscription_introductory_offers": {
      input: {
        /**
         * App Store Connect identifier of the auto-renewable subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Return only entries for these territories, as ISO 3166-1 alpha-3 codes such as USA or DEU.
         * @minItems 1
         */
        territoryIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Introductory offers returned for this page. */
        subscriptionIntroductoryOffers: Array<{
          /** App Store Connect identifier for the introductory offer. */
          id: string;
          /** First day the offer is available, as YYYY-MM-DD. */
          startDate?: string | null;
          /** Last day the offer is available, as YYYY-MM-DD. */
          endDate?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          /** The territory the price applies to, or null when App Store Connect did not return it. */
          territory: {
            /** ISO 3166-1 alpha-3 territory code, such as USA. */
            id: string;
            /** ISO 4217 currency of the territory, such as USD. */
            currency?: string | null;
            [key: string]: unknown;
          } | null;
          /** The subscription price point charged for the offer, or null when App Store Connect did not return it. */
          subscriptionPricePoint: {
            /** App Store Connect identifier for the subscription price point. */
            id: string;
            /** Price the customer pays in the territory currency, as a decimal string. */
            customerPrice?: string | null;
            /** Developer proceeds per period during the first year, as a decimal string. */
            proceeds?: string | null;
            /** Developer proceeds per period after the first year, as a decimal string. */
            proceedsYear2?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the customer-facing localized names and descriptions of one subscription. */
    "app_store_connect.list_subscription_localizations": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscription localizations returned for this page. */
        subscriptionLocalizations: Array<{
          /** App Store Connect identifier for the subscription localization. */
          id: string;
          /** Subscription name shown to customers in this locale. */
          name?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Subscription description shown to customers in this locale. */
          description?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the custom codes generated for one offer code configuration. */
    "app_store_connect.list_subscription_offer_code_custom_codes": {
      input: {
        /**
         * App Store Connect identifier of the offer code configuration.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Custom codes returned for this page. */
        subscriptionOfferCodeCustomCodes: Array<{
          /** App Store Connect identifier for the custom code batch. */
          id: string;
          /** The custom code customers redeem. */
          customCode?: string | null;
          /** Number of redemptions the custom code allows. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the code can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the custom code is active and redeemable. */
          active?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the one-time use code batches generated for one offer code configuration. The code values themselves are only available as a CSV download in App Store Connect. */
    "app_store_connect.list_subscription_offer_code_one_time_use_codes": {
      input: {
        /**
         * App Store Connect identifier of the offer code configuration.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** One-time use code batches returned for this page. */
        subscriptionOfferCodeOneTimeUseCodes: Array<{
          /** App Store Connect identifier for the one-time use code batch. */
          id: string;
          /** Number of single-use codes in the batch. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the codes can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the batch is active and its codes redeemable. */
          active?: boolean | null;
          /** Environment the codes redeem in. */
          environment?: "PRODUCTION" | "SANDBOX" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the territory prices of one offer code configuration, with the currency and customer price of each price point. */
    "app_store_connect.list_subscription_offer_code_prices": {
      input: {
        /**
         * App Store Connect identifier of the offer code configuration.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeId: string;
        /**
         * Return only entries for these territories, as ISO 3166-1 alpha-3 codes such as USA or DEU.
         * @minItems 1
         */
        territoryIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Offer code prices returned for this page. */
        subscriptionOfferCodePrices: Array<{
          /** App Store Connect identifier for the offer code price. */
          id: string;
          /** The territory the price applies to, or null when App Store Connect did not return it. */
          territory: {
            /** ISO 3166-1 alpha-3 territory code, such as USA. */
            id: string;
            /** ISO 4217 currency of the territory, such as USD. */
            currency?: string | null;
            [key: string]: unknown;
          } | null;
          /** The subscription price point charged for the offer, or null when App Store Connect did not return it. */
          subscriptionPricePoint: {
            /** App Store Connect identifier for the subscription price point. */
            id: string;
            /** Price the customer pays in the territory currency, as a decimal string. */
            customerPrice?: string | null;
            /** Developer proceeds per period during the first year, as a decimal string. */
            proceeds?: string | null;
            /** Developer proceeds per period after the first year, as a decimal string. */
            proceedsYear2?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the offer code configurations of one auto-renewable subscription, with the code counts and active state of each configuration. */
    "app_store_connect.list_subscription_offer_codes": {
      input: {
        /**
         * App Store Connect identifier of the auto-renewable subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Return only entries for these territories, as ISO 3166-1 alpha-3 codes such as USA or DEU.
         * @minItems 1
         */
        territoryIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Offer code configurations returned for this page. */
        subscriptionOfferCodes: Array<{
          /** App Store Connect identifier for the offer code configuration. */
          id: string;
          /** Reference name of the offer code configuration. */
          name?: string | null;
          /** Customer groups eligible to redeem the codes. */
          customerEligibilities?: Array<"NEW" | "EXISTING" | "EXPIRED"> | null;
          /** Whether the offer stacks with or replaces introductory offers. */
          offerEligibility?: "STACK_WITH_INTRO_OFFERS" | "REPLACE_INTRO_OFFERS" | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          /** Total number of codes generated so far. */
          totalNumberOfCodes?: number | null;
          /** Number of codes generated for production. */
          productionCodeCount?: number | null;
          /** Number of codes generated for the sandbox. */
          sandboxCodeCount?: number | null;
          /** Whether the configuration is active and its codes redeemable. */
          active?: boolean | null;
          /** Whether the subscription renews automatically after the offer ends. */
          autoRenewEnabled?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the territory availability records of one subscription, one per payment plan. Use list_subscription_plan_availability_territories for the full territory list of a record. */
    "app_store_connect.list_subscription_plan_availabilities": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscription plan availabilities returned for this page. */
        subscriptionPlanAvailabilities: Array<{
          /** App Store Connect identifier for the subscription plan availability. */
          id: string;
          /** Whether the plan becomes available automatically in territories the App Store adds later. */
          availableInNewTerritories?: boolean | null;
          /** Payment plan of the subscription: UPFRONT charges the full price at the start of each period, MONTHLY is a 12-month commitment billed monthly (App Store Connect API 4.4). */
          planType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the territories in which one subscription plan availability sells the plan. */
    "app_store_connect.list_subscription_plan_availability_territories": {
      input: {
        /**
         * App Store Connect identifier of the subscription plan availability.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPlanAvailabilityId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Territories returned for this page. */
        territories: Array<{
          /** ISO 3166-1 alpha-3 territory code that App Store Connect uses as the territory identifier, such as USA. */
          id: string;
          /** ISO 4217 code of the currency prices in the territory are expressed in, such as USD. */
          currency?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the adjusted territory equalizations of one subscription price point, the equivalents App Store Connect proposes after its territory-specific price adjustments. */
    "app_store_connect.list_subscription_price_point_adjusted_equalizations": {
      input: {
        /**
         * App Store Connect identifier of the subscription price point to equalize from.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPricePointId: string;
        /**
         * Return only the adjusted equivalent of this territory. ISO 3166-1 alpha-3 App Store territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territory?: string;
        /**
         * Return only adjusted equivalents that apply to this subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId?: string;
        /** Return only adjusted equivalents of this payment plan. */
        planType?: "MONTHLY" | "UPFRONT";
        /**
         * Return only the monthly price points that pair with this upfront price point.
         * @minLength 1
         * @pattern \S
         */
        upfrontPricePointId?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Adjusted equivalent subscription price points returned for this page. */
        subscriptionPricePoints: Array<{
          /** App Store Connect identifier for the subscription price point. */
          id: string;
          /** Price the customer pays, as a decimal string in the territory currency. */
          customerPrice?: string | null;
          /** Developer proceeds during the first year of a subscription, as a decimal string. */
          proceeds?: string | null;
          /** Developer proceeds after the first year of a subscription, as a decimal string. */
          proceedsYear2?: string | null;
          /** Territory the price point belongs to. ISO 3166-1 alpha-3 App Store territory code, such as USA. */
          territoryId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the price points in other territories that App Store Connect considers equivalent to one subscription price point, which is how a price set in one territory is carried over to the rest. */
    "app_store_connect.list_subscription_price_point_equalizations": {
      input: {
        /**
         * App Store Connect identifier of the subscription price point to equalize from.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPricePointId: string;
        /**
         * Return only the equivalent price point of this territory. ISO 3166-1 alpha-3 App Store territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territory?: string;
        /**
         * Return only equivalents that apply to this subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId?: string;
        /** Return only equivalents of this payment plan. */
        planType?: "MONTHLY" | "UPFRONT";
        /**
         * Return only the monthly price points that pair with this upfront price point.
         * @minLength 1
         * @pattern \S
         */
        upfrontPricePointId?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Equivalent subscription price points returned for this page. */
        subscriptionPricePoints: Array<{
          /** App Store Connect identifier for the subscription price point. */
          id: string;
          /** Price the customer pays, as a decimal string in the territory currency. */
          customerPrice?: string | null;
          /** Developer proceeds during the first year of a subscription, as a decimal string. */
          proceeds?: string | null;
          /** Developer proceeds after the first year of a subscription, as a decimal string. */
          proceedsYear2?: string | null;
          /** Territory the price point belongs to. ISO 3166-1 alpha-3 App Store territory code, such as USA. */
          territoryId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the price points a subscription can be priced at, with the customer price and developer proceeds of each. Apple recommends filtering by territory and plans to make that filter mandatory. */
    "app_store_connect.list_subscription_price_points": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Return only price points of this territory. ISO 3166-1 alpha-3 App Store territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territory?: string;
        /** Return only price points of this payment plan. */
        planType?: "MONTHLY" | "UPFRONT";
        /**
         * Return only the monthly price points that pair with this upfront price point.
         * @minLength 1
         * @pattern \S
         */
        upfrontPricePointId?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscription price points returned for this page. */
        subscriptionPricePoints: Array<{
          /** App Store Connect identifier for the subscription price point. */
          id: string;
          /** Price the customer pays, as a decimal string in the territory currency. */
          customerPrice?: string | null;
          /** Developer proceeds during the first year of a subscription, as a decimal string. */
          proceeds?: string | null;
          /** Developer proceeds after the first year of a subscription, as a decimal string. */
          proceedsYear2?: string | null;
          /** Territory the price point belongs to. ISO 3166-1 alpha-3 App Store territory code, such as USA. */
          territoryId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the current and scheduled prices of one subscription, with the territory and price point of each price. Filter by territory, payment plan, or price point. */
    "app_store_connect.list_subscription_prices": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Return only prices in this territory. ISO 3166-1 alpha-3 App Store territory code, such as USA.
         * @minLength 1
         * @pattern \S
         */
        territory?: string;
        /** Return only prices of this payment plan. */
        planType?: "MONTHLY" | "UPFRONT";
        /**
         * Return only prices based on this subscription price point.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPricePointId?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscription prices returned for this page. */
        subscriptionPrices: Array<{
          /** App Store Connect identifier for the subscription price. */
          id: string;
          /** Date the price takes effect, as YYYY-MM-DD, or null for a price that is not scheduled. */
          startDate?: string | null;
          /** Whether existing subscribers keep this price after a later price increase. */
          preserved?: boolean | null;
          /** Payment plan of the subscription: UPFRONT charges the full price at the start of each period, MONTHLY is a 12-month commitment billed monthly (App Store Connect API 4.4). */
          planType?: "MONTHLY" | "UPFRONT" | null;
          /** Territory the price applies to. ISO 3166-1 alpha-3 App Store territory code, such as USA. */
          territoryId: string | null;
          /** The price point the price is based on. Only the id is filled in when App Store Connect did not return the price point record; null when the price has no price point. */
          subscriptionPricePoint: {
            /** App Store Connect identifier for the subscription price point. */
            id: string;
            /** Price the customer pays, as a decimal string in the territory currency. */
            customerPrice?: string | null;
            /** Developer proceeds during the first year of a subscription, as a decimal string. */
            proceeds?: string | null;
            /** Developer proceeds after the first year of a subscription, as a decimal string. */
            proceedsYear2?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the territory prices of one promotional offer, with the currency and customer price of each price point. */
    "app_store_connect.list_subscription_promotional_offer_prices": {
      input: {
        /**
         * App Store Connect identifier of the promotional offer.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPromotionalOfferId: string;
        /**
         * Return only entries for these territories, as ISO 3166-1 alpha-3 codes such as USA or DEU.
         * @minItems 1
         */
        territoryIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Promotional offer prices returned for this page. */
        subscriptionPromotionalOfferPrices: Array<{
          /** App Store Connect identifier for the promotional offer price. */
          id: string;
          /** The territory the price applies to, or null when App Store Connect did not return it. */
          territory: {
            /** ISO 3166-1 alpha-3 territory code, such as USA. */
            id: string;
            /** ISO 4217 currency of the territory, such as USD. */
            currency?: string | null;
            [key: string]: unknown;
          } | null;
          /** The subscription price point charged for the offer, or null when App Store Connect did not return it. */
          subscriptionPricePoint: {
            /** App Store Connect identifier for the subscription price point. */
            id: string;
            /** Price the customer pays in the territory currency, as a decimal string. */
            customerPrice?: string | null;
            /** Developer proceeds per period during the first year, as a decimal string. */
            proceeds?: string | null;
            /** Developer proceeds per period after the first year, as a decimal string. */
            proceedsYear2?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the promotional offers of one auto-renewable subscription. Use list_subscription_promotional_offer_prices to read the territory prices of an offer. */
    "app_store_connect.list_subscription_promotional_offers": {
      input: {
        /**
         * App Store Connect identifier of the auto-renewable subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Return only entries for these territories, as ISO 3166-1 alpha-3 codes such as USA or DEU.
         * @minItems 1
         */
        territoryIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Promotional offers returned for this page. */
        subscriptionPromotionalOffers: Array<{
          /** App Store Connect identifier for the promotional offer. */
          id: string;
          /** Reference name shown in App Store Connect. */
          name?: string | null;
          /** Offer identifier your app passes to StoreKit when presenting the offer. */
          offerCode?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the localized names and descriptions attached to one subscription version. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.list_subscription_version_localizations": {
      input: {
        /**
         * App Store Connect identifier of the subscription version.
         * @minLength 1
         * @pattern \S
         */
        subscriptionVersionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscription version localizations returned for this page. */
        subscriptionLocalizations: Array<{
          /** App Store Connect identifier for the subscription version localization. */
          id: string;
          /** Subscription name shown to customers in this locale. */
          name?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Subscription description shown to customers in this locale. */
          description?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the versions of one subscription, optionally narrowed by review state. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.list_subscription_versions": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Return only versions in one of these review states.
         * @minItems 1
         */
        states?: Array<"PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED">;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscription versions returned for this page. */
        subscriptionVersions: Array<{
          /** App Store Connect identifier for the subscription version. */
          id: string;
          /** Sequential version number assigned by App Store Connect. */
          version?: number | null;
          /** Review state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | "DEVELOPER_REJECTED" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the auto-renewable subscriptions of one subscription group, optionally narrowed by name, product identifier, or state. */
    "app_store_connect.list_subscriptions": {
      input: {
        /**
         * App Store Connect identifier of the subscription group.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupId: string;
        /**
         * Return only the subscription with this exact reference name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * Return only the subscription with this exact product identifier.
         * @minLength 1
         * @pattern \S
         */
        productId?: string;
        /**
         * Return only subscriptions in one of these states.
         * @minItems 1
         */
        states?: Array<"MISSING_METADATA" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED">;
        /** Sort order for the returned subscriptions. */
        sort?: "name" | "-name";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Subscriptions returned for this page. */
        subscriptions: Array<{
          /** App Store Connect identifier for the subscription. */
          id: string;
          /** Internal reference name of the subscription. */
          name?: string | null;
          /** Product identifier used by the app to purchase the subscription. */
          productId?: string | null;
          /** Whether the subscription can be shared with Family Sharing. */
          familySharable?: boolean | null;
          /** Review and sale state of the subscription. */
          state?: "MISSING_METADATA" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED" | null;
          /** Length of one subscription period. */
          subscriptionPeriod?: "ONE_WEEK" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** Note shown to App Review alongside the subscription. */
          reviewNote?: string | null;
          /** Rank of the subscription within its group, where 1 is the highest level of service. */
          groupLevel?: number | null;
          /** App Store Connect identifier of the group the subscription belongs to. */
          subscriptionGroupId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List every App Store territory with its currency. Territory identifiers are used for license agreements, availability, and pricing. */
    "app_store_connect.list_territories": {
      input: {
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Territories returned for this page. */
        territories: Array<{
          /** ISO 3166-1 alpha-3 territory code that App Store Connect uses as the territory identifier, such as USA. */
          id: string;
          /** ISO 4217 code of the currency prices in the territory are expressed in, such as USD. */
          currency?: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the per-territory availability of an app availability configuration: whether the app is on sale in each territory, its release date, pre-order settings and the content statuses that explain why it is or is not available. */
    "app_store_connect.list_territory_availabilities": {
      input: {
        /**
         * App Store Connect identifier of the app availability, from get_app_availability.
         * @minLength 1
         * @pattern \S
         */
        appAvailabilityId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Territory availabilities returned for this page. */
        territoryAvailabilities: Array<{
          /** App Store Connect identifier for the territory availability. */
          id: string;
          /** Whether the app is available in the territory. */
          available?: boolean | null;
          /** Scheduled release date in the territory, as YYYY-MM-DD, or null when none is set. */
          releaseDate?: string | null;
          /** Whether the app can be pre-ordered in the territory. */
          preOrderEnabled?: boolean | null;
          /** Day the pre-order was or will be published in the territory, as YYYY-MM-DD. */
          preOrderPublishDate?: string | null;
          /** Reasons the app is or is not on sale in the territory, as reported by App Store Connect. */
          contentStatuses?: Array<"AVAILABLE" | "AVAILABLE_FOR_PREORDER_ON_DATE" | "PROCESSING_TO_NOT_AVAILABLE" | "PROCESSING_TO_AVAILABLE" | "PROCESSING_TO_PRE_ORDER" | "AVAILABLE_FOR_SALE_UNRELEASED_APP" | "PREORDER_ON_UNRELEASED_APP" | "AVAILABLE_FOR_PREORDER" | "MISSING_RATING" | "CANNOT_SELL_RESTRICTED_RATING" | "BRAZIL_REQUIRED_TAX_ID" | "BRAZIL_GAMBLING_NOT_VERIFIED" | "MISSING_GRN" | "UNVERIFIED_GRN" | "ICP_NUMBER_INVALID" | "ICP_NUMBER_MISSING" | "TRADER_STATUS_NOT_PROVIDED" | "TRADER_STATUS_VERIFICATION_FAILED" | "TRADER_STATUS_VERIFICATION_STATUS_MISSING" | "CANNOT_SELL_SEVENTEEN_PLUS_APPS" | "CANNOT_SELL_SEXUALLY_EXPLICIT" | "CANNOT_SELL_NON_IOS_GAMES" | "CANNOT_SELL_SEVENTEEN_PLUS_GAMES" | "CANNOT_SELL_CASINO" | "CANNOT_SELL_CASINO_WITHOUT_GRAC" | "CANNOT_SELL_CASINO_WITHOUT_AGE_VERIFICATION" | "CANNOT_SELL_ADULT_ONLY" | "CANNOT_SELL_GAMBLING_CONTESTS" | "CANNOT_SELL_GAMBLING" | "CANNOT_SELL_CONTESTS" | "CANNOT_SELL_NINETEEN_PLUS_WITHOUT_GRAC" | "CANNOT_SELL" | "CANNOT_SELL_FREQUENT_INTENSE_GAMBLING" | "CANNOT_SELL_FREQUENT_INTENSE_ALCOHOL_TOBACCO_DRUGS" | "CANNOT_SELL_FREQUENT_INTENSE_VIOLENCE" | "CANNOT_SELL_FREQUENT_INTENSE_SEXUAL_CONTENT_NUDITY" | "CANNOT_SELL_INFREQUENT_MILD_ALCOHOL_TOBACCO_DRUGS" | "CANNOT_SELL_INFREQUENT_MILD_SEXUAL_CONTENT_NUDITY" | "CANNOT_SELL_FREQUENT_INTENSE" | "CANNOT_SELL_FREQUENT_INTENSE_WITHOUT_GRAC" | "CANNOT_SELL_FREQUENT_GAMBLING" | "CANNOT_SELL_FREQUENT_ALCOHOL_TOBACCO_DRUGS" | "CANNOT_SELL_FREQUENT_VIOLENCE" | "CANNOT_SELL_FREQUENT_SEXUAL_CONTENT_NUDITY" | "CANNOT_SELL_INFREQUENT_ALCOHOL_TOBACCO_DRUGS" | "CANNOT_SELL_INFREQUENT_SEXUAL_CONTENT_NUDITY" | "CANNOT_SELL_FREQUENT" | "CANNOT_SELL_FREQUENT_WITHOUT_GRAC"> | null;
          /** Territory the record applies to, such as USA. */
          territoryId: string | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the apps a pending invitation will make visible to the invited user. */
    "app_store_connect.list_user_invitation_visible_apps": {
      input: {
        /**
         * App Store Connect identifier of the invitation.
         * @minLength 1
         * @pattern \S
         */
        userInvitationId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Apps returned for this page. */
        apps: Array<{
          /** App Store Connect identifier for the app. */
          id: string;
          /** App name shown on the App Store. */
          name?: string | null;
          /** Bundle identifier registered for the app. */
          bundleId?: string | null;
          /** SKU chosen when the app record was created. */
          sku?: string | null;
          /** Primary App Store locale, such as en-US. */
          primaryLocale?: string | null;
          /** Whether the app is or has ever been part of the Kids category. */
          isOrEverWasMadeForKids?: boolean | null;
          /** Third-party content rights declared for the app. */
          contentRightsDeclaration?: "DOES_NOT_USE_THIRD_PARTY_CONTENT" | "USES_THIRD_PARTY_CONTENT" | null;
          /** Whether streamlined purchasing is enabled for the app. */
          streamlinedPurchasingEnabled?: boolean | null;
          /** Accessibility information URL published with the app. */
          accessibilityUrl?: string | null;
          /** Production server-to-server subscription status URL. */
          subscriptionStatusUrl?: string | null;
          /** Version of the production subscription status URL. */
          subscriptionStatusUrlVersion?: "V1" | "V2" | null;
          /** Sandbox server-to-server subscription status URL. */
          subscriptionStatusUrlForSandbox?: string | null;
          /** Version of the sandbox subscription status URL. */
          subscriptionStatusUrlVersionForSandbox?: "V1" | "V2" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List pending invitations to join the App Store Connect team, optionally narrowed by email, role, or visible app. */
    "app_store_connect.list_user_invitations": {
      input: {
        /**
         * Return only the invitation sent to this exact email address.
         * @format email
         */
        email?: string;
        /**
         * Return only invitations granting at least one of these roles.
         * @minItems 1
         */
        roles?: Array<"ADMIN" | "FINANCE" | "ACCOUNT_HOLDER" | "SALES" | "MARKETING" | "APP_MANAGER" | "DEVELOPER" | "ACCESS_TO_REPORTS" | "CUSTOMER_SUPPORT" | "CREATE_APPS" | "CLOUD_MANAGED_DEVELOPER_ID" | "CLOUD_MANAGED_APP_DISTRIBUTION" | "GENERATE_INDIVIDUAL_KEYS">;
        /**
         * Return only invitations that make this app visible.
         * @minLength 1
         * @pattern \S
         */
        visibleAppId?: string;
        /** Sort order for the returned invitations. */
        sort?: "email" | "-email" | "lastName" | "-lastName";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Invitations returned for this page. */
        userInvitations: Array<{
          /** App Store Connect identifier for the invitation. */
          id: string;
          /** Email address the invitation was sent to. */
          email?: string | null;
          /** First name of the invited user. */
          firstName?: string | null;
          /** Last name of the invited user. */
          lastName?: string | null;
          /** When the invitation expires, as an ISO 8601 timestamp. Invitations last three days. */
          expirationDate?: string | null;
          /** Roles the user receives on accepting. */
          roles?: Array<"ADMIN" | "FINANCE" | "ACCOUNT_HOLDER" | "SALES" | "MARKETING" | "APP_MANAGER" | "DEVELOPER" | "ACCESS_TO_REPORTS" | "CUSTOMER_SUPPORT" | "CREATE_APPS" | "CLOUD_MANAGED_DEVELOPER_ID" | "CLOUD_MANAGED_APP_DISTRIBUTION" | "GENERATE_INDIVIDUAL_KEYS"> | null;
          /** Whether the user will see every app on the team. */
          allAppsVisible?: boolean | null;
          /** Whether the user may manage certificates, identifiers, and profiles. */
          provisioningAllowed?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the apps a team member can see. For a user with allAppsVisible the list reflects every app on the team. */
    "app_store_connect.list_user_visible_apps": {
      input: {
        /**
         * App Store Connect identifier of the team member.
         * @minLength 1
         * @pattern \S
         */
        userId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Apps returned for this page. */
        apps: Array<{
          /** App Store Connect identifier for the app. */
          id: string;
          /** App name shown on the App Store. */
          name?: string | null;
          /** Bundle identifier registered for the app. */
          bundleId?: string | null;
          /** SKU chosen when the app record was created. */
          sku?: string | null;
          /** Primary App Store locale, such as en-US. */
          primaryLocale?: string | null;
          /** Whether the app is or has ever been part of the Kids category. */
          isOrEverWasMadeForKids?: boolean | null;
          /** Third-party content rights declared for the app. */
          contentRightsDeclaration?: "DOES_NOT_USE_THIRD_PARTY_CONTENT" | "USES_THIRD_PARTY_CONTENT" | null;
          /** Whether streamlined purchasing is enabled for the app. */
          streamlinedPurchasingEnabled?: boolean | null;
          /** Accessibility information URL published with the app. */
          accessibilityUrl?: string | null;
          /** Production server-to-server subscription status URL. */
          subscriptionStatusUrl?: string | null;
          /** Version of the production subscription status URL. */
          subscriptionStatusUrlVersion?: "V1" | "V2" | null;
          /** Sandbox server-to-server subscription status URL. */
          subscriptionStatusUrlForSandbox?: string | null;
          /** Version of the sandbox subscription status URL. */
          subscriptionStatusUrlVersionForSandbox?: "V1" | "V2" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the members of the App Store Connect team, with the roles granted to each of them. */
    "app_store_connect.list_users": {
      input: {
        /**
         * Return only users holding at least one of these roles.
         * @minItems 1
         */
        roles?: Array<"ADMIN" | "FINANCE" | "ACCOUNT_HOLDER" | "SALES" | "MARKETING" | "APP_MANAGER" | "DEVELOPER" | "ACCESS_TO_REPORTS" | "CUSTOMER_SUPPORT" | "CREATE_APPS" | "CLOUD_MANAGED_DEVELOPER_ID" | "CLOUD_MANAGED_APP_DISTRIBUTION" | "GENERATE_INDIVIDUAL_KEYS">;
        /**
         * Return only the user with this exact Apple Account email.
         * @minLength 1
         * @pattern \S
         */
        username?: string;
        /**
         * Return only users who can see this app.
         * @minLength 1
         * @pattern \S
         */
        visibleAppId?: string;
        /** Sort order for the returned users. */
        sort?: "username" | "-username" | "lastName" | "-lastName";
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Team members returned for this page. */
        users: Array<{
          /** App Store Connect identifier for the user. */
          id: string;
          /** Apple Account email the user signs in with. */
          username?: string | null;
          /** User first name. */
          firstName?: string | null;
          /** User last name. */
          lastName?: string | null;
          /** Roles granted to the user. */
          roles?: Array<"ADMIN" | "FINANCE" | "ACCOUNT_HOLDER" | "SALES" | "MARKETING" | "APP_MANAGER" | "DEVELOPER" | "ACCESS_TO_REPORTS" | "CUSTOMER_SUPPORT" | "CREATE_APPS" | "CLOUD_MANAGED_DEVELOPER_ID" | "CLOUD_MANAGED_APP_DISTRIBUTION" | "GENERATE_INDIVIDUAL_KEYS"> | null;
          /** Whether the user can see every app on the team. */
          allAppsVisible?: boolean | null;
          /** Whether the user may manage certificates, identifiers, and profiles. */
          provisioningAllowed?: boolean | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the delivery attempts of one webhook, newest first as App Store Connect orders them, with the event each attempt carried and the response the endpoint returned. Filter by delivery state or creation time to find failed deliveries to resend. */
    "app_store_connect.list_webhook_deliveries": {
      input: {
        /**
         * App Store Connect identifier of the webhook.
         * @minLength 1
         * @pattern \S
         */
        webhookId: string;
        /**
         * Return only deliveries in these states.
         * @minItems 1
         */
        deliveryStates?: Array<"SUCCEEDED" | "FAILED" | "PENDING">;
        /**
         * Return only deliveries created at or after this ISO 8601 timestamp.
         * @format date-time
         */
        createdSince?: string;
        /**
         * Return only deliveries created before this ISO 8601 timestamp.
         * @format date-time
         */
        createdBefore?: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Delivery attempts returned for this page. */
        webhookDeliveries: Array<{
          /** App Store Connect identifier for the delivery. */
          id: string;
          /** When the delivery was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the request was sent to the webhook URL, as an ISO 8601 timestamp. */
          sentDate?: string | null;
          /** Outcome of the delivery. */
          deliveryState?: "SUCCEEDED" | "FAILED" | "PENDING" | null;
          /** Error App Store Connect recorded when the delivery failed. */
          errorMessage?: string | null;
          /** Whether this delivery was a redelivery of an earlier one. */
          redelivery?: boolean | null;
          /** The request App Store Connect sent. */
          request?: {
            /** URL the notification was posted to. */
            url?: string | null;
            [key: string]: unknown;
          } | null;
          /** The response the webhook URL returned. */
          response?: {
            /** HTTP status code the webhook URL answered with. */
            httpStatusCode?: number | null;
            /** Response body the webhook URL returned. */
            body?: string | null;
            [key: string]: unknown;
          } | null;
          /** The event the delivery carried, or null when App Store Connect did not return it. */
          event: {
            /** App Store Connect identifier for the webhook event. */
            id: string;
            /** Type of the event. */
            eventType?: "ALTERNATIVE_DISTRIBUTION_PACKAGE_AVAILABLE_UPDATED" | "ALTERNATIVE_DISTRIBUTION_PACKAGE_VERSION_CREATED" | "ALTERNATIVE_DISTRIBUTION_TERRITORY_AVAILABILITY_UPDATED" | "APP_STORE_VERSION_APP_VERSION_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_APP_STORE_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_EXTERNAL_BETA_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_INTERNAL_BETA_RELEASE_CREATED" | "BACKGROUND_ASSET_VERSION_STATE_UPDATED" | "BETA_FEEDBACK_CRASH_SUBMISSION_CREATED" | "BETA_FEEDBACK_SCREENSHOT_SUBMISSION_CREATED" | "BUILD_BETA_DETAIL_EXTERNAL_BUILD_STATE_UPDATED" | "BUILD_UPLOAD_STATE_UPDATED" | null;
            /** JSON text of the notification body App Store Connect sent. */
            payload?: string | null;
            /** Whether the event was a test ping rather than an app event. */
            ping?: boolean | null;
            /** When the event was created, as an ISO 8601 timestamp. */
            createdDate?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the webhooks registered on one app, with the URL, enabled state, and subscribed event types of each. */
    "app_store_connect.list_webhooks": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Webhooks returned for this page. */
        webhooks: Array<{
          /** App Store Connect identifier for the webhook. */
          id: string;
          /** Display name of the webhook. */
          name?: string | null;
          /** HTTPS endpoint App Store Connect posts notifications to. */
          url?: string | null;
          /** Whether App Store Connect currently sends notifications to the webhook. */
          enabled?: boolean | null;
          /** Event types the webhook is subscribed to. */
          eventTypes?: Array<"ALTERNATIVE_DISTRIBUTION_PACKAGE_AVAILABLE_UPDATED" | "ALTERNATIVE_DISTRIBUTION_PACKAGE_VERSION_CREATED" | "ALTERNATIVE_DISTRIBUTION_TERRITORY_AVAILABILITY_UPDATED" | "APP_STORE_VERSION_APP_VERSION_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_APP_STORE_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_EXTERNAL_BETA_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_INTERNAL_BETA_RELEASE_CREATED" | "BACKGROUND_ASSET_VERSION_STATE_UPDATED" | "BETA_FEEDBACK_CRASH_SUBMISSION_CREATED" | "BETA_FEEDBACK_SCREENSHOT_SUBMISSION_CREATED" | "BUILD_BETA_DETAIL_EXTERNAL_BUILD_STATE_UPDATED" | "BUILD_UPLOAD_STATE_UPDATED"> | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the territory prices of one win-back offer, with the currency and customer price of each price point. */
    "app_store_connect.list_win_back_offer_prices": {
      input: {
        /**
         * App Store Connect identifier of the win-back offer.
         * @minLength 1
         * @pattern \S
         */
        winBackOfferId: string;
        /**
         * Return only entries for these territories, as ISO 3166-1 alpha-3 codes such as USA or DEU.
         * @minItems 1
         */
        territoryIds?: Array<string>;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Win-back offer prices returned for this page. */
        winBackOfferPrices: Array<{
          /** App Store Connect identifier for the win-back offer price. */
          id: string;
          /** The territory the price applies to, or null when App Store Connect did not return it. */
          territory: {
            /** ISO 3166-1 alpha-3 territory code, such as USA. */
            id: string;
            /** ISO 4217 currency of the territory, such as USD. */
            currency?: string | null;
            [key: string]: unknown;
          } | null;
          /** The subscription price point charged for the offer, or null when App Store Connect did not return it. */
          subscriptionPricePoint: {
            /** App Store Connect identifier for the subscription price point. */
            id: string;
            /** Price the customer pays in the territory currency, as a decimal string. */
            customerPrice?: string | null;
            /** Developer proceeds per period during the first year, as a decimal string. */
            proceeds?: string | null;
            /** Developer proceeds per period after the first year, as a decimal string. */
            proceedsYear2?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** List the win-back offers of one auto-renewable subscription. Use list_win_back_offer_prices to read the territory prices of an offer. */
    "app_store_connect.list_win_back_offers": {
      input: {
        /**
         * App Store Connect identifier of the auto-renewable subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * Maximum number of records to return on this page. App Store Connect allows up to 200.
         * @minimum 1
         * @maximum 200
         */
        limit?: number;
        /**
         * Opaque page cursor taken from the nextCursor value of a previous response for the same query.
         * @minLength 1
         * @pattern \S
         */
        cursor?: string;
      };
      output: {
        /** Win-back offers returned for this page. */
        winBackOffers: Array<{
          /** App Store Connect identifier for the win-back offer. */
          id: string;
          /** Reference name shown in App Store Connect. */
          referenceName?: string | null;
          /** Offer identifier your app passes to StoreKit. */
          offerId?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          periodCount?: number | null;
          /** Minimum number of months the customer must have paid for the subscription before lapsing. */
          customerEligibilityPaidSubscriptionDurationInMonths?: number | null;
          /** Inclusive range of months since the customer last subscribed. */
          customerEligibilityTimeSinceLastSubscribedInMonths?: {
            /** Lower bound in months. */
            minimum?: number | null;
            /** Upper bound in months. */
            maximum?: number | null;
            [key: string]: unknown;
          } | null;
          /** Months a customer must wait after redeeming a win-back offer before another one is offered. */
          customerEligibilityWaitBetweenOffersInMonths?: number | null;
          /** First day the offer is available, as YYYY-MM-DD. */
          startDate?: string | null;
          /** Last day the offer is available, as YYYY-MM-DD. */
          endDate?: string | null;
          /** Priority the App Store uses when several win-back offers apply. */
          priority?: "HIGH" | "NORMAL" | null;
          /** Whether Apple may promote the offer on the App Store with auto-generated assets. */
          promotionIntent?: "NOT_PROMOTED" | "USE_AUTO_GENERATED_ASSETS" | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        }>;
        /** Cursor to pass back as cursor for the next page, or null when this was the last page. */
        nextCursor: string | null;
        /** Total number of records matching the query when App Store Connect reports one, otherwise null. */
        total: number | null;
      };
    };
    /** Send the TestFlight availability notification for one build to every tester who can install it. The notification cannot be recalled, so use it only when automatic notifications were off. */
    "app_store_connect.notify_build_testers": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
      };
      output: {
        /** App Store Connect identifier of the notification record. */
        id: string;
        /** The build the testers were notified about. */
        buildId: string;
        /** Whether the notification was sent. Always true once App Store Connect confirmed the change. */
        notified: boolean;
      };
    };
    /** Send a test ping notification to a webhook so the endpoint and its signature check can be verified. The ping shows up in the webhook deliveries with ping set to true. */
    "app_store_connect.ping_webhook": {
      input: {
        /**
         * App Store Connect identifier of the webhook.
         * @minLength 1
         * @pattern \S
         */
        webhookId: string;
      };
      output: {
        /** App Store Connect identifier for the ping. */
        id: string;
        /** The webhook the ping was sent to. */
        webhookId: string;
      };
    };
    /** Send the event of an earlier delivery again, for example after the endpoint was down. App Store Connect creates a new delivery marked as a redelivery; the original delivery record is kept. */
    "app_store_connect.redeliver_webhook_delivery": {
      input: {
        /**
         * App Store Connect identifier of the delivery whose event to send again.
         * @minLength 1
         * @pattern \S
         */
        webhookDeliveryId: string;
      };
      output: {
        /** One attempt to deliver a webhook event to the configured URL. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        webhookDelivery: {
          /** App Store Connect identifier for the delivery. */
          id: string;
          /** When the delivery was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the request was sent to the webhook URL, as an ISO 8601 timestamp. */
          sentDate?: string | null;
          /** Outcome of the delivery. */
          deliveryState?: "SUCCEEDED" | "FAILED" | "PENDING" | null;
          /** Error App Store Connect recorded when the delivery failed. */
          errorMessage?: string | null;
          /** Whether this delivery was a redelivery of an earlier one. */
          redelivery?: boolean | null;
          /** The request App Store Connect sent. */
          request?: {
            /** URL the notification was posted to. */
            url?: string | null;
            [key: string]: unknown;
          } | null;
          /** The response the webhook URL returned. */
          response?: {
            /** HTTP status code the webhook URL answered with. */
            httpStatusCode?: number | null;
            /** Response body the webhook URL returned. */
            body?: string | null;
            [key: string]: unknown;
          } | null;
          /** The event the delivery carried, or null when App Store Connect did not return it. */
          event: {
            /** App Store Connect identifier for the webhook event. */
            id: string;
            /** Type of the event. */
            eventType?: "ALTERNATIVE_DISTRIBUTION_PACKAGE_AVAILABLE_UPDATED" | "ALTERNATIVE_DISTRIBUTION_PACKAGE_VERSION_CREATED" | "ALTERNATIVE_DISTRIBUTION_TERRITORY_AVAILABILITY_UPDATED" | "APP_STORE_VERSION_APP_VERSION_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_APP_STORE_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_EXTERNAL_BETA_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_INTERNAL_BETA_RELEASE_CREATED" | "BACKGROUND_ASSET_VERSION_STATE_UPDATED" | "BETA_FEEDBACK_CRASH_SUBMISSION_CREATED" | "BETA_FEEDBACK_SCREENSHOT_SUBMISSION_CREATED" | "BUILD_BETA_DETAIL_EXTERNAL_BUILD_STATE_UPDATED" | "BUILD_UPLOAD_STATE_UPDATED" | null;
            /** JSON text of the notification body App Store Connect sent. */
            payload?: string | null;
            /** Whether the event was a test ping rather than an app event. */
            ping?: boolean | null;
            /** When the event was created, as an ISO 8601 timestamp. */
            createdDate?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Register a device with the team so development and ad hoc profiles can include it. Apple limits how many devices a team registers per membership year, and registered devices can only be removed in the Apple Developer website; use update_device to disable one instead. */
    "app_store_connect.register_device": {
      input: {
        /**
         * Name to show for the device in the developer portal.
         * @minLength 1
         * @pattern \S
         */
        name: string;
        /** Platform the device runs. */
        platform: "IOS" | "MAC_OS" | "UNIVERSAL";
        /**
         * Unique device identifier (UDID) of the device, or the provisioning UDID of a Mac.
         * @minLength 1
         * @pattern \S
         */
        udid: string;
      };
      output: {
        /** A device registered with the team for development. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        device: {
          /** App Store Connect identifier of the device. */
          id: string;
          /** Device name given when it was registered. */
          name?: string | null;
          /** Platform the device runs. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Unique device identifier (UDID) of the device. */
          udid?: string | null;
          /** Hardware class of the device. */
          deviceClass?: "APPLE_VISION_PRO" | "APPLE_WATCH" | "IPAD" | "IPHONE" | "IPOD" | "APPLE_TV" | "MAC" | null;
          /** Whether the device is enabled for development. Disabled devices are excluded from new profiles. */
          status?: "ENABLED" | "DISABLED" | null;
          /** Device model name. */
          model?: string | null;
          /** When the device was registered, as an ISO 8601 timestamp. */
          addedDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Release an approved App Store version that is waiting in PENDING_DEVELOPER_RELEASE to the App Store. The release starts immediately and cannot be undone. */
    "app_store_connect.release_app_store_version": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** App Store Connect identifier of the release request. */
        id: string;
        /** The App Store version whose release was requested. */
        appStoreVersionId: string;
      };
    };
    /** Detach achievements from a Game Center activity. The achievements themselves are kept and stay available outside the activity. */
    "app_store_connect.remove_achievements_from_game_center_activity": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
        /**
         * App Store Connect identifiers of the achievements to detach.
         * @minItems 1
         */
        gameCenterAchievementIds: Array<string>;
      };
      output: {
        /** The activity the achievements were removed from. */
        gameCenterActivityId: string;
        /** Identifiers of the achievements that were removed. */
        gameCenterAchievementIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        removed: boolean;
      };
    };
    /** Revoke a TestFlight tester's access to specific apps, removing them from every group and build of those apps while keeping them on the team. */
    "app_store_connect.remove_beta_tester_from_apps": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight tester.
         * @minLength 1
         * @pattern \S
         */
        betaTesterId: string;
        /**
         * Identifiers of the apps to revoke access to.
         * @minItems 1
         */
        appIds: Array<string>;
      };
      output: {
        /** The tester whose access was revoked. */
        betaTesterId: string;
        /** Identifiers of the apps access was revoked for. */
        appIds: Array<string>;
        /** Whether access was revoked. Always true once App Store Connect confirmed the change. */
        removed: boolean;
      };
    };
    /** Remove testers from one TestFlight group. The testers stay on the team and keep access through their other groups. */
    "app_store_connect.remove_beta_testers_from_group": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
        /**
         * Identifiers of the testers to remove.
         * @minItems 1
         */
        betaTesterIds: Array<string>;
      };
      output: {
        /** The TestFlight group the testers were removed from. */
        betaGroupId: string;
        /** Identifiers of the testers that were removed. */
        betaTesterIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        removed: boolean;
      };
    };
    /** Withdraw one build from TestFlight groups so their testers can no longer install it. */
    "app_store_connect.remove_build_from_beta_groups": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /**
         * Identifiers of the TestFlight groups to remove the build from.
         * @minItems 1
         */
        betaGroupIds: Array<string>;
      };
      output: {
        /** The build that was withdrawn. */
        buildId: string;
        /** Identifiers of the groups the build was removed from. */
        betaGroupIds: Array<string>;
        /** Whether the build was withdrawn. Always true once App Store Connect confirmed the change. */
        removed: boolean;
      };
    };
    /** Withdraw several builds from one TestFlight group so its testers can no longer install them. */
    "app_store_connect.remove_builds_from_beta_group": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
        /**
         * Identifiers of the builds to remove from the group.
         * @minItems 1
         */
        buildIds: Array<string>;
      };
      output: {
        /** The group the builds were removed from. */
        betaGroupId: string;
        /** Identifiers of the builds that were removed. */
        buildIds: Array<string>;
        /** Whether the builds were withdrawn. Always true once App Store Connect confirmed the change. */
        removed: boolean;
      };
    };
    /** Stop treating earlier Game Center app versions as multiplayer compatible with this one. Players still on a removed version can no longer be matched with players on this one. */
    "app_store_connect.remove_compatibility_versions_from_game_center_app_version": {
      input: {
        /**
         * App Store Connect identifier of the Game Center app version to remove compatibility from.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAppVersionId: string;
        /**
         * App Store Connect identifiers of the Game Center app versions to stop treating as compatible.
         * @minItems 1
         */
        compatibilityVersionIds: Array<string>;
      };
      output: {
        /** The Game Center app version the compatible versions were removed from. */
        gameCenterAppVersionId: string;
        /** Identifiers of the Game Center app versions that were removed. */
        compatibilityVersionIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        removed: boolean;
      };
    };
    /** Remove the individual assignment of TestFlight testers from one build. Testers keep access through any group that has the build. */
    "app_store_connect.remove_individual_testers_from_build": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /**
         * Identifiers of the testers to unassign.
         * @minItems 1
         */
        betaTesterIds: Array<string>;
      };
      output: {
        /** The build the testers were unassigned from. */
        buildId: string;
        /** Identifiers of the testers that were unassigned. */
        betaTesterIds: Array<string>;
        /** Whether the testers were unassigned. Always true once App Store Connect confirmed the change. */
        removed: boolean;
      };
    };
    /** Detach leaderboards from a Game Center activity. The leaderboards themselves are kept together with the scores already posted to them. */
    "app_store_connect.remove_leaderboards_from_game_center_activity": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
        /**
         * App Store Connect identifiers of the leaderboards to detach.
         * @minItems 1
         */
        gameCenterLeaderboardIds: Array<string>;
      };
      output: {
        /** The activity the leaderboards were removed from. */
        gameCenterActivityId: string;
        /** Identifiers of the leaderboards that were removed. */
        gameCenterLeaderboardIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        removed: boolean;
      };
    };
    /** Take leaderboards out of a Game Center leaderboard set. The leaderboards themselves are kept and stay available on their own. */
    "app_store_connect.remove_leaderboards_from_game_center_leaderboard_set": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
        /**
         * Identifiers of the leaderboards to remove.
         * @minItems 1
         */
        gameCenterLeaderboardIds: Array<string>;
      };
      output: {
        /** The leaderboard set the leaderboards were removed from. */
        gameCenterLeaderboardSetId: string;
        /** Identifiers of the leaderboards that were removed. */
        gameCenterLeaderboardIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        removed: boolean;
      };
    };
    /** Hide apps from a team member so they can no longer open them in App Store Connect. */
    "app_store_connect.remove_visible_apps_from_user": {
      input: {
        /**
         * App Store Connect identifier of the team member.
         * @minLength 1
         * @pattern \S
         */
        userId: string;
        /**
         * Identifiers of the apps to remove.
         * @minItems 1
         */
        appIds: Array<string>;
      };
      output: {
        /** The team member the apps were hidden from. */
        userId: string;
        /** Identifiers of the apps that were removed. */
        appIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        removed: boolean;
      };
    };
    /** Set the display order of the promoted purchases of an app. The list replaces the current order, so include every promoted purchase that should stay listed. */
    "app_store_connect.reorder_promoted_purchases": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * Promoted purchase identifiers in the order they should appear.
         * @minItems 1
         */
        promotedPurchaseIds: Array<string>;
      };
      output: {
        /** The app whose promoted purchases were reordered. */
        appId: string;
        /** The promoted purchases in their new order. */
        promotedPurchaseIds: Array<string>;
        /** Always true once App Store Connect confirmed the new order. */
        replaced: boolean;
      };
    };
    /** Replace the achievements attached to a Game Center detail. The identifiers you pass become the complete set, so an achievement left out is detached from the app. */
    "app_store_connect.replace_game_center_detail_achievements": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Achievements that make up the complete set after the change.
         * @minItems 1
         */
        gameCenterAchievementIds: Array<string>;
      };
      output: {
        /** The Game Center detail whose achievements were replaced. */
        gameCenterDetailId: string;
        /** Identifiers of the achievements now attached to the Game Center detail. */
        gameCenterAchievementIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Replace the earliest App Store versions, one per platform, that may take part in Game Center challenges. The identifiers you pass become the complete set, so a platform left out no longer has a minimum version. */
    "app_store_connect.replace_game_center_detail_challenges_minimum_platform_versions": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Earliest App Store version on each platform that may take part in challenges.
         * @minItems 1
         */
        appStoreVersionIds: Array<string>;
      };
      output: {
        /** The Game Center detail whose challenge minimum platform versions were replaced. */
        gameCenterDetailId: string;
        /** Identifiers of the App Store versions that now set the challenge minimum on their platform. */
        appStoreVersionIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Replace the leaderboard sets attached to a Game Center detail. The identifiers you pass become the complete set, so a leaderboard set left out is detached from the app. */
    "app_store_connect.replace_game_center_detail_leaderboard_sets": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Leaderboard sets that make up the complete set after the change.
         * @minItems 1
         */
        gameCenterLeaderboardSetIds: Array<string>;
      };
      output: {
        /** The Game Center detail whose leaderboard sets were replaced. */
        gameCenterDetailId: string;
        /** Identifiers of the leaderboard sets now attached to the Game Center detail. */
        gameCenterLeaderboardSetIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Replace the leaderboards attached to a Game Center detail. The identifiers you pass become the complete set, so a leaderboard left out is detached from the app. */
    "app_store_connect.replace_game_center_detail_leaderboards": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Leaderboards that make up the complete set after the change.
         * @minItems 1
         */
        gameCenterLeaderboardIds: Array<string>;
      };
      output: {
        /** The Game Center detail whose leaderboards were replaced. */
        gameCenterDetailId: string;
        /** Identifiers of the leaderboards now attached to the Game Center detail. */
        gameCenterLeaderboardIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Set which achievements one Game Center group shares. Pass the complete list: an achievement left out is no longer shared through the group. */
    "app_store_connect.replace_game_center_group_achievements": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
        /**
         * Identifiers of the achievements the group shares.
         * @minItems 1
         */
        gameCenterAchievementIds: Array<string>;
      };
      output: {
        /** The Game Center group whose achievements were replaced. */
        gameCenterGroupId: string;
        /** Identifiers of the achievements the group shares now. */
        gameCenterAchievementIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Set which leaderboard sets one Game Center group shares. Pass the complete list: a leaderboard set left out is no longer shared through the group. */
    "app_store_connect.replace_game_center_group_leaderboard_sets": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
        /**
         * Identifiers of the leaderboard sets the group shares.
         * @minItems 1
         */
        gameCenterLeaderboardSetIds: Array<string>;
      };
      output: {
        /** The Game Center group whose leaderboard sets were replaced. */
        gameCenterGroupId: string;
        /** Identifiers of the leaderboard sets the group shares now. */
        gameCenterLeaderboardSetIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Set which leaderboards one Game Center group shares. Pass the complete list: a leaderboard left out is no longer shared through the group. */
    "app_store_connect.replace_game_center_group_leaderboards": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
        /**
         * Identifiers of the leaderboards the group shares.
         * @minItems 1
         */
        gameCenterLeaderboardIds: Array<string>;
      };
      output: {
        /** The Game Center group whose leaderboards were replaced. */
        gameCenterGroupId: string;
        /** Identifiers of the leaderboards the group shares now. */
        gameCenterLeaderboardIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Replace the whole list of leaderboards in a Game Center leaderboard set. Leaderboards missing from the new list leave the set but are not deleted. */
    "app_store_connect.replace_game_center_leaderboard_set_leaderboards": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
        /**
         * Identifiers of every leaderboard that should stay in the set.
         * @minItems 1
         */
        gameCenterLeaderboardIds: Array<string>;
      };
      output: {
        /** The leaderboard set whose leaderboards were replaced. */
        gameCenterLeaderboardSetId: string;
        /** Identifiers of the leaderboards that are in the set now. */
        gameCenterLeaderboardIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Replace the whole set of apps a team member can see. Apps missing from the new list are hidden from the user. */
    "app_store_connect.replace_user_visible_apps": {
      input: {
        /**
         * App Store Connect identifier of the team member.
         * @minLength 1
         * @pattern \S
         */
        userId: string;
        /**
         * Identifiers of every app that should stay visible.
         * @minItems 1
         */
        appIds: Array<string>;
      };
      output: {
        /** The team member whose visible apps were replaced. */
        userId: string;
        /** Identifiers of the apps that are visible now. */
        appIds: Array<string>;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Send the TestFlight invitation email again to a tester who has not accepted the invitation for one app. */
    "app_store_connect.resend_beta_tester_invitation": {
      input: {
        /**
         * App Store Connect identifier of the app the tester was invited to.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * App Store Connect identifier of the TestFlight tester.
         * @minLength 1
         * @pattern \S
         */
        betaTesterId: string;
      };
      output: {
        /** App Store Connect identifier of the invitation record. */
        id: string;
        /** The app the invitation is for. */
        appId: string;
        /** The tester the invitation was sent to. */
        betaTesterId: string;
        /** Whether the invitation was sent again. Always true once App Store Connect confirmed the change. */
        resent: boolean;
      };
    };
    /** Publish a developer response to an App Store review. App Store Connect treats this as an upsert: an existing response for the same review is replaced, and publication is asynchronous. */
    "app_store_connect.respond_to_customer_review": {
      input: {
        /**
         * App Store Connect identifier of the review to respond to.
         * @minLength 1
         * @pattern \S
         */
        customerReviewId: string;
        /**
         * Text of the developer response.
         * @minLength 1
         * @pattern \S
         */
        responseBody: string;
      };
      output: {
        /** The stored developer response. */
        customerReviewResponse: {
          /** App Store Connect identifier for the response. */
          id: string;
          /** Text of the developer response. */
          responseBody?: string | null;
          /** When the response was last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** Publication state of the response. */
          state?: "PUBLISHED" | "PENDING_PUBLISH" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Revoke a certificate that was lost, stolen, compromised, or is expiring. Revocation is permanent and cannot be undone: apps signed with the certificate can no longer be re-signed with it, every provisioning profile that contains it becomes invalid, and Developer ID or Apple Pay certificates may need Apple to restore service. Issue a replacement with create_certificate first when the certificate is still in use. */
    "app_store_connect.revoke_certificate": {
      input: {
        /**
         * App Store Connect identifier of the certificate.
         * @minLength 1
         * @pattern \S
         */
        certificateId: string;
      };
      output: {
        /** App Store Connect identifier of the revoked certificate. */
        id: string;
        /** Always true once App Store Connect confirmed the deletion. */
        deleted: boolean;
      };
    };
    /** Attach a default App Clip experience to the App Store version it is released with, replacing any version attached before. */
    "app_store_connect.set_app_clip_default_experience_release_version": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceId: string;
        /**
         * App Store Connect identifier of the App Store version to release the experience with.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
      };
      output: {
        /** The default App Clip experience that was changed. */
        appClipDefaultExperienceId: string;
        /** The App Store version now attached. */
        appStoreVersionId: string;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Select the build an App Store version submits for review, replacing any build selected before. Pass null as buildId to detach the current build. */
    "app_store_connect.set_app_store_version_build": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
        /**
         * App Store Connect identifier of the build to attach, or null to detach the current build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string | null;
      };
      output: {
        /** The App Store version whose build was changed. */
        appStoreVersionId: string;
        /** The build now attached, or null when it was detached. */
        buildId: string | null;
        /** Always true once App Store Connect confirmed the change. */
        replaced: boolean;
      };
    };
    /** Attach a Game Center achievement to a game activity, replacing the activity it was attached to before. */
    "app_store_connect.set_game_center_achievement_activity": {
      input: {
        /**
         * App Store Connect identifier of the achievement.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementId: string;
        /**
         * App Store Connect identifier of the game activity the achievement contributes to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
      };
      output: {
        /** The achievement whose activity was changed. */
        gameCenterAchievementId: string;
        /** The activity the achievement is now attached to. */
        gameCenterActivityId: string;
        /** Always true once App Store Connect confirmed the change. */
        updated: boolean;
      };
    };
    /** Score a Game Center challenge on another leaderboard, replacing the leaderboard it used before. App Store Connect does not accept detaching the leaderboard, so a leaderboard identifier is always required. */
    "app_store_connect.set_game_center_challenge_leaderboard": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeId: string;
        /**
         * App Store Connect identifier of the leaderboard the challenge is scored on.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId: string;
      };
      output: {
        /** The challenge whose leaderboard was changed. */
        gameCenterChallengeId: string;
        /** The leaderboard the challenge is now scored on. */
        gameCenterLeaderboardId: string;
        /** Always true once App Store Connect confirmed the change. */
        updated: boolean;
      };
    };
    /** Link a Game Center leaderboard to the game activity it belongs to, replacing any activity linked before. App Store Connect confirms the change without returning the leaderboard. */
    "app_store_connect.set_game_center_leaderboard_activity": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId: string;
        /**
         * App Store Connect identifier of the game activity to link the leaderboard to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
      };
      output: {
        /** The leaderboard that was changed. */
        gameCenterLeaderboardId: string;
        /** The activity now linked to the leaderboard. */
        gameCenterActivityId: string;
        /** Always true once App Store Connect confirmed the change. */
        updated: boolean;
      };
    };
    /** Link a Game Center leaderboard to the challenge that ranks players against it, replacing any challenge linked before. App Store Connect confirms the change without returning the leaderboard. */
    "app_store_connect.set_game_center_leaderboard_challenge": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId: string;
        /**
         * App Store Connect identifier of the challenge to link the leaderboard to.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeId: string;
      };
      output: {
        /** The leaderboard that was changed. */
        gameCenterLeaderboardId: string;
        /** The challenge now linked to the leaderboard. */
        gameCenterChallengeId: string;
        /** Always true once App Store Connect confirmed the change. */
        updated: boolean;
      };
    };
    /** Start an Xcode Cloud build run for a workflow. Pass the Git reference of the branch or tag to build, or the pull request to build; the run is queued and progresses asynchronously, so read it back with get_ci_build_run. */
    "app_store_connect.start_ci_build_run": {
      input: {
        /**
         * App Store Connect identifier of the workflow to run.
         * @minLength 1
         * @pattern \S
         */
        ciWorkflowId: string;
        /**
         * App Store Connect identifier of the Git reference (branch or tag) to build. Use list_scm_repository_git_references to find one.
         * @minLength 1
         * @pattern \S
         */
        sourceBranchOrTagId?: string;
        /**
         * App Store Connect identifier of the pull request to build. Use list_scm_repository_pull_requests to find one.
         * @minLength 1
         * @pattern \S
         */
        scmPullRequestId?: string;
        /** Start this run from a clean environment without cached derived data. */
        clean?: boolean;
      };
      output: {
        /** One run of an Xcode Cloud workflow. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciBuildRun: {
          /** App Store Connect identifier for the build run. */
          id: string;
          /** Sequential build number within the product. */
          number?: number | null;
          /** When the run was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the run started, as an ISO 8601 timestamp. */
          startedDate?: string | null;
          /** When the run finished, as an ISO 8601 timestamp. */
          finishedDate?: string | null;
          /** A commit Xcode Cloud built. */
          sourceCommit?: {
            /** Full commit SHA. */
            commitSha?: string;
            /** Commit message. */
            message?: string;
            /** Who authored the commit. */
            author?: {
              /** Display name of the author. */
              displayName?: string;
              /** Avatar image URL of the author. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** Who committed the change. */
            committer?: {
              /** Display name of the committer. */
              displayName?: string;
              /** Avatar image URL of the committer. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** URL of the commit on the source control provider. */
            webUrl?: string;
            [key: string]: unknown;
          } | null;
          /** A commit Xcode Cloud built. */
          destinationCommit?: {
            /** Full commit SHA. */
            commitSha?: string;
            /** Commit message. */
            message?: string;
            /** Who authored the commit. */
            author?: {
              /** Display name of the author. */
              displayName?: string;
              /** Avatar image URL of the author. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** Who committed the change. */
            committer?: {
              /** Display name of the committer. */
              displayName?: string;
              /** Avatar image URL of the committer. */
              avatarUrl?: string;
              [key: string]: unknown;
            };
            /** URL of the commit on the source control provider. */
            webUrl?: string;
            [key: string]: unknown;
          } | null;
          /** Whether the run was started for a pull request. */
          isPullRequestBuild?: boolean | null;
          /** Number of issues by type. */
          issueCounts?: {
            /** Number of analyzer warnings. */
            analyzerWarnings?: number;
            /** Number of errors. */
            errors?: number;
            /** Number of test failures. */
            testFailures?: number;
            /** Number of warnings. */
            warnings?: number;
            [key: string]: unknown;
          } | null;
          /** Whether the run is pending, running, or complete. */
          executionProgress?: "PENDING" | "RUNNING" | "COMPLETE" | null;
          /** Outcome of the run once it is complete. */
          completionStatus?: "SUCCEEDED" | "FAILED" | "ERRORED" | "CANCELED" | "SKIPPED" | null;
          /** What started the run. */
          startReason?: "GIT_REF_CHANGE" | "MANUAL" | "MANUAL_REBUILD" | "PULL_REQUEST_OPEN" | "PULL_REQUEST_UPDATE" | "SCHEDULE" | null;
          /** Why the run was canceled, when it was. */
          cancelReason?: "AUTOMATICALLY_BY_NEWER_BUILD" | "MANUALLY_BY_USER" | null;
          /** Identifier of the workflow that ran. */
          ciWorkflowId: string | null;
          /** Identifier of the Xcode Cloud product the run belongs to. */
          ciProductId: string | null;
          /** Identifier of the Git reference (branch or tag) the run built. */
          sourceBranchOrTagId: string | null;
          /** Identifier of the destination branch for a pull request run. */
          destinationBranchId: string | null;
          /** Identifier of the pull request the run was started for. */
          scmPullRequestId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Submit a build for TestFlight beta review, which external groups require before they can install it. */
    "app_store_connect.submit_build_for_beta_review": {
      input: {
        /**
         * App Store Connect identifier of the build to submit.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
      };
      output: {
        /** App Store Connect identifier for the beta app review submission. */
        id: string;
        /** State of the TestFlight beta review. */
        betaReviewState: "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "APPROVED" | null;
        /** When the build was submitted for beta review, as an ISO 8601 timestamp. */
        submittedDate: string | null;
      };
    };
    /** Post a score to a Game Center leaderboard on behalf of a player. This is how a server-authoritative game reports scores its own backend computed instead of letting the game client submit them. */
    "app_store_connect.submit_game_center_leaderboard_entry": {
      input: {
        /**
         * Bundle identifier of the game the result was earned in.
         * @minLength 1
         * @pattern \S
         */
        bundleId: string;
        /**
         * Scoped player identifier of the player the submission is made for, as GameKit reports it.
         * @minLength 1
         * @pattern \S
         */
        scopedPlayerId: string;
        /**
         * Game Center challenges this submission also counts towards.
         * @minItems 1
         */
        challengeIds?: Array<string>;
        /**
         * When the player earned the result, as an ISO 8601 timestamp. Defaults to the time App Store Connect receives the submission.
         * @format date-time
         */
        submittedDate?: string;
        /** Set to true when the result was earned in a prerelease build, so it stays out of the live Game Center data. */
        preReleased?: boolean;
        /**
         * Leaderboard identifier the score belongs to, as configured on the leaderboard.
         * @minLength 1
         * @pattern \S
         */
        vendorIdentifier: string;
        /**
         * Score to post, as a decimal string such as 12500 or 1234.56. App Store Connect formats it according to the leaderboard formatter.
         * @minLength 1
         * @pattern \S
         */
        score: string;
        /**
         * Game-defined context value to store with the entry, as a decimal string. Games use it to attach extra data to a score.
         * @minLength 1
         * @pattern \S
         */
        context?: string;
      };
      output: {
        /** A score submitted to a Game Center leaderboard on behalf of a player. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardEntry: {
          /** App Store Connect identifier for the leaderboard entry submission. */
          id: string;
          /** Bundle identifier of the game the score was earned in. */
          bundleId?: string | null;
          /** Leaderboard identifier the score was submitted to. */
          vendorIdentifier?: string | null;
          /** Scoped player identifier the score belongs to. */
          scopedPlayerId?: string | null;
          /** Submitted score, as a decimal string. */
          score?: string | null;
          /** Game-defined context value stored with the score. */
          context?: string | null;
          /** Challenges the score counts towards. */
          challengeIds?: Array<string> | null;
          /** When the score was earned, as an ISO 8601 timestamp. */
          submittedDate?: string | null;
          /** Whether the score was earned in a prerelease build and kept out of the live leaderboard. */
          preReleased?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Report a player's progress towards a Game Center achievement from a server instead of from the game client. Submitting 100 marks the achievement earned. */
    "app_store_connect.submit_game_center_player_achievement": {
      input: {
        /**
         * Bundle identifier of the game the result was earned in.
         * @minLength 1
         * @pattern \S
         */
        bundleId: string;
        /**
         * Scoped player identifier of the player the submission is made for, as GameKit reports it.
         * @minLength 1
         * @pattern \S
         */
        scopedPlayerId: string;
        /**
         * Game Center challenges this submission also counts towards.
         * @minItems 1
         */
        challengeIds?: Array<string>;
        /**
         * When the player earned the result, as an ISO 8601 timestamp. Defaults to the time App Store Connect receives the submission.
         * @format date-time
         */
        submittedDate?: string;
        /** Set to true when the result was earned in a prerelease build, so it stays out of the live Game Center data. */
        preReleased?: boolean;
        /**
         * Achievement identifier the progress belongs to, as configured on the achievement.
         * @minLength 1
         * @pattern \S
         */
        vendorIdentifier: string;
        /**
         * Progress towards the achievement, from 0 to 100. Use 100 to mark it earned.
         * @minimum 0
         * @maximum 100
         */
        percentageAchieved: number;
      };
      output: {
        /** Achievement progress submitted for a player. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterPlayerAchievement: {
          /** App Store Connect identifier for the player achievement submission. */
          id: string;
          /** Bundle identifier of the game the progress was earned in. */
          bundleId?: string | null;
          /** Achievement identifier the progress was submitted for. */
          vendorIdentifier?: string | null;
          /** Scoped player identifier the progress belongs to. */
          scopedPlayerId?: string | null;
          /** Progress towards the achievement, from 0 to 100. */
          percentageAchieved?: number | null;
          /** Challenges the progress counts towards. */
          challengeIds?: Array<string> | null;
          /** When the progress was earned, as an ISO 8601 timestamp. */
          submittedDate?: string | null;
          /** Whether the progress was earned in a prerelease build and kept out of the live achievement. */
          preReleased?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Submit an in-app purchase to App Review. The purchase must be in READY_TO_SUBMIT; once submitted it cannot be edited until review finishes. */
    "app_store_connect.submit_in_app_purchase_for_review": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
      };
      output: {
        /** App Store Connect identifier for the submission. */
        id: string;
        /** The in-app purchase that was submitted. */
        inAppPurchaseId: string;
      };
    };
    /** Send a prepared review submission and all of its items to App Review. The submission moves to WAITING_FOR_REVIEW and can only be taken back with cancel_review_submission. */
    "app_store_connect.submit_review_submission": {
      input: {
        /**
         * App Store Connect identifier of the review submission.
         * @minLength 1
         * @pattern \S
         */
        reviewSubmissionId: string;
      };
      output: {
        /** A review submission that bundles the items sent to App Review together. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        reviewSubmission: {
          /** App Store Connect identifier for the review submission. */
          id: string;
          /** Content platform the submission targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** When the submission was sent to App Review, as an ISO 8601 timestamp, or null while it is still being prepared. */
          submittedDate?: string | null;
          /** Where the submission stands in App Review. */
          state?: "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "UNRESOLVED_ISSUES" | "CANCELING" | "COMPLETING" | "COMPLETE" | null;
          /** App Store Connect identifier of the App Store version under review, or null when the submission carries no version. */
          appStoreVersionForReviewId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Run a Game Center matchmaking rule set against a set of simulated requests and return the matches it would have formed, with the team each simulated player was assigned to. Nothing is stored and no real player is matched, so this is safe to run against a rule set that is already attached to a queue. */
    "app_store_connect.test_game_center_matchmaking_rule_set": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule set to test.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId: string;
        /**
         * Simulated matchmaking requests matched against the rule set.
         * @minItems 1
         */
        requests: Array<{
          /**
           * Name this request is referred to by in the returned matches.
           * @minLength 1
           * @pattern \S
           */
          requestName?: string;
          /** How long the request has already waited in the queue, in seconds. */
          secondsInQueue?: number;
          /** Locale of the player who sent the request. */
          locale?: "AR-SA" | "CA-ES" | "CS-CZ" | "DA-DK" | "DE-DE" | "EL-GR" | "EN-AU" | "EN-GB" | "EN-US" | "EN-KY" | "ES-ES" | "ES-MX" | "FI-FI" | "FR-CA" | "FR-FR" | "HI-IN" | "HR-HR" | "HU-HU" | "ID-ID" | "IT-IT" | "IW-IL" | "JA-JP" | "KO-KR" | "MS-MY" | "NL-NL" | "NO-NO" | "PL-PL" | "PT-BR" | "PT-PT" | "RO-RO" | "RU-RU" | "SK-SK" | "SV-SE" | "TH-TH" | "TR-TR" | "UK-UA" | "ZH-CN" | "ZH-TW" | "ZH-HK";
          /** Where the player who sent the request is located, for a DISTANCE rule. */
          location?: {
            /** Latitude of the player, in degrees. */
            latitude: number;
            /** Longitude of the player, in degrees. */
            longitude: number;
          };
          /** Fewest players the request accepts in its match. */
          minPlayers?: number;
          /** Most players the request accepts in its match. */
          maxPlayers?: number;
          /** Number of players the request brings into the match. */
          playerCount?: number;
          /**
           * Bundle identifier of the app that sent the request.
           * @minLength 1
           * @pattern \S
           */
          bundleId?: string;
          /** Platform the request was sent from. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS";
          /**
           * App version the request was sent from.
           * @minLength 1
           * @pattern \S
           */
          appVersion?: string;
          /**
           * Properties the rule expressions read for the players of this request.
           * @minItems 1
           */
          playerProperties?: Array<{
            /**
             * Identifier used for the simulated player.
             * @minLength 1
             * @pattern \S
             */
            playerId: string;
            /**
             * Key-value properties a rule expression reads for this player.
             * @minItems 1
             */
            properties: Array<{
              /**
               * Property name a rule expression refers to.
               * @minLength 1
               * @pattern \S
               */
              key: string;
              /**
               * Property value for this player.
               * @minLength 1
               * @pattern \S
               */
              value: string;
            }>;
          }>;
        }>;
      };
      output: {
        /** The result of matching a set of simulated requests against a rule set. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingRuleSetTest: {
          /** App Store Connect identifier for the rule set test. */
          id: string;
          /** One entry per match Game Center formed from the simulated requests. */
          matchmakingResults?: Array<Array<{
            /** Name given to the simulated request. */
            requestName?: string | null;
            /** Which team each player of the request was assigned to. */
            teamAssignments?: Array<{
              /** Simulated player identifier. */
              playerId?: string | null;
              /** Team the player was assigned to. */
              team?: string | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          }>> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the supported accessibility features of a declaration, or publish it by passing publish: true, which replaces the currently published declaration for that device family. Pass at least one field. */
    "app_store_connect.update_accessibility_declaration": {
      input: {
        /**
         * App Store Connect identifier of the accessibility declaration.
         * @minLength 1
         * @pattern \S
         */
        accessibilityDeclarationId: string;
        /** Publish the declaration to the App Store when true. */
        publish?: boolean;
        /** Whether the app supports audio descriptions. */
        supportsAudioDescriptions?: boolean;
        /** Whether the app supports captions. */
        supportsCaptions?: boolean;
        /** Whether the app supports a dark interface. */
        supportsDarkInterface?: boolean;
        /** Whether the app conveys information without relying on color alone. */
        supportsDifferentiateWithoutColorAlone?: boolean;
        /** Whether the app supports larger text. */
        supportsLargerText?: boolean;
        /** Whether the app supports reduced motion. */
        supportsReducedMotion?: boolean;
        /** Whether the app supports sufficient contrast. */
        supportsSufficientContrast?: boolean;
        /** Whether the app supports Voice Control. */
        supportsVoiceControl?: boolean;
        /** Whether the app supports VoiceOver. */
        supportsVoiceover?: boolean;
      };
      output: {
        /** An accessibility declaration (Accessibility Nutrition Label) for one device family. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        accessibilityDeclaration: {
          /** App Store Connect identifier for the accessibility declaration. */
          id: string;
          /** Device family the declaration describes. */
          deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION" | null;
          /** Whether the declaration is a draft, published, or replaced by a newer one. */
          state?: "DRAFT" | "PUBLISHED" | "REPLACED" | null;
          /** Whether the app supports audio descriptions. */
          supportsAudioDescriptions?: boolean | null;
          /** Whether the app supports captions. */
          supportsCaptions?: boolean | null;
          /** Whether the app supports a dark interface. */
          supportsDarkInterface?: boolean | null;
          /** Whether the app conveys information without relying on color alone. */
          supportsDifferentiateWithoutColorAlone?: boolean | null;
          /** Whether the app supports larger text. */
          supportsLargerText?: boolean | null;
          /** Whether the app supports reduced motion. */
          supportsReducedMotion?: boolean | null;
          /** Whether the app supports sufficient contrast. */
          supportsSufficientContrast?: boolean | null;
          /** Whether the app supports Voice Control. */
          supportsVoiceControl?: boolean | null;
          /** Whether the app supports VoiceOver. */
          supportsVoiceover?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Answer or change questions of the age rating questionnaire. Only the given answers are overwritten; App Store Connect recalculates the derived age ratings. Pass at least one answer. */
    "app_store_connect.update_age_rating_declaration": {
      input: {
        /**
         * App Store Connect identifier of the age rating declaration, from get_age_rating_declaration.
         * @minLength 1
         * @pattern \S
         */
        ageRatingDeclarationId: string;
        /** Whether the app shows advertising. */
        advertising?: boolean;
        /** Whether the app performs age assurance. */
        ageAssurance?: boolean;
        /** How often alcohol, tobacco, or drug use or references appear. */
        alcoholTobaccoOrDrugUseOrReferences?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** How often contests appear. */
        contests?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** Whether the app offers real gambling. */
        gambling?: boolean;
        /** How often simulated gambling appears. */
        gamblingSimulated?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** How often guns or other weapons appear. */
        gunsOrOtherWeapons?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** Whether the app covers health or wellness topics. */
        healthOrWellnessTopics?: boolean;
        /** How often horror or fear themes appear. */
        horrorOrFearThemes?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** Kids age band the app is designed for, or null to leave the Kids category. */
        kidsAgeBand?: "FIVE_AND_UNDER" | "SIX_TO_EIGHT" | "NINE_TO_ELEVEN" | null;
        /** Whether the app sells loot boxes or similar random rewards. */
        lootBox?: boolean;
        /** How often mature or suggestive themes appear. */
        matureOrSuggestiveThemes?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** How often medical or treatment information appears. */
        medicalOrTreatmentInformation?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** Whether the app offers messaging or chat. */
        messagingAndChat?: boolean;
        /** Whether the app offers parental controls. */
        parentalControls?: boolean;
        /** How often profanity or crude humor appears. */
        profanityOrCrudeHumor?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** How often graphic sexual content or nudity appears. */
        sexualContentGraphicAndNudity?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** How often sexual content or nudity appears. */
        sexualContentOrNudity?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** Whether the app includes social media features. */
        socialMedia?: boolean;
        /** Whether the social media features are restricted by age. */
        socialMediaAgeRestricted?: boolean;
        /** Whether the app offers unrestricted web access. */
        unrestrictedWebAccess?: boolean;
        /** Whether the app shows user-generated content. */
        userGeneratedContent?: boolean;
        /** How often cartoon or fantasy violence appears. */
        violenceCartoonOrFantasy?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** How often realistic violence appears. */
        violenceRealistic?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** How often prolonged graphic or sadistic realistic violence appears. */
        violenceRealisticProlongedGraphicOrSadistic?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT";
        /** Manual override raising the derived age rating, or NONE to remove the override. */
        ageRatingOverrideV2?: "NONE" | "NINE_PLUS" | "THIRTEEN_PLUS" | "SIXTEEN_PLUS" | "EIGHTEEN_PLUS" | "UNRATED";
        /** Manual override of the Korea age rating, or NONE to remove the override. */
        koreaAgeRatingOverride?: "NONE" | "FIFTEEN_PLUS" | "NINETEEN_PLUS";
        /**
         * URL with more information about the age rating, or null to remove it.
         * @format uri
         */
        developerAgeRatingInfoUrl?: string | null;
      };
      output: {
        /** The age rating questionnaire answers for an app info record. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ageRatingDeclaration: {
          /** App Store Connect identifier for the age rating declaration. */
          id: string;
          /** Whether the app shows advertising. */
          advertising?: boolean | null;
          /** Whether the app performs age assurance. */
          ageAssurance?: boolean | null;
          /** How often alcohol, tobacco, or drug use or references appear. */
          alcoholTobaccoOrDrugUseOrReferences?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often contests appear. */
          contests?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Whether the app offers real gambling. */
          gambling?: boolean | null;
          /** How often simulated gambling appears. */
          gamblingSimulated?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often guns or other weapons appear. */
          gunsOrOtherWeapons?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Whether the app covers health or wellness topics. */
          healthOrWellnessTopics?: boolean | null;
          /** How often horror or fear themes appear. */
          horrorOrFearThemes?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Kids age band the app is designed for, or null. */
          kidsAgeBand?: "FIVE_AND_UNDER" | "SIX_TO_EIGHT" | "NINE_TO_ELEVEN" | null;
          /** Whether the app sells loot boxes or similar random rewards. */
          lootBox?: boolean | null;
          /** How often mature or suggestive themes appear. */
          matureOrSuggestiveThemes?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often medical or treatment information appears. */
          medicalOrTreatmentInformation?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Whether the app offers messaging or chat. */
          messagingAndChat?: boolean | null;
          /** Whether the app offers parental controls. */
          parentalControls?: boolean | null;
          /** How often profanity or crude humor appears. */
          profanityOrCrudeHumor?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often graphic sexual content or nudity appears. */
          sexualContentGraphicAndNudity?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often sexual content or nudity appears. */
          sexualContentOrNudity?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Whether the app includes social media features. */
          socialMedia?: boolean | null;
          /** Whether the social media features are restricted by age. */
          socialMediaAgeRestricted?: boolean | null;
          /** Whether the app offers unrestricted web access. */
          unrestrictedWebAccess?: boolean | null;
          /** Whether the app shows user-generated content. */
          userGeneratedContent?: boolean | null;
          /** How often cartoon or fantasy violence appears. */
          violenceCartoonOrFantasy?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often realistic violence appears. */
          violenceRealistic?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** How often prolonged graphic or sadistic realistic violence appears. */
          violenceRealisticProlongedGraphicOrSadistic?: "NONE" | "INFREQUENT_OR_MILD" | "FREQUENT_OR_INTENSE" | "INFREQUENT" | "FREQUENT" | null;
          /** Manual override raising the derived age rating, or NONE. */
          ageRatingOverrideV2?: "NONE" | "NINE_PLUS" | "THIRTEEN_PLUS" | "SIXTEEN_PLUS" | "EIGHTEEN_PLUS" | "UNRATED" | null;
          /** Manual override of the Korea age rating, or NONE. */
          koreaAgeRatingOverride?: "NONE" | "FIFTEEN_PLUS" | "NINETEEN_PLUS" | null;
          /** URL with more information about the age rating, published on the product page. */
          developerAgeRatingInfoUrl?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the package name or replace the full list of signing certificate fingerprints of an Android app mapping. Pass at least one field. */
    "app_store_connect.update_android_to_ios_app_mapping_detail": {
      input: {
        /**
         * App Store Connect identifier of the Android app mapping.
         * @minLength 1
         * @pattern \S
         */
        androidToIosAppMappingDetailId: string;
        /**
         * New Android package name.
         * @minLength 1
         * @pattern \S
         */
        packageName?: string;
        /**
         * Complete new list of SHA-256 certificate fingerprints; fingerprints left out are removed.
         * @minItems 1
         */
        appSigningKeyPublicCertificateSha256Fingerprints?: Array<string>;
      };
      output: {
        /** A mapping from an Android app to this iOS app, used for Android to iPhone migration. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        androidToIosAppMappingDetail: {
          /** App Store Connect identifier for the mapping detail. */
          id: string;
          /** Android package name of the mapped app. */
          packageName?: string | null;
          /** SHA-256 fingerprints of the Android app signing key certificates. */
          appSigningKeyPublicCertificateSha256Fingerprints?: Array<string> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Update app-level settings such as the primary locale, third-party content rights declaration, server notification URLs, or accessibility URL. Overwrites the given attributes; pass null for a URL to clear it. Pass at least one attribute. */
    "app_store_connect.update_app": {
      input: {
        /**
         * App Store Connect identifier of the app.
         * @minLength 1
         * @pattern \S
         */
        appId: string;
        /**
         * New primary App Store locale, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        primaryLocale?: string;
        /**
         * New bundle identifier. App Store Connect only accepts a change before the first build is uploaded.
         * @minLength 1
         * @pattern \S
         */
        bundleId?: string;
        /** Whether the app contains, shows, or accesses third-party content. */
        contentRightsDeclaration?: "DOES_NOT_USE_THIRD_PARTY_CONTENT" | "USES_THIRD_PARTY_CONTENT";
        /**
         * Production server URL that receives App Store Server Notifications, or null to clear it.
         * @format uri
         */
        subscriptionStatusUrl?: string | null;
        /** Notification version for the production URL. */
        subscriptionStatusUrlVersion?: "V1" | "V2";
        /**
         * Sandbox server URL that receives App Store Server Notifications, or null to clear it.
         * @format uri
         */
        subscriptionStatusUrlForSandbox?: string | null;
        /** Notification version for the sandbox URL. */
        subscriptionStatusUrlVersionForSandbox?: "V1" | "V2";
        /** Enable or disable streamlined purchasing. */
        streamlinedPurchasingEnabled?: boolean;
        /**
         * URL with accessibility information for the app, or null to clear it.
         * @format uri
         */
        accessibilityUrl?: string | null;
      };
      output: {
        /** An app registered in App Store Connect. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        app: {
          /** App Store Connect identifier for the app. */
          id: string;
          /** App name shown on the App Store. */
          name?: string | null;
          /** Bundle identifier registered for the app. */
          bundleId?: string | null;
          /** SKU chosen when the app record was created. */
          sku?: string | null;
          /** Primary App Store locale, such as en-US. */
          primaryLocale?: string | null;
          /** Whether the app is or has ever been part of the Kids category. */
          isOrEverWasMadeForKids?: boolean | null;
          /** Third-party content rights declared for the app. */
          contentRightsDeclaration?: "DOES_NOT_USE_THIRD_PARTY_CONTENT" | "USES_THIRD_PARTY_CONTENT" | null;
          /** Whether streamlined purchasing is enabled for the app. */
          streamlinedPurchasingEnabled?: boolean | null;
          /** Accessibility information URL published with the app. */
          accessibilityUrl?: string | null;
          /** Production server-to-server subscription status URL. */
          subscriptionStatusUrl?: string | null;
          /** Version of the production subscription status URL. */
          subscriptionStatusUrlVersion?: "V1" | "V2" | null;
          /** Sandbox server-to-server subscription status URL. */
          subscriptionStatusUrlForSandbox?: string | null;
          /** Version of the sandbox subscription status URL. */
          subscriptionStatusUrlVersionForSandbox?: "V1" | "V2" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the action, business category, default language, place, header image, or localized card text of an advanced App Clip experience. Overwrites the given fields; a given localizations list is written inline and replaces the relationship. Pass at least one field. */
    "app_store_connect.update_app_clip_advanced_experience": {
      input: {
        /**
         * App Store Connect identifier of the advanced App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipAdvancedExperienceId: string;
        /**
         * Identifier of a newly reserved header image (appClipAdvancedExperienceImages record) to replace the current one.
         * @minLength 1
         * @pattern \S
         */
        headerImageId?: string;
        /** New default language. */
        defaultLanguage?: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH";
        /** Whether the experience is submitted by a platform provider that serves multiple businesses. */
        isPoweredBy?: boolean;
        /** New call-to-action verb. */
        action?: "OPEN" | "VIEW" | "PLAY";
        /** New business category. */
        businessCategory?: "AUTOMOTIVE" | "BEAUTY" | "BIKES" | "BOOKS" | "CASINO" | "EDUCATION" | "EDUCATION_JAPAN" | "ENTERTAINMENT" | "EV_CHARGER" | "FINANCIAL_USD" | "FINANCIAL_CNY" | "FINANCIAL_GBP" | "FINANCIAL_JPY" | "FINANCIAL_EUR" | "FITNESS" | "FOOD_AND_DRINK" | "GAS" | "GROCERY" | "HEALTH_AND_MEDICAL" | "HOTEL_AND_TRAVEL" | "MUSIC" | "PARKING" | "PET_SERVICES" | "PROFESSIONAL_SERVICES" | "SHOPPING" | "TICKETING" | "TRANSIT";
        /** New physical place for the experience. */
        place?: {
          /** Apple Maps place identifier when the place is already known to Apple Maps. */
          placeId?: string;
          /** Names of the place. */
          names?: Array<string>;
          /** Main address of the place. */
          mainAddress?: {
            /** Full address as a single line. */
            fullAddress?: string;
            /** Address split into its parts. */
            structuredAddress?: {
              /** Street address lines. */
              streetAddress?: Array<string>;
              /** Floor within the building. */
              floor?: string;
              /** Neighborhood of the place. */
              neighborhood?: string;
              /** City or locality. */
              locality?: string;
              /** State or province. */
              stateProvince?: string;
              /** Postal code. */
              postalCode?: string;
              /** ISO 3166-1 alpha-2 country code, such as US. */
              countryCode?: string;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          };
          /** Where the pin is placed on the map. */
          displayPoint?: {
            /** Geographic coordinates of the pin. */
            coordinates?: {
              /** Latitude in decimal degrees. */
              latitude?: number;
              /** Longitude in decimal degrees. */
              longitude?: number;
              [key: string]: unknown;
            };
            /** Whether the pin was calculated from the address or placed manually. */
            source?: "CALCULATED" | "MANUALLY_PLACED";
            [key: string]: unknown;
          };
          /** Action offered on the place card in Apple Maps. */
          mapAction?: "BUY_TICKETS" | "VIEW_AVAILABILITY" | "VIEW_PRICING" | "HOTEL_BOOK_ROOM" | "PARKING_RESERVE_PARKING" | "RESTAURANT_JOIN_WAITLIST" | "RESTAURANT_ORDER_DELIVERY" | "RESTAURANT_ORDER_FOOD" | "RESTAURANT_ORDER_TAKEOUT" | "RESTAURANT_RESERVATION" | "SCHEDULE_APPOINTMENT" | "RESTAURANT_VIEW_MENU" | "THEATER_NOW_PLAYING" | "AIRLINE_BOOK_TRAVEL" | "AIRLINE_CHECK_IN" | "AIRLINE_FLIGHT_STATUS" | "APPLY" | "BOOK" | "BOOK_ACTIVITIES" | "BOOK_RIDES" | "BOOK_TEETIMES" | "BOOK_TOURS" | "CAREERS" | "CHARGE_EV" | "COUPONS" | "DONATE" | "EVENTS" | "EVENTS_SHOWS" | "EVENTS_SPORTS" | "GIFT_CARD" | "HOTEL_AMENITIES" | "JOIN" | "PARKING_AVAILABLE" | "RESTAURANT_PICKUP" | "RETAIL_SERVICE_QUOTE" | "RETAIL_STORE_DELIVERY" | "RETAIL_STORE_PICKUP" | "RETAIL_STORE_SHOP" | "SERVICES" | "SUPPORT" | "PAY_TO_PARK";
          /** Relationship between the submitter and the place. */
          relationship?: "OWNER" | "AUTHORIZED" | "OTHER";
          /** Phone number of the place. */
          phoneNumber?: {
            /** Phone number of the place. */
            number?: string;
            /** Kind of phone line. */
            type?: "FAX" | "LANDLINE" | "MOBILE" | "TOLLFREE";
            /** What the number is for, such as reservations. */
            intent?: string;
            [key: string]: unknown;
          };
          /** Website of the place. */
          homePage?: string;
          /** Apple Maps categories of the place. */
          categories?: Array<string>;
          [key: string]: unknown;
        };
        /**
         * Localized title and subtitle of the App Clip card. Give an existing localization its id to change it in place; omit the id to add a new one.
         * @minItems 1
         */
        localizations?: Array<{
          /**
           * Identifier of an existing localization to change. Omit it to add a new localization.
           * @minLength 1
           * @pattern \S
           */
          id?: string;
          /** Language of this localization. */
          language: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH";
          /**
           * Title shown on the App Clip card in this language.
           * @minLength 1
           * @pattern \S
           */
          title?: string;
          /**
           * Subtitle shown on the App Clip card in this language.
           * @minLength 1
           * @pattern \S
           */
          subtitle?: string;
        }>;
      };
      output: {
        /** An advanced App Clip experience launched from a URL, QR code, NFC tag, or a place in Apple Maps. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipAdvancedExperience: {
          /** App Store Connect identifier for the advanced App Clip experience. */
          id: string;
          /** Invocation URL of the experience. */
          link?: string | null;
          /** Build version of the App Clip the experience belongs to. */
          version?: number | null;
          /** Whether users can invoke the experience (RECEIVED), it is deactivated, or its app is being transferred. */
          status?: "RECEIVED" | "DEACTIVATED" | "APP_TRANSFER_IN_PROGRESS" | null;
          /** Call-to-action verb shown on the App Clip card. */
          action?: "OPEN" | "VIEW" | "PLAY" | null;
          /** Whether the experience was submitted by a platform provider that serves multiple businesses. */
          isPoweredBy?: boolean | null;
          /** Physical place the experience is tied to, or null. */
          place?: {
            /** Apple Maps place identifier when the place is already known to Apple Maps. */
            placeId?: string;
            /** Names of the place. */
            names?: Array<string>;
            /** Main address of the place. */
            mainAddress?: {
              /** Full address as a single line. */
              fullAddress?: string;
              /** Address split into its parts. */
              structuredAddress?: {
                /** Street address lines. */
                streetAddress?: Array<string>;
                /** Floor within the building. */
                floor?: string;
                /** Neighborhood of the place. */
                neighborhood?: string;
                /** City or locality. */
                locality?: string;
                /** State or province. */
                stateProvince?: string;
                /** Postal code. */
                postalCode?: string;
                /** ISO 3166-1 alpha-2 country code, such as US. */
                countryCode?: string;
                [key: string]: unknown;
              };
              [key: string]: unknown;
            };
            /** Where the pin is placed on the map. */
            displayPoint?: {
              /** Geographic coordinates of the pin. */
              coordinates?: {
                /** Latitude in decimal degrees. */
                latitude?: number;
                /** Longitude in decimal degrees. */
                longitude?: number;
                [key: string]: unknown;
              };
              /** Whether the pin was calculated from the address or placed manually. */
              source?: "CALCULATED" | "MANUALLY_PLACED";
              [key: string]: unknown;
            };
            /** Action offered on the place card in Apple Maps. */
            mapAction?: "BUY_TICKETS" | "VIEW_AVAILABILITY" | "VIEW_PRICING" | "HOTEL_BOOK_ROOM" | "PARKING_RESERVE_PARKING" | "RESTAURANT_JOIN_WAITLIST" | "RESTAURANT_ORDER_DELIVERY" | "RESTAURANT_ORDER_FOOD" | "RESTAURANT_ORDER_TAKEOUT" | "RESTAURANT_RESERVATION" | "SCHEDULE_APPOINTMENT" | "RESTAURANT_VIEW_MENU" | "THEATER_NOW_PLAYING" | "AIRLINE_BOOK_TRAVEL" | "AIRLINE_CHECK_IN" | "AIRLINE_FLIGHT_STATUS" | "APPLY" | "BOOK" | "BOOK_ACTIVITIES" | "BOOK_RIDES" | "BOOK_TEETIMES" | "BOOK_TOURS" | "CAREERS" | "CHARGE_EV" | "COUPONS" | "DONATE" | "EVENTS" | "EVENTS_SHOWS" | "EVENTS_SPORTS" | "GIFT_CARD" | "HOTEL_AMENITIES" | "JOIN" | "PARKING_AVAILABLE" | "RESTAURANT_PICKUP" | "RETAIL_SERVICE_QUOTE" | "RETAIL_STORE_DELIVERY" | "RETAIL_STORE_PICKUP" | "RETAIL_STORE_SHOP" | "SERVICES" | "SUPPORT" | "PAY_TO_PARK";
            /** Relationship between the submitter and the place. */
            relationship?: "OWNER" | "AUTHORIZED" | "OTHER";
            /** Phone number of the place. */
            phoneNumber?: {
              /** Phone number of the place. */
              number?: string;
              /** Kind of phone line. */
              type?: "FAX" | "LANDLINE" | "MOBILE" | "TOLLFREE";
              /** What the number is for, such as reservations. */
              intent?: string;
              [key: string]: unknown;
            };
            /** Website of the place. */
            homePage?: string;
            /** Apple Maps categories of the place. */
            categories?: Array<string>;
            [key: string]: unknown;
          } | null;
          /** Whether Apple Maps matched the place to a point of interest. */
          placeStatus?: "PENDING" | "MATCHED" | "NO_MATCH" | null;
          /** Business category of the experience. */
          businessCategory?: "AUTOMOTIVE" | "BEAUTY" | "BIKES" | "BOOKS" | "CASINO" | "EDUCATION" | "EDUCATION_JAPAN" | "ENTERTAINMENT" | "EV_CHARGER" | "FINANCIAL_USD" | "FINANCIAL_CNY" | "FINANCIAL_GBP" | "FINANCIAL_JPY" | "FINANCIAL_EUR" | "FITNESS" | "FOOD_AND_DRINK" | "GAS" | "GROCERY" | "HEALTH_AND_MEDICAL" | "HOTEL_AND_TRAVEL" | "MUSIC" | "PARKING" | "PET_SERVICES" | "PROFESSIONAL_SERVICES" | "SHOPPING" | "TICKETING" | "TRANSIT" | null;
          /** Default language of the experience. */
          defaultLanguage?: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH" | null;
          /** Localized card text of the experience, empty when none was returned. */
          localizations: Array<{
            /** App Store Connect identifier for the localization. */
            id: string;
            /** Language of the localization. */
            language?: "AR" | "CA" | "CS" | "DA" | "DE" | "EL" | "EN" | "ES" | "FI" | "FR" | "HE" | "HI" | "HR" | "HU" | "ID" | "IT" | "JA" | "KO" | "MS" | "NL" | "NO" | "PL" | "PT" | "RO" | "RU" | "SK" | "SV" | "TH" | "TR" | "UK" | "VI" | "ZH" | null;
            /** Title shown on the App Clip card. */
            title?: string | null;
            /** Subtitle shown on the App Clip card. */
            subtitle?: string | null;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the invocation URLs App Review uses to test a default App Clip experience. Pass an empty list to remove them all. */
    "app_store_connect.update_app_clip_app_store_review_detail": {
      input: {
        /**
         * App Store Connect identifier of the App Clip App Store review detail.
         * @minLength 1
         * @pattern \S
         */
        appClipAppStoreReviewDetailId: string;
        /** Invocation URLs App Review can use to launch the App Clip; replaces the current list. */
        invocationUrls: Array<string>;
      };
      output: {
        /** Invocation URLs App Review uses to test a default App Clip experience. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipAppStoreReviewDetail: {
          /** App Store Connect identifier for the App Clip App Store review detail. */
          id: string;
          /** Invocation URLs App Review can use to launch the App Clip. */
          invocationUrls?: Array<string> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the action of a default App Clip experience or the App Store version it is released with. Pass at least one field. */
    "app_store_connect.update_app_clip_default_experience": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceId: string;
        /** New call-to-action verb. */
        action?: "OPEN" | "VIEW" | "PLAY";
        /**
         * App Store Connect identifier of the App Store version to release the experience with, replacing the current one.
         * @minLength 1
         * @pattern \S
         */
        releaseWithAppStoreVersionId?: string;
      };
      output: {
        /** The default App Clip experience that ships with an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipDefaultExperience: {
          /** App Store Connect identifier for the default App Clip experience. */
          id: string;
          /** Call-to-action verb shown on the App Clip card. */
          action?: "OPEN" | "VIEW" | "PLAY" | null;
          /** App Store Connect identifier of the App Store version the experience is released with, or null when none is attached. */
          releaseWithAppStoreVersionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the subtitle of a default App Clip experience localization, or pass null to remove it. */
    "app_store_connect.update_app_clip_default_experience_localization": {
      input: {
        /**
         * App Store Connect identifier of the default App Clip experience localization.
         * @minLength 1
         * @pattern \S
         */
        appClipDefaultExperienceLocalizationId: string;
        /**
         * New subtitle, or null to remove it.
         * @minLength 1
         * @pattern \S
         */
        subtitle: string | null;
      };
      output: {
        /** Localized subtitle of a default App Clip experience for one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appClipDefaultExperienceLocalization: {
          /** App Store Connect identifier for the default App Clip experience localization. */
          id: string;
          /** Locale the subtitle is written in, such as en-US. */
          locale?: string | null;
          /** Subtitle shown on the App Clip card in this locale. */
          subtitle?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Rename a custom product page or toggle its visibility on the App Store. Overwrites the given fields; pass at least one. */
    "app_store_connect.update_app_custom_product_page": {
      input: {
        /**
         * App Store Connect identifier of the custom product page.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageId: string;
        /**
         * New internal name of the page.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** Show the page on the App Store when true, or hide it when false. Only an approved page can be made visible. */
        visible?: boolean;
      };
      output: {
        /** A custom product page of an app. Each page has its own versions that go through App Review and can be reached through a dedicated App Store URL. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appCustomProductPage: {
          /** App Store Connect identifier for the custom product page. */
          id: string;
          /** Internal name of the custom product page shown in App Store Connect. */
          name?: string | null;
          /** App Store URL that opens this custom product page. */
          url?: string | null;
          /** Whether the page is visible on the App Store. A hidden page keeps its URL but is not shown. */
          visible?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the promotional text of a custom product page localization, or clear it with null. */
    "app_store_connect.update_app_custom_product_page_localization": {
      input: {
        /**
         * App Store Connect identifier of the custom product page localization.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageLocalizationId: string;
        /**
         * New promotional text, up to 170 characters, or null to clear it.
         * @minLength 1
         * @maxLength 170
         * @pattern \S
         */
        promotionalText: string | null;
      };
      output: {
        /** The metadata of a custom product page version in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appCustomProductPageLocalization: {
          /** App Store Connect identifier for the custom product page localization. */
          id: string;
          /** Locale the metadata is written in, such as en-US. */
          locale?: string | null;
          /** Promotional text shown above the description on this custom product page. */
          promotionalText?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set or clear the deep link of a custom product page version. Overwrites the existing deep link; pass null to remove it. */
    "app_store_connect.update_app_custom_product_page_version": {
      input: {
        /**
         * App Store Connect identifier of the custom product page version.
         * @minLength 1
         * @pattern \S
         */
        appCustomProductPageVersionId: string;
        /**
         * New deep link, or null to remove it.
         * @format uri
         */
        deepLink: string | null;
      };
      output: {
        /** One version of a custom product page. A new version is created for each round of edits and App Review. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appCustomProductPageVersion: {
          /** App Store Connect identifier for the custom product page version. */
          id: string;
          /** Version number App Store Connect assigned to this page version. */
          version?: string | null;
          /** Review state of the page version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REPLACED_WITH_NEW_VERSION" | "REJECTED" | null;
          /** Deep link opened when a user taps Get or Open on this custom product page, or null. */
          deepLink?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the reference name, badge, deep link, purchase requirement, locale, priority, purpose, or territory schedules of an in-app event. Overwrites the given fields; pass null for deepLink or purchaseRequirement to clear it. Pass at least one field. */
    "app_store_connect.update_app_event": {
      input: {
        /**
         * App Store Connect identifier of the in-app event.
         * @minLength 1
         * @pattern \S
         */
        appEventId: string;
        /**
         * New internal reference name.
         * @minLength 1
         * @pattern \S
         */
        referenceName?: string;
        /**
         * New deep link, or null to remove it.
         * @format uri
         */
        deepLink?: string | null;
        /** Badge that labels the kind of event. */
        badge?: "LIVE_EVENT" | "PREMIERE" | "CHALLENGE" | "COMPETITION" | "NEW_SEASON" | "MAJOR_UPDATE" | "SPECIAL_EVENT";
        /**
         * New purchase requirement (NO_COST_ASSOCIATED or IN_APP_PURCHASE), or null to remove it.
         * @minLength 1
         * @pattern \S
         */
        purchaseRequirement?: string | null;
        /**
         * Primary locale of the event metadata, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        primaryLocale?: string;
        /** Priority relative to the other events of the app. */
        priority?: "HIGH" | "NORMAL";
        /** Audience the event is meant for. */
        purpose?: "APPROPRIATE_FOR_ALL_USERS" | "ATTRACT_NEW_USERS" | "KEEP_ACTIVE_USERS_INFORMED" | "BRING_BACK_LAPSED_USERS";
        /** Territory schedules of the event. Each entry gives one publish, start, and end time for a group of territories; the whole list replaces the current schedules. */
        territorySchedules?: Array<{
          /**
           * ISO 3166-1 alpha-3 territories this schedule applies to.
           * @minItems 1
           */
          territories?: Array<string>;
          /**
           * When the event becomes visible on the App Store in these territories, as an ISO 8601 timestamp.
           * @format date-time
           */
          publishStart?: string;
          /**
           * When the event starts, as an ISO 8601 timestamp.
           * @format date-time
           */
          eventStart?: string;
          /**
           * When the event ends, as an ISO 8601 timestamp.
           * @format date-time
           */
          eventEnd?: string;
        }>;
      };
      output: {
        /** An in-app event shown on the App Store product page. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appEvent: {
          /** App Store Connect identifier for the in-app event. */
          id: string;
          /** Internal reference name of the event, not shown on the App Store. */
          referenceName?: string | null;
          /** Badge that labels the kind of event. */
          badge?: "LIVE_EVENT" | "PREMIERE" | "CHALLENGE" | "COMPETITION" | "NEW_SEASON" | "MAJOR_UPDATE" | "SPECIAL_EVENT" | null;
          /** Review and publication state of the event. */
          eventState?: "DRAFT" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "REJECTED" | "ACCEPTED" | "APPROVED" | "PUBLISHED" | "PAST" | "ARCHIVED" | null;
          /** Deep link opened in the app when a user taps the event. */
          deepLink?: string | null;
          /** Purchase required to take part, such as NO_COST_ASSOCIATED or IN_APP_PURCHASE. */
          purchaseRequirement?: string | null;
          /** Primary locale of the event metadata, such as en-US. */
          primaryLocale?: string | null;
          /** Priority relative to the other events of the app. */
          priority?: "HIGH" | "NORMAL" | null;
          /** Audience the event is meant for. */
          purpose?: "APPROPRIATE_FOR_ALL_USERS" | "ATTRACT_NEW_USERS" | "KEEP_ACTIVE_USERS_INFORMED" | "BRING_BACK_LAPSED_USERS" | null;
          /** Territory schedules of the event, or null when none are set. */
          territorySchedules?: Array<{
            /** ISO 3166-1 alpha-3 territories this schedule applies to. */
            territories?: Array<string> | null;
            /** When the event becomes visible on the App Store, as an ISO 8601 timestamp. */
            publishStart?: string | null;
            /** When the event starts, as an ISO 8601 timestamp. */
            eventStart?: string | null;
            /** When the event ends, as an ISO 8601 timestamp. */
            eventEnd?: string | null;
            [key: string]: unknown;
          }> | null;
          /** Schedules of past occurrences App Store Connect archived, or null when there are none. */
          archivedTerritorySchedules?: Array<{
            /** ISO 3166-1 alpha-3 territories this schedule applies to. */
            territories?: Array<string> | null;
            /** When the event becomes visible on the App Store, as an ISO 8601 timestamp. */
            publishStart?: string | null;
            /** When the event starts, as an ISO 8601 timestamp. */
            eventStart?: string | null;
            /** When the event ends, as an ISO 8601 timestamp. */
            eventEnd?: string | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the name or descriptions of an in-app event localization. Overwrites the given fields; pass null to clear a field. Pass at least one field. */
    "app_store_connect.update_app_event_localization": {
      input: {
        /**
         * App Store Connect identifier of the in-app event localization.
         * @minLength 1
         * @pattern \S
         */
        appEventLocalizationId: string;
        /**
         * New event name, or null to remove it.
         * @minLength 1
         * @pattern \S
         */
        name?: string | null;
        /**
         * New short description, or null to remove it.
         * @minLength 1
         * @pattern \S
         */
        shortDescription?: string | null;
        /**
         * New long description, or null to remove it.
         * @minLength 1
         * @pattern \S
         */
        longDescription?: string | null;
      };
      output: {
        /** Localized name and descriptions of an in-app event for one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appEventLocalization: {
          /** App Store Connect identifier for the in-app event localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Event name shown on the App Store in this locale. */
          name?: string | null;
          /** Short description shown on the event card. */
          shortDescription?: string | null;
          /** Long description shown on the event details page. */
          longDescription?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set the App Store categories of an app info record. Each given category replaces the current one; subcategories only apply when the matching category is Games or Stickers. Pass at least one category. Use list_app_categories to find identifiers. */
    "app_store_connect.update_app_info": {
      input: {
        /**
         * App Store Connect identifier of the app info record.
         * @minLength 1
         * @pattern \S
         */
        appInfoId: string;
        /**
         * Identifier of the new primary category, such as GAMES.
         * @minLength 1
         * @pattern \S
         */
        primaryCategoryId?: string;
        /**
         * Identifier of the first primary subcategory, such as GAMES_ACTION.
         * @minLength 1
         * @pattern \S
         */
        primarySubcategoryOneId?: string;
        /**
         * Identifier of the second primary subcategory.
         * @minLength 1
         * @pattern \S
         */
        primarySubcategoryTwoId?: string;
        /**
         * Identifier of the new secondary category.
         * @minLength 1
         * @pattern \S
         */
        secondaryCategoryId?: string;
        /**
         * Identifier of the first secondary subcategory.
         * @minLength 1
         * @pattern \S
         */
        secondarySubcategoryOneId?: string;
        /**
         * Identifier of the second secondary subcategory.
         * @minLength 1
         * @pattern \S
         */
        secondarySubcategoryTwoId?: string;
      };
      output: {
        /** App-level App Store information shared by every version of the app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appInfo: {
          /** App Store Connect identifier for the app info record. */
          id: string;
          /** Review state of this app info record. */
          state?: "ACCEPTED" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "PENDING_RELEASE" | "PREPARE_FOR_SUBMISSION" | "READY_FOR_DISTRIBUTION" | "READY_FOR_REVIEW" | "REJECTED" | "REPLACED_WITH_NEW_INFO" | "WAITING_FOR_REVIEW" | null;
          /** App Store state of the app the record belongs to. */
          appStoreState?: "ACCEPTED" | "DEVELOPER_REMOVED_FROM_SALE" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "INVALID_BINARY" | "METADATA_REJECTED" | "PENDING_APPLE_RELEASE" | "PENDING_CONTRACT" | "PENDING_DEVELOPER_RELEASE" | "PREPARE_FOR_SUBMISSION" | "PREORDER_READY_FOR_SALE" | "PROCESSING_FOR_APP_STORE" | "READY_FOR_REVIEW" | "READY_FOR_SALE" | "REJECTED" | "REMOVED_FROM_SALE" | "WAITING_FOR_EXPORT_COMPLIANCE" | "WAITING_FOR_REVIEW" | "REPLACED_WITH_NEW_VERSION" | "NOT_APPLICABLE" | null;
          /** Age rating App Store Connect derived from the age rating declaration. */
          appStoreAgeRating?: "L" | "ALL" | "ZERO_ZERO" | "ONE_PLUS" | "TWO_PLUS" | "THREE_PLUS" | "FOUR_PLUS" | "FIVE_PLUS" | "SIX_PLUS" | "SEVEN_PLUS" | "EIGHT_PLUS" | "NINE_PLUS" | "TEN_PLUS" | "ELEVEN_PLUS" | "TWELVE_PLUS" | "THIRTEEN_PLUS" | "FOURTEEN_PLUS" | "FIFTEEN_PLUS" | "SIXTEEN_PLUS" | "SEVENTEEN_PLUS" | "EIGHTEEN_PLUS" | "NINETEEN_PLUS" | "TWENTY_PLUS" | "TWENTY_ONE_PLUS" | "UNRATED" | null;
          /** Age rating shown on the Brazil storefront. */
          brazilAgeRating?: "L" | "TEN" | "TWELVE" | "FOURTEEN" | "SIXTEEN" | "EIGHTEEN" | null;
          /** Kids age band when the app is in the Kids category, otherwise null. */
          kidsAgeBand?: "FIVE_AND_UNDER" | "SIX_TO_EIGHT" | "NINE_TO_ELEVEN" | null;
          /** Identifier of the primary App Store category, such as GAMES, or null when none is set. */
          primaryCategoryId: string | null;
          /** Identifier of the first primary subcategory, used when the primary category is Games or Stickers. */
          primarySubcategoryOneId: string | null;
          /** Identifier of the second primary subcategory, or null when none is set. */
          primarySubcategoryTwoId: string | null;
          /** Identifier of the secondary App Store category, or null when none is set. */
          secondaryCategoryId: string | null;
          /** Identifier of the first secondary subcategory, or null when none is set. */
          secondarySubcategoryOneId: string | null;
          /** Identifier of the second secondary subcategory, or null when none is set. */
          secondarySubcategoryTwoId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the name, subtitle, or privacy URLs of an app info localization. Overwrites the given fields; pass null to clear a subtitle, URL, or privacy text. Pass at least one field. */
    "app_store_connect.update_app_info_localization": {
      input: {
        /**
         * App Store Connect identifier of the app info localization.
         * @minLength 1
         * @pattern \S
         */
        appInfoLocalizationId: string;
        /**
         * New app name for this locale.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * New subtitle, or null to remove it.
         * @minLength 1
         * @pattern \S
         */
        subtitle?: string | null;
        /**
         * New privacy policy URL, or null to remove it.
         * @format uri
         */
        privacyPolicyUrl?: string | null;
        /**
         * New privacy choices URL, or null to remove it.
         * @format uri
         */
        privacyChoicesUrl?: string | null;
        /**
         * New privacy policy text for Apple TV, or null to remove it.
         * @minLength 1
         * @pattern \S
         */
        privacyPolicyText?: string | null;
      };
      output: {
        /** Localized app-level App Store information for one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appInfoLocalization: {
          /** App Store Connect identifier for the app info localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** App name shown on the App Store in this locale. */
          name?: string | null;
          /** Subtitle shown under the app name in this locale. */
          subtitle?: string | null;
          /** Privacy policy URL published for this locale. */
          privacyPolicyUrl?: string | null;
          /** URL where users can manage their privacy choices, published for this locale. */
          privacyChoicesUrl?: string | null;
          /** Privacy policy text shown on Apple TV, where a URL cannot be opened. */
          privacyPolicyText?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Overwrite the App Review contact details, demo account, or notes of an App Store version. Only the given fields change; pass null to clear a text field. */
    "app_store_connect.update_app_store_review_detail": {
      input: {
        /**
         * App Store Connect identifier of the review detail.
         * @minLength 1
         * @pattern \S
         */
        appStoreReviewDetailId: string;
        /**
         * New contact first name, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        contactFirstName?: string | null;
        /**
         * New contact last name, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        contactLastName?: string | null;
        /**
         * New contact phone number, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        contactPhone?: string | null;
        /**
         * New contact email address, or null to clear it.
         * @format email
         */
        contactEmail?: string | null;
        /**
         * New demo account username, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        demoAccountName?: string | null;
        /**
         * New demo account password, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        demoAccountPassword?: string | null;
        /** Whether App Review needs a demo account to sign in. */
        demoAccountRequired?: boolean;
        /**
         * New notes for the App Review team, or null to clear them.
         * @minLength 1
         * @pattern \S
         */
        notes?: string | null;
      };
      output: {
        /** The App Review contact and demo account information of an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreReviewDetail: {
          /** App Store Connect identifier for the review detail. */
          id: string;
          /** First name of the person App Review may contact. */
          contactFirstName?: string | null;
          /** Last name of the person App Review may contact. */
          contactLastName?: string | null;
          /** Phone number App Review may call. */
          contactPhone?: string | null;
          /** Email address App Review may write to. */
          contactEmail?: string | null;
          /** Username of the demo account provided to App Review. */
          demoAccountName?: string | null;
          /** Password of the demo account provided to App Review. */
          demoAccountPassword?: string | null;
          /** Whether App Review needs a demo account to sign in. */
          demoAccountRequired?: boolean | null;
          /** Additional notes for the App Review team. */
          notes?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the version string, copyright, review or release settings, or attached build of an App Store version. Each given field overwrites the current value; pass null for copyright or earliestReleaseDate to clear them. */
    "app_store_connect.update_app_store_version": {
      input: {
        /**
         * App Store Connect identifier of the version.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionId: string;
        /**
         * New version string shown on the App Store, such as 1.4.1.
         * @minLength 1
         * @pattern \S
         */
        versionString?: string;
        /**
         * New copyright line, or null to clear the current copyright.
         * @minLength 1
         * @pattern \S
         */
        copyright?: string | null;
        /** New review track for the version. */
        reviewType?: "APP_STORE" | "NOTARIZATION";
        /** New release behavior after approval. */
        releaseType?: "MANUAL" | "AFTER_APPROVAL" | "SCHEDULED";
        /**
         * New earliest release time for a SCHEDULED release, as an ISO 8601 timestamp, or null to clear the scheduled date.
         * @format date-time
         */
        earliestReleaseDate?: string | null;
        /** Whether the version can be downloaded from the App Store. */
        downloadable?: boolean;
        /**
         * App Store Connect identifier of the build to attach, replacing the current build.
         * @minLength 1
         * @pattern \S
         */
        buildId?: string;
      };
      output: {
        /** An App Store version of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersion: {
          /** App Store Connect identifier for the version. */
          id: string;
          /** Content platform the version targets. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Version string shown on the App Store, such as 1.4.0. */
          versionString?: string | null;
          /** Current review and release state. */
          appVersionState?: "ACCEPTED" | "DEVELOPER_REJECTED" | "IN_REVIEW" | "INVALID_BINARY" | "METADATA_REJECTED" | "PENDING_APPLE_RELEASE" | "PENDING_DEVELOPER_RELEASE" | "PREPARE_FOR_SUBMISSION" | "PROCESSING_FOR_DISTRIBUTION" | "READY_FOR_DISTRIBUTION" | "READY_FOR_REVIEW" | "REJECTED" | "REPLACED_WITH_NEW_VERSION" | "WAITING_FOR_EXPORT_COMPLIANCE" | "WAITING_FOR_REVIEW" | null;
          /** Copyright line published with the version. */
          copyright?: string | null;
          /** Review track the version goes through. */
          reviewType?: "APP_STORE" | "NOTARIZATION" | null;
          /** Release behavior after approval. */
          releaseType?: "MANUAL" | "AFTER_APPROVAL" | "SCHEDULED" | null;
          /** Earliest scheduled release time, as an ISO 8601 timestamp. */
          earliestReleaseDate?: string | null;
          /** Whether the version is downloadable. */
          downloadable?: boolean | null;
          /** When the version record was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Rename a product page optimization test, change its traffic proportion, or start it by passing started true. Starting a test is not reversible: it begins serving treatments to App Store traffic and can only be stopped, not returned to draft. Pass at least one field. */
    "app_store_connect.update_app_store_version_experiment": {
      input: {
        /**
         * App Store Connect identifier of the product page optimization test.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentId: string;
        /**
         * New test name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** New percentage of App Store traffic directed to the treatments. */
        trafficProportion?: number;
        /** Pass true to start the test once its treatments are approved. Passing false has no effect on a running test. */
        started?: boolean;
      };
      output: {
        /** A product page optimization test (App Store version experiment v2) that splits App Store traffic between the default product page and its treatments. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionExperiment: {
          /** App Store Connect identifier for the experiment. */
          id: string;
          /** Test name shown in App Store Connect. */
          name?: string | null;
          /** Content platform the test runs on. */
          platform?: "IOS" | "MAC_OS" | "TV_OS" | "VISION_OS" | null;
          /** Percentage of App Store traffic directed to the treatments instead of the default product page. */
          trafficProportion?: number | null;
          /** Review and run state of the test. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "ACCEPTED" | "APPROVED" | "REJECTED" | "COMPLETED" | "STOPPED" | null;
          /** Whether the treatments must pass App Review before the test can start. */
          reviewRequired?: boolean | null;
          /** When the test started, as an ISO 8601 timestamp. */
          startDate?: string | null;
          /** When the test ended or was stopped, as an ISO 8601 timestamp. */
          endDate?: string | null;
          /** App Store Connect identifier of the App Store version currently serving as the control (the default product page), or null when App Store Connect did not return it. */
          latestControlVersionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Rename a treatment or change its alternate app icon. Overwrites the given fields; pass null as appIconName to return to the default icon. Pass at least one field. */
    "app_store_connect.update_app_store_version_experiment_treatment": {
      input: {
        /**
         * App Store Connect identifier of the treatment.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionExperimentTreatmentId: string;
        /**
         * New treatment name.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * Name of the alternate app icon asset to use, or null to use the default icon.
         * @minLength 1
         * @pattern \S
         */
        appIconName?: string | null;
      };
      output: {
        /** One treatment of a product page optimization test: an alternative product page shown to part of the traffic. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionExperimentTreatment: {
          /** App Store Connect identifier for the treatment. */
          id: string;
          /** Treatment name shown in App Store Connect. */
          name?: string | null;
          /** Name of the alternate app icon asset the treatment uses, or null for the default icon. */
          appIconName?: string | null;
          /** Template URL and pixel size of the treatment app icon. */
          appIcon?: {
            /** Template URL with width, height, and format placeholders. */
            templateUrl?: string;
            /** Icon width in pixels. */
            width?: number;
            /** Icon height in pixels. */
            height?: number;
            [key: string]: unknown;
          } | null;
          /** When the treatment was promoted to the default product page, as an ISO 8601 timestamp, or null. */
          promotedDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Overwrite the localized App Store metadata of one version in one locale. Only the given fields change; pass null to clear a field. */
    "app_store_connect.update_app_store_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the version localization.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionLocalizationId: string;
        /**
         * New app description, up to 4,000 characters, or null to clear it.
         * @minLength 1
         * @maxLength 4000
         * @pattern \S
         */
        description?: string | null;
        /**
         * New comma-separated search keywords, up to 100 characters in total, or null to clear them.
         * @minLength 1
         * @maxLength 100
         * @pattern \S
         */
        keywords?: string | null;
        /**
         * New marketing website URL, or null to clear it.
         * @format uri
         */
        marketingUrl?: string | null;
        /**
         * New promotional text, up to 170 characters, or null to clear it.
         * @minLength 1
         * @maxLength 170
         * @pattern \S
         */
        promotionalText?: string | null;
        /**
         * New support website URL, or null to clear it.
         * @format uri
         */
        supportUrl?: string | null;
        /**
         * New release notes, up to 4,000 characters, or null to clear them.
         * @minLength 1
         * @maxLength 4000
         * @pattern \S
         */
        whatsNew?: string | null;
      };
      output: {
        /** The App Store metadata of one version in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionLocalization: {
          /** App Store Connect identifier for the version localization. */
          id: string;
          /** Locale the metadata is written in, such as en-US. */
          locale?: string | null;
          /** App description shown on the product page. */
          description?: string | null;
          /** Comma-separated App Store search keywords. */
          keywords?: string | null;
          /** Marketing website URL shown on the product page. */
          marketingUrl?: string | null;
          /** Promotional text shown above the description, editable without a new version. */
          promotionalText?: string | null;
          /** Support website URL shown on the product page. */
          supportUrl?: string | null;
          /** Release notes shown for this version. */
          whatsNew?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Pause, resume, or complete the phased rollout of a released version. COMPLETE releases the version to every user at once and cannot be undone. */
    "app_store_connect.update_app_store_version_phased_release": {
      input: {
        /**
         * App Store Connect identifier of the phased release.
         * @minLength 1
         * @pattern \S
         */
        appStoreVersionPhasedReleaseId: string;
        /** New rollout state: ACTIVE resumes, PAUSED pauses, COMPLETE releases to all users. */
        phasedReleaseState: "INACTIVE" | "ACTIVE" | "PAUSED" | "COMPLETE";
      };
      output: {
        /** The phased release configuration of an App Store version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        appStoreVersionPhasedRelease: {
          /** App Store Connect identifier for the phased release. */
          id: string;
          /** Whether the seven day phased rollout is inactive, active, paused, or complete. */
          phasedReleaseState?: "INACTIVE" | "ACTIVE" | "PAUSED" | "COMPLETE" | null;
          /** When the phased release started, as an ISO 8601 timestamp. */
          startDate?: string | null;
          /** Total number of days the rollout has been paused. */
          totalPauseDuration?: number | null;
          /** Current day of the seven day rollout, starting at 1. */
          currentDayNumber?: number | null;
          [key: string]: unknown;
        };
      };
    };
    /** Archive or unarchive a background asset pack. Archiving is the only change App Store Connect accepts on an asset pack; the identifier and versions stay as they are. */
    "app_store_connect.update_background_asset": {
      input: {
        /**
         * App Store Connect identifier of the background asset pack.
         * @minLength 1
         * @pattern \S
         */
        backgroundAssetId: string;
        /** True to archive the asset pack, false to make it active again. */
        archived: boolean;
      };
      output: {
        /** An Apple-hosted background asset pack of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        backgroundAsset: {
          /** App Store Connect identifier of the background asset pack. */
          id: string;
          /** Asset pack identifier the app uses to request the pack, chosen when the pack was created. */
          assetPackIdentifier?: string | null;
          /** Whether the asset pack has been archived. */
          archived?: boolean | null;
          /** When the asset pack record was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Storage in bytes the asset pack occupies. */
          usedBytes?: number | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          appStoreVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          internalBetaVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          /** A background asset version summary, or null when no version holds this slot or App Store Connect did not return it. */
          externalBetaVersion: {
            /** App Store Connect identifier of the background asset version. */
            id: string;
            /** Version number App Store Connect assigned to the asset pack version. */
            version?: string | null;
            /** Upload and processing state of the version. */
            state?: "AWAITING_UPLOAD" | "PROCESSING" | "FAILED" | "COMPLETE" | null;
            /** Locale declared in the asset pack manifest, when the pack is localized. */
            locale?: string | null;
            /** Platforms the asset pack version targets. */
            platforms?: Array<"IOS" | "MAC_OS" | "TV_OS" | "VISION_OS"> | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the invocation URL of a TestFlight App Clip invocation. The previous URL is overwritten. */
    "app_store_connect.update_beta_app_clip_invocation": {
      input: {
        /**
         * App Store Connect identifier of the App Clip invocation.
         * @minLength 1
         * @pattern \S
         */
        betaAppClipInvocationId: string;
        /**
         * New invocation URL the App Clip is launched with.
         * @format uri
         */
        url: string;
      };
      output: {
        /** An App Clip experience testers can launch from the TestFlight app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppClipInvocation: {
          /** App Store Connect identifier for the App Clip invocation. */
          id: string;
          /** Invocation URL the App Clip is launched with from TestFlight. */
          url?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the title of one localized App Clip invocation title. The locale itself cannot be changed; the previous title is overwritten. */
    "app_store_connect.update_beta_app_clip_invocation_localization": {
      input: {
        /**
         * App Store Connect identifier of the localized title.
         * @minLength 1
         * @pattern \S
         */
        betaAppClipInvocationLocalizationId: string;
        /**
         * New title testers see for the invocation in this locale.
         * @minLength 1
         * @pattern \S
         */
        title: string;
      };
      output: {
        /** A localized title for a TestFlight App Clip invocation. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppClipInvocationLocalization: {
          /** App Store Connect identifier for the localization. */
          id: string;
          /** Title testers see for the App Clip invocation in this locale. */
          title?: string | null;
          /** Locale the title is written in, such as en-US. */
          locale?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the TestFlight test information of one app localization. Each field given replaces the stored value; pass at least one. */
    "app_store_connect.update_beta_app_localization": {
      input: {
        /**
         * App Store Connect identifier of the beta app localization.
         * @minLength 1
         * @pattern \S
         */
        betaAppLocalizationId: string;
        /**
         * Description of the app shown to testers in TestFlight.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
        /**
         * Email address testers send feedback to.
         * @format email
         */
        feedbackEmail?: string;
        /**
         * Marketing URL shown to testers.
         * @format uri
         */
        marketingUrl?: string;
        /**
         * Privacy policy URL shown to testers.
         * @format uri
         */
        privacyPolicyUrl?: string;
        /**
         * Privacy policy text shown to tvOS testers.
         * @minLength 1
         * @pattern \S
         */
        tvOsPrivacyPolicy?: string;
      };
      output: {
        /** TestFlight test information of an app in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppLocalization: {
          /** App Store Connect identifier for the beta app localization. */
          id: string;
          /** Locale the test information is written in, such as en-US. */
          locale?: string | null;
          /** Description of the app shown to testers in TestFlight. */
          description?: string | null;
          /** Email address testers send feedback to. */
          feedbackEmail?: string | null;
          /** Marketing URL shown to testers. */
          marketingUrl?: string | null;
          /** Privacy policy URL shown to testers. */
          privacyPolicyUrl?: string | null;
          /** Privacy policy text shown to tvOS testers. */
          tvOsPrivacyPolicy?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the contact person, demo account, or notes Apple uses when reviewing an app for TestFlight. Each field given replaces the stored value; pass at least one. The identifier comes from get_beta_app_review_detail. */
    "app_store_connect.update_beta_app_review_detail": {
      input: {
        /**
         * App Store Connect identifier of the beta app review detail.
         * @minLength 1
         * @pattern \S
         */
        betaAppReviewDetailId: string;
        /**
         * First name of the person Apple contacts about the review.
         * @minLength 1
         * @pattern \S
         */
        contactFirstName?: string;
        /**
         * Last name of the review contact.
         * @minLength 1
         * @pattern \S
         */
        contactLastName?: string;
        /**
         * Phone number of the review contact.
         * @minLength 1
         * @pattern \S
         */
        contactPhone?: string;
        /**
         * Email address of the review contact.
         * @format email
         */
        contactEmail?: string;
        /**
         * Username of the demo account reviewers sign in with.
         * @minLength 1
         * @pattern \S
         */
        demoAccountName?: string;
        /**
         * Password of the demo account reviewers sign in with.
         * @minLength 1
         */
        demoAccountPassword?: string;
        /** Whether reviewers need a demo account to test the app. */
        demoAccountRequired?: boolean;
        /**
         * Additional notes for the beta review team.
         * @minLength 1
         * @pattern \S
         */
        notes?: string;
      };
      output: {
        /** Contact and demo account information Apple uses when reviewing the app for TestFlight. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaAppReviewDetail: {
          /** App Store Connect identifier for the beta app review detail. */
          id: string;
          /** First name of the person Apple contacts about the review. */
          contactFirstName?: string | null;
          /** Last name of the review contact. */
          contactLastName?: string | null;
          /** Phone number of the review contact. */
          contactPhone?: string | null;
          /** Email address of the review contact. */
          contactEmail?: string | null;
          /** Username of the demo account reviewers sign in with. */
          demoAccountName?: string | null;
          /** Password of the demo account reviewers sign in with. */
          demoAccountPassword?: string | null;
          /** Whether reviewers need a demo account to test the app. */
          demoAccountRequired?: boolean | null;
          /** Additional notes for the beta review team. */
          notes?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the "What to Test" notes of one existing build localization. The previous text is overwritten. */
    "app_store_connect.update_beta_build_localization": {
      input: {
        /**
         * App Store Connect identifier of the beta build localization.
         * @minLength 1
         * @pattern \S
         */
        betaBuildLocalizationId: string;
        /**
         * Test notes shown to testers in that locale.
         * @minLength 1
         * @pattern \S
         */
        whatsNew: string;
      };
      output: {
        /** The "What to Test" notes of one build in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaBuildLocalization: {
          /** App Store Connect identifier for the beta build localization. */
          id: string;
          /** Locale the test notes belong to, such as en-US. */
          locale?: string | null;
          /** Test notes shown to testers for this locale. */
          whatsNew?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the name, public link, feedback, or Apple silicon and Apple Vision availability settings of one TestFlight group. Each field given replaces the stored value; pass at least one. */
    "app_store_connect.update_beta_group": {
      input: {
        /**
         * App Store Connect identifier of the TestFlight group.
         * @minLength 1
         * @pattern \S
         */
        betaGroupId: string;
        /**
         * Group name shown in TestFlight.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** Enable or disable the public TestFlight invitation link. */
        publicLinkEnabled?: boolean;
        /** Enforce a maximum number of testers joining through the public link. */
        publicLinkLimitEnabled?: boolean;
        /**
         * Maximum number of testers allowed to join through the public link, from 1 to 10,000.
         * @minimum 1
         * @maximum 10000
         */
        publicLinkLimit?: number;
        /** Let testers send feedback from TestFlight. */
        feedbackEnabled?: boolean;
        /** Offer the group's iOS builds to Apple silicon Macs. */
        iosBuildsAvailableForAppleSiliconMac?: boolean;
        /** Offer the group's iOS builds to Apple Vision Pro. */
        iosBuildsAvailableForAppleVision?: boolean;
      };
      output: {
        /** A TestFlight beta group. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaGroup: {
          /** App Store Connect identifier for the beta group. */
          id: string;
          /** Group name shown in TestFlight. */
          name?: string | null;
          /** When the group was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Whether the group is an internal group of team members. */
          isInternalGroup?: boolean | null;
          /** Whether the group automatically receives every new build. */
          hasAccessToAllBuilds?: boolean | null;
          /** Whether a public TestFlight link is enabled for the group. */
          publicLinkEnabled?: boolean | null;
          /** Identifier segment of the public TestFlight link. */
          publicLinkId?: string | null;
          /** Full public TestFlight invitation link. */
          publicLink?: string | null;
          /** Whether the public link enforces a tester limit. */
          publicLinkLimitEnabled?: boolean | null;
          /** Maximum number of testers who may join through the public link. */
          publicLinkLimit?: number | null;
          /** Whether testers can send feedback from TestFlight. */
          feedbackEnabled?: boolean | null;
          /** Whether iOS builds are offered to Apple silicon Macs. */
          iosBuildsAvailableForAppleSiliconMac?: boolean | null;
          /** Whether iOS builds are offered to Apple Vision Pro. */
          iosBuildsAvailableForAppleVision?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the license agreement text TestFlight testers of an app must accept. The identifier comes from get_beta_license_agreement. */
    "app_store_connect.update_beta_license_agreement": {
      input: {
        /**
         * App Store Connect identifier of the beta license agreement.
         * @minLength 1
         * @pattern \S
         */
        betaLicenseAgreementId: string;
        /**
         * Full text of the beta license agreement.
         * @minLength 1
         * @pattern \S
         */
        agreementText: string;
      };
      output: {
        /** The license agreement TestFlight testers accept before installing the app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaLicenseAgreement: {
          /** App Store Connect identifier for the beta license agreement. */
          id: string;
          /** Full text of the beta license agreement. */
          agreementText?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the device family and OS version filters of existing recruitment criteria. The previous filters are overwritten. */
    "app_store_connect.update_beta_recruitment_criterion": {
      input: {
        /**
         * App Store Connect identifier of the recruitment criteria.
         * @minLength 1
         * @pattern \S
         */
        betaRecruitmentCriterionId: string;
        /**
         * Device families and OS version ranges a tester must match to join through the public link.
         * @minItems 1
         */
        deviceFamilyOsVersionFilters: Array<{
          /** Device family the tester must use. */
          deviceFamily: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION";
          /**
           * Lowest OS version the device may run, inclusive, such as 17.0.
           * @minLength 1
           * @pattern \S
           */
          minimumOsInclusive?: string;
          /**
           * Highest OS version the device may run, inclusive, such as 18.4.
           * @minLength 1
           * @pattern \S
           */
          maximumOsInclusive?: string;
        }>;
      };
      output: {
        /** Recruitment criteria that limit who can join a TestFlight group through its public link. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        betaRecruitmentCriterion: {
          /** App Store Connect identifier for the recruitment criteria. */
          id: string;
          /** When the criteria were last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** Device families and OS version ranges a tester must match. */
          deviceFamilyOsVersionFilters?: Array<{
            /** Device family the tester must use. */
            deviceFamily?: "IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION";
            /** Lowest OS version allowed, inclusive. */
            minimumOsInclusive?: string | null;
            /** Highest OS version allowed, inclusive. */
            maximumOsInclusive?: string | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the TestFlight flags of one build: expire it so testers can no longer install it, or record whether it uses non-exempt encryption. Expiring a build cannot be undone. Pass at least one field. */
    "app_store_connect.update_build": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /** Set to true to expire the build for TestFlight. */
        expired?: boolean;
        /** Whether the build uses encryption that is not exempt from export compliance. */
        usesNonExemptEncryption?: boolean;
      };
      output: {
        /** A build uploaded to App Store Connect, without its related records. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        build: {
          /** App Store Connect identifier for the build. */
          id: string;
          /** Build number, such as 42. */
          version?: string | null;
          /** When the build finished uploading, as an ISO 8601 timestamp. */
          uploadedDate?: string | null;
          /** When the build stops being installable by testers. */
          expirationDate?: string | null;
          /** Whether the build has expired for TestFlight. */
          expired?: boolean | null;
          /** Processing state of the uploaded build. */
          processingState?: "PROCESSING" | "FAILED" | "INVALID" | "VALID" | null;
          /** Distribution audience the build was uploaded for. */
          buildAudienceType?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE" | null;
          /** Minimum OS version the build supports. */
          minOsVersion?: string | null;
          /** Minimum macOS system version declared by the build. */
          lsMinimumSystemVersion?: string | null;
          /** Minimum macOS version App Store Connect computed for the build. */
          computedMinMacOsVersion?: string | null;
          /** Minimum visionOS version App Store Connect computed for the build. */
          computedMinVisionOsVersion?: string | null;
          /** Whether the build declares non-exempt encryption. */
          usesNonExemptEncryption?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Turn automatic tester notifications on or off for one build. The build beta detail identifier comes from get_build_beta_detail. */
    "app_store_connect.update_build_beta_detail": {
      input: {
        /**
         * App Store Connect identifier of the build beta detail.
         * @minLength 1
         * @pattern \S
         */
        buildBetaDetailId: string;
        /** Notify testers automatically when the build becomes available to them. */
        autoNotifyEnabled: boolean;
      };
      output: {
        /** TestFlight distribution details of one build. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        buildBetaDetail: {
          /** App Store Connect identifier for the build beta detail. */
          id: string;
          /** Whether testers are notified automatically when the build becomes available. */
          autoNotifyEnabled?: boolean | null;
          /** Where the build stands for internal TestFlight testers. */
          internalBuildState?: "PROCESSING" | "PROCESSING_EXCEPTION" | "MISSING_EXPORT_COMPLIANCE" | "READY_FOR_BETA_TESTING" | "IN_BETA_TESTING" | "EXPIRED" | "IN_EXPORT_COMPLIANCE_REVIEW" | null;
          /** Where the build stands for external TestFlight testers, including beta review. */
          externalBuildState?: "PROCESSING" | "PROCESSING_EXCEPTION" | "MISSING_EXPORT_COMPLIANCE" | "READY_FOR_BETA_TESTING" | "IN_BETA_TESTING" | "EXPIRED" | "IN_EXPORT_COMPLIANCE_REVIEW" | "READY_FOR_BETA_SUBMISSION" | "WAITING_FOR_BETA_REVIEW" | "IN_BETA_REVIEW" | "BETA_REJECTED" | "BETA_APPROVED" | "NOT_APPLICABLE" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set the "What to Test" notes a build shows testers in one locale. Updates the existing notes for that locale, or creates them when the locale has none yet. */
    "app_store_connect.update_build_test_notes": {
      input: {
        /**
         * App Store Connect identifier of the build.
         * @minLength 1
         * @pattern \S
         */
        buildId: string;
        /**
         * TestFlight locale the notes are written in, such as en-US.
         * @minLength 1
         * @pattern \S
         */
        locale: string;
        /**
         * Test notes shown to testers in that locale.
         * @minLength 1
         * @pattern \S
         */
        whatsNew: string;
      };
      output: {
        /** App Store Connect identifier for the beta build localization. */
        id: string;
        /** Locale the test notes belong to, such as en-US. */
        locale: string | null;
        /** Test notes shown to testers for this locale. */
        whatsNew: string | null;
        /** True when the locale had no notes yet and they were created. */
        created: boolean;
      };
    };
    /** Rename a bundle ID. The name is the only attribute App Store Connect lets you change; the identifier string and platform are fixed once registered. */
    "app_store_connect.update_bundle_id": {
      input: {
        /**
         * App Store Connect identifier of the bundle ID record (the opaque resource id, not the reverse-DNS identifier string).
         * @minLength 1
         * @pattern \S
         */
        bundleIdId: string;
        /**
         * New display name for the bundle ID.
         * @minLength 1
         * @pattern \S
         */
        name: string;
      };
      output: {
        /** A bundle ID (App ID) registered with the team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        bundleId: {
          /** App Store Connect identifier of the bundle ID record. */
          id: string;
          /** Display name given to the bundle ID when it was registered. */
          name?: string | null;
          /** Platform the bundle ID is registered for. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Reverse-DNS bundle identifier string, such as com.example.app, or a wildcard ending in *. */
          identifier?: string | null;
          /** Team ID prefix (seed ID) associated with the bundle ID. */
          seedId?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the configuration of a capability that is already enabled on a bundle ID, for example the iCloud version or data protection level. Provide at least one of capabilityType or settings; the settings given replace the current ones. */
    "app_store_connect.update_bundle_id_capability": {
      input: {
        /**
         * App Store Connect identifier of the bundle ID capability.
         * @minLength 1
         * @pattern \S
         */
        bundleIdCapabilityId: string;
        /** Capability type to set. */
        capabilityType?: "ICLOUD" | "IN_APP_PURCHASE" | "GAME_CENTER" | "PUSH_NOTIFICATIONS" | "WALLET" | "INTER_APP_AUDIO" | "MAPS" | "ASSOCIATED_DOMAINS" | "PERSONAL_VPN" | "APP_GROUPS" | "HEALTHKIT" | "HOMEKIT" | "WIRELESS_ACCESSORY_CONFIGURATION" | "APPLE_PAY" | "DATA_PROTECTION" | "SIRIKIT" | "NETWORK_EXTENSIONS" | "MULTIPATH" | "HOT_SPOT" | "NFC_TAG_READING" | "CLASSKIT" | "AUTOFILL_CREDENTIAL_PROVIDER" | "ACCESS_WIFI_INFORMATION" | "NETWORK_CUSTOM_PROTOCOL" | "COREMEDIA_HLS_LOW_LATENCY" | "SYSTEM_EXTENSION_INSTALL" | "USER_MANAGEMENT" | "APPLE_ID_AUTH";
        /**
         * Capability settings to configure, for capabilities such as ICLOUD, DATA_PROTECTION, and APPLE_ID_AUTH that expose options. Each setting names its key and the options to enable.
         * @minItems 1
         */
        settings?: Array<{
          /** Setting key. */
          key: "ICLOUD_VERSION" | "DATA_PROTECTION_PERMISSION_LEVEL" | "APPLE_ID_AUTH_APP_CONSENT";
          /**
           * Options of the setting to configure.
           * @minItems 1
           */
          options?: Array<{
            /** Option key. */
            key: "XCODE_5" | "XCODE_6" | "COMPLETE_PROTECTION" | "PROTECTED_UNLESS_OPEN" | "PROTECTED_UNTIL_FIRST_USER_AUTH" | "PRIMARY_APP_CONSENT";
            /** Whether to enable the option. */
            enabled?: boolean;
            [key: string]: unknown;
          }>;
          [key: string]: unknown;
        }>;
      };
      output: {
        /** A capability enabled on a bundle ID. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        bundleIdCapability: {
          /** App Store Connect identifier of the bundle ID capability. */
          id: string;
          /** Capability that is enabled. */
          capabilityType?: "ICLOUD" | "IN_APP_PURCHASE" | "GAME_CENTER" | "PUSH_NOTIFICATIONS" | "WALLET" | "INTER_APP_AUDIO" | "MAPS" | "ASSOCIATED_DOMAINS" | "PERSONAL_VPN" | "APP_GROUPS" | "HEALTHKIT" | "HOMEKIT" | "WIRELESS_ACCESSORY_CONFIGURATION" | "APPLE_PAY" | "DATA_PROTECTION" | "SIRIKIT" | "NETWORK_EXTENSIONS" | "MULTIPATH" | "HOT_SPOT" | "NFC_TAG_READING" | "CLASSKIT" | "AUTOFILL_CREDENTIAL_PROVIDER" | "ACCESS_WIFI_INFORMATION" | "NETWORK_CUSTOM_PROTOCOL" | "COREMEDIA_HLS_LOW_LATENCY" | "SYSTEM_EXTENSION_INSTALL" | "USER_MANAGEMENT" | "APPLE_ID_AUTH" | null;
          /** Configured settings of the capability. */
          settings?: Array<{
            /** Setting key. */
            key?: "ICLOUD_VERSION" | "DATA_PROTECTION_PERMISSION_LEVEL" | "APPLE_ID_AUTH_APP_CONSENT" | null;
            /** Setting name shown in the developer portal. */
            name?: string | null;
            /** Setting description shown in the developer portal. */
            description?: string | null;
            /** Whether the setting is enabled unless configured otherwise. */
            enabledByDefault?: boolean | null;
            /** Whether the setting is shown in the developer portal. */
            visible?: boolean | null;
            /** How many options of the setting may be enabled at once. */
            allowedInstances?: "ENTRY" | "SINGLE" | "MULTIPLE" | null;
            /** Minimum number of options that must be enabled. */
            minInstances?: number | null;
            /** Options available for the setting. */
            options?: Array<{
              /** Option key. */
              key?: "XCODE_5" | "XCODE_6" | "COMPLETE_PROTECTION" | "PROTECTED_UNLESS_OPEN" | "PROTECTED_UNTIL_FIRST_USER_AUTH" | "PRIMARY_APP_CONSENT" | null;
              /** Option name shown in the developer portal. */
              name?: string | null;
              /** Option description shown in the developer portal. */
              description?: string | null;
              /** Whether the option is enabled unless configured otherwise. */
              enabledByDefault?: boolean | null;
              /** Whether the option is currently enabled. */
              enabled?: boolean | null;
              /** Whether the option applies to wildcard bundle IDs. */
              supportsWildcard?: boolean | null;
              [key: string]: unknown;
            }> | null;
            [key: string]: unknown;
          }> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Activate or deactivate a certificate. App Store Connect only exposes the activation state, which applies to Apple Pay payment processing certificates; a deactivated certificate stops being used for payment processing until it is activated again. */
    "app_store_connect.update_certificate": {
      input: {
        /**
         * App Store Connect identifier of the certificate.
         * @minLength 1
         * @pattern \S
         */
        certificateId: string;
        /** Whether the certificate should be active. */
        activated: boolean;
      };
      output: {
        /** A signing certificate issued by Apple. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        certificate: {
          /** App Store Connect identifier of the certificate. */
          id: string;
          /** Certificate name. */
          name?: string | null;
          /** Kind of certificate. */
          certificateType?: "APPLE_PAY" | "APPLE_PAY_MERCHANT_IDENTITY" | "APPLE_PAY_PSP_IDENTITY" | "APPLE_PAY_RSA" | "DEVELOPER_ID_KEXT" | "DEVELOPER_ID_KEXT_G2" | "DEVELOPER_ID_APPLICATION" | "DEVELOPER_ID_APPLICATION_G2" | "DEVELOPMENT" | "DISTRIBUTION" | "IDENTITY_ACCESS" | "IOS_DEVELOPMENT" | "IOS_DISTRIBUTION" | "MAC_APP_DISTRIBUTION" | "MAC_INSTALLER_DISTRIBUTION" | "MAC_APP_DEVELOPMENT" | "PASS_TYPE_ID" | "PASS_TYPE_ID_WITH_NFC" | null;
          /** Certificate display name shown in the developer portal. */
          displayName?: string | null;
          /** Serial number of the certificate. */
          serialNumber?: string | null;
          /** Platform the certificate applies to. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** When the certificate expires, as an ISO 8601 timestamp. */
          expirationDate?: string | null;
          /** Base64-encoded DER certificate. Decode it to a .cer file to install it in a keychain. */
          certificateContent?: string | null;
          /** Whether the certificate is activated. Only Apple Pay payment processing certificates carry an activation state. */
          activated?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change an Xcode Cloud workflow. Every field given replaces the current value; a start condition or the actions array is replaced as a whole, so pass the complete new object. Give at least one field to change. */
    "app_store_connect.update_ci_workflow": {
      input: {
        /**
         * App Store Connect identifier of the workflow.
         * @minLength 1
         * @pattern \S
         */
        ciWorkflowId: string;
        /**
         * Workflow name shown in App Store Connect and Xcode.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** Description shown under the workflow name. */
        description?: string;
        /** Start a build when a matching branch changes. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchstartcondition */
        branchStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
          filesAndFoldersRule?: {
            /** Whether matching files start a build or prevent one. */
            mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
            /** Files and folders the rule matches. */
            matchers?: Array<{
              /** Directory path relative to the repository root. */
              directory?: string;
              /** File extension without the leading dot. */
              fileExtension?: string;
              /** Exact file name. */
              fileName?: string;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Cancel builds that are still running for the same source when a newer change arrives. */
          autoCancel?: boolean;
          [key: string]: unknown;
        };
        /** Start a build when a matching tag is created. See https://developer.apple.com/documentation/appstoreconnectapi/citagstartcondition */
        tagStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
          filesAndFoldersRule?: {
            /** Whether matching files start a build or prevent one. */
            mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
            /** Files and folders the rule matches. */
            matchers?: Array<{
              /** Directory path relative to the repository root. */
              directory?: string;
              /** File extension without the leading dot. */
              fileExtension?: string;
              /** Exact file name. */
              fileName?: string;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Cancel builds that are still running for the same source when a newer change arrives. */
          autoCancel?: boolean;
          [key: string]: unknown;
        };
        /** Start a build when a matching pull request is opened or updated. See https://developer.apple.com/documentation/appstoreconnectapi/cipullrequeststartcondition */
        pullRequestStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          destination?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
          filesAndFoldersRule?: {
            /** Whether matching files start a build or prevent one. */
            mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
            /** Files and folders the rule matches. */
            matchers?: Array<{
              /** Directory path relative to the repository root. */
              directory?: string;
              /** File extension without the leading dot. */
              fileExtension?: string;
              /** Exact file name. */
              fileName?: string;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Cancel builds that are still running for the same source when a newer change arrives. */
          autoCancel?: boolean;
          [key: string]: unknown;
        };
        /** Start builds on a schedule. See https://developer.apple.com/documentation/appstoreconnectapi/cischeduledstartcondition */
        scheduledStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** When the schedule fires. */
          schedule?: {
            /** How often the build starts. */
            frequency?: "WEEKLY" | "DAILY" | "HOURLY";
            /** Days of the week for a WEEKLY schedule. */
            days?: Array<"SUNDAY" | "MONDAY" | "TUESDAY" | "WEDNESDAY" | "THURSDAY" | "FRIDAY" | "SATURDAY">;
            /** Hour of the day the build starts. */
            hour?: number;
            /** Minute of the hour the build starts. */
            minute?: number;
            /** IANA time zone name, such as America/Los_Angeles. */
            timezone?: string;
            [key: string]: unknown;
          };
          [key: string]: unknown;
        };
        /** Branches a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualbranchstartcondition */
        manualBranchStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          [key: string]: unknown;
        };
        /** Tags a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualtagstartcondition */
        manualTagStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          [key: string]: unknown;
        };
        /** Pull requests a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualpullrequeststartcondition */
        manualPullRequestStartCondition?: {
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          source?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
          destination?: {
            /** Match every branch or tag when true. When false, only the listed patterns match. */
            isAllMatch?: boolean;
            /** Name patterns that match when isAllMatch is false. */
            patterns?: Array<{
              /** Branch or tag name, or a name prefix when isPrefix is true. */
              pattern?: string;
              /** Treat the pattern as a prefix instead of an exact name. */
              isPrefix?: boolean;
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          [key: string]: unknown;
        };
        /**
         * Actions the workflow runs, in order.
         * @minItems 1
         */
        actions?: Array<{
          /** Action name shown in Xcode Cloud. */
          name?: string;
          /** What the action does. */
          actionType?: "BUILD" | "ANALYZE" | "TEST" | "ARCHIVE";
          /** Device or simulator family the action targets. */
          destination?: "ANY_IOS_DEVICE" | "ANY_IOS_SIMULATOR" | "ANY_TVOS_DEVICE" | "ANY_TVOS_SIMULATOR" | "ANY_WATCHOS_DEVICE" | "ANY_WATCHOS_SIMULATOR" | "ANY_MAC" | "ANY_MAC_CATALYST" | "ANY_VISIONOS_DEVICE" | "ANY_VISIONOS_SIMULATOR";
          /** Who may install the build an ARCHIVE action produces. */
          buildDistributionAudience?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE";
          /** Test plan configuration for a TEST action. */
          testConfiguration?: {
            /** Use the scheme test settings or a specific test plan. */
            kind?: "USE_SCHEME_SETTINGS" | "SPECIFIC_TEST_PLANS";
            /** Name of the test plan to run when kind is SPECIFIC_TEST_PLANS. */
            testPlanName?: string;
            /** Simulators and Macs the tests run on. */
            testDestinations?: Array<{
              /** Device type name, such as iPhone 15. */
              deviceTypeName?: string;
              /** Device type identifier. */
              deviceTypeIdentifier?: string;
              /** Runtime name, such as iOS 17.4. */
              runtimeName?: string;
              /** Runtime identifier. */
              runtimeIdentifier?: string;
              /** Whether the destination is a simulator or a Mac. */
              kind?: "SIMULATOR" | "MAC";
              [key: string]: unknown;
            }>;
            [key: string]: unknown;
          };
          /** Xcode scheme the action builds. */
          scheme?: string;
          /** Platform the action builds for. */
          platform?: "MACOS" | "IOS" | "TVOS" | "WATCHOS" | "VISIONOS";
          /** Whether the whole build run fails when this action fails. */
          isRequiredToPass?: boolean;
          [key: string]: unknown;
        }>;
        /** Enable the workflow so its start conditions can start builds. */
        isEnabled?: boolean;
        /** Lock the workflow so it cannot be edited in Xcode. */
        isLockedForEditing?: boolean;
        /** Start every build from a clean environment without cached derived data. */
        clean?: boolean;
        /**
         * Path of the Xcode project, workspace, or Swift package the workflow builds, relative to the repository root.
         * @minLength 1
         * @pattern \S
         */
        containerFilePath?: string;
        /**
         * App Store Connect identifier of the Xcode version to switch the workflow to.
         * @minLength 1
         * @pattern \S
         */
        ciXcodeVersionId?: string;
        /**
         * App Store Connect identifier of the macOS version to switch the workflow to.
         * @minLength 1
         * @pattern \S
         */
        ciMacOsVersionId?: string;
      };
      output: {
        /** An Xcode Cloud workflow. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        ciWorkflow: {
          /** App Store Connect identifier for the workflow. */
          id: string;
          /** Workflow name shown in App Store Connect and Xcode. */
          name?: string | null;
          /** Description shown under the workflow name. */
          description?: string | null;
          /** Start a build when a matching branch changes. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchstartcondition */
          branchStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start a build when a matching tag is created. See https://developer.apple.com/documentation/appstoreconnectapi/citagstartcondition */
          tagStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start a build when a matching pull request is opened or updated. See https://developer.apple.com/documentation/appstoreconnectapi/cipullrequeststartcondition */
          pullRequestStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            destination?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Files and folders rule that narrows which changes start a build. See https://developer.apple.com/documentation/appstoreconnectapi/cifilesandfoldersrule */
            filesAndFoldersRule?: {
              /** Whether matching files start a build or prevent one. */
              mode?: "START_IF_ANY_FILE_MATCHES" | "DO_NOT_START_IF_ALL_FILES_MATCH";
              /** Files and folders the rule matches. */
              matchers?: Array<{
                /** Directory path relative to the repository root. */
                directory?: string;
                /** File extension without the leading dot. */
                fileExtension?: string;
                /** Exact file name. */
                fileName?: string;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Cancel builds that are still running for the same source when a newer change arrives. */
            autoCancel?: boolean;
            [key: string]: unknown;
          } | null;
          /** Start builds on a schedule. See https://developer.apple.com/documentation/appstoreconnectapi/cischeduledstartcondition */
          scheduledStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** When the schedule fires. */
            schedule?: {
              /** How often the build starts. */
              frequency?: "WEEKLY" | "DAILY" | "HOURLY";
              /** Days of the week for a WEEKLY schedule. */
              days?: Array<"SUNDAY" | "MONDAY" | "TUESDAY" | "WEDNESDAY" | "THURSDAY" | "FRIDAY" | "SATURDAY">;
              /** Hour of the day the build starts. */
              hour?: number;
              /** Minute of the hour the build starts. */
              minute?: number;
              /** IANA time zone name, such as America/Los_Angeles. */
              timezone?: string;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Branches a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualbranchstartcondition */
          manualBranchStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Tags a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualtagstartcondition */
          manualTagStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Pull requests a build may be started from manually. See https://developer.apple.com/documentation/appstoreconnectapi/cimanualpullrequeststartcondition */
          manualPullRequestStartCondition?: {
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            source?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Branch or tag names the condition applies to. See https://developer.apple.com/documentation/appstoreconnectapi/cibranchpatterns */
            destination?: {
              /** Match every branch or tag when true. When false, only the listed patterns match. */
              isAllMatch?: boolean;
              /** Name patterns that match when isAllMatch is false. */
              patterns?: Array<{
                /** Branch or tag name, or a name prefix when isPrefix is true. */
                pattern?: string;
                /** Treat the pattern as a prefix instead of an exact name. */
                isPrefix?: boolean;
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            [key: string]: unknown;
          } | null;
          /** Actions the workflow runs, in order. */
          actions?: Array<{
            /** Action name shown in Xcode Cloud. */
            name?: string;
            /** What the action does. */
            actionType?: "BUILD" | "ANALYZE" | "TEST" | "ARCHIVE";
            /** Device or simulator family the action targets. */
            destination?: "ANY_IOS_DEVICE" | "ANY_IOS_SIMULATOR" | "ANY_TVOS_DEVICE" | "ANY_TVOS_SIMULATOR" | "ANY_WATCHOS_DEVICE" | "ANY_WATCHOS_SIMULATOR" | "ANY_MAC" | "ANY_MAC_CATALYST" | "ANY_VISIONOS_DEVICE" | "ANY_VISIONOS_SIMULATOR";
            /** Who may install the build an ARCHIVE action produces. */
            buildDistributionAudience?: "INTERNAL_ONLY" | "APP_STORE_ELIGIBLE";
            /** Test plan configuration for a TEST action. */
            testConfiguration?: {
              /** Use the scheme test settings or a specific test plan. */
              kind?: "USE_SCHEME_SETTINGS" | "SPECIFIC_TEST_PLANS";
              /** Name of the test plan to run when kind is SPECIFIC_TEST_PLANS. */
              testPlanName?: string;
              /** Simulators and Macs the tests run on. */
              testDestinations?: Array<{
                /** Device type name, such as iPhone 15. */
                deviceTypeName?: string;
                /** Device type identifier. */
                deviceTypeIdentifier?: string;
                /** Runtime name, such as iOS 17.4. */
                runtimeName?: string;
                /** Runtime identifier. */
                runtimeIdentifier?: string;
                /** Whether the destination is a simulator or a Mac. */
                kind?: "SIMULATOR" | "MAC";
                [key: string]: unknown;
              }>;
              [key: string]: unknown;
            };
            /** Xcode scheme the action builds. */
            scheme?: string;
            /** Platform the action builds for. */
            platform?: "MACOS" | "IOS" | "TVOS" | "WATCHOS" | "VISIONOS";
            /** Whether the whole build run fails when this action fails. */
            isRequiredToPass?: boolean;
            [key: string]: unknown;
          }> | null;
          /** Whether the workflow is enabled and can start builds. */
          isEnabled?: boolean | null;
          /** Whether the workflow is locked against editing in Xcode. */
          isLockedForEditing?: boolean | null;
          /** Whether every build starts from a clean environment without cached derived data. */
          clean?: boolean | null;
          /** Path of the Xcode project, workspace, or Swift package the workflow builds, relative to the repository root. */
          containerFilePath?: string | null;
          /** When the workflow was last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** Identifier of the Xcode Cloud product the workflow belongs to. */
          ciProductId: string | null;
          /** Identifier of the repository the workflow builds from. */
          scmRepositoryId: string | null;
          /** Identifier of the Xcode version the workflow builds with. */
          ciXcodeVersionId: string | null;
          /** Identifier of the macOS version the workflow builds on. */
          ciMacOsVersionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Rename a registered device or change its status. Provide at least one of name or status. Disabling a device invalidates the development and ad hoc profiles that include it, while the device keeps counting toward the yearly device limit; re-enabling it keeps the same record. */
    "app_store_connect.update_device": {
      input: {
        /**
         * App Store Connect identifier of the device.
         * @minLength 1
         * @pattern \S
         */
        deviceId: string;
        /**
         * New name for the device.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** New status for the device. */
        status?: "ENABLED" | "DISABLED";
      };
      output: {
        /** A device registered with the team for development. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        device: {
          /** App Store Connect identifier of the device. */
          id: string;
          /** Device name given when it was registered. */
          name?: string | null;
          /** Platform the device runs. */
          platform?: "IOS" | "MAC_OS" | "UNIVERSAL" | null;
          /** Unique device identifier (UDID) of the device. */
          udid?: string | null;
          /** Hardware class of the device. */
          deviceClass?: "APPLE_VISION_PRO" | "APPLE_WATCH" | "IPAD" | "IPHONE" | "IPOD" | "APPLE_TV" | "MAC" | null;
          /** Whether the device is enabled for development. Disabled devices are excluded from new profiles. */
          status?: "ENABLED" | "DISABLED" | null;
          /** Device model name. */
          model?: string | null;
          /** When the device was registered, as an ISO 8601 timestamp. */
          addedDate?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the text of a custom end user license agreement or the full set of territories it applies in. Pass at least one of agreementText or territoryIds. */
    "app_store_connect.update_end_user_license_agreement": {
      input: {
        /**
         * App Store Connect identifier of the license agreement, from get_end_user_license_agreement.
         * @minLength 1
         * @pattern \S
         */
        endUserLicenseAgreementId: string;
        /**
         * New full text of the license agreement.
         * @minLength 1
         * @pattern \S
         */
        agreementText?: string;
        /**
         * Complete new set of territories the agreement applies in; territories left out are removed.
         * @minItems 1
         */
        territoryIds?: Array<string>;
      };
      output: {
        /** A custom end user license agreement that replaces Apple's standard EULA in some territories. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        endUserLicenseAgreement: {
          /** App Store Connect identifier for the end user license agreement. */
          id: string;
          /** Full text of the custom license agreement. */
          agreementText?: string | null;
          /** ISO 3166-1 alpha-3 territories the custom agreement applies in. */
          territoryIds: Array<string> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the configuration of a Game Center achievement, or archive it so it is no longer offered to players. Overwrites the given fields; pass at least one. The vendor identifier cannot be changed after the achievement was created. */
    "app_store_connect.update_game_center_achievement": {
      input: {
        /**
         * App Store Connect identifier of the achievement.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementId: string;
        /**
         * New internal achievement name, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        referenceName?: string | null;
        /** New points value, from 0 to 100, or null to clear it. */
        points?: number | null;
        /** Whether players can see the achievement before they earn it. */
        showBeforeEarned?: boolean;
        /** Whether players can earn the achievement more than once. */
        repeatable?: boolean;
        /** Archive the achievement when true so it is no longer offered to players, or unarchive it when false. */
        archived?: boolean;
        /** New key-value properties the achievement contributes to a game activity, or null to clear them. */
        activityProperties?: Record<string, unknown> | null;
      };
      output: {
        /** A Game Center achievement. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAchievement: {
          /** App Store Connect identifier for the achievement. */
          id: string;
          /** Internal achievement name shown in App Store Connect. */
          referenceName?: string | null;
          /** Achievement identifier the game reports scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** Points the achievement is worth, from 0 to 100. */
          points?: number | null;
          /** Whether players can see the achievement before they earn it. */
          showBeforeEarned?: boolean | null;
          /** Whether players can earn the achievement more than once. */
          repeatable?: boolean | null;
          /** Whether the achievement is archived and no longer offered to players. */
          archived?: boolean | null;
          /** Key-value properties the achievement contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the name or the descriptions of a Game Center achievement localization, or clear one of them with null. Overwrites the given fields; pass at least one. The locale cannot be changed; delete the localization and create it again instead. */
    "app_store_connect.update_game_center_achievement_localization": {
      input: {
        /**
         * App Store Connect identifier of the achievement localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAchievementLocalizationId: string;
        /**
         * New achievement name shown to players in this locale, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        name?: string | null;
        /**
         * New description shown before the player earns the achievement, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        beforeEarnedDescription?: string | null;
        /**
         * New description shown after the player earns the achievement, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        afterEarnedDescription?: string | null;
      };
      output: {
        /** The achievement text in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAchievementLocalization: {
          /** App Store Connect identifier for the achievement localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Achievement name shown to players in this locale. */
          name?: string | null;
          /** Description shown before the player earns the achievement. */
          beforeEarnedDescription?: string | null;
          /** Description shown after the player earns the achievement. */
          afterEarnedDescription?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the configuration of a Game Center activity or archive it. Overwrites the given fields and passing null clears one; vendorIdentifier cannot change once the activity exists. Pass at least one field. */
    "app_store_connect.update_game_center_activity": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityId: string;
        /**
         * New internal activity name, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        referenceName?: string | null;
        /** New play style, or null to clear it. */
        playStyle?: "ASYNCHRONOUS" | "SYNCHRONOUS" | null;
        /** Fewest players an activity session needs, or null to clear the limit. */
        minimumPlayersCount?: number | null;
        /** Most players an activity session accepts, or null to clear the limit. */
        maximumPlayersCount?: number | null;
        /** Whether players can join the activity with a party code. */
        supportsPartyCode?: boolean;
        /** Archive the activity when true so it is no longer offered, or bring it back when false. */
        archived?: boolean;
        /** New key-value properties the game reads when it starts the activity, or null to clear them. */
        properties?: Record<string, unknown> | null;
      };
      output: {
        /** A Game Center activity: a play session other players can be invited into, with its own achievements and leaderboards. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivity: {
          /** App Store Connect identifier for the activity. */
          id: string;
          /** Internal activity name shown in App Store Connect. */
          referenceName?: string | null;
          /** Activity identifier the game starts sessions with, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** Whether players take turns or play at the same time. */
          playStyle?: "ASYNCHRONOUS" | "SYNCHRONOUS" | null;
          /** Fewest players an activity session needs. */
          minimumPlayersCount?: number | null;
          /** Most players an activity session accepts. */
          maximumPlayersCount?: number | null;
          /** Whether players can join the activity with a party code. */
          supportsPartyCode?: boolean | null;
          /** Whether the activity is archived and no longer offered. */
          archived?: boolean | null;
          /** Key-value properties the game reads when it starts the activity. */
          properties?: Record<string, unknown> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the name or the description of a Game Center activity localization, or clear one with null. The locale cannot change; delete the localization and create it again instead. Pass at least one field. */
    "app_store_connect.update_game_center_activity_localization": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityLocalizationId: string;
        /**
         * New activity name shown to players in this locale, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        name?: string | null;
        /**
         * New activity description shown to players in this locale, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        description?: string | null;
      };
      output: {
        /** The activity text in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivityLocalization: {
          /** App Store Connect identifier for the activity localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Activity name shown to players in this locale. */
          name?: string | null;
          /** Activity description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set or clear the fallback URL of a Game Center activity version. Overwrites the existing URL; pass null to remove it. */
    "app_store_connect.update_game_center_activity_version": {
      input: {
        /**
         * App Store Connect identifier of the Game Center activity version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterActivityVersionId: string;
        /**
         * New web page opened when a player without the game follows an activity invitation, or null to remove it.
         * @format uri
         */
        fallbackUrl: string | null;
      };
      output: {
        /** One version of a Game Center activity. A new version carries the localizations and the default image through App Review. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterActivityVersion: {
          /** App Store Connect identifier for the activity version. */
          id: string;
          /** Version number App Store Connect assigned to this version. */
          version?: number | null;
          /** Review and release state of the version. */
          state?: "PREPARE_FOR_SUBMISSION" | "READY_FOR_REVIEW" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_REJECTED" | "REJECTED" | "ACCEPTED" | "PENDING_RELEASE" | "LIVE" | "REPLACED_WITH_NEW_VERSION" | null;
          /** Web page opened when a player without the game follows an activity invitation. */
          fallbackUrl?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Enable or disable Game Center for the App Store version this record belongs to. Disabling it hides Game Center from players on that release without deleting its achievements or leaderboards. */
    "app_store_connect.update_game_center_app_version": {
      input: {
        /**
         * App Store Connect identifier of the Game Center app version.
         * @minLength 1
         * @pattern \S
         */
        gameCenterAppVersionId: string;
        /** Enable Game Center for this App Store version when true, or disable it when false. */
        enabled?: boolean;
      };
      output: {
        /** The Game Center enablement of one App Store version, plus the earlier versions it stays multiplayer compatible with. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterAppVersion: {
          /** App Store Connect identifier for the Game Center app version. */
          id: string;
          /** Whether Game Center is enabled for this App Store version. */
          enabled?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Rename a Game Center challenge, archive it, change whether players may repeat it, or score it on another leaderboard. Overwrites the given fields; pass at least one. The vendor identifier and the challenge type cannot be changed after creation. */
    "app_store_connect.update_game_center_challenge": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeId: string;
        /**
         * New internal challenge name shown in App Store Connect.
         * @minLength 1
         * @pattern \S
         */
        referenceName?: string;
        /** Archive the challenge when true so it is no longer offered to players, or bring it back when false. */
        archived?: boolean;
        /** Whether a player can take the challenge more than once. */
        repeatable?: boolean;
        /**
         * App Store Connect identifier of the leaderboard the challenge is scored on, replacing the current one.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId?: string;
      };
      output: {
        /** A Game Center challenge: an invitation to beat a score on one leaderboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterChallenge: {
          /** App Store Connect identifier for the challenge. */
          id: string;
          /** Internal challenge name shown in App Store Connect. */
          referenceName?: string | null;
          /** Challenge identifier the game refers to, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** What the challenge is based on. */
          challengeType?: "LEADERBOARD" | null;
          /** Whether a player can take the challenge more than once. */
          repeatable?: boolean | null;
          /** Whether the challenge is archived and no longer offered. */
          archived?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the name or the description of a Game Center challenge localization, or clear one of them with null. Overwrites the given fields; pass at least one. The locale cannot be changed, so replace the localization to move the text to another locale. */
    "app_store_connect.update_game_center_challenge_localization": {
      input: {
        /**
         * App Store Connect identifier of the Game Center challenge localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterChallengeLocalizationId: string;
        /**
         * New challenge name shown to players in this locale, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        name?: string | null;
        /**
         * New challenge description shown to players in this locale, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        description?: string | null;
      };
      output: {
        /** The challenge text in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterChallengeLocalization: {
          /** App Store Connect identifier for the challenge localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Challenge name shown to players in this locale. */
          name?: string | null;
          /** Challenge description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Move a Game Center detail into a group, or change the leaderboards shown by default in the Game Center dashboard. Overwrites the relationships you pass; pass at least one. App Store Connect does not echo the linked identifiers on this call, so read them back with get_game_center_detail. */
    "app_store_connect.update_game_center_detail": {
      input: {
        /**
         * App Store Connect identifier of the Game Center detail.
         * @minLength 1
         * @pattern \S
         */
        gameCenterDetailId: string;
        /**
         * Game Center group the app joins, so that it shares achievements and leaderboards with the other apps of the group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId?: string;
        /**
         * Leaderboard shown by default in the Game Center dashboard of the app.
         * @minLength 1
         * @pattern \S
         */
        defaultLeaderboardId?: string;
        /**
         * Leaderboard shown by default when the app is played as part of its Game Center group.
         * @minLength 1
         * @pattern \S
         */
        defaultGroupLeaderboardId?: string;
      };
      output: {
        /** The Game Center configuration of one app. It owns the app's achievements, leaderboards, leaderboard sets, activities and challenges, and links the app to a Game Center group when it shares them with other apps. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterDetail: {
          /** App Store Connect identifier for the Game Center detail. */
          id: string;
          /** Whether the app is distributed through Apple Arcade. */
          arcadeEnabled?: boolean | null;
          /** Game Center group the app belongs to, or null when the app is not grouped or App Store Connect did not return the relationship. */
          gameCenterGroupId: string | null;
          /** Leaderboard shown by default in the Game Center dashboard, or null when there is none. */
          defaultLeaderboardId: string | null;
          /** Group leaderboard shown by default when the app belongs to a group, or null when there is none. */
          defaultGroupLeaderboardId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Rename a Game Center group. Overwrites the reference name; pass null to clear it. The apps and the shared records of the group are not touched. */
    "app_store_connect.update_game_center_group": {
      input: {
        /**
         * App Store Connect identifier of the Game Center group.
         * @minLength 1
         * @pattern \S
         */
        gameCenterGroupId: string;
        /**
         * New internal group name, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        referenceName?: string | null;
      };
      output: {
        /** A Game Center group. Apps in a group share achievements, leaderboards and leaderboard sets. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterGroup: {
          /** App Store Connect identifier for the Game Center group. */
          id: string;
          /** Internal group name shown in App Store Connect. */
          referenceName?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the configuration of a Game Center leaderboard or archive it. Overwrites the given fields and clears the ones passed as null; pass at least one. The vendorIdentifier cannot be changed. Archiving retires the leaderboard so it stops accepting scores. */
    "app_store_connect.update_game_center_leaderboard": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardId: string;
        /**
         * New internal leaderboard name, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        referenceName?: string | null;
        /** New score formatter used unless a localization overrides it, or null to clear it. */
        defaultFormatter?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
        /** New rule for which submitted score is kept for a player, or null to clear it. */
        submissionType?: "BEST_SCORE" | "MOST_RECENT_SCORE" | null;
        /** New sort order deciding whether a higher or a lower score ranks first, or null to clear it. */
        scoreSortType?: "ASC" | "DESC" | null;
        /**
         * New lowest accepted score as a decimal string, or null to accept any low score.
         * @minLength 1
         * @pattern \S
         */
        scoreRangeStart?: string | null;
        /**
         * New highest accepted score as a decimal string, or null to accept any high score.
         * @minLength 1
         * @pattern \S
         */
        scoreRangeEnd?: string | null;
        /**
         * New start of the first recurring run as an ISO 8601 timestamp, or null to clear it.
         * @format date-time
         */
        recurrenceStartDate?: string | null;
        /**
         * New length of each recurring run as an ISO 8601 duration such as P1D, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        recurrenceDuration?: string | null;
        /**
         * New ICS recurrence rule scheduling the recurring runs, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        recurrenceRule?: string | null;
        /** Pass true to archive the leaderboard so it no longer accepts scores. */
        archived?: boolean;
        /** New key-value properties the leaderboard contributes to a game activity, or null to clear them. */
        activityProperties?: Record<string, unknown> | null;
        /** New visibility of the leaderboard for players, or null to clear it. */
        visibility?: "SHOW_FOR_ALL" | "HIDE_FOR_ALL" | null;
      };
      output: {
        /** A Game Center leaderboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboard: {
          /** App Store Connect identifier for the leaderboard. */
          id: string;
          /** Internal leaderboard name shown in App Store Connect. */
          referenceName?: string | null;
          /** Leaderboard identifier the game submits scores against, unique within the app or group. */
          vendorIdentifier?: string | null;
          /** How scores are formatted unless a localization overrides it. */
          defaultFormatter?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
          /** Which submitted score is kept for a player. */
          submissionType?: "BEST_SCORE" | "MOST_RECENT_SCORE" | null;
          /** Whether a higher or a lower score ranks first. */
          scoreSortType?: "ASC" | "DESC" | null;
          /** Lowest accepted score, as a decimal string. */
          scoreRangeStart?: string | null;
          /** Highest accepted score, as a decimal string. */
          scoreRangeEnd?: string | null;
          /** When the first recurring run of the leaderboard starts, as an ISO 8601 timestamp. */
          recurrenceStartDate?: string | null;
          /** How long each recurring run lasts, as an ISO 8601 duration such as P1D. */
          recurrenceDuration?: string | null;
          /** ICS recurrence rule that schedules the recurring runs. */
          recurrenceRule?: string | null;
          /** Whether the leaderboard is archived and no longer accepting scores. */
          archived?: boolean | null;
          /** Key-value properties the leaderboard contributes to a game activity. */
          activityProperties?: Record<string, unknown> | null;
          /** Whether the leaderboard is shown to players. */
          visibility?: "SHOW_FOR_ALL" | "HIDE_FOR_ALL" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the text or score formatting of a Game Center leaderboard localization. Overwrites the given fields and clears the ones passed as null; pass at least one. The locale cannot be changed. */
    "app_store_connect.update_game_center_leaderboard_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardLocalizationId: string;
        /**
         * New leaderboard name shown to players in this locale, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        name?: string | null;
        /** New score formatter for this locale, or null to fall back to the leaderboard default. */
        formatterOverride?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
        /**
         * New suffix appended to plural score values, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        formatterSuffix?: string | null;
        /**
         * New suffix appended to a score value of one, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        formatterSuffixSingular?: string | null;
        /**
         * New score description shown to players in this locale, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        description?: string | null;
      };
      output: {
        /** The leaderboard text and score formatting in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardLocalization: {
          /** App Store Connect identifier for the leaderboard localization. */
          id: string;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Leaderboard name shown to players in this locale. */
          name?: string | null;
          /** Score formatter used in this locale instead of the leaderboard default. */
          formatterOverride?: "INTEGER" | "DECIMAL_POINT_1_PLACE" | "DECIMAL_POINT_2_PLACE" | "DECIMAL_POINT_3_PLACE" | "ELAPSED_TIME_CENTISECOND" | "ELAPSED_TIME_MINUTE" | "ELAPSED_TIME_SECOND" | "MONEY_POUND_DECIMAL" | "MONEY_POUND" | "MONEY_DOLLAR_DECIMAL" | "MONEY_DOLLAR" | "MONEY_EURO_DECIMAL" | "MONEY_EURO" | "MONEY_FRANC_DECIMAL" | "MONEY_FRANC" | "MONEY_KRONER_DECIMAL" | "MONEY_KRONER" | "MONEY_YEN" | null;
          /** Suffix appended to plural score values, such as points. */
          formatterSuffix?: string | null;
          /** Suffix appended to a score value of one, such as point. */
          formatterSuffixSingular?: string | null;
          /** Score description shown to players in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Rename a Game Center leaderboard set, or clear its reference name with null. The vendor identifier is fixed once the set exists and cannot be changed. */
    "app_store_connect.update_game_center_leaderboard_set": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetId: string;
        /**
         * New internal set name, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string | null;
      };
      output: {
        /** A Game Center leaderboard set that groups related leaderboards under one entry in the Game Center dashboard. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSet: {
          /** App Store Connect identifier for the leaderboard set. */
          id: string;
          /** Internal set name shown in App Store Connect. */
          referenceName?: string | null;
          /** Set identifier the game refers to, unique within the app or group. */
          vendorIdentifier?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the set name of a Game Center leaderboard set localization, or clear it with null. The locale is fixed once the localization exists; delete and recreate it to change the locale. */
    "app_store_connect.update_game_center_leaderboard_set_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetLocalizationId: string;
        /**
         * New set name shown in this locale, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        name: string | null;
      };
      output: {
        /** The leaderboard set name in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSetLocalization: {
          /** App Store Connect identifier for the leaderboard set localization. */
          id: string;
          /** Locale the name is written in, such as en-US. */
          locale?: string | null;
          /** Set name shown to players in this locale. */
          name?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the name a leaderboard is shown under inside a leaderboard set, or clear it with null so the leaderboard falls back to its usual localized name. */
    "app_store_connect.update_game_center_leaderboard_set_member_localization": {
      input: {
        /**
         * App Store Connect identifier of the leaderboard set member localization.
         * @minLength 1
         * @pattern \S
         */
        gameCenterLeaderboardSetMemberLocalizationId: string;
        /**
         * New name the leaderboard is shown under inside the set, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        name: string | null;
      };
      output: {
        /** The name one leaderboard is shown under inside one leaderboard set, in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterLeaderboardSetMemberLocalization: {
          /** App Store Connect identifier for the leaderboard set member localization. */
          id: string;
          /** Locale the name is written in, such as en-US. */
          locale?: string | null;
          /** Name the leaderboard is shown under inside the set. */
          name?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Point a Game Center matchmaking queue at another rule set, change the rule set its experiment runs with, or replace the classic matchmaking bundle identifiers. Overwrites the given fields; pass at least one. The queue name cannot be changed after creation. */
    "app_store_connect.update_game_center_matchmaking_queue": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking queue.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingQueueId: string;
        /** New bundle identifiers whose classic matchmaking requests use this queue, or null to clear them. */
        classicMatchmakingBundleIds?: Array<string> | null;
        /**
         * App Store Connect identifier of the rule set the queue matches requests with from now on.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId?: string;
        /**
         * App Store Connect identifier of the rule set the experiment runs with from now on.
         * @minLength 1
         * @pattern \S
         */
        experimentRuleSetId?: string;
      };
      output: {
        /** A Game Center matchmaking queue. Requests that reach the queue are matched with the rule set attached to it. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingQueue: {
          /** App Store Connect identifier for the matchmaking queue. */
          id: string;
          /** Internal queue name shown in App Store Connect. */
          referenceName?: string | null;
          /** Bundle identifiers whose classic matchmaking requests use this queue. */
          classicMatchmakingBundleIds?: Array<string> | null;
          /** Rule set the queue matches requests with. */
          ruleSetId: string | null;
          /** Rule set a share of the queue traffic is matched with while an experiment runs, or null when no experiment is running. */
          experimentRuleSetId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the expression, the description or the weight of a Game Center matchmaking rule, or clear one of them with null. Overwrites the given fields; pass at least one. The rule name and its type cannot be changed. */
    "app_store_connect.update_game_center_matchmaking_rule": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleId: string;
        /**
         * New description of what the rule checks, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        description?: string | null;
        /**
         * New rule expression evaluated for every candidate match, or null to clear it.
         * @minLength 1
         * @pattern \S
         */
        expression?: string | null;
        /** New weight of the rule against the other rules of the same type, or null to clear it. */
        weight?: number | null;
      };
      output: {
        /** One rule of a Game Center matchmaking rule set. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingRule: {
          /** App Store Connect identifier for the rule. */
          id: string;
          /** Internal rule name shown in App Store Connect. */
          referenceName?: string | null;
          /** What the rule checks, for the team reading the rule set. */
          description?: string | null;
          /** How the rule result is used. */
          type?: "COMPATIBLE" | "DISTANCE" | "MATCH" | "TEAM" | null;
          /** Rule expression evaluated for every candidate match. */
          expression?: string | null;
          /** How much the rule counts against the other rules of the same type when Game Center scores a candidate match. */
          weight?: number | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the player limits of a Game Center matchmaking rule set, or clear a limit with null. Overwrites the given fields; pass at least one. The rule set name and its rule language version cannot be changed. */
    "app_store_connect.update_game_center_matchmaking_rule_set": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking rule set.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingRuleSetId: string;
        /** Fewest players a match created from this rule set may have, or null to clear the limit. */
        minPlayers?: number | null;
        /** Most players a match created from this rule set may have, or null to clear the limit. */
        maxPlayers?: number | null;
      };
      output: {
        /** A Game Center matchmaking rule set: the team layout, player limits and rules a queue matches requests with. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingRuleSet: {
          /** App Store Connect identifier for the rule set. */
          id: string;
          /** Internal rule set name shown in App Store Connect. */
          referenceName?: string | null;
          /** Version of the rule expression language the rules use. */
          ruleLanguageVersion?: number | null;
          /** Fewest players a match created from this rule set may have. */
          minPlayers?: number | null;
          /** Most players a match created from this rule set may have. */
          maxPlayers?: number | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the player limits of a Game Center matchmaking team, or clear a limit with null. Overwrites the given fields; pass at least one. The team name cannot be changed, because rule expressions refer to it. */
    "app_store_connect.update_game_center_matchmaking_team": {
      input: {
        /**
         * App Store Connect identifier of the matchmaking team.
         * @minLength 1
         * @pattern \S
         */
        gameCenterMatchmakingTeamId: string;
        /** Fewest players the team needs, or null to clear the limit. */
        minPlayers?: number | null;
        /** Most players the team accepts, or null to clear the limit. */
        maxPlayers?: number | null;
      };
      output: {
        /** One team of a Game Center matchmaking rule set. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        gameCenterMatchmakingTeam: {
          /** App Store Connect identifier for the team. */
          id: string;
          /** Internal team name the rule expressions refer to. */
          referenceName?: string | null;
          /** Fewest players the team needs. */
          minPlayers?: number | null;
          /** Most players the team accepts. */
          maxPlayers?: number | null;
          [key: string]: unknown;
        };
      };
    };
    /** Update the reference name, review note, or Family Sharing flag of an in-app purchase. Pass at least one field; each given field overwrites the current value. */
    "app_store_connect.update_in_app_purchase": {
      input: {
        /**
         * App Store Connect identifier of the in-app purchase.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseId: string;
        /**
         * New reference name, up to 64 characters.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * New note for App Review.
         * @minLength 1
         * @pattern \S
         */
        reviewNote?: string;
        /** Whether the purchase can be shared through Family Sharing. */
        familySharable?: boolean;
      };
      output: {
        /** An in-app purchase of an app: a consumable, non-consumable, or non-renewing subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        inAppPurchase: {
          /** App Store Connect identifier for the in-app purchase. */
          id: string;
          /** Reference name shown in App Store Connect. */
          name?: string | null;
          /** Product identifier the app passes to StoreKit to request the purchase. */
          productId?: string | null;
          /** Kind of in-app purchase. */
          inAppPurchaseType?: "CONSUMABLE" | "NON_CONSUMABLE" | "NON_RENEWING_SUBSCRIPTION" | null;
          /** Where the in-app purchase stands in metadata preparation, review, and sale. */
          state?: "MISSING_METADATA" | "WAITING_FOR_UPLOAD" | "PROCESSING_CONTENT" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED" | null;
          /** Note shown to App Review alongside the purchase. */
          reviewNote?: string | null;
          /** Whether the purchase can be shared through Family Sharing. */
          familySharable?: boolean | null;
          /** Whether Apple hosts the downloadable content of the purchase. */
          contentHosting?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Overwrite the display name or description of an in-app purchase localization. Pass at least one field; the locale itself cannot be changed. */
    "app_store_connect.update_in_app_purchase_localization": {
      input: {
        /**
         * App Store Connect identifier of the localization.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseLocalizationId: string;
        /**
         * New display name, up to 30 characters.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * New description, up to 45 characters.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** A localized display name and description of an in-app purchase. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        localization: {
          /** App Store Connect identifier for the localization. */
          id: string;
          /** Display name customers see in this locale. */
          name?: string | null;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Description customers see in this locale. */
          description?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Activate or deactivate an in-app purchase offer code. Deactivating stops every custom and one-time-use code of the offer from being redeemed. */
    "app_store_connect.update_in_app_purchase_offer_code": {
      input: {
        /**
         * App Store Connect identifier of the offer code.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseOfferCodeId: string;
        /** Whether the offer can be redeemed. */
        active: boolean;
      };
      output: {
        /** An offer code configuration that lets customers redeem an in-app purchase at a discounted price. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        offerCode: {
          /** App Store Connect identifier for the offer code. */
          id: string;
          /** Reference name of the offer. */
          name?: string | null;
          /** Customer groups allowed to redeem the offer. */
          customerEligibilities?: Array<"NON_SPENDER" | "ACTIVE_SPENDER" | "CHURNED_SPENDER"> | null;
          /** Number of one-time-use codes generated for the production environment. */
          productionCodeCount?: number | null;
          /** Number of one-time-use codes generated for the sandbox environment. */
          sandboxCodeCount?: number | null;
          /** Whether the offer can currently be redeemed. */
          active?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Activate or deactivate one custom code of an in-app purchase offer without touching the other codes of the offer. */
    "app_store_connect.update_in_app_purchase_offer_code_custom_code": {
      input: {
        /**
         * App Store Connect identifier of the custom code.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseOfferCodeCustomCodeId: string;
        /** Whether the custom code can be redeemed. */
        active: boolean;
      };
      output: {
        /** A custom offer code that many customers can redeem by typing the same text. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        customCode: {
          /** App Store Connect identifier for the custom code. */
          id: string;
          /** The code customers enter, such as SPRING2026. */
          customCode?: string | null;
          /** How many redemptions the custom code allows. */
          numberOfCodes?: number | null;
          /** When the code was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the code can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the code can currently be redeemed. */
          active?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Activate or deactivate one batch of one-time-use codes of an in-app purchase offer. */
    "app_store_connect.update_in_app_purchase_offer_code_one_time_use_code": {
      input: {
        /**
         * App Store Connect identifier of the one-time-use code batch.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseOfferCodeOneTimeUseCodeId: string;
        /** Whether the codes in the batch can be redeemed. */
        active: boolean;
      };
      output: {
        /** A batch of one-time-use offer codes; the code values themselves are downloaded from App Store Connect as a CSV file. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        oneTimeUseCode: {
          /** App Store Connect identifier for the batch. */
          id: string;
          /** Number of codes in the batch. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the codes can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the codes can currently be redeemed. */
          active?: boolean | null;
          /** Environment the codes redeem in. */
          environment?: "PRODUCTION" | "SANDBOX" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Overwrite the display name or description of an in-app purchase version localization. Pass at least one field. */
    "app_store_connect.update_in_app_purchase_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the version localization.
         * @minLength 1
         * @pattern \S
         */
        inAppPurchaseVersionLocalizationId: string;
        /**
         * New display name, up to 30 characters.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * New description, up to 45 characters.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** A localized display name and description attached to one in-app purchase version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        localization: {
          /** App Store Connect identifier for the localization. */
          id: string;
          /** Display name customers see in this locale. */
          name?: string | null;
          /** Locale the text is written in, such as en-US. */
          locale?: string | null;
          /** Description customers see in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Replace the sitemap catalog URL of an existing marketplace search detail. */
    "app_store_connect.update_marketplace_search_detail": {
      input: {
        /**
         * App Store Connect identifier of the marketplace search detail.
         * @minLength 1
         * @pattern \S
         */
        marketplaceSearchDetailId: string;
        /**
         * New HTTPS URL of the marketplace sitemap.
         * @format uri
         */
        catalogUrl: string;
      };
      output: {
        /** The search catalog configuration of an alternative marketplace app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        marketplaceSearchDetail: {
          /** App Store Connect identifier of the marketplace search detail. */
          id: string;
          /** URL of the marketplace sitemap Apple crawls to include its apps in Spotlight search. */
          catalogUrl?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Rename an Apple Pay merchant ID. The name is the only attribute App Store Connect lets you change. */
    "app_store_connect.update_merchant_id": {
      input: {
        /**
         * App Store Connect identifier of the merchant ID record (the opaque resource id, not the merchant.com.example identifier string).
         * @minLength 1
         * @pattern \S
         */
        merchantIdId: string;
        /**
         * New display name for the merchant ID.
         * @minLength 1
         * @pattern \S
         */
        name: string;
      };
      output: {
        /** An Apple Pay merchant ID registered with the team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        merchantId: {
          /** App Store Connect identifier of the merchant ID record. */
          id: string;
          /** Display name of the merchant ID. */
          name?: string | null;
          /** Merchant identifier string, such as merchant.com.example.shop. */
          identifier?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change a featuring nomination, submit a draft with submitted true, or archive it with archived true. Overwrites the given fields; a given list of apps, events, or territories replaces the current one. Pass null for publishEndDate or notes to clear it. Pass at least one field. */
    "app_store_connect.update_nomination": {
      input: {
        /**
         * App Store Connect identifier of the nomination.
         * @minLength 1
         * @pattern \S
         */
        nominationId: string;
        /**
         * New name of the nomination.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** New nomination type. */
        type?: "APP_LAUNCH" | "APP_ENHANCEMENTS" | "NEW_CONTENT";
        /**
         * New description for the editorial team.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
        /** Submit the nomination to Apple when true. */
        submitted?: boolean;
        /** Archive the nomination when true. */
        archived?: boolean;
        /**
         * New availability start, as an ISO 8601 timestamp.
         * @format date-time
         */
        publishStartDate?: string;
        /**
         * New availability end, as an ISO 8601 timestamp, or null to clear it.
         * @format date-time
         */
        publishEndDate?: string | null;
        /**
         * New notes for the editorial team, or null to clear them.
         * @minLength 1
         * @pattern \S
         */
        notes?: string | null;
        /**
         * Device families the nomination applies to.
         * @minItems 1
         */
        deviceFamilies?: Array<"IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION">;
        /**
         * Locales the launch or content is available in.
         * @minItems 1
         */
        locales?: Array<string>;
        /** URLs of supplemental materials, such as press kits or videos, for the editorial team. */
        supplementalMaterialsUris?: Array<string>;
        /** Whether in-app events accompany the launch. */
        hasInAppEvents?: boolean;
        /** Whether the launch happens in selected markets before the rest. */
        launchInSelectMarketsFirst?: boolean;
        /** Whether the app is available for pre-order. */
        preOrderEnabled?: boolean;
        /**
         * Apps the nomination is about; replaces the current list.
         * @minItems 1
         */
        relatedAppIds?: Array<string>;
        /**
         * In-app events that accompany the launch; replaces the current list.
         * @minItems 1
         */
        inAppEventIds?: Array<string>;
        /**
         * Territories the launch or content is available in; replaces the current list.
         * @minItems 1
         */
        supportedTerritoryIds?: Array<string>;
      };
      output: {
        /** A featuring nomination that tells Apple's editorial team about an upcoming launch, enhancement, or content drop. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        nomination: {
          /** App Store Connect identifier for the nomination. */
          id: string;
          /** Name of the nomination. */
          name?: string | null;
          /** What the nomination is about. */
          type?: "APP_LAUNCH" | "APP_ENHANCEMENTS" | "NEW_CONTENT" | null;
          /** Description of the launch or content for the editorial team. */
          description?: string | null;
          /** When the nomination was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** When the nomination was last changed, as an ISO 8601 timestamp. */
          lastModifiedDate?: string | null;
          /** When the nomination was submitted, as an ISO 8601 timestamp, or null while it is a draft. */
          submittedDate?: string | null;
          /** Whether the nomination is a draft, submitted, or archived. */
          state?: "DRAFT" | "SUBMITTED" | "ARCHIVED" | null;
          /** When the launch or content becomes available, as an ISO 8601 timestamp. */
          publishStartDate?: string | null;
          /** When the content stops being available, as an ISO 8601 timestamp, or null. */
          publishEndDate?: string | null;
          /** Device families the nomination applies to. */
          deviceFamilies?: Array<"IPHONE" | "IPAD" | "APPLE_TV" | "APPLE_WATCH" | "MAC" | "VISION"> | null;
          /** Locales the launch or content is available in. */
          locales?: Array<string> | null;
          /** URLs of supplemental materials for the editorial team. */
          supplementalMaterialsUris?: Array<string> | null;
          /** Whether in-app events accompany the launch. */
          hasInAppEvents?: boolean | null;
          /** Whether the launch happens in selected markets before the rest. */
          launchInSelectMarketsFirst?: boolean | null;
          /** Additional notes for the editorial team. */
          notes?: string | null;
          /** Whether the app is available for pre-order. */
          preOrderEnabled?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Rename a Wallet pass type ID. The name is the only attribute App Store Connect lets you change. */
    "app_store_connect.update_pass_type_id": {
      input: {
        /**
         * App Store Connect identifier of the pass type ID record (the opaque resource id, not the pass.com.example identifier string).
         * @minLength 1
         * @pattern \S
         */
        passTypeIdId: string;
        /**
         * New display name for the pass type ID.
         * @minLength 1
         * @pattern \S
         */
        name: string;
      };
      output: {
        /** A Wallet pass type ID registered with the team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        passTypeId: {
          /** App Store Connect identifier of the pass type ID record. */
          id: string;
          /** Display name of the pass type ID. */
          name?: string | null;
          /** Pass type identifier string, such as pass.com.example.ticket. */
          identifier?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change whether a promoted purchase is enabled or visible to all customers. Pass at least one field. */
    "app_store_connect.update_promoted_purchase": {
      input: {
        /**
         * App Store Connect identifier of the promoted purchase.
         * @minLength 1
         * @pattern \S
         */
        promotedPurchaseId: string;
        /** Whether every customer sees the promotion. */
        visibleForAllUsers?: boolean;
        /** Whether the promotion is enabled. */
        enabled?: boolean;
      };
      output: {
        /** An in-app purchase or subscription promoted on the App Store product page of an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        promotedPurchase: {
          /** App Store Connect identifier for the promoted purchase. */
          id: string;
          /** Whether every customer sees the promotion, or only those the app selects through StoreKit. */
          visibleForAllUsers?: boolean | null;
          /** Whether the promotion is enabled. */
          enabled?: boolean | null;
          /** Review state of the promotion. */
          state?: "APPROVED" | "IN_REVIEW" | "PREPARE_FOR_SUBMISSION" | "REJECTED" | null;
          /** The promoted in-app purchase, or null when the promotion is for a subscription or the link was not returned. */
          inAppPurchaseId: string | null;
          /** The promoted subscription, or null when the promotion is for an in-app purchase or the link was not returned. */
          subscriptionId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Mark a review submission item as resolved after fixing the issues App Review raised, or mark it as removed so the submission continues without it. */
    "app_store_connect.update_review_submission_item": {
      input: {
        /**
         * App Store Connect identifier of the review submission item.
         * @minLength 1
         * @pattern \S
         */
        reviewSubmissionItemId: string;
        /** Set to true once the issues App Review raised for the item are fixed. */
        resolved?: boolean;
        /** Set to true to remove the item from the submission. */
        removed?: boolean;
      };
      output: {
        /** One item inside a review submission. Exactly one of the identifier fields is set, naming the record being reviewed. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        reviewSubmissionItem: {
          /** App Store Connect identifier for the review submission item. */
          id: string;
          /** Review state of this item. */
          state?: "READY_FOR_REVIEW" | "ACCEPTED" | "APPROVED" | "REJECTED" | "REMOVED" | null;
          /** App Store Connect identifier of the App Store version this item reviews, or null. */
          appStoreVersionId: string | null;
          /** App Store Connect identifier of the custom product page version this item reviews, or null. */
          appCustomProductPageVersionId: string | null;
          /** App Store Connect identifier of the product page optimization experiment (v1) this item reviews, or null. */
          appStoreVersionExperimentId: string | null;
          /** App Store Connect identifier of the product page optimization experiment (v2) this item reviews, or null. */
          appStoreVersionExperimentV2Id: string | null;
          /** App Store Connect identifier of the in-app event this item reviews, or null. */
          appEventId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the territory, interrupted-purchase setting, or subscription renewal rate of a sandbox tester. Changes can take up to an hour to reach the sandbox environment. Pass at least one field to change. */
    "app_store_connect.update_sandbox_tester": {
      input: {
        /**
         * App Store Connect identifier of the sandbox tester.
         * @minLength 1
         * @pattern \S
         */
        sandboxTesterId: string;
        /** App Store territory the tester purchases in, as an ISO 3166-1 alpha-3 code such as USA. */
        territory?: "ABW" | "AFG" | "AGO" | "AIA" | "ALB" | "AND" | "ANT" | "ARE" | "ARG" | "ARM" | "ASM" | "ATG" | "AUS" | "AUT" | "AZE" | "BDI" | "BEL" | "BEN" | "BES" | "BFA" | "BGD" | "BGR" | "BHR" | "BHS" | "BIH" | "BLR" | "BLZ" | "BMU" | "BOL" | "BRA" | "BRB" | "BRN" | "BTN" | "BWA" | "CAF" | "CAN" | "CHE" | "CHL" | "CHN" | "CIV" | "CMR" | "COD" | "COG" | "COK" | "COL" | "COM" | "CPV" | "CRI" | "CUB" | "CUW" | "CXR" | "CYM" | "CYP" | "CZE" | "DEU" | "DJI" | "DMA" | "DNK" | "DOM" | "DZA" | "ECU" | "EGY" | "ERI" | "ESP" | "EST" | "ETH" | "FIN" | "FJI" | "FLK" | "FRA" | "FRO" | "FSM" | "GAB" | "GBR" | "GEO" | "GGY" | "GHA" | "GIB" | "GIN" | "GLP" | "GMB" | "GNB" | "GNQ" | "GRC" | "GRD" | "GRL" | "GTM" | "GUF" | "GUM" | "GUY" | "HKG" | "HND" | "HRV" | "HTI" | "HUN" | "IDN" | "IMN" | "IND" | "IRL" | "IRQ" | "ISL" | "ISR" | "ITA" | "JAM" | "JEY" | "JOR" | "JPN" | "KAZ" | "KEN" | "KGZ" | "KHM" | "KIR" | "KNA" | "KOR" | "KWT" | "LAO" | "LBN" | "LBR" | "LBY" | "LCA" | "LIE" | "LKA" | "LSO" | "LTU" | "LUX" | "LVA" | "MAC" | "MAR" | "MCO" | "MDA" | "MDG" | "MDV" | "MEX" | "MHL" | "MKD" | "MLI" | "MLT" | "MMR" | "MNE" | "MNG" | "MNP" | "MOZ" | "MRT" | "MSR" | "MTQ" | "MUS" | "MWI" | "MYS" | "MYT" | "NAM" | "NCL" | "NER" | "NFK" | "NGA" | "NIC" | "NIU" | "NLD" | "NOR" | "NPL" | "NRU" | "NZL" | "OMN" | "PAK" | "PAN" | "PER" | "PHL" | "PLW" | "PNG" | "POL" | "PRI" | "PRT" | "PRY" | "PSE" | "PYF" | "QAT" | "REU" | "ROU" | "RUS" | "RWA" | "SAU" | "SEN" | "SGP" | "SHN" | "SLB" | "SLE" | "SLV" | "SMR" | "SOM" | "SPM" | "SRB" | "SSD" | "STP" | "SUR" | "SVK" | "SVN" | "SWE" | "SWZ" | "SXM" | "SYC" | "TCA" | "TCD" | "TGO" | "THA" | "TJK" | "TKM" | "TLS" | "TON" | "TTO" | "TUN" | "TUR" | "TUV" | "TWN" | "TZA" | "UGA" | "UKR" | "UMI" | "URY" | "USA" | "UZB" | "VAT" | "VCT" | "VEN" | "VGB" | "VIR" | "VNM" | "VUT" | "WLF" | "WSM" | "XKS" | "YEM" | "ZAF" | "ZMB" | "ZWE";
        /** Interrupt purchases so the tester must resolve a billing issue before buying. */
        interruptPurchases?: boolean;
        /** How fast a one-month subscription renews in the sandbox. */
        subscriptionRenewalRate?: "MONTHLY_RENEWAL_EVERY_ONE_HOUR" | "MONTHLY_RENEWAL_EVERY_THIRTY_MINUTES" | "MONTHLY_RENEWAL_EVERY_FIFTEEN_MINUTES" | "MONTHLY_RENEWAL_EVERY_FIVE_MINUTES" | "MONTHLY_RENEWAL_EVERY_THREE_MINUTES";
      };
      output: {
        /** A Sandbox Apple Account used to test in-app purchases and subscriptions. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        sandboxTester: {
          /** App Store Connect identifier for the sandbox tester. */
          id: string;
          /** Tester first name. */
          firstName?: string | null;
          /** Tester last name. */
          lastName?: string | null;
          /** Sandbox Apple Account email the tester signs in with. */
          acAccountName?: string | null;
          /** App Store territory the tester purchases in, as an ISO 3166-1 alpha-3 code. */
          territory?: "ABW" | "AFG" | "AGO" | "AIA" | "ALB" | "AND" | "ANT" | "ARE" | "ARG" | "ARM" | "ASM" | "ATG" | "AUS" | "AUT" | "AZE" | "BDI" | "BEL" | "BEN" | "BES" | "BFA" | "BGD" | "BGR" | "BHR" | "BHS" | "BIH" | "BLR" | "BLZ" | "BMU" | "BOL" | "BRA" | "BRB" | "BRN" | "BTN" | "BWA" | "CAF" | "CAN" | "CHE" | "CHL" | "CHN" | "CIV" | "CMR" | "COD" | "COG" | "COK" | "COL" | "COM" | "CPV" | "CRI" | "CUB" | "CUW" | "CXR" | "CYM" | "CYP" | "CZE" | "DEU" | "DJI" | "DMA" | "DNK" | "DOM" | "DZA" | "ECU" | "EGY" | "ERI" | "ESP" | "EST" | "ETH" | "FIN" | "FJI" | "FLK" | "FRA" | "FRO" | "FSM" | "GAB" | "GBR" | "GEO" | "GGY" | "GHA" | "GIB" | "GIN" | "GLP" | "GMB" | "GNB" | "GNQ" | "GRC" | "GRD" | "GRL" | "GTM" | "GUF" | "GUM" | "GUY" | "HKG" | "HND" | "HRV" | "HTI" | "HUN" | "IDN" | "IMN" | "IND" | "IRL" | "IRQ" | "ISL" | "ISR" | "ITA" | "JAM" | "JEY" | "JOR" | "JPN" | "KAZ" | "KEN" | "KGZ" | "KHM" | "KIR" | "KNA" | "KOR" | "KWT" | "LAO" | "LBN" | "LBR" | "LBY" | "LCA" | "LIE" | "LKA" | "LSO" | "LTU" | "LUX" | "LVA" | "MAC" | "MAR" | "MCO" | "MDA" | "MDG" | "MDV" | "MEX" | "MHL" | "MKD" | "MLI" | "MLT" | "MMR" | "MNE" | "MNG" | "MNP" | "MOZ" | "MRT" | "MSR" | "MTQ" | "MUS" | "MWI" | "MYS" | "MYT" | "NAM" | "NCL" | "NER" | "NFK" | "NGA" | "NIC" | "NIU" | "NLD" | "NOR" | "NPL" | "NRU" | "NZL" | "OMN" | "PAK" | "PAN" | "PER" | "PHL" | "PLW" | "PNG" | "POL" | "PRI" | "PRT" | "PRY" | "PSE" | "PYF" | "QAT" | "REU" | "ROU" | "RUS" | "RWA" | "SAU" | "SEN" | "SGP" | "SHN" | "SLB" | "SLE" | "SLV" | "SMR" | "SOM" | "SPM" | "SRB" | "SSD" | "STP" | "SUR" | "SVK" | "SVN" | "SWE" | "SWZ" | "SXM" | "SYC" | "TCA" | "TCD" | "TGO" | "THA" | "TJK" | "TKM" | "TLS" | "TON" | "TTO" | "TUN" | "TUR" | "TUV" | "TWN" | "TZA" | "UGA" | "UKR" | "UMI" | "URY" | "USA" | "UZB" | "VAT" | "VCT" | "VEN" | "VGB" | "VIR" | "VNM" | "VUT" | "WLF" | "WSM" | "XKS" | "YEM" | "ZAF" | "ZMB" | "ZWE" | null;
          /** Whether the account can test Apple Pay. */
          applePayCompatible?: boolean | null;
          /** Whether purchases are interrupted so the tester must resolve a billing issue first. */
          interruptPurchases?: boolean | null;
          /** How fast a one-month subscription renews in the sandbox. */
          subscriptionRenewalRate?: "MONTHLY_RENEWAL_EVERY_ONE_HOUR" | "MONTHLY_RENEWAL_EVERY_THIRTY_MINUTES" | "MONTHLY_RENEWAL_EVERY_FIFTEEN_MINUTES" | "MONTHLY_RENEWAL_EVERY_FIVE_MINUTES" | "MONTHLY_RENEWAL_EVERY_THREE_MINUTES" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the reference name, subscription period, Family Sharing setting, review note, or group level of a subscription. Pass at least one field; each given field overwrites the existing value. Family Sharing cannot be turned off again once enabled for an approved subscription. */
    "app_store_connect.update_subscription": {
      input: {
        /**
         * App Store Connect identifier of the subscription.
         * @minLength 1
         * @pattern \S
         */
        subscriptionId: string;
        /**
         * New internal reference name of the subscription.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /** New length of one subscription period. */
        subscriptionPeriod?: "ONE_WEEK" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR";
        /** Whether the subscription can be shared with Family Sharing. */
        familySharable?: boolean;
        /**
         * New note shown to App Review alongside the subscription.
         * @minLength 1
         * @pattern \S
         */
        reviewNote?: string;
        /**
         * New rank of the subscription within its group, where 1 is the highest level of service.
         * @minimum 1
         */
        groupLevel?: number;
      };
      output: {
        /** An auto-renewable subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscription: {
          /** App Store Connect identifier for the subscription. */
          id: string;
          /** Internal reference name of the subscription. */
          name?: string | null;
          /** Product identifier used by the app to purchase the subscription. */
          productId?: string | null;
          /** Whether the subscription can be shared with Family Sharing. */
          familySharable?: boolean | null;
          /** Review and sale state of the subscription. */
          state?: "MISSING_METADATA" | "READY_TO_SUBMIT" | "WAITING_FOR_REVIEW" | "IN_REVIEW" | "DEVELOPER_ACTION_NEEDED" | "PENDING_BINARY_APPROVAL" | "APPROVED" | "DEVELOPER_REMOVED_FROM_SALE" | "REMOVED_FROM_SALE" | "REJECTED" | null;
          /** Length of one subscription period. */
          subscriptionPeriod?: "ONE_WEEK" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** Note shown to App Review alongside the subscription. */
          reviewNote?: string | null;
          /** Rank of the subscription within its group, where 1 is the highest level of service. */
          groupLevel?: number | null;
          /** App Store Connect identifier of the group the subscription belongs to. */
          subscriptionGroupId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the billing grace period settings of an app: turn it on or off in production or the sandbox, set its length, or choose which renewals it covers. Pass at least one field; the change applies to every subscription of the app. */
    "app_store_connect.update_subscription_grace_period": {
      input: {
        /**
         * App Store Connect identifier of the grace period record, as returned by get_subscription_grace_period.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGracePeriodId: string;
        /** Enable or disable the grace period in production. */
        optIn?: boolean;
        /** Enable or disable the grace period in the sandbox. */
        sandboxOptIn?: boolean;
        /** Length of the grace period. */
        duration?: "THREE_DAYS" | "SIXTEEN_DAYS" | "TWENTY_EIGHT_DAYS";
        /** Which renewals get a grace period: every renewal, or only paid-to-paid renewals. */
        renewalType?: "ALL_RENEWALS" | "PAID_TO_PAID_ONLY";
      };
      output: {
        /** Billing grace period settings of an app, which keep subscribers on service while a failed payment is retried. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGracePeriod: {
          /** App Store Connect identifier for the subscription grace period record. */
          id: string;
          /** Whether the grace period is enabled in production. */
          optIn?: boolean | null;
          /** Whether the grace period is enabled in the sandbox. */
          sandboxOptIn?: boolean | null;
          /** Length of the grace period. */
          duration?: "THREE_DAYS" | "SIXTEEN_DAYS" | "TWENTY_EIGHT_DAYS" | null;
          /** Which renewals get a grace period: every renewal, or only paid-to-paid renewals. */
          renewalType?: "ALL_RENEWALS" | "PAID_TO_PAID_ONLY" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Rename a subscription group. The new reference name replaces the existing one. */
    "app_store_connect.update_subscription_group": {
      input: {
        /**
         * App Store Connect identifier of the subscription group.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupId: string;
        /**
         * New internal reference name of the group.
         * @minLength 1
         * @pattern \S
         */
        referenceName: string;
      };
      output: {
        /** A subscription group that holds the auto-renewable subscriptions a customer can switch between. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroup: {
          /** App Store Connect identifier for the subscription group. */
          id: string;
          /** Internal reference name of the group, not shown to customers. */
          referenceName?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the customer-facing name or custom app name of a subscription group in one locale. Pass at least one field; the locale itself cannot be changed. */
    "app_store_connect.update_subscription_group_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription group localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupLocalizationId: string;
        /**
         * New group name shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * New alternative app name shown with the group in this locale.
         * @minLength 1
         * @pattern \S
         */
        customAppName?: string;
      };
      output: {
        /** Customer-facing name of a subscription group in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroupLocalization: {
          /** App Store Connect identifier for the subscription group localization. */
          id: string;
          /** Group name shown to customers in this locale. */
          name?: string | null;
          /** Alternative app name shown with the group in this locale, when one is set. */
          customAppName?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the customer-facing name or custom app name of a subscription group version localization. Pass at least one field; the locale itself cannot be changed. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.update_subscription_group_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription group version localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionGroupLocalizationId: string;
        /**
         * New group name shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * New alternative app name shown with the group in this locale.
         * @minLength 1
         * @pattern \S
         */
        customAppName?: string;
      };
      output: {
        /** Customer-facing name of a subscription group in one locale, attached to a subscription group version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionGroupLocalization: {
          /** App Store Connect identifier for the subscription group version localization. */
          id: string;
          /** Group name shown to customers in this locale. */
          name?: string | null;
          /** Alternative app name shown with the group in this locale, when one is set. */
          customAppName?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the end date of an introductory offer, or remove the end date so the offer runs until it is deleted. Apple allows no other change; create a new offer to change the price, duration, or mode. */
    "app_store_connect.update_subscription_introductory_offer": {
      input: {
        /**
         * App Store Connect identifier of the introductory offer.
         * @minLength 1
         * @pattern \S
         */
        subscriptionIntroductoryOfferId: string;
        /**
         * New last day the offer is available, as YYYY-MM-DD. Pass null to remove the end date.
         * @format date
         */
        endDate: string | null;
      };
      output: {
        /** An introductory offer of an auto-renewable subscription in one territory. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionIntroductoryOffer: {
          /** App Store Connect identifier for the introductory offer. */
          id: string;
          /** First day the offer is available, as YYYY-MM-DD. */
          startDate?: string | null;
          /** Last day the offer is available, as YYYY-MM-DD. */
          endDate?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          /** The territory the price applies to, or null when App Store Connect did not return it. */
          territory: {
            /** ISO 3166-1 alpha-3 territory code, such as USA. */
            id: string;
            /** ISO 4217 currency of the territory, such as USD. */
            currency?: string | null;
            [key: string]: unknown;
          } | null;
          /** The subscription price point charged for the offer, or null when App Store Connect did not return it. */
          subscriptionPricePoint: {
            /** App Store Connect identifier for the subscription price point. */
            id: string;
            /** Price the customer pays in the territory currency, as a decimal string. */
            customerPrice?: string | null;
            /** Developer proceeds per period during the first year, as a decimal string. */
            proceeds?: string | null;
            /** Developer proceeds per period after the first year, as a decimal string. */
            proceedsYear2?: string | null;
            [key: string]: unknown;
          } | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the customer-facing name or description of a subscription in one locale. Pass at least one field; the locale itself cannot be changed. */
    "app_store_connect.update_subscription_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionLocalizationId: string;
        /**
         * New subscription name shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * New subscription description shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** Customer-facing name and description of a subscription in one locale. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionLocalization: {
          /** App Store Connect identifier for the subscription localization. */
          id: string;
          /** Subscription name shown to customers in this locale. */
          name?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Subscription description shown to customers in this locale. */
          description?: string | null;
          /** Review state of the localization. */
          state?: "PREPARE_FOR_SUBMISSION" | "WAITING_FOR_REVIEW" | "APPROVED" | "REJECTED" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Activate or deactivate an offer code configuration. Deactivating it stops every custom and one-time use code of the configuration from being redeemed; Apple allows no other change after creation. */
    "app_store_connect.update_subscription_offer_code": {
      input: {
        /**
         * App Store Connect identifier of the offer code configuration.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeId: string;
        /** Whether the configuration and its codes can be redeemed. */
        active: boolean;
      };
      output: {
        /** An offer code configuration of an auto-renewable subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionOfferCode: {
          /** App Store Connect identifier for the offer code configuration. */
          id: string;
          /** Reference name of the offer code configuration. */
          name?: string | null;
          /** Customer groups eligible to redeem the codes. */
          customerEligibilities?: Array<"NEW" | "EXISTING" | "EXPIRED"> | null;
          /** Whether the offer stacks with or replaces introductory offers. */
          offerEligibility?: "STACK_WITH_INTRO_OFFERS" | "REPLACE_INTRO_OFFERS" | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          /** Total number of codes generated so far. */
          totalNumberOfCodes?: number | null;
          /** Number of codes generated for production. */
          productionCodeCount?: number | null;
          /** Number of codes generated for the sandbox. */
          sandboxCodeCount?: number | null;
          /** Whether the configuration is active and its codes redeemable. */
          active?: boolean | null;
          /** Whether the subscription renews automatically after the offer ends. */
          autoRenewEnabled?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Activate or deactivate a custom code. A deactivated code can no longer be redeemed until it is activated again. */
    "app_store_connect.update_subscription_offer_code_custom_code": {
      input: {
        /**
         * App Store Connect identifier of the custom code batch.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeCustomCodeId: string;
        /** Whether the custom code can be redeemed. */
        active: boolean;
      };
      output: {
        /** A custom code batch of an offer code configuration. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionOfferCodeCustomCode: {
          /** App Store Connect identifier for the custom code batch. */
          id: string;
          /** The custom code customers redeem. */
          customCode?: string | null;
          /** Number of redemptions the custom code allows. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the code can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the custom code is active and redeemable. */
          active?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Activate or deactivate a one-time use code batch. Deactivating it stops every unredeemed code in the batch from being redeemed. */
    "app_store_connect.update_subscription_offer_code_one_time_use_code": {
      input: {
        /**
         * App Store Connect identifier of the one-time use code batch.
         * @minLength 1
         * @pattern \S
         */
        subscriptionOfferCodeOneTimeUseCodeId: string;
        /** Whether the codes in the batch can be redeemed. */
        active: boolean;
      };
      output: {
        /** A batch of one-time use codes of an offer code configuration. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionOfferCodeOneTimeUseCode: {
          /** App Store Connect identifier for the one-time use code batch. */
          id: string;
          /** Number of single-use codes in the batch. */
          numberOfCodes?: number | null;
          /** When the batch was created, as an ISO 8601 timestamp. */
          createdDate?: string | null;
          /** Last day the codes can be redeemed, as YYYY-MM-DD. */
          expirationDate?: string | null;
          /** Whether the batch is active and its codes redeemable. */
          active?: boolean | null;
          /** Environment the codes redeem in. */
          environment?: "PRODUCTION" | "SANDBOX" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the territories a subscription payment plan is sold in, or whether it opens automatically in new territories. Pass at least one field; availableTerritoryIds replaces the whole territory list, so territories left out stop selling the plan. */
    "app_store_connect.update_subscription_plan_availability": {
      input: {
        /**
         * App Store Connect identifier of the subscription plan availability.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPlanAvailabilityId: string;
        /**
         * Complete list of territories the plan is sold in after the update.
         * @minItems 1
         */
        availableTerritoryIds?: Array<string>;
        /** Whether the plan becomes available automatically in territories the App Store adds later. */
        availableInNewTerritories?: boolean;
      };
      output: {
        /** Territory availability of one payment plan of a subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionPlanAvailability: {
          /** App Store Connect identifier for the subscription plan availability. */
          id: string;
          /** Whether the plan becomes available automatically in territories the App Store adds later. */
          availableInNewTerritories?: boolean | null;
          /** Payment plan of the subscription: UPFRONT charges the full price at the start of each period, MONTHLY is a 12-month commitment billed monthly (App Store Connect API 4.4). */
          planType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Set territory prices on an existing promotional offer. Each submitted territory replaces the price already stored for it; other attributes of the offer cannot be changed after creation. */
    "app_store_connect.update_subscription_promotional_offer": {
      input: {
        /**
         * App Store Connect identifier of the promotional offer.
         * @minLength 1
         * @pattern \S
         */
        subscriptionPromotionalOfferId: string;
        /**
         * Territory prices submitted together with the offer. Each territory may appear once.
         * @minItems 1
         */
        prices: Array<{
          /**
           * ISO 3166-1 alpha-3 code of the territory the price applies to, such as USA.
           * @minLength 1
           * @pattern \S
           */
          territoryId: string;
          /**
           * Subscription price point to charge in that territory, taken from the subscription price points of the same territory. Omit for FREE_TRIAL offers.
           * @minLength 1
           * @pattern \S
           */
          subscriptionPricePointId?: string;
        }>;
      };
      output: {
        /** A promotional offer of an auto-renewable subscription for existing or lapsed subscribers. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionPromotionalOffer: {
          /** App Store Connect identifier for the promotional offer. */
          id: string;
          /** Reference name shown in App Store Connect. */
          name?: string | null;
          /** Offer identifier your app passes to StoreKit when presenting the offer. */
          offerCode?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          numberOfPeriods?: number | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the customer-facing name or description of a subscription version localization. Pass at least one field; the locale itself cannot be changed. Versions belong to the App Store Connect API 4.4 review flow: a version is a draft of the localized metadata that goes through App Review, and the live record is replaced once the version is approved. */
    "app_store_connect.update_subscription_version_localization": {
      input: {
        /**
         * App Store Connect identifier of the subscription version localization.
         * @minLength 1
         * @pattern \S
         */
        subscriptionLocalizationId: string;
        /**
         * New subscription name shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * New subscription description shown to customers in this locale.
         * @minLength 1
         * @pattern \S
         */
        description?: string;
      };
      output: {
        /** Customer-facing name and description of a subscription in one locale, attached to a subscription version. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        subscriptionLocalization: {
          /** App Store Connect identifier for the subscription version localization. */
          id: string;
          /** Subscription name shown to customers in this locale. */
          name?: string | null;
          /** Locale the localization is written in, such as en-US. */
          locale?: string | null;
          /** Subscription description shown to customers in this locale. */
          description?: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the availability of an app in one territory: toggle whether it is available, set or clear its release date, or enable pre-orders. Pass at least one field; the others keep their current values. */
    "app_store_connect.update_territory_availability": {
      input: {
        /**
         * App Store Connect identifier of the territory availability, from list_territory_availabilities.
         * @minLength 1
         * @pattern \S
         */
        territoryAvailabilityId: string;
        /** Make the app available (true) or unavailable (false) in the territory. */
        available?: boolean;
        /**
         * Day the app is released in the territory, as YYYY-MM-DD. Pass null to clear a scheduled release date.
         * @format date
         */
        releaseDate?: string | null;
        /** Offer the app for pre-order in the territory. */
        preOrderEnabled?: boolean;
      };
      output: {
        /** The availability of an app in one App Store territory. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        territoryAvailability: {
          /** App Store Connect identifier for the territory availability. */
          id: string;
          /** Whether the app is available in the territory. */
          available?: boolean | null;
          /** Scheduled release date in the territory, as YYYY-MM-DD, or null when none is set. */
          releaseDate?: string | null;
          /** Whether the app can be pre-ordered in the territory. */
          preOrderEnabled?: boolean | null;
          /** Day the pre-order was or will be published in the territory, as YYYY-MM-DD. */
          preOrderPublishDate?: string | null;
          /** Reasons the app is or is not on sale in the territory, as reported by App Store Connect. */
          contentStatuses?: Array<"AVAILABLE" | "AVAILABLE_FOR_PREORDER_ON_DATE" | "PROCESSING_TO_NOT_AVAILABLE" | "PROCESSING_TO_AVAILABLE" | "PROCESSING_TO_PRE_ORDER" | "AVAILABLE_FOR_SALE_UNRELEASED_APP" | "PREORDER_ON_UNRELEASED_APP" | "AVAILABLE_FOR_PREORDER" | "MISSING_RATING" | "CANNOT_SELL_RESTRICTED_RATING" | "BRAZIL_REQUIRED_TAX_ID" | "BRAZIL_GAMBLING_NOT_VERIFIED" | "MISSING_GRN" | "UNVERIFIED_GRN" | "ICP_NUMBER_INVALID" | "ICP_NUMBER_MISSING" | "TRADER_STATUS_NOT_PROVIDED" | "TRADER_STATUS_VERIFICATION_FAILED" | "TRADER_STATUS_VERIFICATION_STATUS_MISSING" | "CANNOT_SELL_SEVENTEEN_PLUS_APPS" | "CANNOT_SELL_SEXUALLY_EXPLICIT" | "CANNOT_SELL_NON_IOS_GAMES" | "CANNOT_SELL_SEVENTEEN_PLUS_GAMES" | "CANNOT_SELL_CASINO" | "CANNOT_SELL_CASINO_WITHOUT_GRAC" | "CANNOT_SELL_CASINO_WITHOUT_AGE_VERIFICATION" | "CANNOT_SELL_ADULT_ONLY" | "CANNOT_SELL_GAMBLING_CONTESTS" | "CANNOT_SELL_GAMBLING" | "CANNOT_SELL_CONTESTS" | "CANNOT_SELL_NINETEEN_PLUS_WITHOUT_GRAC" | "CANNOT_SELL" | "CANNOT_SELL_FREQUENT_INTENSE_GAMBLING" | "CANNOT_SELL_FREQUENT_INTENSE_ALCOHOL_TOBACCO_DRUGS" | "CANNOT_SELL_FREQUENT_INTENSE_VIOLENCE" | "CANNOT_SELL_FREQUENT_INTENSE_SEXUAL_CONTENT_NUDITY" | "CANNOT_SELL_INFREQUENT_MILD_ALCOHOL_TOBACCO_DRUGS" | "CANNOT_SELL_INFREQUENT_MILD_SEXUAL_CONTENT_NUDITY" | "CANNOT_SELL_FREQUENT_INTENSE" | "CANNOT_SELL_FREQUENT_INTENSE_WITHOUT_GRAC" | "CANNOT_SELL_FREQUENT_GAMBLING" | "CANNOT_SELL_FREQUENT_ALCOHOL_TOBACCO_DRUGS" | "CANNOT_SELL_FREQUENT_VIOLENCE" | "CANNOT_SELL_FREQUENT_SEXUAL_CONTENT_NUDITY" | "CANNOT_SELL_INFREQUENT_ALCOHOL_TOBACCO_DRUGS" | "CANNOT_SELL_INFREQUENT_SEXUAL_CONTENT_NUDITY" | "CANNOT_SELL_FREQUENT" | "CANNOT_SELL_FREQUENT_WITHOUT_GRAC"> | null;
          /** Territory the record applies to, such as USA. */
          territoryId: string | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the roles, app visibility, or provisioning access of a team member. Roles and visible apps replace the current values rather than adding to them. Pass at least one field to change. */
    "app_store_connect.update_user": {
      input: {
        /**
         * App Store Connect identifier of the team member.
         * @minLength 1
         * @pattern \S
         */
        userId: string;
        /**
         * Full set of roles the user should hold afterwards.
         * @minItems 1
         */
        roles?: Array<"ADMIN" | "FINANCE" | "ACCOUNT_HOLDER" | "SALES" | "MARKETING" | "APP_MANAGER" | "DEVELOPER" | "ACCESS_TO_REPORTS" | "CUSTOMER_SUPPORT" | "CREATE_APPS" | "CLOUD_MANAGED_DEVELOPER_ID" | "CLOUD_MANAGED_APP_DISTRIBUTION" | "GENERATE_INDIVIDUAL_KEYS">;
        /** Give the user access to every app on the team, or restrict them to the visible apps when false. */
        allAppsVisible?: boolean;
        /** Let the user manage certificates, identifiers, and profiles. */
        provisioningAllowed?: boolean;
        /**
         * Full set of apps the user may see when allAppsVisible is false.
         * @minItems 1
         */
        visibleAppIds?: Array<string>;
      };
      output: {
        /** A member of the App Store Connect team. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        user: {
          /** App Store Connect identifier for the user. */
          id: string;
          /** Apple Account email the user signs in with. */
          username?: string | null;
          /** User first name. */
          firstName?: string | null;
          /** User last name. */
          lastName?: string | null;
          /** Roles granted to the user. */
          roles?: Array<"ADMIN" | "FINANCE" | "ACCOUNT_HOLDER" | "SALES" | "MARKETING" | "APP_MANAGER" | "DEVELOPER" | "ACCESS_TO_REPORTS" | "CUSTOMER_SUPPORT" | "CREATE_APPS" | "CLOUD_MANAGED_DEVELOPER_ID" | "CLOUD_MANAGED_APP_DISTRIBUTION" | "GENERATE_INDIVIDUAL_KEYS"> | null;
          /** Whether the user can see every app on the team. */
          allAppsVisible?: boolean | null;
          /** Whether the user may manage certificates, identifiers, and profiles. */
          provisioningAllowed?: boolean | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the name, URL, secret, event types, or enabled state of a webhook. Each given field replaces the current value; eventTypes replaces the whole list. */
    "app_store_connect.update_webhook": {
      input: {
        /**
         * App Store Connect identifier of the webhook.
         * @minLength 1
         * @pattern \S
         */
        webhookId: string;
        /**
         * New display name for the webhook.
         * @minLength 1
         * @pattern \S
         */
        name?: string;
        /**
         * New HTTPS endpoint App Store Connect posts notifications to.
         * @format uri
         */
        url?: string;
        /**
         * New shared secret App Store Connect uses to sign every notification: the x-apple-signature header carries hmacsha256=<hex HMAC-SHA256 of the request body>. Choose a value that is hard to guess.
         * @minLength 1
         * @pattern \S
         */
        secret?: string;
        /**
         * New list of event types the webhook receives.
         * @minItems 1
         */
        eventTypes?: Array<"ALTERNATIVE_DISTRIBUTION_PACKAGE_AVAILABLE_UPDATED" | "ALTERNATIVE_DISTRIBUTION_PACKAGE_VERSION_CREATED" | "ALTERNATIVE_DISTRIBUTION_TERRITORY_AVAILABILITY_UPDATED" | "APP_STORE_VERSION_APP_VERSION_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_APP_STORE_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_EXTERNAL_BETA_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_INTERNAL_BETA_RELEASE_CREATED" | "BACKGROUND_ASSET_VERSION_STATE_UPDATED" | "BETA_FEEDBACK_CRASH_SUBMISSION_CREATED" | "BETA_FEEDBACK_SCREENSHOT_SUBMISSION_CREATED" | "BUILD_BETA_DETAIL_EXTERNAL_BUILD_STATE_UPDATED" | "BUILD_UPLOAD_STATE_UPDATED">;
        /** Enable or disable notifications for the webhook. */
        enabled?: boolean;
      };
      output: {
        /** A webhook registered on an app. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        webhook: {
          /** App Store Connect identifier for the webhook. */
          id: string;
          /** Display name of the webhook. */
          name?: string | null;
          /** HTTPS endpoint App Store Connect posts notifications to. */
          url?: string | null;
          /** Whether App Store Connect currently sends notifications to the webhook. */
          enabled?: boolean | null;
          /** Event types the webhook is subscribed to. */
          eventTypes?: Array<"ALTERNATIVE_DISTRIBUTION_PACKAGE_AVAILABLE_UPDATED" | "ALTERNATIVE_DISTRIBUTION_PACKAGE_VERSION_CREATED" | "ALTERNATIVE_DISTRIBUTION_TERRITORY_AVAILABILITY_UPDATED" | "APP_STORE_VERSION_APP_VERSION_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_APP_STORE_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_EXTERNAL_BETA_RELEASE_STATE_UPDATED" | "BACKGROUND_ASSET_VERSION_INTERNAL_BETA_RELEASE_CREATED" | "BACKGROUND_ASSET_VERSION_STATE_UPDATED" | "BETA_FEEDBACK_CRASH_SUBMISSION_CREATED" | "BETA_FEEDBACK_SCREENSHOT_SUBMISSION_CREATED" | "BUILD_BETA_DETAIL_EXTERNAL_BUILD_STATE_UPDATED" | "BUILD_UPLOAD_STATE_UPDATED"> | null;
          [key: string]: unknown;
        };
      };
    };
    /** Change the schedule, priority, promotion intent, or customer eligibility of a win-back offer. Only the given fields change; the identifier, duration, mode, period count, and prices cannot be changed after creation. */
    "app_store_connect.update_win_back_offer": {
      input: {
        /**
         * App Store Connect identifier of the win-back offer.
         * @minLength 1
         * @pattern \S
         */
        winBackOfferId: string;
        /**
         * New minimum number of months the customer must have paid before lapsing.
         * @minimum 0
         */
        customerEligibilityPaidSubscriptionDurationInMonths?: number;
        /** Inclusive range in months. Leave a bound out to keep that side open. */
        customerEligibilityTimeSinceLastSubscribedInMonths?: {
          /**
           * Lower bound of the range in months.
           * @minimum 0
           */
          minimum?: number;
          /**
           * Upper bound of the range in months.
           * @minimum 0
           */
          maximum?: number;
        };
        /**
         * New waiting period in months between win-back offers. Pass null to remove the waiting period.
         * @minimum 0
         */
        customerEligibilityWaitBetweenOffersInMonths?: number | null;
        /**
         * New first day the offer is available, as YYYY-MM-DD.
         * @format date
         */
        startDate?: string;
        /**
         * New last day the offer is available, as YYYY-MM-DD. Pass null to remove the end date.
         * @format date
         */
        endDate?: string | null;
        /** New priority the App Store uses when several win-back offers apply. */
        priority?: "HIGH" | "NORMAL";
        /** Whether Apple may promote the offer on the App Store with auto-generated assets. */
        promotionIntent?: "NOT_PROMOTED" | "USE_AUTO_GENERATED_ASSETS";
      };
      output: {
        /** A win-back offer that targets lapsed subscribers of an auto-renewable subscription. An attribute App Store Connect has no value for is returned as null, and older records may leave it out entirely. */
        winBackOffer: {
          /** App Store Connect identifier for the win-back offer. */
          id: string;
          /** Reference name shown in App Store Connect. */
          referenceName?: string | null;
          /** Offer identifier your app passes to StoreKit. */
          offerId?: string | null;
          /** Length of one offer period. */
          duration?: "THREE_DAYS" | "ONE_WEEK" | "TWO_WEEKS" | "ONE_MONTH" | "TWO_MONTHS" | "THREE_MONTHS" | "SIX_MONTHS" | "ONE_YEAR" | null;
          /** How the customer pays during the offer. */
          offerMode?: "PAY_AS_YOU_GO" | "PAY_UP_FRONT" | "FREE_TRIAL" | null;
          /** Number of offer periods. */
          periodCount?: number | null;
          /** Minimum number of months the customer must have paid for the subscription before lapsing. */
          customerEligibilityPaidSubscriptionDurationInMonths?: number | null;
          /** Inclusive range of months since the customer last subscribed. */
          customerEligibilityTimeSinceLastSubscribedInMonths?: {
            /** Lower bound in months. */
            minimum?: number | null;
            /** Upper bound in months. */
            maximum?: number | null;
            [key: string]: unknown;
          } | null;
          /** Months a customer must wait after redeeming a win-back offer before another one is offered. */
          customerEligibilityWaitBetweenOffersInMonths?: number | null;
          /** First day the offer is available, as YYYY-MM-DD. */
          startDate?: string | null;
          /** Last day the offer is available, as YYYY-MM-DD. */
          endDate?: string | null;
          /** Priority the App Store uses when several win-back offers apply. */
          priority?: "HIGH" | "NORMAL" | null;
          /** Whether Apple may promote the offer on the App Store with auto-generated assets. */
          promotionIntent?: "NOT_PROMOTED" | "USE_AUTO_GENERATED_ASSETS" | null;
          /** Subscription plan type the offer targets. */
          targetSubscriptionPlanType?: "MONTHLY" | "UPFRONT" | null;
          [key: string]: unknown;
        };
      };
    };
  }
}
